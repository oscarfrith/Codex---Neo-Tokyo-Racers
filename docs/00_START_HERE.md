# Neo Tokyo Racers Project Context

Last updated: 2026-07-07

This folder is the handoff pack for new Codex or ChatGPT sessions. Read this file first, then use the other docs only as needed.

## Current Project

Neo Tokyo Racers is a Roblox open-world hover racing game with modular hovercars. The main vehicle category currently being built is `BRUISER`.

The vehicle system is category-based: cockpits and modules inside the same category share fixed slot locations, so modules can be swapped between similar cockpits.

## Current Script State

Known from chat:

- Architecture migration Phases 15-21 were committed after successful testing. Main client extraction Phase A-E later removed the final active legacy-named `HOVER_RACING` owner from live use.
- Main client extraction Phase A-E has passed. Phase D switched the active main client owner to `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.NeoTokyoRacersClient_Bootstrap_Shadow_Disabled`; Phase E audit passed cleanly with no active legacy-named `HOVER_RACING` scripts. The old `HOVER_RACING_V2_Client` is disabled and kept as rollback.
- Architecture Phase K completed successfully on 2026-05-29 at 17:31:59 in Studio: `ReplicatedStorage.HOVER_RACING_V2_KIT` contents moved into `ReplicatedStorage.NeoTokyoRacers`, 20 source objects patched, 112 replacements applied, and final legacy source hits were 0.
- Architecture Phase L completed successfully on 2026-05-29 at 17:35:09 in Studio: migrated folders were present, legacy source/ObjectValue references were 0, and `ReplicatedStorage.HOVER_RACING_V2_KIT` no longer exists.
- Architecture Phase N completed successfully on 2026-05-29 at 18:36:09 in Studio: 10 source objects patched, 24 replacements applied, stale ObjectValues repaired, `Workspace.NeoTokyoRacersWorld.SpawnPoints.VehicleSpawnPoint` created, and final old runtime source hits were 0.
- World Phase F is prepared to move city blocks from `Workspace.GeneratedCityBlocks` into `Workspace.NeoTokyoRacersWorld.City.Block S#` and patch the active LOD client root resolver.
- World Phase J is prepared to move `ReplicatedStorage.FarLOD5` into `ReplicatedStorage.NeoTokyoRacers.Assets.World.FarLOD5Proxies` and patch the active LOD client far-proxy resolver.
- Cleanup Phase G is a read-only full hierarchy audit for identifying old inactive folders, disabled legacy scripts, rollback scripts, and report folders before any deletion script is generated.
- Cleanup Phase H is the targeted deletion phase for Phase G's confirmed inactive legacy items. It must be run in `DRY_RUN` first, then `DELETE` only after reviewing the exact path list.
- Cleanup Phase I is an aggressive one-step migration clutter cleanup. It deletes stale reports, reference ObjectValues, mirror-only generated config, non-live scaffold/shadow/snapshot code, and empty placeholder folders after confirming active owners are healthy.
- Cleanup Phase M is a post-K/L read-only audit for stale legacy kit references, empty compatibility folders, old reports, nil ObjectValues, and remaining cleanup candidates after `HOVER_RACING_V2_KIT` removal.
- Cleanup Phase O completed successfully. Final Phase M verification at 2026-05-29 18:55:53 showed 0 warnings, 0 missing required paths, 0 source legacy hits, 0 stale ObjectValues, 0 auto cleanup candidates, and 0 review-before-delete candidates.
- Architecture Phase P is present in the Git repo as the conservative first garage runtime startup repair, but it is superseded by Phase Q if the line 23 garage controller error remains.
- Architecture Phase Q repaired the post-Phase-N/P garage startup regression where `GarageActionController_Shadow_Disabled` errors near line 23 and the garage UI does not load. The user reported Phase Q worked.
- Lighting Phase R is prepared to repair `Fogcolor` typo warnings by changing lighting presets to Roblox's valid `FogColor` property and adding a compatibility alias in `LightingService_Active`.
- Lighting Phase AP was installed and user-confirmed working. Building window
  MeshParts now use `Windows Day` / `Windows Night` MaterialVariants and switch
  with lighting mode. Separate Command Bar scripts preview either complete
  condition in Studio edit mode.
- Vehicle Phase AI removes/deprioritises the cockpit car-light experiments from Phases S through AH. No cockpit SpotLight, Beam, smoother, projector, or diagnostic runtime should be considered current. Ordinary cosmetic neon colour channels remain.
- VFX Phase AJ is prepared to repair thrust VFX preview after Dealership Intro Phase 4 moved the local preview vehicle to `Workspace._NTR_ClientOnly.VehiclePreview`. Run `scripts/roblox_vfx_phaseAJ_thrust_preview_root_repair.lua` if thrust VFX no longer previews while editing thrust colour in the customisation menu.
- VFX baseline was restored from the repo mirror after the late-socket/rescan repair sequence caused memory growth and VFX cut-outs. The confirmed follow-up is `scripts/roblox_vfx_mobile_delayed_attach_once.lua`, which delays mobile-only `VehicleVFXController.Attach` briefly and then performs one attach pass without continuous rescans or rebuild loops. The user reported VFX working again after this sequence.
- Vehicle Phase AK was installed and confirmed working through its follow-up repairs. Bruiser modules now use per-cockpit Standard/Lightweight/Power front engine, rear engine, stabiliser, and boost sets plus Lvl 1-3 bumpers/spoilers/side pods. Cockpit purchase grants standard core modules, dealership stats include them, required modules are gated, camera entry views are corrected, per-cockpit default colours live on cockpit attributes, and spawned module colours match preview.
- Vehicle Phase AL was installed and its read-only audit passed on 2026-06-08: 5 cockpits, 72 active modules, 23 planned upgrades, and 0 warnings.
- Vehicle Phase AM was installed and confirmed working on 2026-06-08. The isolated `VehiclePerformanceRuntimeService_Active` writes complete-build raw/normalized/headline/rating data; the audit passed `17/17`, `17/17`, `6/6`, and 0 warnings with a test rating of `D 407`. Detailed-variable V75 physics was then enabled and reported working well.
- Vehicle Phase AN was installed and confirmed working on 2026-06-08. Module-ID-scoped Fuel Injection level 1 cost `$4000`, changed the profile rating from `D 407` to `D 410`, reached the spawned engine as EngineOutput `+2` and TopSpeed `+1`, and passed the spawned-effect audit with 0 warnings.
- Vehicle Phase AO was installed and confirmed working on 2026-06-08. The visible legacy Brakes, Converter, Fuel System, and generic upgrade controls are replaced by per-module Performance cards, next-level previews, and purchases through the confirmed Phase AN action. The stats panel now keeps the E-S tier/index visible and shows contextual detailed variables for the selected module.
- Dealership Intro Phases 1-7 were installed and confirmed working by the user on 2026-06-03. The flow now uses editable dealership markers, opens the full garage only at `GarageDeskTrigger`, delays the local preview until cockpit purchase/select succeeds, restores preview orbit camera behavior, spawns the final drivable vehicle from `VehicleExitSpawnPoint`, and includes an Exit button that only reopens after the player leaves and re-enters the desk zone. Phase 8 is generated in Git as the next Studio install/test step: it replaces fixed path arrows with a dynamic client-only arrow tether to the desk and persists first desk-objective completion.
- Dealership cockpit module-slots text removal was installed and confirmed working. `scripts/roblox_dealership_remove_cockpit_module_slots_text.lua` removes the overlapping module count text from the cockpit selection stats panel without changing stats, purchase/select, or Build Modules behavior.
- Dealership / Customisation Split Phases 1-11 are in progress. If Play reports `Out of local registers when trying to allocate init` after Phase 6, run `scripts/roblox_dealership_customisation_split_phase6_register_limit_repair.lua` in Edit mode. Phase 7 adds the responsive cockpit grid, Phase 8 adds compact image cards, Phase 9 moves ratings into top-right image badges, Phase 10 (`scripts/roblox_dealership_customisation_split_phase10_responsive_layout_polish.lua`) polishes mobile/right-column layout, category spacing, stat-bar values, desktop card text size, and free-roam car icon scale, and Phase 11 (`scripts/roblox_dealership_customisation_split_phase11_sorted_cockpit_cards.lua`) sorts dealership cockpits by price, customisation cockpits by rating, and categories alphabetically.
- Free Roam Vehicle Spawn Phase 1 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase1_audit.lua`. It is read-only and should be run before adding click-to-spawn/swap from the free-roam car menu; the main open question is whether closest-road placement can use existing road markers or needs a marker-generation/setup phase.
- Free Roam Vehicle Spawn Phase 2 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase2_road_spawn_markers.lua`. Phase 1 output showed no explicit road spawn markers, so Phase 2 creates tagged `NTR_RoadSpawnPoint` markers under `Workspace.NeoTokyoRacersWorld.SpawnPoints.RoadSpawnMarkers` from safer exact `Road` / `Road Asphalt` parts and adds `Config.Runtime.FreeRoamVehicleSpawn` tuning values.
- Preferred spawn-marker first pass is now `scripts/roblox_freeroam_vehicle_spawn_phase2_blockout_road_markers.lua`, because the user confirmed curated road parts already exist under `Workspace.Test + WIP Assets.Blockout.Roads`. It creates one lightweight invisible tagged marker above each `Road` part centre only when the part colour is `RGB(95, 95, 95)` / `#5f5f5f`, skipping differently coloured wall pieces with the same name, and supersedes broad city-road scanning unless the blockout source is missing.
- Free Roam Vehicle Spawn Phase 3 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase3_click_spawn.lua`. It patches `SpawnOwnedVehicleFromFreeRoam` into the active garage server and makes free-roam owned cockpit cards spawn/swap vehicles at the nearest clear `NTR_RoadSpawnPoint`, with server-side ownership, cooldown, core-module, and `10 MPH` speed checks.
- Free Roam Vehicle Spawn Phase 4 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4_despawn_exit_prompt.lua`. It separates parked exit from true despawn: free-roam `DESPAWN` destroys the current vehicle, a new bottom-centre driving-only `EXIT VEHICLE` button parks the player 10 studs left of the driver seat while leaving the car spawned, and an owner-only `E` / touch prompt re-enters parked vehicles.
- Free Roam Vehicle Spawn Phase 4B is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4b_remote_handoff_repair.lua`. It fixes Phase 4's exit-button remote path, adds an explicit free-roam cockpit-card spawn-to-driving handoff, repairs owner lookup for seats inside sub-models, and disables the old automatic walk-up re-entry loop so parked entry is prompt-only.
- Free Roam Vehicle Spawn Phase 4C is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4c_exit_stop_parked_hover.lua`. It makes `EXIT VEHICLE` stop the local driving loop/HUD/controls, return the camera to the player, keep the car hovering through an isolated parked-hover client without drive input, and hide the free-roam car menu immediately after successful cockpit-card spawn.
- Free Roam Vehicle Spawn Phase 4D is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4d_despawn_speed_card_polish.lua`. It keeps already-exited players in place when despawning a parked vehicle, applies the `10 MPH` spawn gate and vehicle-position spawn anchor only while actively seated/driving, narrows the desktop car menu to three compact cards, and removes the pink card/image outlines from free-roam cockpit cards.
- Racing Phase 1 was run by the user and the first race zones/checkpoints were moved in Studio. `scripts/roblox_racing_phase1_audit_and_sample_route_setup.lua` created only the editable `ShiftedCanalSprint` route/config scaffold with tiered time-trial medal attributes; it installed no gameplay services, rewards, HUD, or source patches. Before the next runtime phase, keep the route system source-agnostic for future player-created races by adding a `RaceConfigReader` / `RaceRouteDefinition` layer, and plan route arrow assets through an `ArrowMarkers` folder rendered locally by the route-guide client.
- Racing Phase 2 was installed/tested by the user and reported working well. `scripts/roblox_racing_phase2_solo_time_trial_mvp.lua` installs isolated `RaceRouteDefinition` / `RaceConfigReader` modules, `Shared.Remotes.Racing`, `TimeTrialService_Active`, and `RaceClient_Active` for a server-authoritative solo time-trial loop on `ShiftedCanalSprint`. The amended next phase should replace the instant `E` start with a themed race entry menu, track image/map, `START RACE` / `START TIME TRIAL` / `EXIT`, owned vehicle selection using dealership/customisation card styling, start-line staging/countdown, and first same-server session separation before adding rewards or matchmaking.
- Racing Phase 3 is generated in Git as `scripts/roblox_racing_phase3_entry_menu_staging_session.lua`. It canonically replaces only the isolated Racing service/client with a themed start-zone race menu, owned vehicle picker, selected-vehicle spawn through existing garage actions, start-grid teleport/freeze/countdown, server timing after `GO`, a lightweight `RaceInstances.<RunId>` container, and first local participant/non-participant visibility filtering. It intentionally does not add medals, rewards, personal bests, multiplayer matchmaking, cloned route pockets, or collidable race-only assets yet. If Phase 3 installs cleanly but pressing `E` does not open the menu, run `scripts/roblox_racing_phase3b_prompt_connection_repair.lua`; it repairs the isolated prompt connection path and the Phase 3 installer now includes the same startup prompt recreation fix. If Output then shows `Race entry prompt triggered by <player>` but the menu still does not open, run `scripts/roblox_racing_phase3c_client_event_repair.lua`; it patches the isolated race menu client startup/event logging and installs a tiny client probe. If `START TIME TRIAL` from `RaceStartZone` spawns the car around free roam/customisation instead of staging it, run `scripts/roblox_racing_phase3d_time_trial_event_pairing_repair.lua`; it resolves the paired time-trial event before spawning and makes the server tolerant of race event ids for solo time trials. If the countdown finishes but the vehicle is not drivable or map assets stream/flicker around the start, run `scripts/roblox_racing_phase3e_release_drive_handoff_repair.lua`; it repairs the post-`GO` vehicle/network ownership, streaming focus, and driving handoff.
- Racing plan update on 2026-07-06: multiplayer races should launch with one open queue/category per race event to avoid splitting the playerbase by tier. Time trials remain tiered by vehicle tier for medal targets/rewards. The compressed next recommended phases are: time-trial results/medals/PB UI, route guidance/session assets, guarded rewards, free-roam Race panel, then open-category multiplayer matchmaking.
- Racing Phase 4 was installed/tested by the user and reported working well on 2026-07-06. `scripts/roblox_racing_phase4_time_trial_results_pack.lua` patches only isolated Racing scripts to add server-authoritative time-trial medal calculation, checkpoint split payloads, in-session personal bests per player/event/tier, and a themed finish panel with `RETRY` / `EXIT`. It intentionally does not add cash rewards, persistent bests, route arrows, session assets, or multiplayer matchmaking.
- Racing Phase 5 is generated in Git as `scripts/roblox_racing_phase5_route_guidance_session_assets.lua`. It installs a new isolated `RaceRouteGuideClient_Active` that listens to Racing events and renders local-only checkpoint/finish frames, dynamic next-gate chevrons, authored `ArrowMarkers`, and a wrong-way prompt. It also creates `Config.Racing.RouteGuide` tuning attributes and confirms `ArrowMarkers` / `SessionAssetTemplates` folders for each route. Collidable ramps, boost strips, barriers, and jump pads remain deferred until the race-pocket/collision layer is stronger. If two checkpoint markers appear, run Racing Phase 5B (`scripts/roblox_racing_phase5b_marker_dedupe_transparency_repair.lua`). Phase 5F (`scripts/roblox_racing_phase5f_checkpoint_pill_label.lua`) is now the preferred checkpoint-guide visual: world-space text above the physical checkpoint, a small configurable transparent black pill behind only the text, generated checkpoint frames off by default, and `WRONG WAY` delayed until `3` seconds of sustained wrong-way driving.
- Racing Phase 6 is generated in Git as `scripts/roblox_racing_phase6_time_trial_rewards_pack.lua`. It installs a guarded `RaceRewardService_Active`, creates `Config.Racing.Rewards`, adds a tiny server-only garage cash bridge so rewards update the usable garage cash profile, patches time-trial finish payloads, and shows payout text on the existing results panel. It intentionally does not edit `Config.Racing.RouteGuide`; keep Phase 5F pill-label attributes as the current visual baseline.
- Racing Phase 6B is generated in Git as `scripts/roblox_racing_phase6b_global_reward_config_rounding.lua` after Phase 6 was reported working well. It is the preferred reward-balancing model: `Config.Racing.Rewards.TimeTrial` and `Config.Racing.Rewards.Race` own all medal/tier/placement/repeat multipliers, time-trial payouts round to the nearest `$250` by default, and track/event folders keep only `BaseReward` for reward balancing.
- Racing Phase 7 is generated in Git as `scripts/roblox_racing_phase7_free_roam_race_browser.lua` after Phase 6B was reported good for now. It installs an isolated free-roam Race browser, an `OpenRaceBrowser` bridge from the `RACE` tile, and a local start-zone waypoint. It intentionally does not edit `Config.Racing.Rewards`, reward multipliers, `Config.Racing.RouteGuide`, checkpoint timing, or payout logic.
- Racing Phase 7B is generated in Git as `scripts/roblox_racing_phase7b_race_browser_teleport.lua` after the Phase 7 menu was reported looking good. It replaces the browser waypoint with `TELEPORT TO START`, closes the Race browser after successful teleport, creates editable `RaceRoutes.<RouteId>.TeleportPoints.RaceBrowserTeleportPoint` parts, and installs a server-owned teleport/despawn sequence so the character is moved before the old spawned vehicle is destroyed. It intentionally does not edit reward config, route-guide config, checkpoint timing, or payout logic.
- Racing Phase 8 is generated in Git as `scripts/roblox_racing_phase8_open_category_multiplayer_race_mvp.lua` after Phase 7B was confirmed working. It adds the first open-category multiplayer race queue with selected-vehicle spawn, min/max player queue status, grid staging, countdown, checkpoint placement, and race finish UI. It intentionally defers race cash rewards and does not edit `Config.Racing.Rewards` or existing route-guide tuning values.
- Racing Phase 8B is generated in Git as `scripts/roblox_racing_phase8b_multiplayer_race_drive_handoff_repair.lua` after the Phase 8 queue worked but both race vehicles stopped hovering/were not drivable at `GO`, with nearby map content appearing to switch off. Root cause: multiplayer `RaceStarted` did not re-fire the Phase 3E-style local driving/streaming handoff. Phase 8B patches only `RaceMatchmakingService_Active` and `RaceQueueClient_Active`.
- Racing Phase 8H was installed/tested by the user and reported working well. It is the preferred reset architecture: reset fades client-side, the server clones the active race vehicle, destroys the old assembly, pivots the clean replacement to the checkpoint/start reset CFrame, zeroes momentum, reseats the player, updates the session vehicle reference, and resumes normal race driving prep. It intentionally supersedes the 8E/8F/8G live-reset handoff attempts. Future racing phases should preserve this reset model unless there is a deliberate architecture change.
- Racing Phase 9A was installed/tested by the user and reported working well. It adds route `RouteType`, time-trial lap selection (`1-10` plus `Infinite`), circuit lap looping, best-lap session results, and one best-result reward on finish/quit. It intentionally does not edit reward multipliers, route-guide config, checkpoint visuals, multiplayer race rewards, or the register-limited bootstrap.
- Racing Phase 10A is generated in Git as `scripts/roblox_racing_phase10a_session_asset_collision_foundation.lua`. It adds hidden route `SessionAssetMarkers`, server-owned `SessionAssetTemplates`, `RaceSessionAssetService_Active`, fixed participant/session-asset collision groups, and runtime session asset cloning/cleanup. It intentionally does not edit reward config, route-guide config, Phase 8H reset behavior, or Phase 9A lap/session scoring.
- Drive-In Customisation Phase 1 is prepared as `scripts/roblox_drive_in_customisation_phase1.lua`. It creates `Workspace.NeoTokyoRacersWorld.Dealership.Customisation.DriveInCustomisationTrigger`, shows a local-only visible zone while driving, runs a `3` second countdown, then despawns the live driven vehicle and opens the existing garage UI directly to `Build Modules` for the driven vehicle instance.
- Drive-In Customisation Phase 2 is prepared as `scripts/roblox_drive_in_customisation_phase2_garage_entry_world_prompt.lua`. It replaces the screen countdown with a world-space prompt on `DriveInCustomisationTrigger`, toggles trigger VFX descendants locally while driving, installs a small session-lock service to hide/freeze the player, and repairs the drive-in handoff so the live car despawns and the normal garage preview camera/vehicle are active in Build Modules.
- Drive-In Customisation Phase 3/3B was installed and confirmed working on 2026-07-06. The active baseline now has a countdown-only in-world prompt that appears only once the driven vehicle is inside `DriveInCustomisationTrigger`, opens Build Modules with the normal garage preview/hold behavior, and releases the drive-in hold before the normal garage `SpawnVehicle` handoff so `Start Driving` returns to a drivable car. `scripts/roblox_drive_in_customisation_phase3b_spawn_unlock_anchor_repair.lua` is the targeted repair for the partial Phase 3 installer failure caused by Lua pattern matching.
- Phase 15 successfully moved live server action ownership to `ServerScriptService.NeoTokyoRacers.Services.Garage.GarageActionController_Shadow_Disabled`. The old `HOVER_RACING_V2_Server` remains available for rollback but is no longer the live server action owner.
- `V74` restored the pre-V72/default Roblox driving camera feel and added a light camera assist. The user confirmed this worked well.
- `V75` was generated next to add boost recharge delay and low-speed hover wobble. At the time of writing, no later user confirmation is present in this chat history.
- `scripts/roblox_driving_speed_sensitive_steering_curve.lua` was reported working nicely by the user. It adds tunable low/high-speed steering curve attributes plus reverse steering and boost steering multiplier support to `DrivingControllerV47`.
- `scripts/roblox_driving_accel_brake_pitch_tilt.lua` was reported looking great by the user. It adds configurable acceleration, braking, reverse, and boost pitch tilt on top of the existing hover wobble/bank alignment.
- `scripts/roblox_driving_drift_boost_accel_gate_reverse_speed.lua` was reported working and looking good by the user. It makes drift mini-boost require acceleration on drift exit by default and raises/tunes reverse speed to `40 MPH`.
- `scripts/roblox_driving_slope_hover_height_compensation.lua` was reported working well by the user. It fixes uphill/downhill hover-height drift by damping hover springs relative to expected slope-following vertical movement instead of raw world-Y velocity.
- Character sprint install was run and reported working on 2026-06-04. The live runtime hierarchy now includes `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.Controllers.Runtime.CharacterSprintController_Active`; Shift sprint works on foot and Shift drift remains available while seated in a `VehicleSeat`. Mobile auto-sprint support is prepared in the installer and needs mobile/emulator verification.
- The fixed mobile steering thumbstick V1 is installed and the user likes its current behavior/UI. The current design uses an outer drift ring with `1.8x` the inner radius, a darker translucent band, light-green idle cues, coordinated red drift cues, full outer-edge pointer travel, and `1.275x` frameless pedals. Installed V2/V2.1 places should run `scripts/roblox_mobile_drive_thumbstick_v2_2_drift_feedback.lua`.
- Mobile thumbstick V2.4 is installed and user-confirmed working well. It keeps zero deadzone/linear steering, enlarges the inner circle to `1.4x`, makes the outer drift ring `1.35x` the enlarged inner circle, and triggers drift at `0.95x` outer-ring travel with a `0.88` exit threshold.
- Persistence Phase 1 was installed in Studio and its audit passed on 2026-06-30. It added `ReplicatedStorage.NeoTokyoRacers.Shared.Modules.Data.PlayerProfileSchema` and `ReplicatedStorage.NeoTokyoRacers.Shared.Config.Persistence_EditAttributes` for instance-based cockpit/module ownership, two starting garage spaces, visitable garage access modes, and DataStore-safe encode/decode helpers. It does not switch active garage actions or write DataStores yet.
- Persistence Phase 2 was installed in Studio and its server audit passed on 2026-06-30. It installs `ServerScriptService.NeoTokyoRacers.Services.Player.ProfileService_Active`, keeps `DataStoreEnabled = false` by default, and exposes server BindableFunctions for future garage-profile bridge phases without patching the active garage action owner.
- Persistence Phase 3 was installed in Studio and its server audit passed on 2026-06-30. It installs `ReplicatedStorage.NeoTokyoRacers.Shared.Modules.Data.LegacyGarageProfileMapper` and `ServerScriptService.NeoTokyoRacers.Services.Player.LegacyGarageProfileBridge_Active` to convert the current V56 session-memory profile shape into the new instance-based profile schema.
- Persistence Phase 4 was installed in Studio and all audits passed on 2026-06-30. It patches `GarageActionController_Shadow_Disabled` to mirror the current V56 session profile into `ProfileService_Active` after `GetInitial` and successful garage actions. The current V56 profile remains the live source of truth; ProfileService receives converted snapshots only.
- Persistence Phase 5 was installed/tested in Studio and passed on 2026-06-30 after the `ImportProfileSnapshot` repair. Mirrored ProfileService snapshots can be saved/loaded through DataStore when Studio API access is enabled. It still does not make ProfileService the live garage source of truth.
- Persistence Phase 6 was installed in Studio and worked as expected on 2026-06-30. It patches `GarageActionController_Shadow_Disabled` so buying a new cockpit is blocked when the current V56 owned cockpit count reaches `Persistence_EditAttributes.StartingGarageCapacity`, default 2. Already-owned cockpit selection still works.
- Persistence Phase 7 was installed in Studio and worked as expected on 2026-06-30. It adds tunable upgrade cost/max/step config attributes, an `UpgradeGarageCapacity` garage action, Garage capacity data in profile responses, and mapper support so upgraded capacity mirrors into ProfileService snapshots.
- Persistence Phase 8 was installed and worked functionally, but its first `Garage Spaces` panel overlapped the left Categories panel. Phase 9 supersedes its visible layout and changes the direction from generic upgrades to physical garage-property purchases.
- Persistence Phase 9 was installed but caused the active client bootstrap to exceed Roblox's 200 local-register limit at `V75Driving`. Run the Phase 9 register-limit repair before retesting. The intended Phase 9 behavior moves the compact `Garage Spaces` panel beside the cash panel, removes the old next-price line, changes the button to `Buy More`, opens a garage-card menu, and seeds `GaragePropertyCatalog` for future server-owned garage locations.
- Persistence Phase 10 was installed and worked as expected. It stacks `Garage Spaces` above `Available Cash`, shrinks Categories around the left-column stack, and adds a 30% black modal backdrop behind the garage property menu.
- Persistence Phase 11 was installed and worked as expected. `Garage Spaces` is visible only on the cockpit-selection screen and hides during paint, module building, and customisation.
- Persistence Phase 12 was installed and worked as expected. It extracts the garage property menu card rendering into `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.Controllers.UI.GaragePropertyMenuController` and leaves the active bootstrap with a small bridge so future garage-card work does not bloat the main client script.
- Persistence Phase 13 was installed through the repair path and the client smoke confirmed the core purchase/mirror/controller path on 2026-07-01. It adds real `BuyGarageProperty` server ownership, purchasable prices in `GaragePropertyCatalog`, `OwnedGarageProperties` profile/mirror data, capacity calculated from owned properties while preserving old Phase 7 capacity upgrades, and switches `GaragePropertyMenuController` from the temporary `UpgradeGarageCapacity` backend to `BuyGarageProperty`. The smoke test now treats the visual garage UI check as optional when the player has not reached the dealership desk yet.
- Persistence Phase 14 was installed and reported working well on 2026-07-01. It adds instance-backed `Vehicles`, `CurrentVehicleId`, `OwnedCockpitInstances`, and `OwnedModuleInstances` fields to the active garage profile while preserving the existing legacy UI fields. It adds direct server actions for `BuyCockpitInstance`, `BuyModuleInstance`, and `EquipModuleInstance`.
- Persistence Phase 15 was installed and reported working well on 2026-07-01. It adds visible duplicate-copy UI: owned cockpits show copy counts and `Buy Another`, and owned module options can `Equip Copy` or `Buy Copy` through the Phase 14 instance actions.
- Persistence Phase 16 was installed and its scripts worked well on 2026-07-01, but the module picker needs follow-up fixes: front/rear engine filtering, owned-vs-buy module menu separation, and per-instance owned module cards.
- Persistence Phase 17 is installed and stabilised as the current module-picker baseline. It keeps Phase 16 source-cockpit purchase locks, restores front/rear engine slot filtering, adds `OWNED MODULES` / `BUY MODULES` bottom menu buttons, shows owned module copies as separate cards, removes `Buy Copy` / `Equip Copy` / `No Free Copy`, previews locked buy modules on click, and uses the scale-aware selected-card popup anchor for BUY/LOCKED/EQUIP alignment.
- Persistence Phase 18 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase18_profile_source_handoff.lua` hydrated the active garage session from `ProfileService_Active` saved instance data on first use; the client smoke reported `Hydrated=true`, `source=ProfileService`, and `savedVehicles=1`.
- Persistence Phase 19 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase19_instance_compat_sync.lua` synced legacy UI/customisation writes back into current vehicle/module instance data before mirroring to ProfileService; the client smoke reported `installedInstanceSlots=4`, `missingInstances=0`, `Phase19Synced=true`, and `syncCount=4`.
- Persistence Phase 20 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase20_garage_profile_runtime_extract.lua` extracted the Phase 19 instance sync into `ServerScriptService.NeoTokyoRacers.Services.Garage.GarageProfileRuntime`; the client smoke reported `RuntimeModule=GarageProfileRuntime`, `syncCount=4`, and `missingInstances=0`.
- Persistence Phase 21 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase21_private_garage_interior_mvp.lua` added the private owner-only garage interior MVP; the client smoke reported `EnterOwnGarage OK`, `InGarage=true`, and `ReturnToCity OK`. `streamOk=false` in Studio, so Phase 22 adds stream error diagnostics while continuing the garage interior path.
- Persistence Phase 22 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase22_garage_display_vehicle_mvp.lua` installed `GarageDisplayRuntime`, hooks Phase 21 garage entry to refresh a non-drivable anchored display clone from the current saved vehicle, and the client smoke reported `displayOk=true`, `displayExists=true`, and `displaySource=ProfileServiceCurrentVehicle`.
- Persistence Phase 23 was installed through the canonical repair path and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase23_garage_interior_service_canonical_repair.lua` replaced only the isolated `GarageInteriorService_Active` source with a Phase 21-23 canonical service after incremental source-splice repairs proved brittle; the client smoke reported `SetAccessMode OK`, `VisitGarage OK`, `InGarage=true`, `displayExists=true`, `ReturnToCity OK`, and cleanup back to `Private`.
- Persistence Phase 24 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase24_garage_surfaces_decor_mvp.lua` installs a separate `GarageInteriorCustomizationInvoke` remote plus isolated `GarageInteriorCustomizationService_Active` for garage surface colours/materials and simple decoration anchors, avoiding another text patch against `GarageInteriorService_Active`; the client smoke reported `Applied surfaces/decor`, `Customization state OK`, `persisted=true`, and `ReturnToCity OK`.
- Persistence Phase 25 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase25_garage_customization_ui_mvp.lua` installs an isolated owner-only in-garage customization LocalScript that calls the Phase 24 remote for surface/decor presets without patching the main dealership bootstrap; the client smoke reported the UI panel visible and the customization backend responding.
- Persistence Phase 26 was smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase26_garage_customization_validation.lua` applied owner customization presets through the Phase 24 backend and confirmed the Phase 25 UI summary reflected `SURFACES 2  DECOR 1`.
- Persistence Phase 27 was installed and smoke-tested on 2026-07-02. `scripts/roblox_persistence_phase27_garage_access_ui_mvp.lua` installs an isolated same-server garage access UI for entering your own garage, setting access mode, visiting an owner user ID, and returning to the city without Command Bar calls; the client smoke reported the UI shell visible, `VisitGarage OK`, and `ReturnToCity OK`.
- Persistence Phase 28 was smoke-tested on 2026-07-02 and completes the garage MVP run. `scripts/roblox_persistence_phase28_garage_mvp_final_audit.lua` verified the Phase 21-27 runtime stack, access UI, owner garage entry, display vehicle, customization UI, persisted surfaces/decor summary, interior state, and return-to-city path. The remaining handoff work is a DataStore-enabled save/rejoin check when practical plus final Studio mirror refresh.
- Dealership / Customisation Split Phase 1 was run by the user and reported working on 2026-07-04. The dealership is now intended to be buy-only, with `Buy` / `Buy Another` instead of owned-cockpit `Select`; the starter Bruiser cockpit is `$15000`; and fresh session profiles no longer receive `bruiser_01` for free.
- Dealership / Customisation Split Phase 2 was run by the user and reported working as intended on 2026-07-04. It adds a separate customisation trigger zone, an isolated zone client, a `SelectVehicleInstance` server action, and a customisation-mode cockpit grid that shows owned cockpits before opening the existing Customise stage.
- Dealership / Customisation Split Phase 3 was installed, initially exposed a nil call inside `V90_vehicleSummaries`, then was repaired by `scripts/roblox_dealership_customisation_split_phase3_vehicle_summary_repair.lua`. The customisation-mode grid now shows one card per owned vehicle instance, removes `Owned xN` card text, adds per-vehicle tier/index labels such as `A 920`, and sends `VehicleId` to `SelectVehicleInstance`.
- Dealership / Customisation Split Phase 4 was run and reported looking good by the user on 2026-07-04. It repairs Phase 3's rough rating fallback, adds a colour-coded tier badge to owned vehicle cards, and makes the customisation-zone action open Build Modules / ModuleShop instead of the final Customise screen.
- Dealership / Customisation Split Phase 5 was run after the catalogue-anchor repair and the user reported the fix worked, but the cockpit image still showed the fallback pink bar in the dealership UI. Phase 6 adds live cockpit-model image lookup, configurable square thumbnail/card values under `ReplicatedStorage.NeoTokyoRacers.Config.UI.CockpitMenuCards`, and removes the duplicate right-panel rating above `Customise`. Phase 7 adds 4-column desktop / 3-column mobile grid sizing. Phase 8 made cards fit around image/text/cost and added inline tier/rating. Phase 9 moved the rating into a top-right image badge, corrected dealership base ratings, and restored the free-roam car icon to the configured plain icon by default. Phase 10 is prepared in Git as `scripts/roblox_dealership_customisation_split_phase10_responsive_layout_polish.lua`; it narrows mobile stats, shortens the exit frame, removes the Categories heading, improves stat bar values, and tunes desktop/mobile card text separately.
- Free Roam Map Stack Phase 2 is generated in Git as `scripts/roblox_freeroam_map_stack_phase2.lua`. It supersedes the Phase 1 left rail with an isolated top-right stack: square map placeholder, full-width car button, and 2x2 Shop/Race/Home/Settings buttons. The Phase 2.11 repair preserves uploaded icon IDs, keeps icons as square centred overlays, makes the desktop/laptop stack about 20% smaller, makes the car row match the other button row height/icon scale, layers stack buttons as dark base plus pink outline plus inset configurable gradient plus hover overlay plus icon, preserves existing `FreeRoamNav` Bool/Number/Color tuning values on rerun, removes old button bevel bars, keeps pop-out action buttons as solid fills without gradients, adds local free-roam text-glow controls, makes action pop-outs match the full stack height, shrinks/tucks the mobile layout into the top-right corner, patches the isolated mobile drive controls for stable visibility, bottom-centred boost/MPH, lower edge placement, shared UI theme colours, gas-pedal alignment with the map stack right edge, shows the stack during normal on-foot and driving free roam, hides it while dealership/customisation UI is open, and repairs the mobile desktop-HUD helper block if Phase 2.4 removed it. It keeps using `ReplicatedStorage.NeoTokyoRacers.Config.UI.FreeRoamNav`, suppresses the old top-right drive exit menu and old `NTR_GarageAccessUI` toggle, and needs Studio install/client smoke before being treated as stable. The actual minimap image/calibration/movement is intentionally deferred. Free Roam Car Menu Phase 3 (`scripts/roblox_freeroam_car_menu_phase3_owned_cockpit_cards.lua`) updates only the isolated car pop-out to show owned cockpit cards with tier/rating badges and a fixed `Despawn` button. Phase 4 (`scripts/roblox_freeroam_car_menu_phase4_card_scaling_sort_future_spawn.lua`) enlarges images, changes the car pop-out to 3 desktop columns / 2 mobile columns, sorts by rating descending, removes badge glow/blue outlines, and adds card attributes for a later spawn/select phase. Phase 5 (`scripts/roblox_freeroam_car_menu_phase5_image_fit_border_padding.lua`) supersedes the image zoom with fitted images plus fixed inner padding, normalizes borders, and aligns `Despawn` bottom padding. Phase 6 (`scripts/roblox_freeroam_car_menu_phase6_card_surface_root_fix.lua`) separates the transparent grid click cell from the visible `CardSurface`. Phase 7 (`scripts/roblox_freeroam_car_menu_phase7_borderless_card_frames_compact_width.lua`) removes `UIStroke` from those cockpit-card/image borders entirely and uses filled border frames, while narrowing the pop-out to fit 3 compact desktop cards or 2 mobile cards. Broader dealership/customisation text glow should be handled as a separate shared UI theme phase.

Recommended baseline:

- Server action owner baseline: `GarageActionController_Shadow_Disabled` is currently the tested live owner after Phase 20. Phase 21-25 deliberately avoid patching that controller and install private garage interior/display/visit/customisation runtime beside it.
- Dealership intro baseline: Phases 1-7 are the current tested startup/customisation flow. Phase 8 is prepared but needs Studio/DataStore play-test confirmation before treating it as stable. Use `docs/dealership-intro-flow-2026-06-03.md` before changing garage startup, preview timing, desk open behavior, objective persistence, or final vehicle spawn placement.
- Use `scripts/roblox_hover_racing_v74_pre_v72_camera_assist.lua` if camera stability is the priority.
- Use `scripts/roblox_hover_racing_v75_boost_delay_hover_wobble.lua` as the latest generated patch, then play-test fresh in Roblox Studio.
- Use `scripts/roblox_driving_speed_sensitive_steering_curve.lua` after confirming the active `DrivingControllerV47` should build on the current V75-style source. It is guarded and will stop if the expected turn-rate block is not present.
- Use `scripts/roblox_driving_accel_brake_pitch_tilt.lua` to add front/back pitch response for acceleration, braking, reversing, and boost. It is guarded and will stop if the expected align block is not present.
- Use `scripts/roblox_driving_drift_boost_accel_gate_reverse_speed.lua` to require acceleration for drift-exit mini-boost and set configurable reverse speed to `40 MPH`. It is guarded and will stop if the expected drift/reverse blocks are not present.
- Use `scripts/roblox_driving_slope_hover_height_compensation.lua` to fix speed-dependent uphill/downhill hover height drift while preserving slope alignment, banking, brake/accel pitch, and wobble. It is guarded and will stop if the expected corner hover block is not present.
- Use `scripts/roblox_character_sprint_controller_install.lua` to install or roll back the on-foot sprint controller. It writes tuning attributes under `ReplicatedStorage.NeoTokyoRacers.Shared.Config.CharacterMovement_EditAttributes`.
- Use `scripts/roblox_mobile_drive_thumbstick_v2_visual_refinement.lua` on the installed V1 mobile controller as the next mobile UI test. It is a guarded exact-source follow-up; refresh the Studio mirror after running it.
- Current mobile controls baseline is `scripts/roblox_mobile_drive_thumbstick_v2_4_large_edge_drift.lua`, installed and confirmed working.
- Current VFX baseline is the restored mirror source plus `scripts/roblox_vfx_mobile_delayed_attach_once.lua`, installed and confirmed working. Keep `scripts/roblox_vfx_restore_mirror_known_good_baseline.lua` as the rollback/recovery script for this VFX sequence.
- Do not reintroduce the removed VFX late-socket/rescan/rebuild repair ladder unless deliberately reproducing the failed experiment. That direction caused or failed to resolve RuntimeVFXController memory growth, 3-5 second cut-outs, frozen all-on VFX, or unparented Beam buildup.
- Use `scripts/roblox_dealership_remove_cockpit_module_slots_text.lua` to remove the overlapping "Module Slots" count text from the dealership cockpit selection panel. It is a guarded exact-source UI cleanup; refresh the Studio mirror after running and confirming it.
- Use `scripts/roblox_ui_shared_theme_back_exit_and_intro.lua` to add configurable `Back` and `Exit` colours to the shared UI theme and make the dealership intro objective use the same theme as the dealership UI. It is a guarded exact-source UI patch; refresh the Studio mirror after running and confirming it.
- Phase AK is installed. Keep its main installer and targeted repair scripts as recovery/history tools; do not rerun them on the confirmed live baseline unless the matching problem returns. These scripts use guarded source text replacement.
- Phases AL-AO are the confirmed current vehicle performance/customisation baseline. Do not rerun their installers on the working place unless recovering from a matching rollback. The Studio mirror was refreshed on 2026-06-30 at 17:37:33 and imported 65 scripts, including the confirmed mobile thumbstick, UI theme, lighting, and VFX delayed attach-once changes.
- If Phase 9 has been installed and the client reports `Out of local registers ... V75Driving`, run `scripts/roblox_persistence_phase9_register_limit_repair.lua`, then `scripts/roblox_persistence_phase9_register_limit_repair_audit.lua`, then restart Play and run `scripts/roblox_persistence_phase9_garage_property_menu_client_smoke.lua` from the CLIENT Command Bar.
- Use `scripts/roblox_persistence_phase10_garage_ui_layout_modal.lua` after the Phase 9 register repair to fix left-column stacking and add the garage property modal backdrop. Then run `scripts/roblox_persistence_phase10_garage_ui_layout_modal_audit.lua`, enter Play mode, and run `scripts/roblox_persistence_phase10_garage_ui_layout_modal_client_smoke.lua` from the CLIENT Command Bar.
- Use `scripts/roblox_persistence_phase11_garage_spaces_cockpit_only.lua` after Phase 10 if the Garage Spaces panel appears during customisation. Then run `scripts/roblox_persistence_phase11_garage_spaces_cockpit_only_audit.lua`, enter Play mode, and run `scripts/roblox_persistence_phase11_garage_spaces_cockpit_only_client_smoke.lua` from the CLIENT Command Bar.
- Use `scripts/roblox_persistence_phase12_garage_property_menu_controller_extract.lua` after Phase 11 to move the garage property menu rendering into a dedicated controller module. Then run `scripts/roblox_persistence_phase12_garage_property_menu_controller_audit.lua`, enter Play mode, and run `scripts/roblox_persistence_phase12_garage_property_menu_controller_client_smoke.lua` from the CLIENT Command Bar.
- Phase 13 is installed on the current Studio place. Do not rerun `scripts/roblox_persistence_phase13_garage_property_ownership.lua` or `scripts/roblox_persistence_phase13_garage_property_ownership_repair.lua` unless recovering from a matching rollback. To recheck it, enter Play mode and run `scripts/roblox_persistence_phase13_garage_property_ownership_client_smoke.lua` from the CLIENT Command Bar, then manually open the garage at the dealership desk to verify the `Buy More` gallery.
- Use `scripts/roblox_persistence_phase14_instance_inventory_bridge.lua` after Phase 13 to add the duplicate-capable instance inventory bridge. Then enter Play mode and run `scripts/roblox_persistence_phase14_instance_inventory_client_smoke.lua` from the CLIENT Command Bar. This is a guarded server-controller patch; if it aborts on a missing source anchor, refresh the Studio mirror before writing another Phase 14 patch.
- Use `scripts/roblox_persistence_phase15_duplicate_copy_ui.lua` after Phase 14 to expose duplicate cockpit/module copy controls in the current dealership UI. Then enter Play mode and run `scripts/roblox_persistence_phase15_duplicate_copy_ui_client_smoke.lua` from the CLIENT Command Bar. This is a guarded client-bootstrap patch; if it aborts on a missing source anchor, refresh the Studio mirror before writing another Phase 15 UI patch.
- Use `scripts/roblox_persistence_phase16_module_family_locks_and_sorting.lua` after Phase 15 to lock module purchases behind their source cockpit family, keep included starter modules as one attached set per cockpit instance, charge for extra Standard copies, and sort module cards owned-first with locked modules on the far right. Then enter Play mode and run `scripts/roblox_persistence_phase16_module_family_locks_and_sorting_client_smoke.lua` from the CLIENT Command Bar. This is a guarded server/client text patch; if it aborts on a missing source anchor, refresh the Studio mirror before writing another Phase 16 patch.
- Use `scripts/roblox_persistence_phase17_module_owned_buy_tabs.lua` after Phase 16 to split the module picker into `OWNED MODULES` and `BUY MODULES`, show owned copies as separate instance cards, remove copy wording, and enforce front/rear engine filtering. Then enter Play mode and run `scripts/roblox_persistence_phase17_module_owned_buy_tabs_client_smoke.lua` from the CLIENT Command Bar. This is a guarded server/client text patch; if it aborts on a missing source anchor, refresh the Studio mirror before writing another Phase 17 patch.
- If the first Phase 17 installer causes `Incomplete statement: expected assignment or a function call` in `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled`, run `scripts/roblox_persistence_phase17_owned_buy_tabs_repair_v3.lua` in Edit mode, restart Play, then rerun the Phase 17 client smoke. V3 supersedes the earlier v2 repair if the line 408 parse error remains.
- If the Phase 17 smoke reports all engines as rear, for example `Front=0, Rear=30`, run `scripts/roblox_persistence_phase17_front_rear_engine_metadata_repair.lua` in Edit mode. It makes front/rear engine identity explicit with `EnginePosition`, refreshes the catalog fields, and updates the Phase 17 slot checks to use that metadata.
- If the Phase 17 smoke passes but Play still reports the client bootstrap line 408 parse error, run `scripts/roblox_persistence_phase17_client_parse_line408_cleanup.lua` in Edit mode, then restart Play. If it says no dangling continuation lines were found, copy `ReplicatedStorage.NTR_DEBUG.COPY_THIS_LINE408_SOURCE_DUMP.Value` or the big Output copy block and paste it before another patch.
- If the line-408 dump shows a single stray `l` before `-- NTR_PERSISTENCE_PHASE17_MODULE_TABS_V4`, run `scripts/roblox_persistence_phase17_remove_orphan_l_line408.lua` in Edit mode, then restart Play.
- If Play enters the dealership and reports `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:3475: attempt to call a nil value`, run `scripts/roblox_persistence_phase17_client_slot_helpers_and_engine_metadata_repair.lua` in Edit mode. It restores the missing client `getCategory`/`sortedSlots`/`getSlot`/`getCockpit`/`getModule` helper block before `modulesForSlot` and reapplies explicit front/rear engine metadata to Bruiser engine assets.
- If cockpit paint/customisation opens but no colour controls show and Output reports `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:1928: attempt to call a nil value` inside `renderColourPicker`, run `scripts/roblox_persistence_phase17_colour_picker_color3_repair.lua` in Edit mode. It normalizes raw `Color3` values and persisted table-shaped colours before HSV conversion.
- If the colour picker still fails after that repair with a shifted line such as `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:1962: attempt to call a nil value`, run `scripts/roblox_persistence_phase17_colour_picker_slider_signal_repair.lua` in Edit mode. It keeps the same picker UI but guards the slider input signal hookups.
- If the same colour-picker failure persists after the slider signal repair, run `scripts/roblox_persistence_phase17_colour_picker_root_repair.lua` in Edit mode. It replaces the fragile drag-slider colour picker path with a simpler swatch plus RGB-step picker while preserving the same paint/customisation callbacks.
- If Play then reports `Out of local registers when trying to allocate mobileInputState`, run `scripts/roblox_persistence_phase17_colour_picker_register_limit_repair.lua` in Edit mode. It removes the bulky colour-picker repair locals and reinstalls the swatch/RGB-step picker through the existing `NTRPersistencePhase15` table to reduce top-level client register pressure.
- After the register-limit repair is stable, run `scripts/roblox_persistence_phase17_colour_picker_hsb_slider_restore.lua` to restore the original-style draggable H/S/B colour sliders with gradient tracks while keeping the register-safe table-backed helper structure.
- If Front Engine only shows the old `Engine V1` through `Engine V4` options, run `scripts/roblox_persistence_phase17_front_engine_family_and_module_card_text.lua` in Edit mode. It retires/hides those old flat front-engine templates, restores the Phase AK family-style front-engine catalogue, and updates module card text to top cockpit/variant, green middle price, and bottom `Owned xN`.
- If pressing Start Driving spawns a vehicle but it does not hover/drive and Output reports `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:<line>: attempt to call a nil value` inside `closeGarage`, run `scripts/roblox_persistence_phase17_close_garage_drive_handoff_repair.lua` in Edit mode. It makes `closeGarage()` tolerate missing optional colour-picker cleanup helpers and guards the final `SpawnVehicle -> closeGarage -> startDriving` handoff so UI cleanup cannot block the driving controller.
- If the bottom line of module option cards is clipped, or if the `OWNED MODULES` / `BUY MODULES` tab buttons still show owned-count sublabels, run `scripts/roblox_persistence_phase17_module_button_layout_polish.lua` in Edit mode. It gives module option cards more vertical room, centres the two tab labels, removes tab owned counts, and shows `Locked` instead of `Owned x0` on locked buy cards.
- If locked/buyable module card colours are inverted, BUY/LOCKED popups drift when scrolled, or Shift changes camera FOV while menus are open, run `scripts/roblox_persistence_phase17_module_card_colour_popup_sprint_guard.lua` in Edit mode. It makes buyable module cards lighter, locked cards darker with muted title/darker price, centres popups from rendered card position, hides popups on carousel scroll, and blocks sprint FOV while the garage UI is open.
- If BUY/LOCKED/EQUIP module popups still sit off-centre on scrolled module cards, or if players can still go back to the dealership from Paint Cockpit after buying/selecting a cockpit, run `scripts/roblox_persistence_phase17_module_popup_position_and_cockpit_back_lock.lua` in Edit mode. It repositions popups from rendered card coordinates again after layout settles, places them above the bottom module frame with a small gap, hides them on carousel scroll, and hides only the Paint Cockpit Back button while keeping Next available.
- If the Paint Cockpit Back button is fixed but BUY/LOCKED/EQUIP module popups are still left-offset, run `scripts/roblox_persistence_phase17_module_popup_screen_layer_repair.lua` in Edit mode. The fresh mirror showed the popup was still parented to the padded ModuleOptions panel; this repair moves it to a full-screen overlay layer and positions it from the selected card's screen coordinates above the bottom frame.
- If the screen-layer popup repair leaves a large vertical gap or the popup is still not centred over the selected module card, run `scripts/roblox_persistence_phase17_module_popup_card_anchor_repair.lua` in Edit mode. It adds an invisible top-centre anchor to each selected module card, positions the popup 6px above that anchor, and hides popups on Next, Back, and stage changes.
- If selecting/buying a cockpit leaves the dealership UI visible and does not open Paint Cockpit after the popup card-anchor repair, run `scripts/roblox_persistence_phase17_cockpit_paint_stage_scope_repair.lua` in Edit mode. It fixes `showStage()` so stage changes hide module popups inline instead of calling a helper that may be out of scope during cockpit selection.
- If Build Modules opens again but BUY/LOCKED/EQUIP popups still float high or stay off-centre, run `scripts/roblox_persistence_phase17_module_popup_card_child_repair.lua` in Edit mode. It makes the visible popup a direct child of the selected module card, positions it at the card's top-centre with a 6px gap, parks it back on a stable UI parent when hidden, and hides it before card rerenders, scrolling, Next, and Back.
- If the card-child popup repair centres BUY/LOCKED/EQUIP correctly but module cards spill outside the bottom UI frame into the cash/customise/back panels, run `scripts/roblox_persistence_phase17_module_carousel_clip_popup_overlay_repair.lua` in Edit mode. It restores clipping on the module carousel while moving only the popup to an overlay positioned from the selected card's rendered centre.
- If the carousel clipping works but BUY/LOCKED/EQUIP popups are offset again, use `scripts/roblox_persistence_phase17_module_popup_tracker_layer_repair.lua` in Edit mode as the preferred root fix. It keeps the module carousel clipped, parks the popup on a full-screen `UI.ModuleShop` overlay, and tracks the selected card's rendered centre every frame while the popup is visible.
- If the popup tracker-layer repair makes Play fail with `Out of local registers when trying to allocate V75Driving`, run `scripts/roblox_persistence_phase17_module_popup_tracker_register_repair.lua` in Edit mode. It keeps the same clipped-carousel/tracked-popup behavior but moves the popup helpers onto `NTRPersistencePhase15` instead of adding top-level local functions to the already-large client bootstrap.
- If entering Build Modules after Paint Cockpit reports `The Parent property of ModulePopup is locked, current parent: NULL`, run `scripts/roblox_persistence_phase17_module_popup_destroyed_instance_repair.lua` in Edit mode. It keeps the register-safe tracked-popup helper but validates and rebuilds `UI.ModulePopup` if a previous card-parented popup was destroyed by module-card pooling.
- Do not treat `scripts/roblox_persistence_phase17_module_popup_action_rail_repair.lua` as the desired Phase 17 UI baseline. It was installed as a stability workaround, but the user rejected it because BUY/LOCKED/EQUIP appears centred above the module frame instead of directly above the clicked module card. The card-tracked overlay repair restored carousel clipping but still left the popup offset/high in Studio; the diagnostic showed `dx=-47.3` and `gap=57.5`. Use `scripts/roblox_persistence_phase17_module_popup_anchor_target_repair.lua` next. It creates a selected-card invisible `ModulePopupAnchor`, positions the overlay from that anchor, and hides instead of clamping when the selected card is not sufficiently visible.
- If the source-marker audit says the anchor-target helper is installed but the diagnostic still shows `gap=57.5`, run `scripts/roblox_persistence_phase17_module_popup_pool_target_reset_repair.lua` in Edit mode. It clears stale popup-target attributes and old `ModulePopupAnchor` children from reused pooled buttons before the selected module card is tagged again.
- If `scripts/roblox_persistence_phase17_module_popup_runtime_anchor_diagnostic.lua` shows one correct target card and anchor but the popup centre/bottom still differs from that anchor, run `scripts/roblox_persistence_phase17_module_popup_absolute_correction_repair.lua` in Edit mode. It measures the popup's actual absolute centre/bottom after placement, probes the parent/UI scale, and nudges it onto the anchor. This supersedes the earlier version that could throw `Installer source text leaked into bootstrap unexpectedly` after partially installing.
- If Play then reports `GarageActionController_Shadow_Disabled` reached EOF while expecting a final `end`, run `scripts/roblox_persistence_phase17_garage_server_final_end_repair.lua` in Edit mode. It restores only the missing final `end` before `-- V56_CONSOLIDATED_ACTION_CONTROLLER_END`.
- If that exact-tail repair does not clear the EOF error, run `scripts/roblox_persistence_phase17_garage_server_force_final_end_repair.lua` in Edit mode. It line-scans for the final V56 marker, inserts one `end` before it when missing, and prints a tail dump.
- After the mirror refresh, the confirmed garage server EOF cause is a duplicated `V85_attachDefaultModuleInstancesToCurrentVehicle = function(profile)` header. Run `scripts/roblox_persistence_phase17_garage_server_duplicate_attach_header_repair.lua` in Edit mode to remove that exact duplicate header.
- If the Phase 17 client smoke then reports `GarageActionController_Shadow_Disabled:927: attempt to call a nil value`, run `scripts/roblox_persistence_phase17_garage_server_total_stats_repair.lua` in Edit mode. It restores the missing `V56_totalStats` helper used by the profile/performance summary.
- If Phase 17 smoke keeps surfacing later garage-server nil helper calls, such as `GarageActionController_Shadow_Disabled:1297: attempt to call a nil value`, use `scripts/roblox_persistence_phase17_garage_server_foundation_repair_and_audit.lua` in Edit mode. It restores the Phase 14 `V84_*` helper foundation before the Phase 16/17 `V85_attachDefaultModuleInstancesToCurrentVehicle` function, also restores `V56_totalStats` if missing, and audits the related helper set together.
- If the smoke still reports a shifted garage-server nil call such as `GarageActionController_Shadow_Disabled:1445: attempt to call a nil value`, run the read-only `scripts/roblox_persistence_phase17_garage_server_line1445_audit.lua` in Edit mode and paste the printed line-window dump. The live Studio line numbers have shifted after repairs, so this confirms the exact failing call before another patch.
- If that audit warns `missing V56_catalog definition`, run `scripts/roblox_persistence_phase17_garage_server_catalog_repair.lua` in Edit mode. It restores `V56_catalog` and its compact helper family, including the Phase 17 front/rear engine metadata fields used by the module picker.
- If the Phase 17 smoke still reports shifted garage-server nil calls on the `GetInitial` path, such as `GarageActionController_Shadow_Disabled:1639: attempt to call a nil value`, run `scripts/roblox_persistence_phase17_garage_server_startup_dependency_repair.lua` in Edit mode. It restores and audits the whole startup dependency chain together: module-type helpers, default/core module helpers, V84 instance helpers before V85 attach, catalog, total stats, and profile response helpers.

## Important Working Style

- Prefer small command-bar scripts that modify one system at a time.
- Do not create in-game backup copies unless explicitly asked. Roblox version history is the preferred backup.
- Avoid large fragile patches against the main client script when a ModuleScript replacement or config folder can solve it.
- Hard rule: treat `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.NeoTokyoRacersClient_Bootstrap_Shadow_Disabled` as already at Roblox's Luau local-register limit. Do not add new top-level local functions, local constants, or large feature blocks to this script. New UI/free-roam/customisation behavior should live in isolated controllers/modules, with only a tiny table-backed event bridge in the bootstrap if absolutely required.
- If a patch depends on a specific older script shape and may fail, say that before writing the script.
- Read `docs/12_continuous_improvement_workflow.md` for the current rules on implementation triage, `follow:` / `suggest:` chat modes, learning from each task, handling failed source anchors, condensing Studio scripts safely, and refreshing the mirror.
- If two or more source-anchor repairs fail in the same live script, stop and inspect the live source/mirror before creating another patch; prefer an isolated canonical replacement when practical.

## Quick Links

- Current mechanics index: `docs/current-mechanics.md`
- Prompt pack for ChatGPT/Codex workflows: `prompts/README.md`
- Architecture reorganisation plan: `docs/architecture-reorganisation-plan.md`
- Game overview: `docs/01_game_overview.md`
- Vehicle folders/assets: `docs/02_vehicle_folder_system.md`
- Driving mechanics: `docs/03_driving_mechanics.md`
- Customisation UI: `docs/04_customisation_ui.md`
- VFX system: `docs/05_vfx_system.md`
- Known issues: `docs/06_current_known_issues.md`
- Patch history: `docs/07_patch_history.md`
- Script source sync workflow: `docs/10_script_source_sync_workflow.md`
- Manual script copy map: `docs/11_manual_script_copy_map.md`
- Continuous improvement workflow: `docs/12_continuous_improvement_workflow.md`
- Script architecture review: `docs/script-architecture-review-2026-05-28.md`
- Phase 5 UI migration plan: `docs/phase5-ui-migration-plan-2026-05-28.md`
- Phase 7 shared UI helpers: `docs/phase7-shared-ui-helpers-2026-05-28.md`
- Phase 8 client UI controller scaffold: `docs/phase8-client-ui-controller-scaffold-2026-05-28.md`
- Phase 9 server services scaffold: `docs/phase9-server-services-scaffold-2026-05-28.md`
- Phase 10 runtime controller scaffold: `docs/phase10-runtime-controller-scaffold-2026-05-28.md`
- Phase 11 architecture readiness audit: `docs/phase11-architecture-readiness-audit-2026-05-28.md`
- Phase 12 server action snapshot: `docs/phase12-server-action-snapshot-2026-05-28.md`
- Phase 13 server parity harness: `docs/phase13-server-parity-harness-2026-05-28.md`
- Phase 14 server shadow action controller: `docs/phase14-server-shadow-action-controller-2026-05-28.md`
- Phase 15 server action owner switch: `docs/phase15-server-action-owner-switch-2026-05-28.md`
- Phase 16 runtime helper owner switch: `docs/phase16-runtime-helper-owner-switch-2026-05-28.md`
- Phase 17 driver seat owner switch: `docs/phase17-driver-seat-owner-switch-2026-05-28.md`
- Phase 18 LOD client owner switch: `docs/phase18-lod-client-owner-switch-2026-05-28.md`
- Phase 19 lighting service owner switch: `docs/phase19-lighting-service-owner-switch-2026-05-28.md`
- Phase 20 thrust preview owner switch: `docs/phase20-thrust-preview-owner-switch-2026-05-28.md`
- Phase 21 post-switch architecture audit: `docs/phase21-post-switch-architecture-audit-2026-05-28.md`
- Main client extraction plan: `docs/main-client-extraction-plan-2026-05-29.md`
- Main client Phase A core boundary: `docs/main-client-phaseA-core-boundary-2026-05-29.md`
- Main client Phase B preview/colour modules: `docs/main-client-phaseB-preview-colour-2026-05-29.md`
- Main client Phase C garage screen controllers: `docs/main-client-phaseC-garage-screens-2026-05-29.md`
- Main client Phase D owner switch: `docs/main-client-phaseD-owner-switch-2026-05-29.md`
- Main client Phase E post-switch audit: `docs/main-client-phaseE-post-switch-audit-2026-05-29.md`
- World Phase F city hierarchy and LOD migration: `docs/world-phaseF-city-hierarchy-lod-migration-2026-05-29.md`
- World Phase J Far LOD5 assets migration: `docs/world-phaseJ-far-lod5-assets-migration-2026-05-29.md`
- Architecture Phase K kit migration: `docs/architecture-phaseK-hover-kit-migration-2026-05-29.md`
- Architecture Phase L final kit removal: `docs/architecture-phaseL-final-hover-kit-removal-2026-05-29.md`
- Architecture Phase N runtime world path repair: `docs/architecture-phaseN-runtime-world-path-repair-2026-05-29.md`
- Architecture Phase P garage runtime startup repair: `docs/architecture-phaseP-garage-runtime-startup-repair-2026-06-02.md`
- Architecture Phase Q garage controller header repair: `docs/architecture-phaseQ-garage-controller-header-repair-2026-06-02.md`
- Lighting Phase R FogColor property repair: `docs/lighting-phaseR-fogcolor-property-repair-2026-06-02.md`
- VFX Phase AJ thrust preview root repair: `docs/vfx-phaseAJ-thrust-preview-root-repair-2026-06-05.md`
- Vehicle Phase AI cockpit light system removal: `docs/vehicle-phaseAI-cockpit-light-system-removal-2026-06-03.md`
- Dealership intro flow marker setup: `docs/dealership-intro-flow-2026-06-03.md`
- Character sprint controller handoff: `docs/character-sprint-controller-2026-06-04.md`
- Vehicle Phase AK Bruiser modular defaults: `docs/vehicle-phaseAK-bruiser-modular-defaults-2026-06-05.md`
- Vehicle Phase AL performance foundation: `docs/vehicle-phaseAL-performance-foundation-2026-06-08.md`
- Vehicle Phase AM runtime integration: `docs/vehicle-phaseAM-runtime-integration-2026-06-08.md`
- Vehicle Phase AN module upgrades: `docs/vehicle-phaseAN-module-upgrades-2026-06-08.md`
- Vehicle Phase AO upgrade UI: `docs/vehicle-phaseAO-upgrade-ui-2026-06-08.md`
- Persistence and garage profile plan: `docs/persistence-garage-profile-plan-2026-06-30.md`
- Cleanup Phase G full hierarchy audit: `docs/cleanup-phaseG-full-hierarchy-audit-2026-05-29.md`
- Cleanup Phase H legacy inactive deletion: `docs/cleanup-phaseH-delete-legacy-inactive-items-2026-05-29.md`
- Cleanup Phase I aggressive migration cleanup: `docs/cleanup-phaseI-aggressive-migration-cleanup-2026-05-29.md`
- Cleanup Phase M post kit migration audit: `docs/cleanup-phaseM-post-kit-migration-audit-2026-05-29.md`
- Cleanup Phase O stale metadata/report cleanup: `docs/cleanup-phaseO-stale-metadata-report-cleanup-2026-05-29.md`
- Compressed architecture roadmap: `docs/compressed-architecture-roadmap-2026-05-28.md`

## Diagrams

- `diagrams/vehicle_asset_system.svg`
- `diagrams/driving_runtime_system.svg`
