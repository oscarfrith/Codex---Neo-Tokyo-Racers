# Patch History

## 2026-07-26 - Shared responsive UI foundation V1.1 confirmed and handed off

- Confirmed V1 working from the user report and refreshed `21:09:56` mirror.
- Diagnosed mobile free-roam drift: its navigation buttons skipped the bevel helper and its generic panels/buttons retained `2 px` / `1.7 px` strokes, while desktop, garage and racing owners used separate hard-coded widths.
- Corrected the first V1.1 read-only preflight failure: `GarageExperienceController_Active` is intentionally disabled and superseded by `NTR_GARAGE_REPLACEMENT_BROWSER_V1_4`, so it is excluded rather than enabled or rewritten.
- Corrected the following read-only projection failure: the mobile bevel-helper splice consumed the newline between `end` and `local`, producing `endlocal`. The same installer now retains the delimiter newline; the failed run made no Studio changes.
- Advanced the same canonical installer to `NTR_SHARED_RESPONSIVE_UI_FOUNDATION_V1_1`; no patch ladder or blanket runtime scan was added.
- Added shared structural (`1.2` desktop / `1` mobile), emphasis (`1.5` / `1.25`) and glow (`3` / `2`) tokens plus one idempotent neutral bevel applicator.
- Routed common desktop/mobile free-roam surfaces and the enabled shared racing/garage renderers through those tokens. Cash remains dark blue/blue; selected, destructive and tutorial semantics retain cyan, red and gold.
- Preserved vehicle-picker geometry/state/actions, driving controls, transactions, persistence, notifications and all existing responsive/state contracts.
- The user confirmed the result working. The `21:28:23` mirror has 189 matching source/checksum/manifest entries and all 18 V1.1 source owners.
- Recorded Studio's mixed persistence outcome accurately: optional new Theme nodes and revision attributes reverted, while the complete V1.1 source graph persisted and uses identical audited defaults. The installer audit now warns on that optional hierarchy state.

## 2026-07-26 - Shared responsive UI foundation V1 generated

- Corrected the first read-only preflight failure: `RaceClient_Active` is the intentionally disabled Phase 2 HUD retired by the Phase 3/16 presentation stack, so the canonical installer no longer targets or requires it. The failed run made no Studio changes.
- Corrected the second read-only projection failure by replacing the three-match generic `return M` anchor with the unique final module-tail anchor. The failed run also made no Studio changes.
- Added one canonical High-Risk installer: `scripts/roblox_shared_responsive_ui_foundation_v1.lua`.
- Added `ResponsiveUIFoundation` as the single owner for desktop/mobile corner scaling, compact money, economy response projection, replicated Cash binding, shared confirmation lifecycle and bounded top-notification layout.
- Extended `UITheme`, `UIFactory`, `RacingUIComponents` and garage replacement renderers instead of copying page coordinates or scanning every `UICorner`.
- Routed canonical garage/browser Cash and Spaces through one bold metric renderer; existing authoritative `Profile` and `ManagementState` responses remain immediate, with event-driven `leaderstats.Cash` reconciliation and no two-second polling loop.
- Routed desktop/mobile free-roam Cash, confirmation and corner construction through shared contracts. Mobile Cash retains dark blue and replaces its inherited pink outline with blue.
- Routed active racing shared/direct corner construction through the semantic token. The register-limited legacy bootstrap, mobile drive controls, world/intro prompts and vehicle-picker composition are explicit non-owners/exclusions.
- Replaced the shared top-notification owner with a borderless grey-gradient, white-text, responsive safe-area stack bounded to three cards.
- V1 was subsequently installed and mirrored at `21:09:56`, then advanced to the confirmed V1.1 handoff above.

## 2026-07-26 - Presentation audio V1.3.2 confirmed and handed off

- The user confirmed immediate presentation playback working and requested handoff after the V1.3.2 install/audit.
- Verified the complete `20:46:20` mirror: 188 entries match across exported scripts, source manifest and checksums; the V1.3.2 installer revision, immediate-one-shot controller source and described preload controls are present.
- Locked asynchronous warmup at a 24-asset cap and retained the existing eight-voice pool. Ignition/Idle timing and all cue semantics remain unchanged.
- Closed the intermittent UI/race latency issue. Cold-start, alternating UI/race, landscape-mobile and bounded-pool checks remain release regression rather than an open implementation gate.
- The canonical installer remains exact-scope recovery/config evidence. No further Presentation Audio Studio command is pending.

## 2026-07-26 - Customisation refinement V1.1 confirmed and handed off

- The user confirmed the corrective result working and requested handoff.
- Locked zero-vehicle rejection before loading, exact shared top copy, `UI.PurchaseRejected`, same-session sandbox purchase access, deterministic stale-selection repair and full-session objective suppression.
- Locked physical module-instance colour completion as the canonical saved/spawn projection while preserving explicit colours, Neon, upgrades, protected lights, Cash, ownership, transactions and ProfileService.
- Verified the refreshed `20:46:20` mirror: 188 entries match across checksums, source manifest and exported scripts; all seven V1.1 source owners and the shared notification hierarchy are present.
- The canonical installer remains exact-scope recovery/audit evidence. No further Customisation Studio command is pending.

## 2026-07-26 - Customisation refinement V1.1 owned-access/loading repair generated

- Confirmed V1 physical module colours and zero-vehicle notification from user testing; inspected the fresh `20:32:12` installed mirror.
- Diagnosed owned access failure at line 1427: `V102_ensureCustomisationAccess` captured no local `V56_profileForClient` because that function is declared later. The zero-vehicle branch returned before the bad call.
- Repaired the same canonical installer. V1.1 removes the unused profile response, upgrades installed V1 exactly, performs both entry checks before loading, and emits `UI.PurchaseRejected` only for the exact vehicle-required denial.
- Studio sandbox remains unchanged: it resets at Play start and suppresses saving, but same-session purchases remain authoritative.
- Studio V1.1 installation, focused verification, final audit and mirror refresh remain pending.

## 2026-07-26 - Presentation audio V1.3.2 immediate one-shot repair generated

- Diagnosed intermittent UI click/checkpoint latency from the confirmed mirror: the bounded presentation pool reused any idle `Sound`, replaced its `SoundId` and immediately requested playback, allowing uncached assets to start late.
- Advanced the same canonical installer to V1.3.2. The controller now asynchronously warms configured UI, objective and racing one-shots and preferentially reuses an idle voice already associated with the requested cue/asset.
- Added described `PreloadOneShotsEnabled=true` and `PreloadOneShotAssetLimit=24` controls while retaining the existing maximum-eight active voice budget.
- Ignition/Idle readiness timing, preview loops, sound groups, cue semantics, remotes, UI/race owners, persistence and economy are unchanged. Studio installation, focused latency verification and mirror refresh remain pending.

## 2026-07-26 - Customisation access, onboarding visibility, and physical colours V1 generated

- Diagnosed the persistent grey physical-module fault at the saved module-instance/runtime projection boundary: new purchases can own an empty colour table, preview supplies display fallbacks, and spawned vehicles skip their fallback because the empty table is truthy.
- Generated one canonical Edit-mode installer that completes only missing instance colour channels, repairs stale owned-vehicle selection at the existing authority, gates all canonical Customisation routes, adds one shared top notification, and binds objective visibility to the full server session.
- Added exact-source preflight, compile-before-mutation, transactional rollback, idempotency, `AUDIT`, guarded `ROLLBACK`, and authoritative catalogue coverage for all current cockpits and eight module locations.
- Preserved the three workshops, explicit colours, Neon, upgrades, physical identity, ProfileService, protected lights, transactions, preview, bootstrap, and unrelated systems.
- Studio installation, runtime/device/profile verification, user confirmation, final audit, and full mirror refresh remain pending.

## 2026-07-26 - Presentation audio V1.3.1 confirmed and handed off

- The user confirmed the repaired local Ignition and retained presentation/garage cue behaviour working, then refreshed the full Studio mirror and requested handoff.
- The complete `2026-07-26 19:03:42` mirror contains 187 matching exported scripts/source-manifest/checksum entries, the V1.3.1 installer revision, the V5 confirmed-local-ignition marker and the corrected `Catalog.GetProfile(profileId)` call.
- Locked the live ignition tuning at enabled, `0.1` seconds post-ready delay, `0.07` seconds playback confirmation, three attempts, `0.1` seconds retry delay and `0.15` seconds Ignition-to-Idle lead.
- Closed the V1.3 missing-Ignition issue. Retained repeated spawn/destruction, same-car re-entry, two-client 3D and landscape-mobile coverage as release regression rather than an open implementation gate.
- The one canonical presentation-audio installer remains exact-scope recovery/config evidence; no Studio command is pending for ordinary use.

## 2026-07-26 - Presentation audio V1.3.1 confirmed-playback ignition repair generated

- Inspected the fresh `16:26:41` installed-V1.3 mirror after the user reported working garage cues but no local Ignition.
- Found the exact runtime defect: the new coordinator called `VehicleAudioCatalog.Get`, which does not exist; the canonical API is `GetProfile`. Because the coordinator runs after loop targets update, the fault explains delayed Idle with no ignition request.
- Repaired the same canonical installer rather than creating a patch ladder. Its installed-V1.3 projection replaces the isolated coordinator block, corrects profile lookup and retains the existing graph-safe local lane.
- Added playback confirmation through `AudioPlayer.IsPlaying`, three bounded attempts by default, retry/confirmation tuning, one observable exhaustion warning and a short Ignition-to-Idle lead. A failed asset releases loops instead of leaving local engine audio held.
- Preserved V1.3 semantic garage cues, remote 3D ignition, server state, parked re-entry, driving, VFX, UI, persistence and economy. Studio INSTALL/AUDIT, focused runtime verification and mirror refresh remain pending.

## 2026-07-26 - Presentation audio V1.3 semantic owned-garage cues and reliable local ignition generated

- Advanced the one canonical presentation-audio installer from the user-confirmed/mirrored V1.2 baseline.
- Added separately configured decoration purchase/equip and structure purchase/equip success cues at the existing authoritative owned-garage result boundary. Purchase-and-place/equip emits one purchase cue; successful display assignment reuses the confirmed vehicle-purchase cue.
- Diagnosed local Ignition firing while loading audio is ducked and then being destroyed with the External-to-Internal graph rebuild. Added one local coordinator that prepares the profile Ignition asset, waits for seating/loading/internal-route readiness and plays through a persistent vehicle-bus lane outside the graph.
- Preserved remote 3D ignition, vehicle loops, server semantic state, parked-running re-entry, VFX, driving, UI rendering, persistence and economy. Added no remote, emitter, saved field or continuous city scan.
- Added six described vehicle-global ignition controls and four described presentation cue folders with add-if-missing defaults. All projected sources are unique-anchor checked and compile before the rollback-safe transaction mutates Studio.
- Studio INSTALL/AUDIT, focused runtime verification and mirror refresh remain pending.

## 2026-07-26 - Presentation audio V1.2 unique vehicle-purchase cue generated

- Confirmed the `2026-07-26 14:39:49` mirror contains installed V1.1, both refinement markers and Presentation installer revision V1.1.
- Added described `Config.Audio.Presentation.UI.VehiclePurchaseSuccess`, blank and safely silent until its asset is assigned.
- Routed only successful authoritative `BuyCockpitInstance` results to the new cue. Rejected/busy/unavailable vehicle purchases retain `UI.PurchaseRejected`; module and other purchase routes retain their V1.1 semantics.
- Updated the canonical bridge module with the new semantic success mapping and added a guarded V1.1-to-V1.2 controller projection. Both current anchors occur exactly once and projected/new source compiles before mutation.
- Added no remote, persistence, economy, ownership, UI-layout or gameplay owner. Studio install, focused purchase verification and mirror refresh remain pending.

## 2026-07-26 - Presentation audio V1.1 module-purchase and onboarding-hover refinement installed/mirrored

- Confirmed the refreshed `2026-07-26 13:36:41` mirror contains the complete working V1 installation and all eight source markers.
- Repaired the same canonical installer so successful `BuyModuleInstance` results select `UI.ModuleEquipSuccess`, matching explicit module equip. Failure/busy/unavailable results remain `Purchase` and retain `UI.PurchaseRejected`.
- Applied the existing per-button override `UIAudioHoverCue=""` to the onboarding shades, advance catcher and `NEXT`. Mouse hover and controller focus are silent throughout the prompt; the unchanged visible `NEXT.Activated` path still plays the normal click and advances for mouse, touch and controller.
- Added installed-V1 and clean-baseline projection paths with two new V1.1 revision markers. All six guarded installed-source anchors occur once; every projected source compiles before mutation and the existing rollback remains intact.
- No controller, cue asset, config tuning, UI layout, objective state, remote, persistence, economy or gameplay owner changed. The `14:39:49` mirror now contains both V1.1 refinement markers and installer revision V1.1.

## 2026-07-26 - Presentation audio UI, preview, objective and racing V1 generated

- Added one canonical transactional installer for a local presentation-audio bridge, catalogue, controller and starter. It reuses `NTR_UI`, `NTR_Vehicle` and `NTR_GameplaySFX`, with a bounded eight-voice default pool and no new remote or gameplay owner.
- Added an event-driven global `GuiButton` binder for mouse hover, controller focus and cross-device `Activated`, including silent/cue override attributes and visibility/content filtering.
- Kept dealership/customisation Idle and thrust-colour Boost loops outside rebuildable preview clones. A stable profile seam, rebuild grace and crossfade preserve playback through car/module/paint changes and prepare future whole-vehicle sound packages without implementing purchase or persistence.
- Added guarded semantic hooks for authoritative garage outcomes and onboarding false-to-true objective completion. Existing server-time race/countdown/checkpoint/result events drive racing cues with semantic dedupe.
- Added described `Config.Audio.Presentation` global/section/cue controls. Blank asset IDs remain safe; preview loops can reuse existing profile layers.
- Checked all four exact source anchors as unique against the `2026-07-25 09:55:57` mirror. Studio install, audible verification and post-install mirror refresh remain pending.

## 2026-07-25 - Player onboarding V1.13 confirmed and handed off

- Inspected the refreshed V1.12 mirror after the first full new-player race test. The reset was authoritative: generic garage/racing snapshot conversion omitted `Onboarding`, and ProfileService reconciliation deleted the missing section. The next event command rebuilt empty seen-page state, reviving Objective 1 and previously dismissed prompts.
- Updated the same canonical installer so ProfileService retains its authoritative onboarding table across every generic snapshot import. No new persistence owner, field or migration was added.
- Excluded race-participant/run vehicles from the first-free-roam PC controls trigger and expanded the existing desktop objective card description region to two lines with `N/3` vertically aligned beside it. Mobile layout and V1.12 UI/VFX remain unchanged.
- The user confirmed the complete flow in a fresh four-client server test: race vehicles did not trigger controls, completed objectives and shortcuts stayed dismissed, and already-seen customisation prompts did not replay.
- The first cross-session attempt correctly exposed that `StudioVehicleSandboxEveryPlay=true` suppresses all profile saves. With both Studio test overrides disabled, the user repeated the test and confirmed onboarding completion and first-view prompts persisted into a second server session.
- Relaxed only the local recovery installer's audit so both deliberate boolean Studio modes are valid and printed. Installed runtime source is unchanged. The final live config needs one more full mirror refresh because the current snapshot still records the earlier sandbox value.

## 2026-07-24 - Player onboarding V1.12 textured chevron guide beam generated

- Replaced the primary moving-chevron presentation with an optional wrapped Beam texture layered above the existing tutorial-gold aura and core beams. The same renderer, destination lifecycle and cleanup owner remain authoritative.
- Added config for texture asset, speed, repeat length, width, transparency, brightness and Z offset. Numeric IDs and `rbxassetid://` URIs are accepted.
- Added `GuideTrailPartArrowsEnabled=false`. A valid texture avoids creating physical chevron Parts; an empty texture automatically retains them as a safe fallback because the repo contains no suitable uploaded chevron texture ID.
- Folded the uninstalled V1.11 pulse-amplitude and lower beam-origin work into V1.12. Only V1.12 needs to be run. The `23:16:44` mirror is current for confirmed V1.10 UI and pre-install for this renderer.

## 2026-07-24 - Player onboarding V1.11 configurable guide pulse and beam origin generated

- Traced the apparently inactive pulse to a fixed `0.08`-stud amplitude. `GuideTrailPulseSpeed` was changing frequency correctly, but the displacement was too small to see.
- Added `GuideTrailPulseAmplitude`, default `.4` studs, and retained `GuideTrailPulseSpeed` as the independent frequency control. A zero speed or amplitude produces a fixed trail.
- Added `GuideTrailBeamStartHeightOffset`, default `-1` stud relative to `HumanoidRootPart`. Beam start no longer inherits the higher `GuideTrailHeightOffset`; arrow height and destination beam-end height are unchanged.
- Preserved the confirmed V1.10 UI, lifecycle, endpoints, colour, persistence and single local guide owner. The repo mirror remains at V1.8 and must be refreshed after installation.

## 2026-07-24 - Player onboarding V1.10 content-fit mobile objective cards generated

- Preserved the user-confirmed V1.9 top-row anchor and reduced only the landscape-phone card height from 58 to 48 pixels, with a 46-pixel responsive minimum.
- Defined the phone description area as two calculated text lines. The progress label bottom now uses the same calculated second-line bottom, leaving one shared three-pixel margin beneath both.
- Added a transactional attribute migration for either installed V1.8 or V1.9 so existing 58/52 height values cannot silently defeat the new layout.
- The user confirmed the V1.10 UI looks good. Its live source has not yet been mirrored; V1.11 preserves it exactly.

## 2026-07-24 - Player onboarding V1.9 live top-row mobile objective anchor generated

- Confirmed the refreshed `2026-07-24 22:49:44` mirror contains installed V1.8. Its prompt positions and desktop typography worked; only the landscape-phone objective stack remained too low.
- Removed `GuiService:GetGuiInset()` from the landscape-phone objective Y calculation. The phone stack now follows the live free-roam Car/Race/Garage shortcut row plus a configurable clearance that matches the visible Roblox-control baseline, with a fixed fallback only when that semantic row is unavailable.
- Repacked the unchanged mobile text sizes into sequential label/title/description bounds. The title-to-description gap is one pixel and the description gains the lower card space required for its approved two-line copy; progress remains isolated at lower right.
- The user confirmed the live top-row placement working. The subsequent screenshot was not accompanied by another mirror refresh, so the repo mirror remains at V1.8; V1.10 preserves this confirmed V1.9 anchor.

## 2026-07-24 - Player onboarding V1.8 responsive top-bar, objective and callout layout generated

- Confirmed the refreshed `2026-07-24 22:23:11` mirror contains installed V1.7 before changing the same canonical installer.
- Split onboarding layout safety into physical screen-edge insets and an optional Roblox top-bar exclusion. Landscape-phone callouts no longer reserve the top-bar height across the full screen, fixing the shared low fallback that affected G4 and B2/B3/B4.
- Kept G4's left placement so its bubble can remain vertically centred beside the stats panel. Narrowed landscape-phone shortcut bubbles and reduced their target gap while retaining target-centred connectors and edge clamping.
- Increased every desktop objective text role by 1.5 times without changing desktop card dimensions. Kept landscape-phone text sizes unchanged, reduced phone card height/gaps and anchored the stack just below the Roblox controls with a live Boost-overlap check.
- V1.8 was installed successfully and is present in the refreshed `2026-07-24 22:49:44` mirror. Prompt positioning and desktop typography were confirmed working; V1.9 supersedes only its remaining low mobile-objective anchor.

## 2026-07-24 - Player onboarding V1.7 compact objective layout and Studio vehicle sandbox generated

- Repaired the same V1.7 installer after its first run stopped before mutation on a duplicate projected ProfileService marker. The legacy `..._V1` marker is a prefix of installed `..._V1_2`; V1.7 now handles V1.2 explicitly before legacy detection, preserving strict unique-anchor validation.
- Changed Objective 1's display title to `BUY AND CUSTOMISE A CAR` without changing its trusted purchase, driving and B2 completion boundary.
- Replaced shared heading-size lookup inside objective cards with explicit responsive onboarding sizes, reserved two title lines and gave `1/3`, `2/3`, `3/3` a non-truncating width.
- Moved each visual card inside a padded, non-clipping animation shell. The CanvasGroup still owns the approved slide/fade lifecycle, while its bounds no longer clip the shared panel stroke or glow.
- Added a hard Studio-only vehicle-testing sandbox inside authoritative ProfileService. It clears vehicle ownership and display references only in the loaded session, preserves garage properties/customisation, raises session cash to the configured test amount and suppresses every save path. No DataStore, production schema or secondary profile owner was added.
- V1.7 was installed successfully and is present in the refreshed `2026-07-24 22:23:11` mirror. Its presentation is the confirmed pre-install baseline for V1.8.

## 2026-07-24 - Player onboarding V1.6 objective lifecycle, scoped cards and original trail generated

- Replaced the single stage-swapped objective panel with state-owned objective cards. Objective 1 enters alone; after trusted purchase/driving and the non-navigating B2 acknowledgement it exits left and is destroyed, then Garage and Race enter together with a short stagger.
- Completed Garage/Race cards slide and fade left, are removed, and the remaining card reflows upward with configurable Quint/Quart timing. Menu/loading suppression only changes visibility and does not recreate cards or restart lifecycle animation.
- Reduced objective title and progress sizing, allowed two-line titles and changed progress to compact `1/3`, `2/3`, `3/3`.
- Changed G2, K1 and AA1 from full-scroller/root bounds to the union of visible canonical cards inside their semantic scroller.
- Replaced the simplified arrow-only renderer with the original dealership chevron geometry and aura/core beam presentation, recoloured through the shared tutorial-gold token and reused for the owned-garage desk.
- The refreshed mirror is current for installed V1.5. V1.6 Studio installation, focused Play verification and the post-install full mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1.5 semantic targets, objective card and shared guide trail generated

- Inspected the refreshed installed V1.4 mirror. The missing owned-garage prompts had one confirmed cause: the shared renderer creates `CanonicalGarageWorkspace`, but `OwnedGarageWorkspaceController` renames its instance `OwnedGarageCanonicalWorkspace` while onboarding filtered by the original name.
- Upgraded the same canonical installer to register every shared workspace with `TutorialWorkspace=true`, retain its `TutorialPageId`, and publish one `TutorialCardScroller` target. K1 now follows the bottom row as one responsive container instead of switching between a card union and the left rail.
- Reordered the derived objective stages and shortcut introductions to Vehicle, Garage, Race without changing saved fields or trusted completion boundaries. Replaced the objective card with number, uppercase title, contextual hint and `N / 3`.
- Added explicit left placement for mobile G4 and a smaller configurable shortcut-callout gap.
- Migrated the legacy dealership arrow concept to one reusable local `OnboardingGuideTrailRenderer`. The legacy objective/tether presentation is disabled; tutorial-yellow arrows lead to the dealership until purchase and to the nearest owned-garage management desk until management opens.
- Studio install, Edit-mode audit, PC/landscape-phone verification and post-install full mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1.4 shortcut-choice and overlay-space repair generated

- Inspected the complete `19:43:19` installed V1.3 mirror. Found an exact root cause in `GarageWorkspaceController`: a semantic `--` marker inside compressed code commented out the remainder of the generated-card selection/action line.
- Added one guarded block-comment repair in the same canonical installer and changed workspace discovery to scan all visible semantic roots for the requested page ID.
- Returned K1 to the left module-location rail and updated its owned-vehicle module-swap copy. O1 now uses the below-centred group placement.
- Changed B2/B4/B3 from real action steps to sequential Next introductions. They unlock their shortcuts without opening menus; Race and Garage can subsequently be completed in either order while authoritative gameplay milestones remain unchanged.
- Replaced camera-viewport layout bounds with the onboarding overlay's absolute origin/size for targets, dim tiles, callouts and safe clamping. Added overlay geometry invalidation to correct right-side landscape-phone drift and clipping.
- Allowed the objective while walking inside a garage, anchored beneath Private/Invite, and added explicit desktop/mobile car-menu suppression. Studio installation, Play verification and mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1.3 responsive semantic-target installer generated

- Inspected the complete refreshed `18:56:55/56` V1.2 mirror and retained the substantially more reliable pinned-page presentation rather than reverting or creating a second tutorial owner.
- Added renderer-owned `TutorialPageId` and `CanonicalGarageCardId` metadata to the existing shared workspace path. Add Modules, Paint Shop and owned-garage prompts now resolve from the actual current page and semantic card IDs.
- Repositioned bottom-card prompts above-centred and free-roam shortcuts below-centred with safe edge clamping. K1 now encloses the bottom module cards, AA1 only the display-space cards and `AB1` introduces Structure/Decorations/Lighting once across Build and Style.
- Removed S5/S6. B2/B3/B4/N6/X3 now leave the real target actionable, block every non-target region and advance only after the actual button is activated.
- Scaled only tutorial visual metrics and the objective through the nearest owning `UIScale`, with a bounded landscape-phone fallback, wrapped copy and a 44-pixel minimum Next target. Highlight geometry remains unscaled live absolute geometry. Portrait handling is intentionally omitted for the landscape-locked game.
- Objectives now yield to every dealership/customisation/race/garage/modal presentation, and the PC My Vehicles introduction waits until the existing Controls popup closes. Installer run, Play verification and mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1.2 presentation replacement generated

- Inspected the refreshed `18:11:16/17` mirror after V1.1 testing. The persisted objective state and single installed onboarding owner were correct; the flicker/reset came from continuous page discovery competing with the active step, while overlapping dim tiles and target-derived typography caused the visual inconsistencies.
- Replaced only the isolated onboarding client presentation inside the same canonical installer. Active pages and targets are pinned per step, geometry is accepted after two stable rendered frames, changes are event-driven and temporary renderer rebuilds no longer reset the sequence.
- Loading/start now explicitly gates the entire onboarding ScreenGui and retains the higher display layer. Tutorial presentation resumes after loading is inactive and two frames have rendered.
- Replaced overlapping shade regions with exact non-overlapping tiles plus outer overscan. Retained the live target rather than cloning it, preserving one interaction/render owner.
- Body and Next now share one responsive tutorial text size independent of the highlighted control. Shared gradient, glow, gold highlight/callout borders and measured callout fitting remain.
- Studio installation, focused cross-device verification and post-install mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1.1 objective and responsive-overlay repair generated

- Inspected the installed `17:23:58` mirror after the first Play test. `StudioReplayEveryPlay=true` was present, so saved ownership was not completing the replay objective; the completion contract itself only checked an occupied owned vehicle and did not require a purchase milestone.
- Repaired the same canonical installer so a successful authoritative `BuyCockpitInstance` and active owned-vehicle driving are independent milestones. Objective 1 now requires both, Studio resets both each Play and established production profiles retain an explicit vehicle-ownership migration.
- Studio replay now exercises the production shortcut gates: Car unlocks at Stage 2, Race is introduced at Stage 2 and Garage at Stage 3. Full-capacity test profiles must use the existing capacity purchase control before buying the replay vehicle.
- Replaced global target-name discovery with active-page-scoped resolution and full ancestor visibility checks. Added transient page-loss tolerance and immediate local seen-state closure so live UI rebuilding or server round-trip latency cannot restart a prompt at G1.
- Rebuilt the shared overlay presentation around `RacingUIComponents`: measured responsive copy, shared gradient/glow Next button, glowing panel/highlight borders, connector, safe callout placement and live target tracking.
- Rounded target geometry outward and added configurable shade overlap plus viewport overscan to remove seams and cover curved mobile edges. Added designer attributes for tutorial gold, highlight padding, dim strength, overlap, overscan, page grace, callout margin and text-size bounds.
- Installation, Edit-mode audit, desktop/iPhone/tablet geometry checks and post-install full mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1 preflight repair and Studio replay

- Confirmed the first installation attempt stopped before mutation on the guarded multiplayer race anchor.
- Refreshed the complete mirror at `17:14:28`; no onboarding source marker, object or attribute was present.
- Repaired the same canonical installer by replacing the whitespace-sensitive multiline race hook with one unique semantic driving-start call token. Time-trial, garage-management and desktop-controls hooks now use the same bounded token approach.
- Added `Config.Runtime.Onboarding_EditAttributes.StudioReplayEveryPlay`, default true. Studio uses fresh session-only onboarding state on every Test Play and never alters saved onboarding data; published servers retain ProfileService persistence.
- Replay mode leaves existing shortcuts available to prevent established/full-capacity test profiles from becoming unable to exercise Objective 1. Production locks remain unchanged.
- Installer rerun and runtime verification remain pending.

## 2026-07-24 - Player onboarding V1 generated

- Converted the approved mockups and final copy into one canonical Edit-mode installer.
- Added one isolated client overlay/objective owner and one isolated authoritative server service without adding to the register-limited bootstrap.
- Added ProfileService-owned saved objective/first-view state, with client requests restricted to reading state and marking allowlisted page IDs seen.
- Hooked objective completion to server-trusted active vehicle, actual race/time-trial start and accepted owned-garage management boundaries.
- Reused the existing PC Controls popup, disabled only the legacy objective text and preserved the dealership trail VFX.
- Added transactional preflight, source compilation, idempotency, audit and rollback. Studio install, cross-device/two-player verification and mirror refresh remain pending.

## 2026-07-24 - Player onboarding V1 design locked

- Locked the three-objective flow: first owned vehicle driven, first race/time trial entered, and first owned Garage Management entry.
- Separated objective progress, per-page first-view explanations and free-roam shortcut unlocks so completing an objective cannot suppress unseen menu guidance.
- Finalised the reduced prompt list and copy across dealership, vehicle customisation, race entry and owned-garage management, including reuse of the existing PC Controls popup and mobile-only drift/Boost highlights.
- Defined one shared semantic-target overlay with responsive target fitting, surrounding input blockers, automatic callout placement and same-click navigation activation.
- Preserved the dealership trail VFX and existing UI/gameplay/persistence owners. Implementation, mobile visual approval, Studio verification and mirror refresh remain pending; no Studio source or hierarchy changed.

## 2026-07-24 - Parked and exit-coasting external 3D audio V1 generated

- Added `scripts/roblox_audio_parked_vehicle_3d_v1.lua`, one guarded `INSTALL`/`AUDIT`/`DISABLE` Command Bar installer based on the fresh `11:51:34` mirror.
- Unoccupied runtime player vehicles now remain logically powered and reuse the existing external 3D graph. Exit coasting presents Coast plus reduced EngineLow/Idle; authoritative parking transitions to Idle plus reduced EngineLow.
- Suppressed EngineHigh, Acceleration, DriftLoop, BoostLoop and driver wind after exit. Re-entry returns to the existing non-positional driver route without adding ignition/shutdown chatter.
- Added ten described global tuning attributes while preserving every existing audio attribute and asset ID. No emitter, asset, remote, continuous update loop, driving/VFX/parking owner, persistence or purchase system was added.
- The installer unique-checks six guarded replacements across two source owners, compiles projected source before mutation, rolls back source/config/descriptions on failure and supports a non-destructive feature disable. Studio installation, runtime verification and mirror refresh remain pending.

## 2026-07-24 - Vehicle audio tuning/cue expansion confirmed and mirrored

- The user reported the tuning/cue system working and refreshed the full mirror at `11:51:34`.
- The mirror contains both V2 audio source markers, the populated/described configuration and enabled master audio state. The tuning/cue installer is now recovery-only; parked external audio starts from this baseline.

## 2026-07-26 - Multiplayer vehicle exit coasting V1.1 confirmed and handed off

- Recorded the user's confirmation that V1 worked well and that its complete `2026-07-24 11:23:06` Studio mirror is current.
- Advanced the same canonical installer so exits at or below `10 mph` retain immediate fixed parking, while faster exits preserve linear/angular momentum and use the existing parked-hover owner for bounded horizontal drag.
- Added a transient `NTR_ExitCoasting` lifecycle: character pass-through and prompt/re-entry lock while moving, then authoritative server parking below `8 mph` after `0.75` seconds or at a `6` second safety timeout.
- Preserved `RaceSessionAssetService_Active` ownership by refusing this free-roam exit path for active race participants. Added five editable tuning attributes and expanded the committed-state audit from 10 to 14 checks.
- The user confirmed the V1.1 behaviour working and requested handoff. The later `2026-07-26 19:03:42` full mirror contains 187 matching exported scripts/source-manifest/checksum entries, all four runtime lifecycle markers and the V1.1 tuning revision.
- Installation, audit, focused verification and mirror refresh are closed. V1.1 is now the confirmed rollback baseline; the canonical installer is retained as exact-scope recovery evidence.

## 2026-07-24 - Multiplayer vehicle VFX, collision, parking and exit V1 generated

- Traced opponent VFX loss to the runtime reading driving attributes written only by each driver's client, while the installed audio system already provides validated replicated semantic state.
- Generated one transactional installer that keeps semantic replication alive independently from audible playback, uses it only for remote VFX and preserves the existing race VFX gate.
- Added one isolated free-roam collision lifecycle with vehicle pass-through, `20/16 mph` character hysteresis and explicit isolation from active race participants/assets without replacing the race collision owner.
- Authoritative exit now validates seating, zeros/server-fixes the vehicle before unseating, moves the character to a configurable right-side offset and safely reverses the fixed state on re-entry.
- Added the High-Risk contract and two-client verification matrix in `docs/vehicle-multiplayer-vfx-collision-exit-v1.md`. The user later confirmed V1 working and refreshed the complete mirror at `2026-07-24 11:23:06`.

## 2026-07-23 - Customisation three-workshop flow V1 generated

- Analysed the confirmed customisation controller and found the requested screens already share one workspace, rail, cards, popup, upgrade budget, colour sliders, preview and server action owner. Chose a bounded navigation reorganisation instead of a replacement UI/system.
- Added Add Modules, Upgrade Modules and Paint Shop routes. Add preserves the existing flow; Upgrade uses fixed module locations and direct upgrade paths only; Paint uses special plus module targets, Paint/Neon action isolation and dynamic post-purchase colour channels.
- Preserved the existing meaning of Underglow as bulk installed-module Neon rather than introducing an unrequested underbody asset/schema. Added authoritative read-only `NeonAvailable` and non-negative `NeonPrice` catalogue fields so unsupported/owned/unaffordable states do not depend on client guesses; the existing purchase path now clamps the same authored value before charging.
- Added one canonical, transactional installer with unique full-boundary projection, pre-write/committed compilation, idempotency and rollback. Studio install, restart, cross-device Play matrix and mirror refresh remain pending.
- Repaired the first read-only preflight failure in the same installer: a client-only canonical marker had been required from the server controller. Preflight now checks three unique server-owned contracts; the failed run made no changes and needs no rollback.

## 2026-07-23 - Owned garage ClearNight V1.2 cached-prompt lifecycle generated

- Traced the no-Output immediate re-entry failure to cached interior reuse, not vehicle spawning or streaming: session setup disconnected all prompt callbacks, but display rendering rebound only prompts it had just created.
- Reworked the same canonical installer to replace the flat connection list with a prompt-keyed exactly-once registry. Existing Drive Out, foot-exit and management-desk prompts are rebound whenever a cached interior is configured or displays rerender, while any previous connection for that prompt is disconnected first.
- Preserved the 20-second unload policy, verified vehicle handoff, saved display state, UI, lighting, assets and lifecycle authority. A cooldown was rejected because it would only hide the broken reuse path.
- Added guarded unique-boundary projection, pre-write and committed-source compilation, transaction rollback and explicit cached/non-cached regression checks. Studio install, restart, Play verification and mirror refresh remain pending.

## 2026-07-23 - Owned garage ClearNight V1.1 and verified drive-out recovery generated

- Diagnosed the V1 event recursion to broad Lighting, child and effect listeners responding to the controller's own Sky/effect mutations; removed those feedback paths and made preset/Sky writes idempotent.
- Kept the existing city-cycle and local garage-presentation owners. Reapplication is now a bounded generation-tokened response to only session, config and published city-stage changes.
- Hardened the existing drive-out transaction so vehicle creation is followed by bounded exterior seat/position verification before session completion.
- Added compensating cleanup, display-state restoration, interior return and prompt recovery for incomplete handoffs. No new remote, lifecycle owner, saved field or schema was introduced.
- Updated the same canonical installer rather than adding a patch ladder. Studio restart, exact multi-vehicle transition regression and mirror refresh remain pending.

## 2026-07-23 - Owned garage lighting-channel and Decoration Style flow V1 generated

- Repaired the same canonical installer after its first read-only preflight incorrectly required a `Navigation.Save` child `StringValue`; the established icon contract uses a string attribute on the `Navigation` folder.
- Traced blue Option 1 housings and split Primary/Secondary response to stale direct metadata overriding the already-correct folder placement.
- Added a folder-first finish resolver with explicit `Fixed`/`Technical` protection and supported-light capability discovery. Saved per-preset colours and all existing authority boundaries remain unchanged.
- Added transactional cleanup of duplicated channel metadata only inside authoritative ServerStorage lighting option branches; no asset is moved, created or deleted and ZZZ is not read.
- Routed Style Decorations directly into the existing shared colour sliders and supplied the configured icon to Lighting SAVE. Installation, restart, desktop/mobile regression testing and mirror refresh remain pending.

## 2026-07-23 - Owned garage ClearNight environment V1 generated

- Confirmed the city day/night cycle remains owned by `LightingService_Active`, with the current stage published through `Lighting.NTR_LightingPreset`.
- Added one isolated client World controller that applies the existing shared ClearNight properties/effects/Sky only for the local player while inside an owned garage. It tracks global stage changes without modifying their attributes and reapplies the latest city preset on exit.
- Added three config attributes and no remote, server loop, saved field, UI owner, fixture mutation or global-cycle branch.

## 2026-07-23 - Owned garage mobile access HUD and world entries V1 generated

- Repaired the same canonical installer after its first preflight stopped before mutation: five interior replacements were missing the `interiorSource` argument. Replacement input types are now asserted so a future baseline mismatch produces a useful guarded-anchor error instead of a nil-concatenation error.
- Traced the oversized phone dropdown to compact access controls that still used unscaled pixel dimensions while the main garage UI used responsive scaling. The new touch contract scales the access buttons, dropdown, icons and labels together and exposes reference/minimum/maximum scale attributes.
- Confirmed the mirrored exterior hierarchy had only the two return spawns. The bounded installer adds one movable foot marker and one movable drive-in marker, each with a native tap/E/X prompt, and routes them into the existing browser and authoritative entry action.
- No new remote, teleport, vehicle lifecycle, profile, persistence or garage-selection owner is introduced.

## 2026-07-23 - Owned garage material icon sizing V1 generated

- Added a bounded `OwnedGarageIcons.Sizing.MaterialImageZoom` config, default `1.0` with a `0.2–1.5` range.
- Material cards now pass this value to the existing shared artwork renderer; shared card geometry and all non-material cards remain unchanged.
- The installer compiles before/after assignment, is idempotent and restores source/config metadata on failure. Studio verification and mirror refresh remain pending.

## 2026-07-22 - Owned garage Style UX V1 generated

- Added one transactional/idempotent installer for central Save/Colour/Material/Locked/All/material icons, shared shop-state pricing, All Structure/All Decorations finish editing, material-rail clearance and navigation placement.
- Added atomic server-derived bulk finish commands behind the existing ProfileService command seam with no new remote or saved schema.
- Replaced destructive full-folder physical redraws with in-place finish application, staged asset swaps, per-slot decoration updates and one client preview-cancel action.
- Added the High-Risk acceptance, verification and rollback contract in `docs/owned-garage-style-ux-v1.md`. Studio and Play verification remain pending.

## 2026-07-22 - Owned Garage central icon configuration V1.1 generated

- Accepted V1 as user-confirmed working; its Studio state still requires a full mirror refresh.
- Extended the same canonical installer with a typed `Sizing` config group rather than introducing a patch script or another UI owner. Structure and Decoration location icons now default independently to `1.0`, exactly twice the shared `0.5` navigation-card scale.
- Changed only the two stable location-card routes. Mode, Family, vehicle, listing, material, paint, access and browser sizing remain owned by their existing shared renderers.
- Added dual-baseline projection, compile-before-write, idempotency and rollback so the installer supports current live V1 and the older pre-V1 mirror. Studio/mobile verification and mirror refresh remain pending.

## 2026-07-22 - Owned Garage central icon configuration V1 generated

- Audited the confirmed V2.2 mirror and found no `OwnedGarageIcons` hierarchy. Root/family icons currently fall back to shared NavigationIcons, while all Structure locations and all Decoration locations reuse one family icon.
- Added one canonical guarded installer creating 30 designer String attributes across nine grouped folders and connecting four existing UI consumers. Stable Structure section IDs and Decoration slot IDs select their individual images.
- Preserved current shared icons and glyphs as fallbacks, including blank optional location, Access, Browser, Capacity and Install Asset values. No saved state, server, economy, remote, physical asset or gameplay lifecycle changed.
- Added a complete designer map and verification/rollback instructions in `docs/owned-garage-icon-configuration.md`. Studio install and mirror refresh remain pending.

## 2026-07-22 - Owned Garage Phase 14 V2.2 responsive navigation closure confirmed

- Accepted V2.1 as user-confirmed and verified its revision in both fresh mirror areas before advancing the same canonical installer.
- Changed only the existing owned workspace controller. Structure and Decoration pages now share stable family rail keys, allowing the existing shared scroll-memory owner to preserve long-list position across nested Build/Style pages.
- Added a shared-card `INSTALL ASSET` route for empty Decoration locations in Style Garage. It opens Build on the same target but performs no mutation, preserving Build-only purchase/equip and Style-only finish editing.
- Updated the read-only audit for stable rail keys, empty-location routing and the full prior lighting/Build/Style/card-parity contracts.
- User confirmed the complete V2.2 flow working. The `21:37:10/11` full export contains the V2.2 source marker, navigation contract `3`, matching manifests/hierarchy and current checksums. Phase 14 and the approved owned-garage overhaul are closed.

## 2026-07-22 - Owned Garage Phase 14 V2.1 shared category-card parity confirmed

- Traced the screenshot mismatch to image zoom rather than a duplicated card implementation: bottom and sidebar navigation already call the same shared `ModuleCategoryCard` renderer.
- Advanced the same canonical installer with one guarded client-source refinement and one config attribute. Sidebar mode/location cards and image-backed bottom navigation cards now consume `OwnedGarageCategoryCardImageZoom=.5` through the same helper.
- Left vehicle/listing/material/paint cards and all Build/Style behaviour untouched. Updated the restart audit for shared-renderer, shared-consumer and tuning-value evidence.
- User confirmed the refinement working and refreshed the full mirror; exported source and hierarchy both contain the V2.1 marker.

## 2026-07-22 - Owned Garage Phase 14 V2 shared Build/Style composition generated

- Confirmed the refreshed mirror contains the user-approved V1 lighting/state foundation and four whole-garage option templates, then advanced the same canonical installer rather than adding a patch script.
- Reworked only the existing owned workspace controller into `DISPLAY CARS`, `BUILD GARAGE` and `STYLE GARAGE`; all server, profile, economy, asset and preview authority remains unchanged.
- Reused the shared category/listing/action/paint/channel-tab renderers. One left rail swaps between the three modes and Structure/Decoration locations, avoiding stacked sidebars and preserving mobile scrolling.
- Build now previews and exposes only CURRENT/EQUIP/BUY, with authoritative purchase auto-equip. Style exposes only equipped Structure Colour/Material, Decoration Colour and whole-garage Lighting colours. SAVE retains its editor and Back cancels previews.
- Updated the committed-state audit for V2 route, single-rail, auto-equip and Style isolation contracts. Studio install/audit, desktop/mobile navigation and regression checks remain pending.

## 2026-07-22 - Owned Garage Phase 14 V1 whole-garage lighting and atomic drafts generated

- Repaired an audit-only Lua multiple-return defect before committed-state evidence was accepted. The final `assert(instance, message)` in the source table also returned its message as a sixth array item, causing line 24 to call `find` on nil. Parenthesised assertions now force one value, cardinality is fixed at five and every entry is explicitly validated as a non-empty `LuaSourceContainer`. No installer or Studio state change is required.
- Added the canonical Phase 14 installer and restart audit against the user-confirmed Phase 13 V1.4 baseline. Five existing source owners are projected and compiled before assignment; no new state, UI, remote or geometry owner is introduced.
- Replaced the repeated three-slot lighting runtime with one TemplateOrigin option model. Four editable Primary/Secondary/Fixed/Technical templates are baked from the confirmed fixture placements, while the legacy `StandardFixture` remains untouched.
- Added per-preset lighting finish overrides that preserve authored defaults, server-derived channel availability and inherited colour application for neon parts plus SurfaceLight/PointLight/SpotLight descendants.
- Fixed cross-channel preview loss for structure, decoration and lighting through complete client drafts plus same-target server preview merging. SAVE now remains on Structure Colour, Structure Material, Decoration Colour and Lighting Colour pages.
- Preserved revisioned ProfileService mutations, cash checks, purchase auto-equip, owned Equip, bounded shadowless dynamic lights, Back/Exit cancellation, shared paint/channel tabs and the current responsive workspace.
- The approved Display Cars / Build Garage / Style Garage navigation remains the next internal stage after V1 install/audit/mobile/rejoin confirmation and mirror refresh.

## 2026-07-22 - Owned Garage Phase 13 V1.4 ServerStorage/shadow/district hardening generated

- Replaced the completed V1.3 recovery command with one compact canonical V1.4 installer against the confirmed V1.3 source baseline. It projects and compiles only `OwnedGarageFinishRuntime`, snapshots source/config state and supports attribute-only recovery if Team Create commits source before attributes.
- Removed every editing-library dependency from the production installer and committed-state audit. Both now preflight/audit only authoritative `ServerStorage.NeoTokyoRacers.OwnedGarage` templates, structure, decorations, collision and populated starter platform.
- Removed the runtime's forced shadow disable for cloned structure/decorations, preserving the authoritative source parts' authored `CastShadow`. Dynamic `Light.Shadows`, collision ownership, vehicle displays and lighting runtime remain unchanged.
- Configured the runtime interior district at `(7000,3200,0)` with four columns and `512 x 512` spacing. No profile schema, saved state, economy, remotes, UI, template/asset geometry or vehicle lifecycle changed.
- Updated the read-only audit for V1.4 source/config/district/shadow contracts and authoritative assets. Studio install/audit, two-player visibility, mobile profiling, four transition paths and a complete non-stale mirror refresh remain pending.

## 2026-07-22 - Owned Garage Phase 13 V1.3 platform attribute recovery repaired

- The refreshed mirror proved the eight V1.3 source owners committed successfully and both Platform Option 1 copies were populated/available with six matching parts. The separate read-only audit correctly rejected the new geometry because it had been added after scaffold normalisation and carried no `GarageColourChannel` metadata.
- Repaired the same canonical installer rather than adding a patch script. Its all-sources-committed path now performs zero Source assignments, derives colour/protection attributes from the existing folders on all twelve platform parts, stamps revision metadata, verifies persistence and restores every touched attribute on failure.
- The recovery does not move, clone, delete, create or re-pivot any asset. A full Studio restart, committed-state audit, Play verification and mirror refresh remain pending.

## 2026-07-22 - Owned Garage Phase 13 V1.3 authored defaults and shared material tabs generated

- Repaired the finish ownership contract at its source: catalogue presets no longer overwrite newly purchased/equipped asset colours or materials. ServerStorage models remain the authored default and persistence records only deliberate player overrides.
- Added a one-time declarative starter loadout for Street Workshop, Open Rack and Platform Option 1. `StarterItemId` is independent of `Required`, so initial equipment does not accidentally define whether an optional zone may be cleared.
- Replaced material display strings with ten stable internal definitions covering Concrete, Metal, Wood, Paint and Tiles A-F. Runtime resolution applies both `Enum.Material` and `BasePart.MaterialVariant`, filters unavailable definitions server-side and never allows material on Neon.
- Removed the intermediate structure-material channel page. `GarageWorkspaceController` now exposes one shared channel-tab renderer used by both colour and material pages; the owned controller renders material cards beneath Primary/Secondary/Detail tabs.
- Structure finishes are retained per StyleId, so changing styles no longer copies one asset's appearance onto another and returning to a customised style restores its own overrides.
- Updated the same canonical Phase 13 installer as a guarded eight-source V1.3 transaction with recoverable revision metadata, platform/material preflight, unique anchors, projected-source compilation, idempotent mixed-attribute recovery and all-source rollback. The current empty/unavailable Platform Option 1 mirror intentionally blocks installation until the user populates both asset copies.

## 2026-07-22 - Owned Garage Phase 13 V1.2 submission streaming/collision stabilisation generated

- Traced the new empty-air arrival against the fresh mirror and July 20-22 history. No garage assets were deleted: Phase 13 V1.1 preserved transforms and the current ServerStorage structure library is populated. The canonical owner teleports to a dynamically-created interior at `Y=3200` without a client streaming request, while the shared loading UI completes on the server response. The only previous streaming helper belongs to the disabled legacy garage flow.
- Confirmed a second regression in the installed V1.1 source: the shared finish clone makes every runtime structure part non-collidable while tagged template structure is hidden and disabled. The pre-V1.1 structure path preserved authored `CanCollide`, explaining why prior sessions could remain walkable even when streaming timing happened to succeed.
- Repaired the same canonical Phase 13 installer as V1.2 with an automatically staged two-run transaction. Run one projects, compiles and writes exactly `OwnedGarageManagementRuntime`, `OwnedGarageInteriorRuntime` and `OwnedGarageBrowserController`; after restart, the same unchanged command detects those committed markers and performs only hierarchy/config/attribute work. This avoids repeating the documented Team Create mixed-persistence failure.
- Added a request-scoped streaming acknowledgement over the existing `OwnedGarageEvent`. The server owns tokens, destinations, timeout, transition order and gameplay mutations; the client may only acknowledge whether the named interior/exterior marker became locally available after `LocalPlayer:RequestStreamAroundAsync`. Foot/vehicle entry and exit all fail closed before teleport/despawn/assignment clear/vehicle spawn.
- Added protected `CollisionShell` models beneath authoritative and ZZZ `Templates.StarterTwoBay`, derived from Option01's floor and authored collidable boundary parts. Collision parts are invisible, anchored, non-touchable, queryable, finish-protected and explicitly stripped of structure/surface/colour classification so cosmetic swaps cannot remove navigation collision.
- Expanded the committed-state audit to eleven source contracts, the streaming timeout/config contract, both collision shells, protected floor presence and ServerStorage/ZZZ parity. Studio execution, runtime/mobile/repetition evidence and mirror refresh remain pending; Phase 12 remains rollback.


## 2026-07-22 - Owned Garage Phase 13 V1.1 attribute/platform repair generated

- The refreshed `15:04:04` mirror proved the corrected installer entered another Studio mixed-persistence state: all eight V1.1 source contracts committed, while every companion attribute and created platform container disappeared. The template gate therefore remained `catalogue=2`, `config/template=1` and teleport still failed.
- Replaced the same canonical installer with an exact hybrid-state, attribute/hierarchy-only recovery. It compiles existing source read-only, performs zero Source assignments, snapshots every attribute, tracks only newly-created containers and asserts that every pre-existing BasePart keeps its parent/CFrame.
- Added one separate read-only committed-state audit for after a full Studio restart. It checks all eight source/revision contracts, config/API/schema versions, both template versions, asset attributes/channel protection, both platform roots and authoritative/editing model-count parity before Play testing.
- Repaired a pre-execution V1.1 Command Bar parse failure by expanding the asset audit and adding its missing outer-loop `end`; the failed paste made no Studio changes.
- The refreshed mirror proved that Phase 13 V1 source was installed but garage entry was rejected before teleport because the property catalogue required template contract `2` while the live `StarterTwoBay` template and runtime config still advertised `1`.
- Repaired the same canonical Phase 13 installer to align the template/config contract, normalise finish and identity attributes across both authoritative `ServerStorage.NeoTokyoRacers.OwnedGarage` assets and the user's `ReplicatedStorage.ZZZ` editing copies, and preserve every existing Instance, transform and duplicate display-pad part.
- Added an optional `DisplayPlatforms` decoration zone with three empty authoring models in each asset root. It reuses the decoration purchase/place/preview/persistence and shared Primary/Secondary/Detail/Neon colour system; the zone remains hidden until an option contains geometry and has `Available=true`.
- Existing structure/decoration assets remain `GaragePlacementMode=SlotLocal` so no geometry moves. The new display-platform options use `TemplateOrigin`, allowing their parts to be authored at final garage coordinates around the common `0,0,0` origin.
- Added capability filtering and authoritative availability validation, plus restoration/hiding of the template `DisplayPads` surface group when a platform decoration is removed/placed. The installer still preflights and compiles all projected source, is idempotent, and rolls back source, attributes and only newly-created containers on failure.
- Studio installation, teleport regression, platform population, PC/mobile finish verification and mirror refresh remain pending.

## 2026-07-22 - Vehicle audio tuning and conditioned cue expansion generated

- Added one transactional `INSTALL`/`AUDIT`/`DISABLE` Command Bar installer that upgrades the confirmed audio catalogue, client presentation, state contract and server cue replication owners while preserving every existing attribute and asset ID.
- Added managed accelerator enter/release cues with confirmation windows, minimum-session filtering, retrigger cooldown and one transient voice channel so rapid tapping cannot stack or chatter locally or for external listeners.
- Converted the existing DriftLoop target to a continuous-duration gain/pitch ramp and exposed package master, local/external/one-shot mix, speed breakpoints, pitch ranges, per-family fades and an external attenuation midpoint.
- Added a driver-only cancellable BoostRecharge one-shot sourced from the existing shared boost percentage, plus validated discrete external cues for BoostEmpty and uninterrupted FullBoostSpent. The latter replaces the ordinary empty cue by default.
- Kept `DrivingControllerV47`, bootstrap, physics, VFX, UI, persistence, economy and customisation unchanged. Added companion `Descriptions` folders beneath existing audio config/profile owners because Roblox attributes have no native description field.
- The installer uses guarded exact anchors against the known Phase 1 sources, compiles every projected source before mutation and rolls back source/config/documentation together on failure. Studio installation, audit, asset population, runtime/mobile evidence and mirror refresh remain pending.

## 2026-07-21 - Audio Phase 3 installed/audited and mirrored

- The user reported the Phase 3 audit passing with the intentionally blank readiness state and all playback disabled.
- Imported the `21:00:59` full mirror with 175 scripts. `AcousticsController` and `AcousticsRuntimeController_Active` exactly match the canonical installer; the Phase 3 revision, bounded quality attributes and owned `NTR_ContextAmbienceReverb` are present.
- The three approved implementation phases are now structurally installed and mirrored. No further architecture patch is recommended before assets are selected.
- Next work is config-only vehicle/context asset population, guarded ACTIVATE, then one-player, two-client, context-transition, repeated-lifecycle and low-end-mobile evidence. Purchasable profiles and saved selections remain deferred.

## 2026-07-21 - Audio Phase 2 installed/mirrored; Phase 3 acoustics generated

- The user reported the Phase 2 audit passing with four context profiles, zero populated tracks and disabled playback. The `20:48:08` mirror contains 173 scripts and exact copies of all three Phase 2 sources.
- Added one additive Phase 3 installer for Roblox-native vehicle occlusion/diffraction/reverberation, capped to the nearest four desktop or two small-touch emitters within 180 studs at 2 Hz.
- Added one owned, disabled `NTR_Ambience` reverb effect driven by the Phase 2 dealership/owned-garage context definitions. No per-wall emitter, reflection probe, manual raycast or world object is created.
- Added readiness counts plus guarded `ACTIVATE` and `DISABLE` modes. Activation requires all five critical standard-vehicle layers and at least two populated context definitions, and rolls back the three enable attributes on mutation failure.
- Phase 1/2 source, driving, UI, VFX, loading, garage, persistence and economy remain unchanged. Assets, activation and audible/mobile/multi-client evidence remain pending.

## 2026-07-21 - Audio Phase 1 installed/mirrored; Phase 2 context installer generated

- The user reported the Phase 1 Edit install/audit passing for six cockpit templates. The captured audit correctly reports `0/13` populated assets and `enabled=false`.
- Imported the `20:36:31` full mirror with 170 scripts and SoundService coverage. All six installed Phase 1 sources exactly match the canonical installer; six cockpit profile attributes and all six mix groups are present.
- Added one additive transactional Phase 2 installer for on-foot, driving, dealership-interior and owned-garage-interior music/ambience contexts. It uses existing session/seat attributes, two bounded local channels, crossfades, ordered/random playlists and sparse CollectionService zone registration.
- Phase 2 creates no world zones, audio assets, remotes or persistence. Blank tracks and `ContextAudioEnabled=false` keep installation silent; a tagged BasePart/Model with stable `AudioContextId` is sufficient for a future key area.
- Audible Phase 1/2, two-client, repeated-transition and mobile evidence remain pending approved assets and activation.

## 2026-07-21 - Audio system Phase 1 standard vehicle foundation generated

- Added one transactional Edit-mode installer with `INSTALL`, `AUDIT` and `DISABLE` modes. It creates a versioned semantic vehicle-audio contract, validated/rate-limited driver remote, server replication owner, folder-driven catalogue, advanced-audio bus bridge and isolated client runtime.
- Local drivers receive a non-positional graph; other vehicles receive distance-attenuated 3D emitters. Work is bounded to one local detailed graph, six nearest detailed remote graphs, six simple remote graphs, 15 Hz parameter updates, 4 Hz priority updates and eight concurrent one-shots per vehicle by default.
- Preserved the existing SoundGroups/loading mixer and all driving, bootstrap, UI, VFX, garage, persistence and economy source. Every cockpit inherits `GENERIC_STANDARD_AUDIO`; blank assets and `AudioSystemEnabled=false` make the unverified install safely silent.
- Kept future whole-vehicle sound packages behind stable profile IDs and a server-stamped `ResolvedAudioProfileId` without implementing purchase, apply or persistence paths.
- Extended the full Studio exporter to include `SoundService` and documented the Standard readiness contract, verification matrix, remaining contextual-audio/acoustics phases and rollback in `docs/16_audio_system.md`. Studio installation, audible assets, multi-client/mobile checks and mirror refresh remain pending.

## 2026-07-21 - Owned Garage Phase 13 typed fixture and finish installer generated

- Audited the refreshed Phase 9-12 property, structure, decoration, ProfileService-command, management and shared-workspace owners before planning the expansion.
- Added one canonical no-yield installer for five typed perimeter zones, fifteen editable blockout assets and a shared folder-driven Primary/Secondary/Detail/Neon finish contract across Decorations and Structure.
- Added isolated server capability inspection/application, deterministic table fingerprints, property-scoped decoration colours, structure Neon colour, material-lock support, dynamic channel projection and shared-slider preview/SAVE/cancel flows without adding a remote or UI owner.
- Preserved the desk prompt and existing garage lifecycle owners, retained legacy structure attributes as a compatibility fallback, and protected Fixed/Technical geometry. The installer preflights exact anchors, compiles projected sources and rolls source/hierarchy/config back together on failure.
- Documented the High-Risk acceptance, performance, persistence, mobile and rollback contract in `docs/owned-garage-phase13-typed-finishes.md`. Studio verification and mirror refresh remain pending.

## 2026-07-21 - Loading Phase 6 closure audit generated from fresh V1.3 mirror

- Imported the user's full Studio mirror generated at `10:48:31`. It contains 163 scripts, configured start-client checksum `303682342`, V1.4 loading-view checksum `215716094`, all responsive/grid config attributes and all Phase 1-4 plus race-readiness markers.
- Added one read-only Edit-mode audit covering source compilation/marker uniqueness, canonical hierarchy owners, responsive/grid config, complete six-tile artwork/fallback, SoundService groups and absence of persistent runtime UI/sound. It performs no writes, creates no Instances and executes no project module.
- Added the Phase 6 closure handoff with device, included/excluded transition, rejection, repetition and low-end performance checks. The current `1.2`-second minimum versus the earlier `1.5` target and blank music are explicit non-blocking warnings.
- Decided against `TeleportGui` for the current single-place in-place flows. Loading music and a second catalogue artwork remain optional post-closure work.
- The user subsequently reported everything working as expected. Phase 6 is closed and handed off; the live `1.2`-second minimum is accepted, blank music remains intentional, and no post-audit mirror refresh is required because the closure audit was read-only.

## 2026-07-21 - Loading start-button configured position V1.3 generated

- The user confirmed the retryable Grid3x2 V1.2 repair working well and requested lower PLAY/SHOP placement with config-driven, mobile-consistent tuning.
- Extended the existing config-only companion with `StartScreenButtonYScaleDesktop=0.82`, `StartScreenButtonYScaleLandscapePhone=0.84` and `StartScreenButtonYScalePortrait=0.80`. Existing values remain untouched; values are audited between `0.5` and `0.95`, and higher values move the menu down.
- Updated the same canonical loading installer to `NTR_LOADING_SYSTEM_PHASE5_START_BUTTON_POSITION_V1_3`. It accepts only known V1/V1.1/V1.2 start-client sources, preserves the installed V1.4 Grid3x2 view, and replaces only the isolated initial/start client. All device branches call one safe-area position helper which clamps the entire menu to an eight-pixel bottom inset; button sizes and orientations are unchanged.
- No loading runtime, artwork catalogue, transition, input, audio, racing, camera, server, persistence or economy owner changed. The user subsequently confirmed the resulting placement looking good. V1.3 is now the working Studio baseline; full device/transition closure and mirror refresh remain pending.

## 2026-07-21 - Loading Grid3x2 fetch-status V1.2 repair generated

- The user installed V1.1, restarted Studio and refreshed the mirror at `10:17:35`. The mirror contains all four `ReplicatedFirst.NTRLoading` sources, exact compact-button V1.1 and a `13,024`-byte start client, disproving a source-length failure. The two icon attributes are absent because V1.1 intentionally read optional keys without creating them.
- The mirror contains all six Grid3x2 IDs/coordinates and the single fallback. Source diagnosis found the resolution/black-screen cause: V1.3 of `LoadingScreenView` keeps the grid hidden, performs one `PreloadAsync`, immediately requires every hidden ImageLabel's `IsLoaded`, and never retries or reports a failed tile. The low-resolution view can therefore remain the single fallback; removing it exposes the black backing.
- Added config-only `scripts/roblox_ui_loading_start_screen_config.lua` to create both icon String attributes plus bounded grid attempts/retry/promotion timings without mixing config and source. It preserves existing values, audits types/ranges and rolls back only attributes it added.
- Updated the canonical installer to source-only replace the exact V1.3/V1.4 view with `NTR_LOADING_SYSTEM_PHASE1_SCREEN_VIEW_V1_4_GRID_FETCH_STATUS`. It records final per-tile fetch statuses, retries boundedly, renders the fetched grid behind the opaque fallback before promotion, waits boundedly for all six ImageLabels, and logs exact tile/status/ID failures while safely retaining the fallback. Catalogue, runtime, V1.1 start client, racing, input, audio, server and persistence owners remain unchanged.

## 2026-07-21 - Loading/start-screen Phase 5 V1.1 compact icon buttons generated

- The user confirmed Phase 5 V1 working well. Screenshot review showed that the generated title/subtitle duplicated branding already present in the artwork and that a landscape phone's wide viewport selected the oversized desktop button branch.
- Updated the same canonical installer to `NTR_LOADING_SYSTEM_PHASE5_INITIAL_START_SCREEN_V1_1_COMPACT_ICON_BUTTONS`. It accepts only the known V1/V1.1 client marker, replaces that one isolated source, removes both headings, lowers the button-only composition and preserves every confirmed loading/runtime/racing owner.
- Added touch plus short-side phone detection: compact horizontal `188x40` actions in landscape and compact vertical `190x40` actions in portrait, with separate tablet/narrow and wide metrics. Both shared-theme buttons now support centred left icons through optional `LoadingSystem.StartScreenPlayIconAssetId` and `StartScreenShopIconAssetId` String attributes; absent IDs consume no icon space.
- The installer deliberately does not write blank central attributes alongside source because the project has reproduced Studio's mixed source/attribute persistence fault. SHOP failure remains retryable without the removed description label by showing `SHOP - TRY AGAIN` and logging the detailed reason. Studio desktop/tablet/phone visual/action regression and refreshed `ReplicatedFirst` mirror remain pending.

## 2026-07-21 - Loading/start-screen Phase 5 generated

- The user confirmed the readiness-gated countdown working and refreshed the `09:20:03` full mirror; all four readiness markers are present. That gate is now the confirmed racing baseline.
- Updated the same canonical `scripts/roblox_ui_loading_and_start_screen_system.lua` to Phase 5 revision `NTR_LOADING_SYSTEM_PHASE5_INITIAL_START_SCREEN_V1`. It adds one early `ReplicatedFirst` client and leaves the confirmed catalogue/view/runtime sources unchanged. The client applies artwork eligibility and a long timeout only to its local replicated config while beginning the retained generation, then restores those values immediately.
- Initial progress waits boundedly for game/world/character readiness, then reveals a static responsive PLAY/SHOP composition inside the existing full-bleed/safe-area view. PLAY releases the existing fade; SHOP reuses the existing server-authoritative dealership teleport and remains retryable on rejection. Release restores the shared progress/status visibility and disconnects the responsive viewport listener before destroying the menu, preventing later loading-screen regressions. The start screen never writes camera or spawn state.
- The first generated design used an internal `task.wait()` between hierarchy/config and source stages, but project history proves that pattern crosses Studio's Command Bar history boundary. It was removed before handoff. The final installer is hierarchy-only, no-yield, compile/audit/idempotent and rollback-scoped, with no backup Instances or existing-source/config mutation. The full exporter now scans `ReplicatedFirst`, repairing the blind spot in the `09:20:03` mirror so the required post-test refresh can capture all four loading-package scripts. Studio first-join/long-idle/button/device/audio/input/later-transition verification remains pending.

## 2026-07-21 - Race staging readiness gate generated

- The user confirmed Loading Phase 4 working and refreshed the `09:02:33` mirror, then reported that the race started with almost no visible countdown. Source inspection confirmed both authoritative race services count down immediately after staging while the full-screen loading cover remains active.
- Added `scripts/roblox_racing_staging_readiness_gate.lua`, a guarded source-only transaction over the existing Time Trial server, Matchmaking server, race transition client and shared countdown presenter. It validates two client readiness phases per run and begins a server-time synchronized five-second countdown only after loading has fully faded for every active participant.
- Added bounded preparation/server timeouts, stale run/membership/phase rejection, idempotent acknowledgements, disconnect exclusion and degraded-preload diagnostics. Missing readiness cancels staging through existing cleanup rather than unlocking behind the cover or leaving vehicles frozen.
- No new remote/hierarchy owner, saved schema, economy, reward, PB, queue, checkpoint, reset, results or vehicle-construction path was added. Exact-anchor dry projection against the fresh mirror passes; Studio compile/install, Time Trial/two-client/disconnect/timeout/device regression and mirror refresh remain pending.

## 2026-07-21 - Loading/start-screen Phase 4 racing transitions generated

- The user confirmed Phase 3 working and refreshed the `08:39:10` mirror, which contains both owned-garage source markers and revision attributes. Phase 3 is now the rollback baseline.
- Updated the same canonical installer to revision `NTR_LOADING_SYSTEM_PHASE4_RACING_TRANSITIONS_V1`. `RaceTransitionClient_Active` remains the camera/session-transition owner and delegates Race Browser teleport, time-trial start, post-queue Race staging, active exit and results Exit to Start to the shared loading runtime.
- Multiplayer queue waiting remains visible and starts loading only at authoritative `RaceStaged`. Time-trial start covers immediately after the selected-vehicle action. Reset keeps the existing lightweight black fade; reset, retry/race-again, ordinary vehicle spawn/switch and non-relocating results remain excluded.
- No remote, server staging, timing, checkpoint, matchmaking, result, reward, persistence, economy or vehicle-lifecycle contract changed. Eleven guarded source anchors match the refreshed mirror exactly once; projected/final Studio compilation, transaction rollback and desktop/touch lifecycle verification remain required.
- The first Phase 4 install reached final source compilation but rolled back because Studio dropped the loading config `Revision` attribute, repeating the documented mixed source/attribute transaction fault. Repaired the same canonical installer as a no-yield source-only transaction: it read-only verifies the existing loading config, artwork and six foundation sources, writes only the four Racing sources, and uses their exact markers as Phase 4 revision evidence. No repair script or hierarchy recovery was added.

## 2026-07-21 - Loading/start-screen Phase 3 owned-garage transitions generated

- The user confirmed Phase 2 working and refreshed the `08:19:22` mirror, which contains its entrance and garage UI source markers plus scanned revision attributes. Phase 2 is now the rollback baseline.
- Updated the same canonical installer to revision `NTR_LOADING_SYSTEM_PHASE3_OWNED_GARAGE_TRANSITIONS_V1`. `OwnedGarageBrowserController` wraps selected-property entry, replacement-space confirmation and browser Return-to-City; its event-driven prompt listener wraps physical Foot Exit and Drive Out.
- `OwnedGarageManagementRuntime` retains all trusted prompt, teleport, saved assignment, compensation and vehicle lifecycle work. Its physical foot-exit callback now publishes success/failure through the existing owned-garage event, matching the existing drive-out result contract. No remote, schema, economy, geometry, management UI or visitor behaviour changed. Studio desktop/touch lifecycle, compensation, duplicate-safety and mirror verification remain pending.

## 2026-07-21 - Loading/start-screen Phase 2 entry/exit integration generated

- The user confirmed Phase 1 V1.3 motion looks good. The same canonical installer now targets revision `NTR_LOADING_SYSTEM_PHASE2_DEALERSHIP_CUSTOMISATION_ENTRY_EXIT_V1` and preserves all confirmed Phase 1 artwork, timing, progress, input, audio, HUD and teleport behaviour.
- Added guarded integrations to the canonical `GarageEntranceController_Active` and `ModuleShopUIController`. Dealership, on-foot customisation and drive-in entry begin before the existing session request and complete after profile/camera/UI readiness; browser Exit and Drive-to-free-roam complete after existing session/vehicle readiness.
- No remote, persistence, economy, vehicle constructor, camera owner or duplicated garage UI was added. Missing-core-module Drive rejection remains pre-loading; session/profile failures retain the current view. Drive returns the hidden character to the saved entrance under cover before invoking the existing exit-marker spawn, so a later spawn failure recovers to usable on-foot free roam. The user subsequently confirmed the complete Phase 2 flow working and refreshed the mirror.
- The first run stopped safely in preflight because it checked an older canonical-installer marker on `GarageEntranceController_Active`. The refreshed source and Studio error agree that the isolated live owner uses `NTR_GARAGE_NATIVE_ENTRANCE_PROMPTS_V1`; the same installer now checks that exact marker. No mutation or rollback recovery was required.

## 2026-07-21 - Loading/start-screen Phase 1 V1.3 motion tuning generated

- The user confirmed V1.2 Grid3x2 working and refreshed the `07:57:44` mirror with all six tile IDs populated. The existing motion path was active but its 15-second `Sine InOut` opening produced almost imperceptible movement during the 1.5-second minimum.
- Updated the same canonical installer to revision `NTR_LOADING_SYSTEM_PHASE1_DEALERSHIP_TELEPORT_V1_3_MOTION_TUNING`: five-second duration, `Sine Out`, `1.06→1.10` zoom and `1.2%` travel. Motion still targets the one common `ArtworkMotion` parent and remains disabled for the future start screen.
- No grid, fallback, timing, progress, audio, input, driving, teleport or HUD ownership changed. The user subsequently confirmed the motion looks good; the mirror still predates V1.3 and omits `ReplicatedFirst`.

## 2026-07-20 - Loading/start-screen Phase 1 V1.2 Grid3x2 generated

- The user confirmed V1.1 timing/progress working and refreshed the `15:49:06` mirror, then verified that the visible softness persisted with a high-quality 4K source independent of preloading and motion scale. The current Roblox 4K streaming path does not include `ImageLabel`, so the same canonical installer now provides a tiled UI workaround.
- Revision `NTR_LOADING_SYSTEM_PHASE1_DEALERSHIP_TELEPORT_V1_2_GRID3X2` adds six coordinate-owned tile config folders, catalogue readiness/signature data, a responsive 16:9 3x2 composite, common pan/zoom/fade ownership, one-pixel seam overlap and bounded warm pooling.
- The grid is revealed only after all six IDs are populated and all six images load; otherwise the confirmed single image remains visible atomically. No driving, audio, teleport, HUD, server, persistence or later-phase source is changed. Studio install, six IDs, seam/fallback/device verification and mirror refresh remain pending.

## 2026-07-20 - Loading/start-screen Phase 1 V1.1 smooth minimum generated

- The user confirmed Phase 1 V1 working but reported that fast dealership transitions appeared and disappeared too abruptly. The refreshed `15:10:19` mirror contains the installed V1 sources for scanned services; `ReplicatedFirst` remains outside exporter coverage.
- Updated the same canonical installer to revision `NTR_LOADING_SYSTEM_PHASE1_DEALERSHIP_TELEPORT_V1_1_SMOOTH_MINIMUM`: `MinimumVisibleSeconds=1.5`, `CompletionFillSeconds=0.2`, continuous render-frame progress, monotonic external progress adoption and completion-only `100%`.
- Delayed music now checks whether the underlying transition is genuinely unfinished, so the visual minimum cannot trigger a short music burst. Image/music IDs, `0.3` second fade, neutral input, UI reveal, teleport authority and Phase 1 scope are preserved. Studio installation, Play verification and mirror refresh are pending.

## 2026-07-20 - Loading/start-screen Phase 1 generated

- Added the single canonical `scripts/roblox_ui_loading_and_start_screen_system.lua` and `docs/loading-and-start-screen-system-plan.md` from the approved six-phase High-Risk integration contract.
- Phase 1 installs a versioned single-image artwork catalogue, full-bleed/safe-area boost-style view, retained generation state, neutral gameplay-input gate, delayed loading-music/category mixer and one dealership-teleport vertical slice for both active free-roam HUD owners.
- The driving bridge returns throttle/brake/steer/drift/boost/reset intent to neutral and clears transition-triggered boost/drift rewards without anchoring, braking or changing velocity. The server teleport service and all garage/race/persistence owners remain unchanged.
- The visible destination HUD is restored beneath a `0.3` second modal fade. Artwork/music IDs default blank, installer mutation is projected/compiled and rollback-safe, and Studio install/desktop-touch Play verification/full mirror refresh remain pending.

## 2026-07-20 - Owned garage and readiness handoff locked

- Added `docs/owned-garage-and-readiness-handoff-2026-07-20.md` and corrected the canonical status to the evidence in the refreshed `11:10:20` mirror: 156 scripts, all seven Phase 12 source contracts and current V1.1 revision `NTR_OWNED_GARAGE_PHASE12_ACCESS_INVITATIONS_V1_1_HUD_DROPDOWNS`.
- Commit `c46ead4` is already on `main` and `origin/main` and contains the confirmed owned-garage mirror plus proportional new-system readiness workflow. V1.1 remains the rollback baseline; V1.2 is optional unconfirmed submission polish in the same canonical Phase 12 installer.
- Recorded exact optional V1.2 verification, visitor/offline-search/readiness-audit deferrals, canonical owners and future-chat startup rules. Documentation only; no Studio source, hierarchy, config or saved data changed.

## 2026-07-20 - Proportional new-system readiness workflow adopted

- Added `docs/14_new_system_readiness_standard.md` with optimised Fast, Standard and High-Risk gates plus ownership, server-authority, data/API compatibility, lifecycle, performance, mobile/input, streaming, failure-containment and observability rules.
- Added `docs/15_new_system_contract_template.md`, keeping Standard contracts compact while giving High-Risk systems explicit migration, threat, budget, rollback and verification fields.
- Wired mandatory future-chat routing through `AGENTS.md`, `docs/00_START_HERE.md`, `docs/12_continuous_improvement_workflow.md` and version 1.1 of `docs/13_efficient_feature_delivery_protocol.md`.
- Preserved proportionality: Fast Lane avoids speculative ceremony, `N/A` is valid for irrelevant concerns, and only applicable high-risk boundaries are mandatory. Documentation only; no Studio source, hierarchy, config or saved data changed.

- 2026-07-20: Updated the canonical Owned Garage Phase 12 installer to V1.2 after review of the working V1.1 HUD. Both Access and Invite dropdowns now use the complete two-button width, a transparent borderless container, independent shared-theme gradient rows at exactly the live top-button height, per-access icons with editable asset attributes and safe glyph fallbacks, and shared right-side chevrons that reflect open state. The V1.1 authoritative access/invitation mutations, scrolling bounds, cash/settings visibility and visitor gate are preserved. Studio installation, desktop/mobile Play verification and mirror refresh are pending.
- 2026-07-20: User confirmed Owned Garage Phase 12 V1 working and refreshed the full mirror. Updated the same canonical installer to V1.1 for the approved HUD refinement: Access and Invite now use one shared anchored-dropdown component instead of opening garage management, mutate through the existing revisioned commands, refresh same-server invitations on demand, retain cash/settings, close on outside input/lifecycle changes and use configurable event-driven desktop/mobile sizing with bounded scrolling. No server, persistence, visitor or vehicle-sort source is changed by the V1.1 refinement.

- 2026-07-19: User confirmed Owned Garage Phase 11 Lighting V1.1 working after restarting Studio and refreshed the mirror. Generated `scripts/roblox_owned_garage_phase12_access_invitations.lua` as the next bounded slice: it enables definition/config Access and Invitations, advances API/definition version to 4, normalises persistent invited user IDs, routes mode/invite mutations through the existing authoritative ProfileService command seam, and reuses the shared workspace from the persistent interior HUD. Visitor entry remains disabled to avoid competing with the legacy interior service owner.

- 2026-07-19: Hardened the same Phase 11 Lighting installer as V1.1 after Play reported an infinite yield for missing `OwnedGarageLightingCatalog`. This is the recurring Studio live-editing transaction fault: existing Script source survived while the newly created ModuleScript hierarchy did not. V1.1 accepts the confirmed Phase 10 baseline or either partial Phase 11 revision, recreates all missing catalogue/slot/fixture objects idempotently, yields once, and verifies the exact catalogue parent before PASS. Restart Studio before rerunning; no separate recovery installer is needed.

- 2026-07-19: User confirmed Owned Garage Phase 10 Decorations working and refreshed the `19:27:42` mirror. Phase 10 is now the rollback baseline. Generated `scripts/roblox_owned_garage_phase11_lighting_vertical_slice.lua`: it advances the property/state API to 3, adds four definition/template fixture slots, one editable fixture asset, four priced room presets, three intensity levels, session preview and authoritative purchase/equip/intensity persistence. Runtime is capped at four shadowless local fixture lights per interior and does not touch world Lighting/day-night/VFX. Studio verification is pending.

- 2026-07-19: Repaired the same canonical Phase 10 Decorations installer after its first run rolled back at `Decoration marker missing: OwnedGaragePropertyCatalog`. The catalogue source had been projected correctly; the final audit incorrectly required one generic marker substring even though that module intentionally uses `NTR_OWNED_GARAGE_PROPERTY_CATALOG_V5_DECORATION_ANCHORS`. The installer now records every target's exact marker during preflight and verifies those exact markers after assignment, while separately checking the new decoration catalogue. No separate patch or recovery step is required.

- 2026-07-19: User confirmed Owned Garage Phase 9 V1.1 working and refreshed the full mirror. Phase 9 is now the confirmed asset-backed Structure rollback baseline. Generated `scripts/roblox_owned_garage_phase10_decorations_vertical_slice.lua` for the next approved slice: it corrects the three-anchor definition/template mismatch, adds a versioned six-category/twelve-item catalogue, editable server asset templates, anchor-bounded runtime preview, atomic ProfileService-owned purchase/place/remove state, and shared workspace cards/actions. Lighting and arbitrary transform editing remain gated. Studio verification is pending.

- 2026-07-19: Repaired the same canonical Phase 9 installer as V1.1 after Play showed listing/preview worked but purchases were rejected. The refreshed mirror proved `EnableStructure=false` and showed the client ignored its category `Enabled` value. V1.1 enables Structure only, blocks disabled future tabs, replaces `UNIVERSAL` style lineage with the selected section, creates six template slots and 24 preserved editable section-option models, and changes physical preview/commit presentation to clone the selected asset while keeping functional garage objects outside replaceable models. Studio verification is pending.

- 2026-07-19: Generated `scripts/roblox_owned_garage_phase9_structure_vertical_slice.lua` from the confirmed Phase 8 mirror. It replaces the coarse staged Walls/Floor/Roof style path with a versioned six-section/four-preset catalogue, additive owned/selected/channel state, atomic ProfileService-owned purchase/equip/colour/material commands, session-only preview, attributed template targeting and shared card/popup/H-S-B UI. The transaction projects and compiles six sources before mutation, attributes eight existing template parts and rolls back source/config/attributes on failure. Studio verification is pending.

- 2026-07-19: Confirmed Owned Garage Phase 8 transition completion in Play and refreshed the full Studio mirror. The mirror imported 154 scripts and contains `OwnedGarageExteriors.STARTER_TWO_BAY` with attributed foot/vehicle markers plus the catalogue, management, lifecycle and thrust transition contracts. Phase 8 is now the rollback baseline; Phase 9 Structure is next.

- 2026-07-19: Hardened the same Phase 8 transition-completion installer after Studio again retained Script source edits but discarded its revision attribute and new `OwnedGarageExteriors` instances. The rerun now recognizes the exact already-repaired thrust refresh contract, restores its source marker if missing, and continues through the existing transactional hierarchy creation/audit. This keeps recovery idempotent and avoids a separate patch ladder.

- 2026-07-19: Generated `scripts/roblox_owned_garage_phase8_transition_completion.lua` after V1.8 fixed display persistence but drive-in teleport could remove the car and leave the player outside. Mirror tracing showed the call order was already despawn then teleport; the real gap was the generic despawn helper relocating the seated character, not waiting for `SeatPart` to clear, and management never verifying arrival. The focused installer adds a garage-only detachment handshake, verified/retried teleport, slot preservation for a driven vehicle already displayed in the target property, revision compensation/vehicle restoration on failure, and property-owned exterior foot/vehicle markers. It also removes the stale undefined `applyThrustOnly` live-vehicle call from the single-owner preview bridge. Normal free-roam despawn, saved schema, UI and other VFX/driving systems remain unchanged; Studio verification is pending.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.8 after V1.7 proved the action UI could reach the server but displayed cars still vanished on menu close or the next assignment. Root cause is the `GetProfile` BindableFunction copy boundary: management mutated a detached profile copy and `MarkDirty` only dirtied the unchanged live session. V1.8 adds an isolated authoritative command runtime invoked by ProfileService against the exact active `session.Profile`; management uses copied profiles only for reads, commits before presentation, and revision-compensates drive-in/out lifecycle failures. It retains schema V2, the duplicate-only move rule, vehicle lifecycle ownership and all V1.7 UI contracts. Studio verification and mirror refresh are pending.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.7 after the refreshed V1.6 mirror still produced preview-only cars. Source ownership remains correct: `OwnedGarageProfileRuntime.Assign` owns duplicate-safe saved slots and `OwnedGarageDisplayAssignmentRuntime` owns the revisioned transaction. The remaining boundary was the owned page's inferred row callback plus a second read after mutation. V1.7 adds an explicit reusable `SelectedAction` contract to `GarageWorkspaceController` while mounting the existing dealership popup, returns the complete authoritative management projection with the successful mutation, validates the requested slot and adopts that response directly. Legacy workspace row actions remain supported; no schema, vehicle ownership or unrelated system changes. Studio verification is pending.
- 2026-07-19: Corrected the same Phase 8 V1.6 installer after its first run stopped before mutation at `Duplicate-only vehicle move contract is missing`. The safety assertion targeted the revision/idempotency transaction wrapper instead of `OwnedGarageProfileRuntime`, where `Assign` actually owns duplicate removal. The refreshed source confirms the exact rule is present; V1.6 now resolves and checks that correct owner. No recovery installer or gameplay change was needed.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.6 from the refreshed V1.5 mirror and five user screenshots. The rail already used the shared Build card size but omitted its `.5` artwork zoom; cars still used `ModuleCategoryCard` instead of dealership `Card`; and a visible vehicle with `0/2` proved preview was being mistaken for a saved display, so Back/Exit correctly removed it. V1.6 adds one reusable `Vehicle` card-kind route to the exact dealership component/metrics, adaptive carousel stepping, title-case `.5` rail items, preview-success gating for the shared `DISPLAY` popup, captured slot/vehicle callbacks and authoritative assignment-response validation. Desktop/mobile free-roam HUDs now suppress themselves while `NTR_OwnedGarageManagementOpen` is true, leaving only management economy cards. Existing assignment/persistence and later feature gates are preserved; Studio verification is pending.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.5 after V1.4 Play was broadly successful but one display assignment closed management while the player remained inside. Mirror tracing found two client refresh owners: the successful mutation immediately read state while the server's revision push started another read; the controller closed on any failed read. V1.5 coalesces pushes during mutation/refresh, retains the workspace on transient failures and closes only on authoritative `InGarage=false`. It also removes the owned-only compact/fit-content rail settings and calls the confirmed Build Modules shared card size and carousel-bottom alignment contract, allowing all four categories to use the shared renderer without the incorrect text-button height calculation. No server, persistence, hierarchy or shared UI source is changed; Studio verification is pending.
- 2026-07-19: Reworked the same canonical Owned Garage Phase 8 installer as V1.4 after the refreshed `16:06:21` mirror and Play errors proved a mixed install: existing source edits persisted, but the new icon folder/HUD module and Phase 8 attributes did not. The lifecycle summary also reached `VehicleModuleUpgradeRuntime` with a nil Player. V1.4 repairs the mixed source state, passes Player explicitly, uses existing navigation icons, folds access/invite HUD rendering into the existing `GarageInteriorModeController`, removes the missing module from startup and creates zero instances. Verification remains pending.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.3 after V1.2 reached its final projection and exposed a missing closing parenthesis in the compressed mobile cash-position expression. Replaced that expression with named, multiline layout values and added inside-state layout invalidation so entering/leaving the garage updates placement even when viewport size is unchanged. The failure was still pre-mutation; all earlier projected sources had compiled successfully.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.2 after V1.1 stopped before mutation at `workspace marker anchor missing`. The installer had borrowed a marker from `ModuleShopUIController` instead of the actual shared workspace marker; a complete marker-only audit also found the mobile HUD lacked the desktop performance marker. V1.2 targets `NTR_GARAGE_WORKSPACE_CONTROLLER_V3` and the mobile-specific Phase 1O marker, preventing the next projection from failing for the same class of assumption.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 8 installer as V1.1 after its first run stopped before mutation while compiling the projected `GarageActionController_Shadow_Disabled`: the vehicle-card `elseif` had been inserted after the lifecycle operation chain's closing `end`. V1.1 inserts the branch before that `end`; Phase 7 remained intact and no recovery installer was created.
- 2026-07-19: Phase 7 passed and was refreshed into the `15:32:55` mirror. The focused client check returned API/definition `2/2`, `CacheHit=true`, and correct stale-write conflict rejection. Generated `scripts/roblox_owned_garage_phase8_canonical_vertical_slice.lua` after mirror analysis showed the unchanged UI came from the Phase 6 workspace bypassing authoritative summaries and repeatedly rebuilding/auditing the shared presentation. Phase 8 installs one canonical Display Cars vertical slice with dealership identity/image/rating cards, preview and cross-garage confirmation, async warm opens, incremental selection, tap prompts, drive-out feedback, inside-garage HUD policy, feature/capability/rate/template/interior-count safeguards and production audit-loop gating. Structure, Decorations, Lighting, Invitations and Visitors remain disabled; Studio verification and mirror refresh are pending.
- 2026-07-19: The corrected ProfileService ownership audit passed `40/0/0` in fresh Play, including five seconds of stable profile availability, runtime marker, session generation and session ID. Closed the persistence gate and generated the canonical `scripts/roblox_owned_garage_phase7_reusable_property_framework.lua`. Phase 7 adds an immutable versioned property-definition contract, dynamic display-space projection, capability/access/decoration metadata, revision-aware idempotent mutations, stale-write conflict responses, keyed state caching, read cooldown exemption and client request IDs. It preserves the Phase 6 UI, `OwnedGarage` schema and all vehicle data; Studio install/Play verification and mirror refresh remain pending.
- 2026-07-19: Repair V1.1 passed static verification and is mirrored at `14:45:19`. Its completed Play audit returned `39/0/1`; only caller-side profile-root equality failed. Roblox documents that BindableFunctions copy tables passed or returned through `Invoke`, so this assertion could not measure the stored server table. Corrected the read-only audit to sample profile availability and the server-owned runtime marker object/generation/session ID continuously for five seconds. No Studio patch was added.
- 2026-07-19: The first ProfileService repair persisted every semantic source contract, but follow-up audit returned `19/0/2` Edit and `35/0/3` Play. Studio dropped the two new convenience attributes across the command boundary, so the audit now uses durable source markers instead. The remaining runtime identity failure traced to `OwnedGarageProfileRuntime.Ensure` and `Restore`, which still replaced the nested table during schema/reset and rollback. Corrected the same canonical installer as V1.1 to reconcile both paths in place and split the runtime audit into independent five-second root and `OwnedGarage` identity checks.
- 2026-07-19: The pre-Phase-7 ProfileService gate failed `10/0/4` in Edit and `25/0/6` in fresh Play. Play proved the authoritative profile/`OwnedGarage` table identity was replaced during an idle two-second sample. Mirror tracing found recurring read-only garage `GetInitial` polling importing a legacy snapshot, while ProfileService replaced `session.Profile`; removal/generation lifecycle gaps were also confirmed. Generated the separate canonical `scripts/roblox_profile_service_authoritative_session_lifecycle_repair.lua` and strengthened the same read-only audit. The pending atomic repair adds exact-Player generation/session ownership, post-yield departure rejection, complete cleanup, runtime identity, in-place snapshot reconciliation and a no-import read boundary without modifying profile data.
- 2026-07-19: Phase 6 V1.1 was confirmed, mirrored at `13:35:37` and committed as `ae490e7`. Added `scripts/roblox_profile_service_session_ownership_readonly_audit.lua` as the required pre-Phase-7 persistence gate. The current source already suppresses duplicate and late replacement loads, but mirror review found that a yielded load can outlive player departure, removal does not clear in-flight/cleanup ownership, and no general session generation/identity is exposed; the audit verifies these contracts and samples live profile table identity without mutating or saving data.
- 2026-07-19: Repaired the canonical Phase 6 installer after its preflight stopped before mutation at `attempt to call missing method 'IsA' of string`. Luau expanded both return values from each raw `assert(instance, message)` call inside the legacy-owner array, inserting the message strings as array entries. Phase 6 V1.1 now resolves each required legacy child through a single-return helper before constructing the array; no separate repair installer or Studio-side partial activation was created.
- 2026-07-19: Owned Garage Phase 5 passed and was mirrored at `12:14:59`. Generated `scripts/roblox_owned_garage_phase6_atomic_activation.lua`. Final preflight caught that staged `profile.Garage` replacement conflicted with the authoritative vehicle-capacity schema, so Phase 6 namespaces canonical property/display state under `profile.OwnedGarage` and preserves it through vehicle snapshot imports. The transaction starts two isolated owners, switches both HOME buttons, binds existing local vehicle helpers, retires five legacy physical-garage scripts plus their old prompt, performs a one-token owned-garage-only tester reset, and adds death/reset/disconnect cleanup. Play verification remains required.
- 2026-07-19: Phase 4 recovery passed `18/0` and was mirrored at `12:04:54`. A generic Command Bar Folder probe reproduced the missing-instance symptom before a full Studio restart and persisted afterward, identifying a session-level Studio transaction fault rather than a garage installer defect. Generated `scripts/roblox_owned_garage_phase5_mobile_safety_hardening.lua`: a focused inactive transaction that preserves HUD minimap ownership, adds inside-only map suppression, 44-pixel scaled touch hardening, event-driven owned-UI cleanup and server-authoritative guards against external vehicle/dealership/race transitions. Phase 6 remains the only activation/reset/lifecycle switch.
- 2026-07-19: Locked the clean physical owned-garage replacement acceptance contract and generated `scripts/roblox_owned_garage_phase0_ownership_runtime_audit.lua`. The same read-only script audits Edit source/owners, Play-server profile/display references and runtime counts, and Play-client HOME/minimap/menu presentation without invoking remotes or mutating profile/UI/Attributes. Added `docs/owned-garage-canonical-replacement-plan.md`; implementation and tester reset remain blocked pending all three Studio result blocks.
- 2026-07-19: Owned Garage Phase 0 passed `41/2/0` Edit, `3/0/0` server and `4/1/0` client. Generated the first inactive checkpoint in the single `scripts/roblox_owned_garage_canonical_replacement.lua`: a shared starter-property catalogue, clean per-property schema/validator and idempotent assignment transaction runtime with compile, baseline, sample duplicate-move and rollback audits. New modules use owned-garage-specific names so the existing legacy `GarageProfileRuntime` remains untouched. It does not activate UI/services, reset data or retire the old stack.
- 2026-07-19: Owned Garage Phase 1 passed and was mirrored at `10:52:51` with 143 exported scripts. Extended the same canonical installer to inactive Phase 2: an editable `StarterTwoBay` ServerStorage template, configurable isolated grid, explicit-pool interior runtime and saved-`VehicleId` display builder. Installer audits a temporary interior and real cockpit display, duplicate visual rejection and mobile-safe sanitization; it preserves valid template edits/config tuning on rerun and still performs no active or legacy-owner switch.
- 2026-07-19: Owned Garage Phase 2 passed and was mirrored at `11:02:32` with 145 exported scripts. Extended the same canonical installer to inactive Phase 3 staging: a shared-module `1200 x 720` My Garages browser, server entry/exit and display-replacement orchestrator, inert remotes/open event, configurable city exits and an intentionally unowned vehicle-lifecycle binding. Compile/contract tests cover empty/full/invalid slot selection and guarantee no service, UI, Workspace interior, HOME switch or vehicle owner activates at this checkpoint.
- 2026-07-19: Repaired the same canonical Owned Garage Phase 3 staging installer after Studio rolled back on an attempted read of write-only `RemoteFunction.OnServerInvoke`. V1.1 performs no callback-member reads; inert contracts are audited through explicit staging attributes and the continued absence of active starter scripts. No separate repair installer or Studio-side partial state was retained.
- 2026-07-19: Corrected Owned Garage Phase 3 passed and was mirrored at `11:17:57` with 147 exported scripts. Extended the same canonical installer to inactive Phase 4 management staging: `OwnedGarageWorkspaceController` instantiates the real shared `GarageWorkspaceController`; Display Cars assigns/clears stable saved vehicle IDs; Interior uses a shared style catalogue and persistent per-surface selections; Access persists validated policy without enabling visitors. All new sources compile and transaction tests cover style/access writes while no active script or legacy switch is installed.
- 2026-07-19: The first Phase 4 run printed PASS, but the refreshed `11:36:04` mirror and live inspection still lacked its style catalogue, workspace controller and open event, with all revisions remaining Phase 3. Repaired the same canonical installer as Phase 4 V2: every target receives a generated install-run ID, new modules receive source before parenting, and a delayed post-listener persistence audit verifies exact parents/run IDs before success is reported. This distinguishes a true persistent install from a different DataModel/window or post-command external reversion without creating a repair-script ladder.
- 2026-07-19: Phase 4 V2 rolled back at its delayed check of the pre-existing property catalogue. The failure proved the deliberate `task.wait` crossed the multi-line Command Bar's ChangeHistory transaction boundary, making the in-command delayed audit invalid. Repaired the same installer as no-yield V3 and added one small read-only post-command persistence audit that checks 18 revision/run-ID/source/inert contracts from a separate command boundary. No gameplay owner or Phase 5 work was activated.
- 2026-07-19: The separate V3 audit returned `1 PASS / 17 FAIL`, an empty run ID and Phase 3 config revision. The mirror proves Phase 4 source edits persisted in the existing Profile, Assignment and Management modules while all hierarchy/attribute changes did not. Replaced the oversized Phase 4 execution path with `scripts/roblox_owned_garage_phase4_missing_objects_recovery.lua`, a focused sub-20 KB installer that validates the partial baseline and creates only the missing style catalogue, shared workspace controller and open event. Updated the audit to validate the mixed confirmed-source/recovery-instance baseline.

## 2026-07-18 - Canonical garage experience confirmed and handed off

- The user confirmed the accumulated canonical garage flow, responsive touch UI, module instance/customisation/upgrade presentation, transient preview cleanup, camera mapping, scroll preservation, hover presentation and VFX behaviour working.
- Final VFX root cause was dual ownership: `CachedThrustVisualRuntime` treated `ForceThrustPreview` as accelerating + boosting + drifting while `ThrustPreviewController_Active` attached and updated a second template controller. `scripts/roblox_ui_garage_preview_vfx_single_owner_installer.lua` makes the cached runtime the sole attachment/state owner and uses `PreviewVFXMode` for Idle versus ThrustColour.
- The refreshed `2026-07-18 23:14:48` mirror contains all three single-owner markers plus the confirmed camera, flow, responsive, navigation and compact-rail sources. Added `docs/garage-canonical-handoff-2026-07-18.md`; older visual and VFX repair scripts remain historical and must not be treated as the current path.

## 2026-07-18 - Efficient feature delivery protocol V1 adopted

- Added `docs/13_efficient_feature_delivery_protocol.md` as the editable workflow authority for proportional Fast/Standard/High-Risk lanes, acceptance contracts, ownership gates, true shared-component reuse, one-installer delivery, failure budgets, runtime evidence and handoff boundaries.
- Added `audit:`, `continue:` and `handoff:` alongside the existing `follow:` and `suggest:` routing contracts. `continue:` executes the next uncompleted step of an approved plan without creating extra phases or reopening settled decisions absent new evidence.
- Updated `AGENTS.md`, the continuous-improvement workflow and the new-chat prompt so future chats automatically load and apply the protocol. The system explicitly keeps small changes fast and allows future confirmed evidence to refine or narrow its rules.

## 2026-07-18 - Canonical garage compact Customise rail V2.2 generated

- Screenshot verification showed V2.1's shared-size flag was correctly suited to the two-card Build/Customise navigation rail but made the long Customise category list too tall and its icons too small.
- Generated `scripts/roblox_ui_garage_customise_compact_rail_v2_2_installer.lua` to restore only the category list's earlier compact height/image area and `1.04` icon scale.
- The floating rail surface, Build/Customise card sizing, Neon tab and transient preview cleanup remain intact. The one-module installer compiles and audits before retaining the change and rolls back on failure.

## 2026-07-18 - Canonical garage flow refinement V2.1 generated

- Confirmed the existing transient cleanup already owns `PreviewNeonSlot`; the leak occurred because Customise rail navigation bypassed that owner. Category changes now invoke the same cleanup before rendering the next target.
- Restored Neon under All > Change Colour, made the Customise rail float, and moved both Build/Customise floating rails onto the exact shared bottom-card geometry and artwork scale.
- Generated `scripts/roblox_ui_garage_flow_refinement_v2_1_installer.lua` as an atomic two-module follow-up. No server, profile, inventory, purchase, bootstrap, camera, or driving code changes.

## 2026-07-18 - Canonical garage flow refinement V2 generated

- Traced intermittent owned-Customisation-to-Paint routing to a mutable mode read after a yielding server call, compounded by unordered entrance-event binding. V2 captures the route before the request and binds all three entry events explicitly.
- Generated `scripts/roblox_ui_garage_flow_refinement_v2_installer.lua` as one atomic update to the four canonical UI modules: shared headers, floating Build/Customise rail, wheel forwarding, Customise All actions, and the 15-column current-colour paint palette.
- The bootstrap, server, persistence, inventory, performance, driving, camera, racing and retired legacy UI sources remain untouched. Projected sources are below the Studio limit and all source/config assignments roll back together on failure.
- See `docs/garage-flow-refinement-v2-2026-07-18.md` for the acceptance matrix.

## 2026-07-18 - Canonical garage scaled-touch V1.2 correction generated

- The refreshed V1.1 mirror proved its physical `targetSize` pass was still overriding uniform canvas scaling for economy, navigation and card-action buttons, while `TextScaled` was enlarging/wrapping budget labels.
- Updated the same installer to restore exact PC dimensions for every real control except the intentionally larger carousel arrows. The category rail now copies free roam's `68px` tiny / `82px` normal physical top contract.
- The Workspace budget now spans exactly three shared module cards plus two gaps, keeps fixed `13px` title/used typography, and reserves independent left/centre/right regions. Shared terminal status copy uses the same size with truncation and wrapping disabled.
- The installer compiles and transactionally assigns both shared and Workspace sources, audits both readbacks and restores both sources plus responsive Attributes on any failure.

## 2026-07-18 - Canonical garage scaled-touch V1.1 refinement generated

- Device-emulator review confirmed the shared scaling path works, while V1's full-canvas safe area, universal touch target and broad text multiplier made edge spacing and several controls too large.
- Updated the same canonical installer to accept either the pre-responsive or V1 live source and replace one uniquely marked shared-layout section. V1.1 uses `4px` canvas insets, a left-rail-only Roblox-control clearance, `30px` actions/popups, `32px` arrows and `24px` economy actions.
- Retired broad touch typography enlargement and added fit protection only for upgrade-budget and terminal-status labels. The modal shade follows the near-full renderable canvas; Device Emulator bezel pixels remain outside Roblox's ScreenGui viewport.
- Source compilation, exact shared-owner audits, Attribute audits and atomic source/Attribute rollback remain in the same installer.

## 2026-07-18 - Canonical garage scaled-touch V1 installer generated

- Added `scripts/roblox_ui_garage_responsive_scaled_touch_installer.lua` against the refreshed `2026-07-17 22:20:49` mirror and completed Phase 0 outputs.
- The installer changes only the isolated shared component module plus responsive config Attributes. Browser and Workspace retain one UI tree and continue calling `Shared.LayoutGarageShell`; desktop geometry, bootstrap, server actions, preview, persistence and performance logic are untouched.
- Touch now uses the confirmed race safe-area values, a `0.25` true-fit minimum, real 44px essential controls, compact 36px economy actions, configurable small-text scaling and existing native rails. Stable shell names and one-per-page runtime geometry PASS/FAIL output are included.
- The command-bar installer compiles before assignment, readback-compiles after assignment, audits exact config and ownership contracts, and rolls back source/Attributes together on failure. See `docs/garage-responsive-scaled-touch-v1-2026-07-18.md`.

## 2026-07-18 - Canonical garage responsive Phase 0 audit completed

- Reviewed the Edit result (`18/7/9`) and desktop Play Client result (`7/1/1`). Canonical ownership, shared layout reuse, viewport relayout and native scrolling all passed; the shell remained separated across all eight simulated profiles.
- Confirmed the true responsive gates: missing touch safe-area ownership, minimum-scale clipping on the two smallest phones, sub-44px touch actions and sub-10px copy at smaller touch sizes.
- Classified the live header's negative absolute Y as the exact 58px `IgnoreGuiInset` coordinate-basis shift, preserving the user-confirmed desktop geometry. Also corrected the interpretation of two marker warnings: both confirmed markers live in `ModuleShopUIController`.
- Recommended the next single installer: extend only the shared touch layout with the proven race safe-area fit, lower true-fit scale, transparent padded targets, shared text floors and stable geometry names. See `docs/garage-responsive-phase0-audit-2026-07-17.md`.

## 2026-07-17 - Canonical garage responsive Phase 0 audit generated

- Added `scripts/roblox_ui_garage_responsive_phase0_audit.lua` as one read-only Edit/Play Client audit before tablet/mobile scaling work.
- The Edit path verifies the shared canonical owner graph and simulates eight desktop, tablet and phone geometries. The runtime path measures the visible canonical page, clipping, overlaps, popup/budget separation, inset state and physical touch targets.
- The responsive direction remains one shared UI tree scaled like the confirmed race UI. Safe-area padding, touch hit-target expansion, readability floors and overflow behavior are permitted responsive exceptions; a mobile-only visual fork is not.
- No Studio source or runtime state is changed. Responsive installation is gated on the complete Edit and Play Client outputs; see `docs/garage-responsive-phase0-audit-2026-07-17.md`.

- 2026-07-17: User confirmed the Roblox-owned default vehicle camera V6.1 now looks good. The refreshed `2026-07-17 10:13:20` mirror contains both V6.1 markers, exact module fingerprint `12611/581983456`, `ConfigVersion=DRIVING_CAMERA_DEFAULT_VEHICLE_V6_1`, and current tuning. This closes the camera hitch/framing stream: preserve Roblox-owned continuous motion/collision/orbit plus bounded three-frame pre-camera framing; do not return to Scriptable V4/V5. Added `docs/driving-camera-handoff-2026-07-17.md` for the next chat.

- 2026-07-16: After the user confirmed atomic module purchase/equip/reassignment working, generated `scripts/roblox_ui_garage_module_shared_cards_modal.lua` against the refreshed mirror. Root cause of `UNIVERSAL`, missing green prices and incorrect card treatments was dropped row data in the existing shared carousel bridge, not missing server data or a bad card component. The guarded client-only installer forwards the established listing contract, canonically orders physical Owned copies by state then rating, consumes `SourceCockpitDisplayName`, and makes the existing common modal fill/audit the canonical scaled canvas. Awaiting Edit install and desktop/mobile Play verification.

- 2026-07-16: Generated `scripts/roblox_ui_garage_module_atomic_transactions.lua` against the freshly exported reference-authority/read-only-preview mirror. The guarded installer adds an isolated rollback-capable `GarageModuleTransactionRuntime`, canonically replaces only the physical-copy Buy/Equip function pair, and updates the existing Buy card action to purchase-and-equip. Cross-vehicle moves preserve the selected instance's customisation, use canonical slot references, backfill the old slot with the lowest-rated compatible available copy, reject required-slot gaps, and restore the full profile on any failed invariant. Awaiting Edit install plus fresh Play/save-rejoin verification.
- 2026-07-16: Corrected the same atomic installer after its first compile-only preflight reported `Expected '}' ... got 'end'` in `ModuleShopUIController`. The injected marker had been a line comment inside a one-line table constructor and hid the existing closing `})`; the replacement now emits a terminating newline before that closure. The failure occurred before any Source assignment, so no revert or repair installer is required.

- 2026-07-15: Repaired the Canonical Garage V3 preview-camera state initialization after the refreshed mirror confirmed correct pad placement/camera ownership but Play raised `fadeFrame.Parent` and nil transition-counter errors. `fadeFrame`/`fadeTween` now start nil and `transitionSerial` starts at zero. The correction remains in the same idempotent canonical installer and does not change preview geometry, input, fade tuning, or UI ownership.

- 2026-07-15: Corrected Garage Phase 1 V3 after its first command stopped before mutation with `Missing source anchor: bootstrap open gate`. The V2 bootstrap already contained the final unconditional gate, so the V3 preflight no longer attempts to reconstruct the superseded intermediate attribute gate. Open and drive-in gates are now migrated independently and accept pristine, intermediate and already-installed V2 shapes; a local exact-anchor simulation confirmed one marker on first upgrade and a clean skip on rerun.
- 2026-07-15: Upgraded the same existing-instance Garage Phase 1 installer to V3 after the user confirmed V2 worked substantially better. V3 preserves the ownership bridge and makes no new bootstrap feature patch. The isolated preview builder now resolves `GaragePreviewPad` through `PathResolver`, preserves pad orientation and focuses completed vehicle bounds. The isolated camera adds UI-aware desktop/touch orbit, wheel/pinch zoom and attribute-configurable fades/interpolation. Build and Customise share one image category card; Customise uses a scrolling artwork rail; Owned/Shop use separate image-free listing cards, group owned copies, show lineage/type/status, and match Select Vehicle pink-unselected/cyan-selected border semantics. The installer compiles every generated source and rolls back sources/config attributes on failure.
- 2026-07-15: Garage Phase 0 ownership/dependency audit completed. Static result was `pass=6 warn=1 blocker=6`; runtime was `pass=0 warn=0 blocker=1`. Browser presentation and geometry passed, then Workspace blocked on absent `GarageModuleArtworkRegistry`; the refreshed config also lacked all eleven artwork folders, leaving the legacy ScreenGui as the only visible owner. The audit additionally traced bootstrap line 3675 to an undefined `showCashShop` callback. Recorded the exact retain/extract/replace/retire ownership map and a new transactional rule: stage, compile, read back and validate the complete canonical dependency graph before the final activation switch, then verify after a fresh Play restart.
- 2026-07-15: Added the read-only Garage Phase 0 ownership/dependency audit after the refreshed mirror and screenshot proved V3/V3.1 could still fall back to the legacy UI. `scripts/roblox_ui_garage_phase0_ownership_dependency_audit.lua` audits static constructors, bootstrap page/action ownership, every legacy GUI-name reference, canonical modules, artwork schema and server boundaries in Edit mode, then audits actual visible legacy/canonical roots and runtime controller state from the Play Client Command Bar. No further garage replacement is generated until both outputs define the extraction map and the legacy zero-reference gate.
- 2026-07-15: Corrected the same Garage Workspace installer to V3.1 after V3 printed `INSTALL PASS` and then Studio emitted locked-Parent warnings for obsolete module-artwork values. A Studio mirror/import listener retained references and attempted to re-parent each value after `Destroy()`. V3.1 atomically detaches the complete obsolete artwork root without destroying its children, creates the clean category/attribute root, and remains idempotent for the already-installed clean V3 state. No source, gameplay, layout, or rollback change was required.
- 2026-07-15: Upgraded the same canonical Garage Workspace installer to V3 as the first controlled legacy-UI retirement boundary. Browser and Workspace now acquire an independent runtime `CanonicalGarageGui` and one shared canonical scale from `GarageReplacementComponents`, and bootstrap constructors no longer receive `UI.Gui` or `UI.Scale`. Added `GarageModuleArtworkRegistry` plus an exact eleven-folder `ModuleArtwork` schema; every category folder has attributes only and zero children, and Build/Customise derive their category order, labels and images from that same registry. Existing image values are migrated where a direct category match exists. Added generated-source compilation, constructor-count guards, schema audits and runtime host/scale checks. Legacy constructors remain temporarily for cash/property/action bridges until the approved zero-reference gate passes.
- 2026-07-15: Corrected the same Garage Workspace V2 installer after its first paste failed to parse at Command Bar line 249 (`Expected <eof>, got ']'`) before mutation. The shared performance/shell and browser insertion blocks are now outside their surrounding generated-source strings, and V1 migration anchors that end with table-index brackets use equals-delimited long strings. No revert or separate repair script is required.
- 2026-07-15: Upgraded the same canonical Garage Workspace installer to V2 after the Build Modules review. Extracted the confirmed vehicle-browser geometry and performance bars into shared component functions used by both browser and workspace rather than copied page implementations. Module Slots now has no left rail; Paint has no Back; all pages after vehicle selection have no Exit; Back uses the red Exit treatment; the continue label is `CUSTOMISE`; and module-card names are larger/bold. Replaced the mismatched metadata-only core gate with one current-vehicle instance/template resolver shared by EQUIPPED badges and progression. Added safe V1-to-V2 migration, right-edge runtime checks and shared-contract installation assertions. Awaiting Studio rerun and Play verification.
- 2026-07-15: Generated `scripts/roblox_ui_garage_workspace_remaining_menus.lua` against the fresh user-exported mirror after the V1.4 browser was approved. The one consolidated installer creates an isolated `GarageWorkspaceController` and register-safe bridges for Paint Cockpit, Build Module Slots, Owned/Buy Modules and module customisation. It preserves distinct owned module instances, source locks, immediate post-buy equip, live preview, Phase AO upgrade purchases, neon/colour actions, required-core gating and Start Driving. The confirmed header/stats/economy/carousel shell and card-relative popup are reused; module cards reserve artwork with names below, and editable artwork values are created for fixed slots plus every current module ID. Added generated-source compilation, fresh-anchor guards, rerun idempotence and runtime geometry/popup audits. Awaiting Studio install and complete PC/touch flow verification.
- 2026-07-15: Corrected the same Garage Workspace installer after Studio rejected a `200083`-character bootstrap Source before assignment. The installer now removes the superseded legacy slot, module-option, customise-left and cosmetics renderer bodies while retaining their shared data/action/popup helpers, reducing the patched bootstrap well below the hard limit. Added an explicit pre-assignment `<195000` guard. The partial attempt requires no revert; rerunning updates the already-created isolated controller/config idempotently.
- 2026-07-15: Added a canonical single-presentation owner to the same Garage Workspace installer after a property purchase re-enabled retired legacy cash/stats/navigation panels. `GarageReplacementComponents` now owns one persistent suppression set shared by the browser and workspace, hides a previous canonical root when ownership changes, never restores retired legacy roots, and audits ownership after renders. The property menu now notifies the active canonical page after profile changes so cash/capacity update without invoking legacy visual ownership. The migrated bootstrap remains about `186.6k`, below the guarded `195k` ceiling. Awaiting rerun and purchase/transition regression verification.
- 2026-07-15: Superseded the repeatedly conflicting dealership/customisation V3 visual stack with `scripts/roblox_ui_garage_replacement_foundation_browser.lua`. The guarded installer creates shared race/free-roam-styled garage components, including image-first vehicle/module cards and one card-relative BUY/EQUIP/CUSTOMISE popup positioner; installs a canonical dealership/owned browser; disables the old presentation owner; and replaces the bootstrap cockpit renderer with a small data/action bridge. Vehicles sort rating-low-to-high and auto-preview leftmost. Added generated-source compilation and runtime absolute-geometry checks for duplicate legacy surfaces, overlap, paired chip width, fitted stats and popup centring. Awaiting Studio install and PC/touch verification before replicating the shell to Build Modules, Paint and module customisation.
- 2026-07-15: Corrected the same replacement installer to V1.1 after its first Play startup reported a non-function Exit connection in `setupUI` and nil Position in `GarageBrowserController.Layout`. Replaced invalid `UDim2.zero`, restored the original proven inline legacy Exit handler, gave the replacement browser an inline equivalent callback, guarded optional economy callbacks and added an idempotent partial-V1 migration. No new phase or separate repair script was created.
- 2026-07-15: Refined the confirmed-loading replacement to V1.2 in the same installer. Removed chrome panel borders/glow while retaining gradients; inset compact categories and reserved action clearance; changed 512x340 vehicle art to rounded full-card Crop with larger centred name overlays; removed the popup wrapper; explicitly ordered/restyled headline stats; shortened paired economy cards; and added larger borderless directional arrows driven by measured overflow plus automatic short-row centring without reducing carousel width. Added the server's already-calculated `Headline` to owned summaries to repair zero stat values, plus runtime arrow/centring audits. Awaiting Studio rerun and visual confirmation.
- 2026-07-15: Refined the same replacement to V1.3 after Play review. Short-row centring now waits for and observes final scaled `AbsoluteSize`, converts it through the active `UIScale`, and audits against full-screen centre. Vehicle artwork changed from Crop to full-height Fit and its name plate moved to the card bottom. Cash/garage chips gained a blue no-glow outline. The replacement-owned top-centre header now directly ports the race session `metricCard` background transparency, corner radius and transparent gradient rather than approximating the legacy-looking surface. Awaiting Studio rerun and confirmation.
- 2026-07-15: Added narrow V1.4 polish in the same garage replacement installer. Vehicle artwork now renders at `1.06` Fit scale inside rounded holder/image clipping, filling card width with slight accepted vertical crop, while future module cards remain at `1.0`. Increased the replacement metric-card header title and description and changed both to the standard white text token. No layout, stats, carousel, callback, server-calculation or mobile-scaling behavior changed.

- 2026-07-14: Generated Canonical Garage Experience V3 in the same approved installer against the refreshed `22:52:39` mirror. Replaced the legacy bottom backdrop with clipped floating vehicle cards and persistent edge rails, redesigned header/Exit/economy/stats presentation from the approved race/free-roam tokens, added touch-specific scaled-desktop fitting, removed artificial owned-card number suffixes, and moved the original BUY/CUSTOMISE control to a permanent garage overlay owner so stats rerenders cannot destroy it. Awaiting Studio install and full PC/mobile flow verification.
- 2026-07-14: Repaired the V3 install audit after Studio reported `Stat-column renderer missing`. The V3 stat function intentionally supersedes the old stat-column marker; preflight, migration and final audit now accept the V3 marker and safely recognize the already-assigned partial-success source on rerun.
- 2026-07-14: Screenshot review found V3 styling active but legacy bootstrap geometry still winning after each cockpit render. Updated the same installer to reduce bootstrap `applyDealershipLayout()` to scroll invariants, establish the isolated controller as sole geometry owner, restore the category back panel, fit the top-right stats/economy stack, enlarge and recompose vehicle cards, place a small Exit above the carousel, and periodically reconcile pooled browser cards/actions only while that browser is visible.

- 2026-07-14: Refined the same canonical garage installer after the bottom carousel exposed an overhead close camera, undersized nested card artwork, missing CUSTOMISE popup and flat styling. The entry camera now uses the exact Cockpit Colour/Engine1 framing values; the isolated presentation controller fills the card image region, derives popup visibility from selected-card state, and reads the approved DesktopFreeRoamHud colour/effect tokens for idempotent gradients, semantic strokes, restrained glow and hover states. Purchase/customise logic remains the existing reparented authoritative button, and the bootstrap receives no new top-level helper.
- 2026-07-14: Revised the same canonical garage installer after the initial installed/mirrored V1 showed working stat columns but retained the bootstrap-owned full-height vehicle grid and free-roam camera. The canonical rerun now replaces `applyDealershipLayout` with bottom-only horizontal carousel ownership, converts the cockpit scroller/arrows to horizontal, activates the existing garage preview camera immediately, defaults entry to `ALL`, sorts dealership and owned vehicles lowest-to-highest rating from left to right with unrated entries last, and automatically builds the leftmost preview on entry or category change. This remains one approved phase and adds no repair script or new bootstrap helper.
- 2026-07-14: Generated one consolidated Canonical Dealership/Customisation Experience installer against the refreshed `15:34:29` mirror. It installs isolated shared E/tap/controller entrances, a server-authoritative character hide/freeze/return session, and a responsive free-roam/racing-themed presentation owner; retires the old on-foot auto-open and drive-in countdown clients; adds ALL plus alphabetical categories, rating-descending bottom vehicle carousel, shared image/name/rating cards, a selected-card-centred BUY/CUSTOMISE popup, four-column label/bar/value/delta stats, duplicate ownership counts and category-specific module diagram config; and routes owned/drive-in customisation to Cockpit Colour. The popup reparents the existing authoritative action button rather than duplicating purchase logic and uses rendered coordinates plus `UIScale`, matching the proven module-popup approach. Existing catalogue, economy, persistence, vehicle/module instances, preview, Phase AO upgrade and vehicle spawn behavior remain authoritative. The installer preflights exact bootstrap/server bridges and runs a static post-install audit; Studio install, full cross-device/two-client verification and post-install mirror refresh remain required.

- 2026-07-14: Generated Mobile Free-Roam UI Phase 1O as one guarded mobile-only visibility/input refinement. Settings, Get Cash and dealership confirmation now keep only their modal/shade visible while hiding navigation, map, cash, toast, telemetry, Exit and every vehicle control. A shared local `NTRMobileMajorMenuOpen` flag bridges the HUD and independent control owners; disabled control `ScreenGui` state also counts as blocked for Race Browser/Entry. The existing once-per-block `clearInputs()` path releases held steering, drift, boost, throttle and brake. PC UI, car-menu top-HUD behaviour, geometry, assets, gameplay and racing layouts remain unchanged. The user confirmed the completed behaviour works well; Phase 1O is now the mobile major-menu ownership baseline. Post-install mirror refresh remains required.

- 2026-07-14: The user confirmed Roblox-owned Camera V6 is smooth, but height, pitch and look-target edits had no visible effect. The refreshed mirror proved exact V6 and saved active values (`DefaultPitchDegrees=-20`, `DefaultHeightStuds=7`, `LookTargetHeightStuds=10`). Diagnosed the timing defect: V6 wrote one CFrame after Roblox's camera calculation, then Roblox restored its retained orbit state. Upgraded the same fingerprinted canonical installer to Initial Framing V6.1. It queues framing immediately before Roblox's camera calculation for a bounded default of three initialization frames and requeues on framing-attribute edits, while leaving Roblox as the continuous motion/collision/orbit owner and preserving every existing config value. Awaiting rerun, framing/live-edit/smoothness verification, and mirror refresh.

- 2026-07-14: Retired the unsuccessful Scriptable-camera direction after V4 and the attempted V5 attachment change left the hitch unchanged. Diagnosed the reversed zoom-out effect in mirrored V4: velocity prediction advanced the camera position itself by roughly `19.2` studs at `100 MPH`. Generated `scripts/roblox_driving_camera_default_vehicle_system.lua` as one canonical replacement of only the isolated camera module. Roblox now owns continuous CFrame, collision, orbit and platform input; NTR retains locked smooth state distance, state FOV, one-time initial pitch/yaw framing, trailer handoff and restoration. A new small described `DrivingCamera_Default_EditAttributes` folder copies compatible current values on first install; the legacy 106-attribute folder is untouched and no longer read. Awaiting Studio installation, verification and mirror refresh.

- 2026-07-14: Camera Smoothness V4 was installed and mirrored but left the reported hitch exactly unchanged, ruling out the confirmed extra camera writer as the dominant cause. Upgraded the same fingerprinted canonical installer to Camera Render Anchor V5. The isolated module now buffers `PostSimulation` poses, interpolates a virtual render anchor, carries camera translation with that anchor before applying the existing relative response, interpolates velocity prediction, snaps on large position/rotation changes, exposes anchor diagnostics, and retains `RenderAnchorEnabled=false` as a direct V4 fallback. Existing camera tuning Attributes are verified unchanged; only five described render-anchor controls are added. Awaiting Studio installation and Play verification.

- 2026-07-14: Diagnosed the remaining driving-camera hitch from the refreshed mirror: enabled `ThrustPreviewController_Active` still forced the camera to `Custom`/seat on every rendered frame while confirmed Camera V3 forced `Scriptable`. Upgraded the same canonical installer to Camera Smoothness V4 as one stage. It fingerprint-gates the exact V3 module, exact-preflights the preview helper, preserves all existing camera Attributes, caches live config, throttles debug writes, skips PC gamepad polling, samples collision with release hysteresis, updates Scriptable-camera focus, and publishes bounded frame/raycast/ownership diagnostics. Awaiting Studio installation, Play verification, and mirror refresh.

- 2026-07-14: Generated one unified Race Flow installer for the approved countdown, queue, result-exit and menu-ownership scope. It adds a responsive central 5-to-GO overlay, canonically replaces the old queue/finish panel with a compact banner, publishes queue presentation state, adds a server-owned queued vehicle lock plus garage rejection, removes Browser/Entry/Results header X buttons, makes result EXIT await cleanup, removes the generic TimeTrialEnded fallback reopen, and fixes the shared HUD-only Time Trial RESET clock restart on both PC and mobile. Server authoritative Time Trial elapsed time was already correct and remains unchanged. After the initial countdown was accepted, upgraded the same installer with a borderless racing-palette gradient, fully centred countdown text, two editable visual values, and an exact-preflighted route-guide gate that keeps checkpoint presentation hidden until GO. The current presentation was user-confirmed working. A two-player release regression, smoke and mirror refresh remain recommended.

- 2026-07-14: Generated one guarded post-drift boost calibration stage against the refreshed `11:55:47` controller. It preflights both related exact source anchors, compiles the generated controller, snapshots every existing dynamics-config Attribute, and adds only missing described values under `03_Drifting`. Drift charge now sets reward quality, mapped BoostForce scales acceleration, mapped BoostDuration scales bounded duration, the default absolute cap is `0.90 s`, and normal boost no longer pauses/queues the reward. Existing config values remain unchanged. Awaiting Studio install, cross-tier/upgrade verification, and mirror refresh.

- 2026-07-14: Generated Mobile Racing UI Phase 2 as one guarded canonical installer. It reuses the confirmed PC Phase 16 in-race widgets for mobile lap, time/position, PB/laps, and standings; removes race maps on touch; keeps the established mobile speed/boost and input geometry; moves race EXIT to the free-roam EXIT region with RESET above; and extends the mobile presentation owner to honour `KeepTelemetry` while hiding top free-roam and legacy HUD elements. The user confirmed the refined larger, farther-left and more widely separated RESET/EXIT layout works well. Preserve the approved size, manual config edits and every existing mobile bottom control; mirror refresh remains required.

- 2026-07-14: Generated one consolidated single-owner driving-camera installer after diagnosing competing continuous writes from Roblox's seat-follow camera, V74's `PrimaryPart` assist, and the controller's heartbeat `Custom` reset. The installer preflights the exact V74 block by normalised length/hash plus one tiny exact heartbeat guard, compile-checks an isolated `DrivingCameraController`, and adds one flat described config Folder without overwriting existing values. It adds camera collision, velocity prediction, explicit acceleration/high-speed/boost FOV and distance stages, orbit/zoom, recentring, state restoration, debug Attributes, and P/C/V trailer suspension with B resume. Awaiting Studio install, Play verification, and mirror refresh.

- 2026-07-14: Corrected the camera input configuration in the same canonical installer: the original mouse-wheel branch was not gated by `ManualOrbitEnabled`. Added a separate `ManualZoomEnabled` toggle defaulting false, reset any prior manual offset when disabled, retained orbit as an independent option, and added a full-source fingerprint so only the exact original isolated camera module is upgraded on rerun. No driving or existing camera tuning values are overwritten. Awaiting rerun, wheel-lock verification, and mirror refresh.

- 2026-07-14: User confirmed the single-owner camera and zoom lock work well and that touch orbit works, but PC orbit did not. Updated the same canonical installer to accept either fingerprinted prior module, capture right-mouse press/movement before Roblox's processed-input gate, and add `InvertVerticalOrbit=true` consistently across mouse, touch, and gamepad. Existing camera/driving values remain preserved. Awaiting canonical rerun, cross-device orbit verification, and mirror refresh.

- 2026-07-14: The processed-input repair did not restore PC orbit, and collision was activating against seemingly empty space. Updated the same fingerprinted canonical module to V3: right mouse now explicitly locks/hides the cursor and uses render-step `GetMouseDelta`; collision uses bounded multi-pass filtering for non-collidable, highly transparent, tagged, or attributed ignore parts and publishes the accepted hit path for diagnosis. Added described controls without changing existing values. The user confirmed V3 works well, and the refreshed `11:55:47` mirror contains the confirmed camera module/bridge/config baseline.

- 2026-07-14: Generated one post-Phase-3 driving-tuning organisation stage, then revised the same canonical installer to the user's preferred flat layout rather than adding another phase. It snapshots original flat values or prior nested `01_Value` values, creates only seven ordered category folders, and writes each live number directly as a category Attribute followed by a `<Name>_RaisingThisDoes` string Attribute. Existing edited values always win over defaults and reruns preserve category values. The `10:29:30` mirror confirmed the flat layout and the user's edited acceleration values. Extended the same organiser with six described physical TopSpeed curve controls, monotonic raw-to-physical mapping, debug output, an editable absolute safety limit, and removal of the two hidden `260 MPH` clamps. Three related TopSpeed anchors and both reader states are preflighted before mutation. Raw V2 TopSpeed/rating/module values and all existing tuning numbers remain unchanged.

- 2026-07-13: Refreshed the Studio mirror at `21:09:59`, confirming Phase 1M in the live control owner. Generated Mobile Free-Roam UI Phase 1N as one exact geometry replacement: Accelerator and Brake become equal `104 px` square image slots, share a `10 px` bottom offset, sit `10 px` from the right with a `10 px` gap, and expose all four values as config attributes. Existing image asset slots, Phase 1M invisible cards/image opacity, input actions, hitboxes, other controls, HUD, racing, PC, gameplay, server, bootstrap, VFX, and LOD remain unchanged.

- 2026-07-13: User completed Vehicle Performance V2 Phase 3 and refreshed the `21:01:42` mirror, which contains the component allocation policy and all six profile hierarchies. Added Phase 4 shadow variant calibration: each vehicle Sheet page now records Standard/Lightweight/Power policies, equal 12%-of-cockpit guide prices, and six-point capacity; generated a config-only installer that validates 48 variant improvements, Lightweight/Power gain parity within 1.5 PI, and eight monotonic E–S donor ladders. Individual upgrade paths and live assets remain deferred.
- 2026-07-13: Vehicle Performance V2 Phase 4 passed `7/0/0` and the `21:21:01` mirror confirms its shadow hierarchy. Added Phase 5 shadow upgrade paths: each Lightweight/Power engine, stabiliser, and boost has three three-point paths but a six-point total cap, with `3%` per-point raw deltas and total-points-spent costs rising from 8% to 22% of module guide price. The installer validates every point, all two-path six-point pairings, costs, Standard lockout, V1 source preservation, hierarchy preservation, and disabled runtime switches. Live upgrades/assets/economy/runtime remain deferred.
- 2026-07-13: The first Vehicle Performance V2 Phase 5 run returned `9/0/12`; all failures were confined to A/S engine Lightweighting points whose complete-build Weight had already reached the rating curve's technical minimum. Updated the canonical script and all six Sheet dossiers to use an Efficiency path: Weight `-3%` plus EngineOutput `+1.5%` per point. This preserves lightweight vehicle feel, guarantees visible progression after the Weight floor, and avoids destabilising the confirmed global curve/stock catalogue.
- 2026-07-13: User confirmed the corrected Vehicle Performance V2 Phase 5 passed and refreshed the mirror at `21:51:45`, which contains all 48 upgrade-path folders and corrected Efficiency definitions. Added Phase 6 non-live asset materialisation: a source-free Command Bar installer clones the current Viper cockpit and 12 core module donors into six cockpits/72 modules under intentional ServerStorage staging, applies the confirmed V2 raw values, point paths, and price guides, and validates six stock builds without entering automatic garage discovery.
- 2026-07-13: User confirmed Vehicle Performance V2 Phase 6 passed. Reconciled the expanded audit/calibration numbering with the original five-part implementation plan and condensed all remaining work into two gates: Phase 7 disabled compatibility/runtime/persistence integration plus migration validation, then Phase 8 atomic publication and full live verification. The locally received mirror still shows `21:51:45` and no ServerStorage staging root, so its reported post-Phase-6 refresh is not yet present in the repo and must be repeated before Phase 7 source/hierarchy work.
- 2026-07-13: Generated one consolidated Vehicle Performance V2 Phase 7 installer without source replacement or live publication. It hard-preflights the real Phase 6 staging root; installs isolated V2 runtime, compatibility, six-point module-instance upgrade, purchase/PI preview, persistence-safe preview migration, and event-driven shadow comparison owners; preserves legacy upgrade levels and calculates refund credit for points beyond the six-point cap; and leaves every live V2 switch false. Recorded the user's workflow rule that approved tasks/phases must not be subdivided without asking first; repairs stay in the same canonical phase by default.
- 2026-07-13: The first Vehicle Performance V2 Phase 7 Edit run returned `15/0/2` after all isolated owners installed. Live V1 sources, live catalogue hierarchy, disabled switches, six stock builds, allocation, migration, DataStore safety, compatibility, and purchase checks passed. Corrected two validation-only assumptions in the same canonical installer: preview now evaluates the Power engine inside a complete Viper build instead of a zero-stat shell, and the write guard audits `VehiclePerformanceV2Runtime` as the actual `V2Shadow...` writer plus the service caller. No rollback or additional phase was created.
- 2026-07-13: User confirmed the corrected Vehicle Performance V2 Phase 7 Edit/Play flow works well. Generated Phase 8 as the one final atomic live-launch stage without subdividing the approved scope. Its canonical installer hard-preflights the live Phase 7 hierarchy and one fragile garage catalogue anchor; installs switch-aware isolated calculator/performance/upgrade owners; publishes six Viper-based cockpits and 72 V2 core modules while preserving cosmetic families; performs lazy module-instance migration with preserved legacy levels and one-time overflow refund; and enables rating, raw physics data, purchases, migration, and publication last. The same script includes switch rollback mode and creates no in-game backups; full asset rollback uses Roblox version history.
- 2026-07-13: Added `scripts/roblox_studio_cash_grant_hotkey.lua`, an isolated development-only economy helper. It installs config-driven `=` / `$100,000` input for `LucidityStudios`, validates the request server-side, uses the existing garage cash/persistence mutation bridge, rate-limits requests, updates leaderstats, and contains a hard non-Studio refusal so the debug remote cannot grant cash in published servers. No bootstrap or garage source is patched.
- 2026-07-14: Generated Driving Feel Phase 3 as one consolidated tier-safe calibration stage rather than splitting handling, boost, acceleration, and drift. The canonical dynamics replacement bounds raw E-S steering and boost timing through configurable diminishing physical curves; acceleration now has soft launch, stronger post-10-MPH pull, EngineOutput-dependent power-band length, high-speed fade, and quadratic aero resistance; drift gains more grip, less sideways shove/drag, throttle engine assist, and force-based velocity alignment. Two guarded controller anchors add safe weight influence and drift drive without touching the bootstrap, rating, economy, UI, VFX, or vehicle raw stats. Awaiting Forge/Viper/Zenith Studio verification and mirror refresh.
- 2026-07-13: Vehicle Performance V2 Phase 2 rerun passed `8/0/0`; all six balanced stock profiles hit their intended tier/PI guides, the E-versus-S `+20 EngineOutput` gain was `48.12` versus `3.04`, runtime switches remained false, and the `20:53:08` mirror contains the source/config/profile state. Added Phase 3 shadow component allocation: all six Sheet dossiers, including a new Zenith dossier, split each profile across cockpit/front engine/rear engine/stabilisers/boost with `102/102 MATCH`; generated a config-only installer with recombination and cross-tier module-swap audits.
- 2026-07-13: The first Vehicle Performance V2 Phase 2 Studio run installed its saved source/config but audited `6/0/6`: all six headline sets matched the Sheet, while PI was inflated because Studio reused the already-required Phase 1 calculator function and applied the new scale without the new origin. Updated the canonical installer to validate through a fresh, immediately destroyed ModuleScript clone. No calibration values or live runtime systems were changed.
- 2026-07-13: Vehicle Performance V2 Phase 1 Studio audit passed `12/0/0`; V1 source stayed unchanged, reverse delay returned to `1.0`, and the refreshed `20:22:34` mirror contains the shadow calculator/config. Added Phase 2 catalogue calibration: the linked Sheet now has six editable balanced E–S stock profiles, `TopSpeed.Reference = 180`, `PerformanceOrigin = 54.0526`, and `RatingScale = 50.4351`. Generated a shadow-only Studio installer with one guarded isolated-calculator replacement and profile/tier/balance/monotonicity/diminishing-return checks. No live vehicle, module, price, asset, rating, or physics change yet.

- 2026-07-13: User confirmed Phase 1L looks good and refreshed the mirror at `20:22:34`. Generated Mobile Free-Roam UI Phase 1M as one guarded visual-factory replacement in the isolated Phase 1K controls owner: turn/drift cards lose border/glow, gain a dark configurable gradient and separate card/image opacity; pedal cards default to opacity `0` while pedal image opacity remains configurable. Pressed opacity feedback is configurable. No input action, hitbox, layout, Thumbstick, Tilt, boost, HUD/modal, racing, PC, gameplay, server, bootstrap, VFX, or LOD change.

- 2026-07-13: Refreshed the Studio mirror at `20:07:14`, confirming Mobile Racing UI Phase 1 and the Phase 1B `SafeTop = 72` config. Generated Mobile Free-Roam UI Phase 1L as a guarded two-anchor patch to the isolated Phase 1K HUD owner. Settings uses a `720 x 420` centred reference while retaining Mobile Controls first; Dealership confirmation uses `650 x 270`; Get Cash ports the PC `840 x 650` balance chip, 2x2 cards, Robux labels, Best Value, gradients/facets, Close, and footer, scaled inside the same `72/10/10 px` safe area. Cash remains explicitly non-transactional. No PC, racing, car-menu, driving, gameplay, server, bootstrap, VFX, or LOD source is changed.

- 2026-07-13: User confirmed Mobile Racing UI Phase 1 worked and looked good across the shared scaled PC menus. Added config-only Phase 1B to reduce `MobileScaledDesktop.SafeTop` from `84` to `72`, leaving only a small gap beneath Roblox's built-in buttons. No source owner, PC layout, menu scale, in-race HUD, gameplay, reward/data, bootstrap, server, driving, VFX, or LOD change.

- 2026-07-13: Refreshed the Studio mirror at `19:38:44`, confirming Mobile Free-Roam Phase 1K and Racing Phase 16F are live. Generated Mobile Racing UI Phase 1 as `scripts/roblox_racing_ui_mobile_phase1_scaled_desktop_trial.lua`: a guarded six-anchor patch makes Browser, Entry submenus, and unified Results select their existing approved PC composition on touch and fit one `1200 x 720` shell into a config-driven mobile safe area through `RacingMobileScaledDesktopLayout`. PC keeps its existing responsive path; in-race HUD, racing logic, rewards, PBs, bootstrap, server, driving, VFX, and LOD are unchanged. Added feature-flag rollback, Edit-mode smoke checks, and the mobile verification handoff. The user subsequently installed and confirmed the phase working.

- 2026-07-13: User confirmed Mobile Free-Roam UI Phase 1J good and requested a smaller Boost visual on a circular boost-colour gradient card plus Exit aligned to the turning-button bottom. Generated Phase 1K in the canonical installer: preserve the `44/52 px` Boost hit target, migrate icon scale `1.5 -> 1.05`, add an `84%` circular Electric Blue-to-Telemetry Cyan gradient plate, and derive Exit Y from telemetry scale/bottom plus the controls owner's `10/16 px` steering bottom margin. Input actions, card/menu layout, telemetry behavior, gameplay, and physics remain unchanged. No fragile text replacement, PC owner, bootstrap, server gameplay, VFX, or LOD changes. The user later confirmed it looked good and the `19:38:44` mirror contains both Phase 1K owners.

- 2026-07-13: User confirmed Mobile Free-Roam UI Phase 1I's three-row layout looks great and requested final card-art/Despawn polish. Generated Phase 1J in the canonical installer: configurable vehicle-art Y offset moves from `0.08` to `0.13` card height (with matching fallback movement), and the existing background-only Despawn gradient overlay is strengthened to `0.72` transparency while keeping its border/glow removed. Three-row sizing, card details, cash containment, dropdown behavior, and gameplay/input contracts remain unchanged. No fragile text replacement, PC owner, bootstrap, server gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun, final visual confirmation, and mirror refresh.

- 2026-07-13: Phase 1H screenshot review still showed only two vertical card rows. Root cause was the responsive solver's unchanged `CarMenuVisibleRows = 2`, so reducing target width alone did not guarantee the requested third complete row. Generated Phase 1I in the canonical installer: migrate visible rows `2 -> 3`, target width `108 -> 92` (`92 x 80`), allow short-screen height fitting to approximately `86 x 75`, and scale badges, names, fallback text, strokes, and Buy More content again. Phase 1H cash containment and all Phase 1G menu/gameplay contracts remain. No fragile text replacement, PC owner, bootstrap, server gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun, populated three-row device test, and mirror refresh.

- 2026-07-13: Mobile Free-Roam UI Phase 1G looked better in follow-up testing, but the user requested vehicle cards at roughly two-thirds size with matching internal scaling and reported long cash balances escaping their card. Generated Phase 1H in the canonical installer: target cards change from `160` to `108 px` (`108 x 95` at the `0.88` aspect), tier/rating/name/fallback/Buy More content and card strokes scale down, names self-fit at `5-7 px`, and cash reserves the Plus-button area with `14-5 px` text scaling plus container clipping. Phase 1G gradients, borderless chrome, dropdown behavior, gameplay contracts, and input handoff remain. No fragile text replacement, PC owner, bootstrap, server gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun, device confirmation, and mirror refresh.

- 2026-07-13: Launched-phone review of Mobile Free-Roam UI Phase 1F found long vehicle names clipped and too much vertical space spent on `MY VEHICLES`, field captions, dropdowns, and Despawn. Generated Phase 1G in the canonical installer: one-line `TextScaled` vehicle names constrained to `6-8 px`, `160 px` target cards, a caption-free `28 px` dropdown row at the panel top, `36 px` total header, `20 px` Despawn footer, tighter safe padding/gaps, and removal of the outer-panel and Despawn border/glow while approved gradients and card borders remain. Same-field dropdown toggle and all existing menu/gameplay contracts are preserved. No fragile text replacement, PC owner, bootstrap, gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun, device confirmation, and mirror refresh.

- 2026-07-13: Phase 1E dropdown styling was approved, but screenshot review showed its strict three-row interpretation over-shrank cards to roughly `120 x 105` while retaining an unnecessarily wide panel. Generated Phase 1F in the canonical installer: two complete visible rows, target `180 x 158` cards at the reported viewport, panel width derived from two cards plus compact padding/gap, smaller title/dropdowns/Despawn/footer, tighter internal card spacing, 3px left and 2px bottom defaults, and explicit dropdown-owner tracking so tapping the open field closes it. Untouched Phase 1E gradients and safe-padding contracts remain. No PC controller, bootstrap, gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun and mirror refresh.

- 2026-07-13: Phase 1D mobile car-menu behavior worked overall, but screenshot review exposed a width-only card sizing bug: two 0.88-aspect rows exceeded the clipped grid viewport and ran into the fixed Despawn footer. Expanded dropdown choices also inherited the general pink-outlined button instead of the PC neutral surface. Generated Phase 1E in the canonical installer with a width-and-three-row height solver, centred two-column grid, PC stroke/top/bottom padding and actual-content canvas sizing, 32px Despawn, exact PC effects-driven panel/button gradients/glow/facet values, and borderless neutral dropdown choice rows. No PC controller, bootstrap, gameplay, physics, VFX, or LOD changes. Awaiting Studio rerun and mirror refresh.

- 2026-07-13: User approved Mobile Free-Roam UI Phase 1C and refreshed the Studio mirror at 15:33:06; both canonical Phase 1C owners and version attributes are present. Generated Phase 1D in the same canonical installer: a responsive left-side PC-parity car menu ports category/sort, two-column cards, images, tier/rating/selected styling, Buy More, spawn, and Despawn contracts without editing the confirmed PC controller or bootstrap. A transparent outside-tap layer keeps the top HUD visible but non-interactive, while a shared menu-open attribute clears and hides driving input/UI until close. Awaiting Studio install and device confirmation.

- 2026-07-13: Generated Mobile Free-Roam UI Phase 1C from the user's refined landscape sketch. Upgraded the same canonical installer without text replacement or bootstrap edits: action buttons now align left of the top-right map, cash sits directly below, arrow width is configurable at `1.5x` with gaps/alignment preserved, the real PC lightning boost target is centred above the steering cluster at `1.5x` visual scale, and the lowered bottom telemetry uses a shallow 16-segment overhead arc plus a bottom-up vertical boost meter and display-only lightning icon. Phase 1B map parity and exact legacy-child suppression remain. Awaiting Studio install, device testing, and post-install mirror refresh.

- 2026-07-13: Generated Mobile Free-Roam UI Phase 1B after the post-Phase-1 mirror refresh at 14:58:02 and screenshot review. Root causes were an incomplete map port (missing flips/offset/exact Y sign/fades), a custom vertical navigation layout, approximate 12-segment telemetry, small controls with overflowing fallbacks, and the bootstrap-created `HOVER_RACING_V2_DriveHUD` remaining visible. Upgraded the same canonical installer to port the Phase 4A map/action/boost/metric/16-segment contracts, enlarge controls, embed Exit left of MPH, use the boost icon as the touch target, and hide only the exact legacy visual children without editing the bootstrap or disabling the ScreenGui compatibility signal used by the mobile/VFX helper. Awaiting Studio rerun and visual/device confirmation.

- 2026-07-13: Generated Mobile Free-Roam UI Phase 1. Added a read-only contract audit and one canonical installer that replaces the retired Phase 16E mobile compatibility loaders with isolated touch-only HUD and control owners. The HUD reuses the Phase 4A map, theme, telemetry, garage, race-browser, dealership teleport, spawn/despawn, and exit contracts. Mobile Settings puts Arrows, Thumbstick, and gyroscope-gated Tilt first; Arrows are default, Tilt has held Drift and Recenter, and the choice remains session-only. Added four deterministic transparent Roblox-ready arrow/pedal PNGs plus a reproducible renderer. Awaiting Studio install, image ids, emulator/real-device testing, and mirror refresh.

- 2026-07-13: User confirmed Racing UI Phase 16F works; local authored course arrows now appear and the Phase 16E lag improvement remains. Locked the completed PC Racing UI stream in `docs/racing-ui-final-handoff-2026-07-13.md`, including current screen contracts, ownership boundaries, config paths, local-only map-marker policy, performance lessons, StreamingEnabled arrow lesson, and required next-chat startup. The post-16F Studio mirror still needs refreshing/pushing before another source patch.
- 2026-07-13: User confirmed Racing UI Phase 16E substantially improved the driving lag, but local course-guidance arrows remained invisible. The refreshed mirror established the root cause: with StreamingEnabled, `RaceSessionAssetsClient_Active` could cache a segment folder before its MeshParts arrived, permanently retaining an empty part list and then short-circuiting on an unchanged run/route/segment signature. Added Phase 16F as `scripts/roblox_racing_ui_phase16f_streaming_safe_arrow_visibility.lua`: late streamed parts inherit current segment visibility, missing routes retry, route identifiers receive normalized fallback resolution, and the existing attribute-or-zero transparency rule is preserved. No opponent markers, broad scans, LOD changes, gameplay progression changes, or server collision changes were added.
- 2026-07-13: User confirmed Driving Feel Phase 2.1 feels much better. Added a plain-language tuning reference covering the shared dynamics folder, everyday feel controls, detailed vehicle-stat scaling, parking/reverse thresholds, advanced switches, and the separate legacy driving-mechanics attributes. Post-confirmation Studio mirror refresh remains required.
- 2026-07-13: Phase 2 handling was reported good overall, but drift lost nearly all forward drive and reverse did not move. Refreshed the post-Phase-2 Studio mirror and confirmed two direct causes: fixed full-drift forward drag reached `1.14`, independent of the low Drift stat, and `SnapForwardStop` continued after Reverse mode began. Added guarded Phase 2.1 with configurable drift drag defaulting to `0.28`, a one-second reverse delay, and stopped-hold release when reverse engages.
- 2026-07-13: Added Racing UI Phase 16E runtime ownership cleanup. It permanently retires obsolete PC free-roam/race presentation owners without constructing their UI, preserves the legacy mobile paths behind touch-only loaders, replaces the mixed legacy entry menu with a headless spawn/start/handoff bridge, removes the old checkpoint/session/results presentation, removes Phase 8H's repeated PlayerGui suppression scan while preserving reset/camera fades, changes participant visibility to event-driven updates, keeps only the local race-map marker, and repairs local route-arrow visible transparency to use `NTR_ArrowOriginalTransparency` or `0`. LOD, rewards, PB ownership, matchmaking, server route progression, collision proxies, Phase 8H reset, and Phase 11Y cleanup are unchanged. Awaiting Studio install/play-test.
- 2026-07-13: User confirmed Driving Feel Phase 1 worked well and refreshed the Studio mirror at 12:30:18; the mirror contains 113 scripts including `VehicleDynamicsModel`, the safe controller require, dynamics bridge, and 26-attribute config. Added guarded Phase 2 handling/drift calibration: detailed LateralGrip/Downforce normal grip, DriftGrip slip resistance, DriftControl side-force/turn authority, DriftChargeRate charge speed, HoverStability alignment response, independent `HandlingEnabled` rollback, and no changes to Phase 1 longitudinal behavior.
- 2026-07-13: First Driving Feel Phase 1 Play startup reported `Infinite yield possible on ... Controllers:WaitForChild("VehicleDynamicsModel")`, followed by bootstrap line 3837 `startDriving` nil calls. The controller bridge persisted but the isolated module was absent at runtime. Hardened the same canonical Phase 1 installer so reruns recreate/refresh the module and upgrade the original blocking require to a non-yielding guarded load with legacy-force fallback; no bootstrap patch or additional recovery script was added.
- 2026-07-13: Driving Feel Phase 0 passed `11/10/0` (`pass/warn/fail`) in Edit mode. The client Play run captured a `C 538` vehicle with legacy/detailed pairs `81/81`, `45/45`, `9/9`, `106/106`, and `25/25`, plus `TopSpeed 139`, `Weight 137`, `Drag 50`, a collidable `Default` root, and assembly mass `241`. Its two failures were client-context server-visibility false negatives; the canonical audit now skips those checks on the client. Added `scripts/roblox_driving_feel_phase1_core_dynamics.lua` and its handoff: four guarded controller anchors, an isolated `VehicleDynamicsModel`, config-driven progressive acceleration/coasting/braking/stopped/reverse behavior, detailed-stat reconnection, and feature-flag rollback. Awaiting Studio install/play-test.
- 2026-07-13: Analysed the fresh 10:59:59 Studio mirror for acceleration, braking, handling, drift, parking, exit placement, and player pushing. The active controller retains the high initial acceleration/linear limiter/combined brake-reverse path and no longer contains the documented Phase AM detailed-stat bridge while its enable flag remains true. Added the read-only Phase 0 gate plus four condensed driving-feel working phases and `scripts/roblox_driving_feel_phase0_live_audit.lua`. The first paste reported `CommandBar:222: Expected identifier when parsing expression, got '..'`; the canonical audit was repaired by removing the premature closing parenthesis in its runtime root-metrics print expression.

- 2026-07-11: Generated Racing UI Phase 1F after changing only `RaceCatalog.ShiftedCanalSprint.DisplayName` updated some but not all menus. Root cause: paired Race/Time Trial catalogs and route metadata each carried their own display name, while the browser preferred the time-trial summary. Phase 1F adds an explicit Race Catalog `SharedMenuDisplayName` owner plus an isolated synchronizer for matching Race, Time Trial, and route names; it avoids patching every menu or critical racing service.
- 2026-07-11: User reported Racing UI Phase 1 V4 looking good and requested final browser polish. Updated the canonical installer to V5: one top-left `RACE BROWSER` title, removed duplicate centred header text, made the header divider 50% transparent, and added background-only neutral gradients to event cards beneath their images/text. Awaiting rerun/final Phase 1 confirmation.
- 2026-07-11: Racing UI Phase 1 V3 was reported good overall, but footer text still had pink/cyan outlines and the content-to-footer gap remained too large. Root cause was the shared UI stroke using Roblox's contextual default rather than `ApplyStrokeMode.Border`; no old UI owner was fighting it. Updated the canonical installer to V4 with border-only strokes, `16 px` taller desktop content, and footer buttons moved up `8 px`. Awaiting rerun/confirmation.
- 2026-07-11: Racing UI Phase 1 V2 screenshot review exposed the same selected-card stroke clipping previously solved in the free-roam car menu, plus footer buttons made faint because the gradient was attached directly to the `TextButton`. Updated the same canonical installer to V3 with a physically inset `CardContent` frame and the exact Phase 4A background-only gradient plus paired border/glow pattern. Awaiting rerun/confirmation.
- 2026-07-11: Racing UI Phase 1 installed and started cleanly in Play. Screenshot review found redundant availability chips/detail borders, an over-boxed event-list shell, PB copy where route format was more useful, and a saturated Teleport button that did not match Phase 4A. Updated the same canonical Phase 1 installer to V2: full-width simplified event cards, unboxed availability and route summary, cyan selected event, simplified detail panel, `TELEPORT`, and shared neutral button gradients. Awaiting rerun/visual confirmation.
- 2026-07-11: Racing UI Phase 0 passed in Studio Edit mode with `pass=103 warn=1 fail=0`; the only warning was the already-understood missing Phase 5F comment marker while the current Phase 11L V2 route/session asset owner passed. Added Phase 1 as `scripts/roblox_racing_ui_phase1_shared_shell_browser.lua`: shared semantic config/components plus a canonical isolated Race Browser replacement. It is awaiting Studio install/playtest and does not touch entry, rewards, PBs, matchmaking, reset, result lifecycle, or the bootstrap.
- 2026-07-11: User approved the Racing UI design gates for time-trial/race entry, records, tier vehicle selection, equal-column results, simplified free-roam Race Browser, responsive mobile reflow, and shared component states. Added `docs/racing-ui-design-system-2026-07-11.md`, saved the approved Gate A/B boards under `assets/ui/mockups/racing/`, and added the read-only pre-install gate `scripts/roblox_racing_ui_phase0_preinstall_audit.lua`. No Studio UI or gameplay source was changed.
- 2026-07-11: User confirmed PC free-roam UI Phase 4A working, refreshed the Studio mirror, and pushed Git. Phase 4A is now the locked free-roam UI baseline: responsive desktop HUD/car menu, live speed and boost, north-up four-tile minimap with image-only markers, and shared server-authoritative dealership teleport. Later free-roam phases are intentionally deferred while work moves to race/time-trial menus and prize presentation. Added the final free-roam handoff and updated startup, UI, known-issues, continuous-improvement, and racing handoff guidance.
- 2026-07-11: User confirmed the Phase 3D HUD/map looked good and refreshed the Studio mirror. Added Phase 4A as `scripts/roblox_ui_freeroam_pc_phase4a_dealership_teleport.lua` plus its handoff doc. It preserves the canonical HUD, installs an isolated `FreeRoamHudTeleportInvoke` and `FreeRoamHudTeleportService_Active`, and connects the existing YES modal action to server-side cooldown/race validation, unseat, owned runtime vehicle destruction, editable dealership-marker teleport, and the existing client driving-exit handoff.
- 2026-07-11: Added Phase 3D as `scripts/roblox_ui_freeroam_pc_phase3d_image_only_map_markers.lua` plus its handoff doc. It preserves Phase 3C's north-up clipped 2D map, makes the centred player marker a fully transparent image-only label, and adds a transparent bottom-right `MapNorthArrow` image with configurable asset, size, and margin. Empty asset values render nothing; no fallback frames/shapes are created.
- 2026-07-11: User selected the lower-cost north-up 2D minimap after Phase 3B's ViewportFrame cropped correctly but changed image appearance and complicated UV/coordinate calibration. Added Phase 3C as `scripts/roblox_ui_freeroam_pc_phase3c_north_up_2d_minimap.lua` plus its handoff doc. Phase 3C translates an unrotated four-ImageLabel canvas for reliable clipping and original pixel appearance, applies independent `MapCoordinateRotationDegrees=90`, and adds configurable `Assets.MapPlayerIcon`, `MapPlayerIconSize`, and icon-heading rotation.
- 2026-07-11: Added Phase 3B as `scripts/roblox_ui_freeroam_pc_phase3b_viewportframe_minimap.lua` plus its handoff doc after Phase 3A's rotated stitched UI escaped the minimap box and screenshot annotations showed a 90-degree coordinate mismatch. Phase 3B replaces the 2D canvas with a ViewportFrame, WorldModel, four top-Decal tiles, and a top-down camera; adds independent `MapCoordinateRotationDegrees=90`; and preserves live movement, ROTATE/NORTH modes, flips, smoothing, and `MapVisibleStuds` zoom without changing the box size.
- 2026-07-11: Repaired the canonical Phase 3A minimap installer after its first Play run failed at controller line 831. Root cause was one stale `arrow.ZIndex = 16` reference left after renaming the minimap arrow to `playerArrow`; the nil error interrupted construction before layout and legacy-UI suppression, causing partial top-left icons plus old UI. Removed the stale line from `scripts/roblox_ui_freeroam_pc_phase3a_four_tile_live_minimap.lua`; rerun the same canonical installer rather than adding a source-patch ladder.
- 2026-07-10: User refreshed the Studio mirror after approving the Phase 2I HUD and supplied minimap calibration: four 1024px images form a 2048px square, 207px represents 2850 studs, and world 0,0,0 is the image centre. Added Phase 3A as `scripts/roblox_ui_freeroam_pc_phase3a_four_tile_live_minimap.lua` plus its handoff doc. It canonically replaces only the isolated desktop HUD controller, adds four tile asset slots, translates/rotates the stitched canvas around a centred player arrow, and exposes world-centre, scale, visible range, smoothing, rotation offset, and X/Z flip controls.
- 2026-07-10: Added Phase 2I as `scripts/roblox_ui_freeroam_pc_phase2i_borderless_boost_icon.lua` plus its handoff doc after the user requested image-only boost presentation. Phase 2I removes the boost icon fill, border, glow, rounded frame, and inset so the configured image fills the complete icon area while retaining Phase 2H positioning/config and Phase 2G live telemetry.
- 2026-07-10: User confirmed Phase 2G looked good and requested the boost group move up 5px plus a configurable icon instead of text. Added Phase 2H as `scripts/roblox_ui_freeroam_pc_phase2h_configurable_boost_icon.lua` plus its handoff doc. It sets `BoostBarOffsetY=67`, replaces `BOOST >` with a 32px blue-accent image box, adds `Assets.BoostIcon`, `BoostIconSize`, and `BoostIconGap`, and preserves live boost telemetry without touching driving/bootstrap/mobile owners.
- 2026-07-10: User confirmed Phase 2F's physical vehicle-card offset works well. Added Phase 2G as `scripts/roblox_ui_freeroam_pc_phase2g_live_boost_telemetry.lua` plus its handoff doc. Phase 2G canonically replaces only the isolated desktop HUD controller, consumes the existing shared V75 `BoostPercent`, smooths the left-to-right fill, doubles the bar thickness to 24px, moves it lower/left, and adds editable width/height/X/Y/smoothing values. It does not patch boost mechanics, the bootstrap, mobile UI, server, or VFX.
- 2026-07-10: Phase 2E installed but the requested card-box movement was not visible. Added Phase 2F as `scripts/roblox_ui_freeroam_pc_phase2f_vehicle_card_content_offset.lua` plus its handoff doc. Phase 2F removes the ineffective `UIPadding` route and physically positions a transparent `CardContent` frame below the scrolling boundary, parenting the grid and every vehicle card to it while retaining header-region clipping.
- 2026-07-10: User confirmed all Phase 2D component changes worked except first-row vehicle-card top clipping. Identified the root cause: `VehicleGrid.ClipsDescendants` clipped card strokes because `UIGridLayout` began at local `Y = 0`; moving the scrolling frame moved the cards and clipping boundary together. Added Phase 2E as `scripts/roblox_ui_freeroam_pc_phase2e_vehicle_grid_internal_padding.lua` plus its handoff doc. Phase 2E canonically replaces only the isolated desktop HUD controller and uses an internal `UIPadding.PaddingTop` while preserving header-region clipping and all confirmed Phase 2D behavior.
- 2026-07-10: User confirmed Phase 2C substantially repaired PC/Laptop placement and visibility, then refreshed the Studio mirror. Added Phase 2D as `scripts/roblox_ui_freeroam_pc_phase2d_component_polish.lua` plus its handoff doc. It canonically replaces only the isolated desktop HUD controller, adds configurable first-row card clearance, same-header dropdown toggling, a neutral borderless dropdown shell, paired border/glow accent updates, a borderless minimap, shared neutral button gradients, and semantic Bold/Italic config values. It does not patch the register-limited bootstrap or gameplay/server/VFX/driving systems.
- 2026-07-10: Phase 2B was installed and runtime-audited at `pass=42 warn=1 fail=0`. Screenshot review found a full-HUD downward shift, clipped vehicle-card strokes, intermittent startup visibility, and missing Laptop-emulator UI. Added Phase 2C as `scripts/roblox_ui_freeroam_pc_phase2c_inset_visibility_card_edge_repair.lua` plus its handoff doc. Root causes were `IgnoreGuiInset=false`, a broad name-based major-menu scan that could mistake dealership companion UI for the actual menu, touch-only desktop rejection, exact-fit scroll clipping, and reliance on viewport signals. Phase 2C canonically replaces only the isolated controller and leaves gameplay/bootstrap/mobile owners untouched.
- 2026-07-10: Phase 2A runtime diagnostics completed with `fail=0` across two viewports, including a confirmed driving capture. Added `scripts/roblox_ui_freeroam_pc_phase2b_canonical_visual_foundation.lua` and its handoff doc. Phase 2B canonically replaces only the isolated desktop HUD controller, fixes the measured responsive layout/typography problems, adds semantic typography/effect tuning, preserves existing gameplay actions, and leaves the register-limited bootstrap untouched. Updated the Phase 2A diagnostic for action-bar safe-area and four-edge minimap verification. Phase 2B is not installed or confirmed yet.
- 2026-07-10: User refreshed the Studio mirror after the PC free-roam Phase 1 screenshot review. Added the read-only Phase 2A runtime layout diagnostic `scripts/roblox_ui_freeroam_pc_phase2a_runtime_layout_diagnostic.lua` and handoff `docs/ui-free-roam-pc-phase2a-runtime-layout-diagnostic-2026-07-10.md`. It measures the eight reported layout/typography problems in live screen coordinates and gates a canonical replacement of the isolated desktop HUD controller; it does not change Studio or the register-limited bootstrap.
- 2026-07-10: Phase 0 PC free-roam UI audit passed in Studio Edit mode with `pass=35 warn=2 fail=0`; both warnings were expected missing future setup objects. Added `scripts/roblox_ui_freeroam_pc_phase1_visual_shell.lua` plus its handoff doc. Phase 1 installs an isolated configurable PC HUD/controller, real existing free-roam car actions, shared modal presentation, and an editable dealership teleport marker without patching the register-limited bootstrap. It is not installed or tested yet.
- 2026-07-10: Locked the user-approved six-screen PC free-roam UI visual baseline. Added the six concept images under `assets/ui/mockups/free_roam_pc/`, the authoritative colour/component specification `docs/ui-free-roam-pc-design-system-2026-07-10.md`, and the read-only pre-install audit `scripts/roblox_ui_freeroam_pc_phase0_audit.lua`. No Studio UI or gameplay source was changed.
- 2026-07-10: User confirmed Racing Phase 11Z as the locked racing prototype release-candidate baseline, refreshed the Studio mirror, and pushed Git. Added `docs/racing-next-chat-handoff-2026-07-10.md` for future racing chats, summarising the confirmed baseline, key scripts, next branch options, workflow rules, and remaining prototype risks.
- 2026-07-10: User confirmed Racing Phase 11Y works after repeated time-trial finish/exit testing; re-entry, race browser teleport, and free-roam vehicle spawning recovered. Added Racing Phase 11Z Post-11Y Release Candidate Audit as `scripts/roblox_racing_phase11z_post_11y_release_candidate_audit.lua` plus `docs/racing-phase11z-post-11y-release-candidate-audit-2026-07-10.md`. Phase 11Z is read-only and extends the 11X audit with Phase 11Y source-marker checks plus runtime detection for stale finished-pending or orphan grid-spawned vehicles.
- 2026-07-10: Added Racing Phase 11Y Time Trial Finish Lifecycle Recovery as `scripts/roblox_racing_phase11y_time_trial_finish_lifecycle_recovery.lua` plus `docs/racing-phase11y-time-trial-finish-lifecycle-recovery-2026-07-10.md` after Phase 11X initially passed but repeated finish/exit loops could still leave the player unable to re-enter, teleport, or spawn. Root cause from the mirror: `finishRun()` could make the finished time-trial vehicle drive-ready while result-exit cleanup was still pending. Phase 11Y keeps finished vehicles frozen/drive-disabled/pending cleanup, adds stale pending cleanup recovery, and keeps the isolated result coach visible until server cleanup confirms.
- 2026-07-10: User confirmed Racing Phase 11W V2 works after PB save/rejoin testing. Added Racing Phase 11X Prototype Release Candidate Audit as `scripts/roblox_racing_phase11x_prototype_release_candidate_audit.lua` plus `docs/racing-phase11x-prototype-release-candidate-audit-2026-07-10.md`. Phase 11X is read-only and checks the current racing prototype loop before the next feature/polish branch.
- 2026-07-10: Updated Racing Phase 11W to V2 after the first Edit-mode audit failed on missing `RacePersonalBestBindings`. Root cause: those BindableFunctions are created by `RacePersonalBestService_Active` during Play/runtime, not persisted in Edit mode. V2 treats missing runtime bindings as Edit-mode warnings, preserves hard checks for Play/server context, and prints a clearer reminder that saved PBs require `MODE = "ENABLE_DATASTORE_TEST"` plus a Play restart.
- 2026-07-10: User-provided Racing Phase 11V Play-client audit passed with `pass=79 warn=1 fail=0`; the only warning was expected because server scripts are not replicated to Play clients. Added Racing Phase 11W Time Trial PB DataStore Verification as `scripts/roblox_racing_phase11w_time_trial_pb_datastore_verification.lua` plus `docs/racing-phase11w-time-trial-pb-datastore-verification-2026-07-10.md`. Phase 11W is a config/audit helper: default `AUDIT` is read-only, `ENABLE_DATASTORE_TEST` deliberately enables PB DataStore testing, and `DISABLE_DATASTORE_TEST` turns it back off.
- 2026-07-10: Updated Racing Phase 11V Prototype Release Audit to V2 after the first Studio live-edit run showed `pass=73 warn=4 fail=2` only because runtime collision groups were not registered in Edit mode. V2 still checks the server source marker for `RegisterCollisionGroup`, inspects server scripts in Edit even when Studio reports `IsClient=true`, treats missing live collision groups as Edit-mode warnings, and keeps live collision policy checks for Play/runtime.
- 2026-07-10: Added Racing Phase 11V Prototype Release Audit as `scripts/roblox_racing_phase11v_prototype_release_audit.lua` plus `docs/racing-phase11v-prototype-release-audit-2026-07-10.md`. It is read-only and checks the post-11U racing prototype baseline: remotes/config, services/source markers, rewards, PBs, result exit, result coach, narrow HUD cleanup, route arrows, collision groups, visibility markers, runtime race instances, and Play-client UI state.
- 2026-07-10: User confirmed Racing Phase 11U V2 works. Current result/HUD baseline: Phase 11T result coach remains visible, Phase 11U V2 hides only the stale top `NTR_RaceHud_Phase3.Panel`, and re-entry/teleport are restored.
- 2026-07-10: Updated Racing Phase 11U to V2 after testing showed the first cleanup was too broad: hiding legacy result/control panels could remove the medal UI/fallback exit path and leave re-entry/teleport blocked. V2 replaces `RaceHudExitCleanupClient_Active` again but only hides `NTR_RaceHud_Phase3.Panel`, restores result ScreenGuis to enabled, and leaves medal/result/exit UI ownership to Phase 11T or the legacy result panel.
- 2026-07-10: Added Racing Phase 11U Time Trial HUD Exit Cleanup as `scripts/roblox_racing_phase11u_time_trial_hud_exit_cleanup.lua` plus `docs/racing-phase11u-time-trial-hud-exit-cleanup-2026-07-10.md` after Phase 11T worked but the old top `NTR_RaceHud_Phase3` finished-time card could remain visible after `EXIT TO START`. Phase 11U is an isolated local cleanup client that hides known legacy race HUD/result/control panels on racing finish/end/exit events and the existing `FreeRoamVehicleExited` handoff, without patching `RaceEntryMenuClient_Active` again.
- 2026-07-10: Added Racing Phase 11T Isolated Time Trial Result Coach as `scripts/roblox_racing_phase11t_isolated_time_trial_result_coach.lua` plus `docs/racing-phase11t-isolated-time-trial-result-coach-2026-07-10.md`. Phase 11T avoids another `RaceEntryMenuClient_Active` patch by installing a separate local result coach that hides the old result panel, shows clearer PB/next-medal/prize/lap feedback, and uses existing `StartStagedTimeTrial` / `ExitFinishedTimeTrial` actions.
- 2026-07-10: Updated Racing Phase 11S Stability Baseline Audit to V2 after the first Play-client run showed false server failures. Roblox clients cannot see `ServerScriptService`, so V2 skips server-only source checks with a warning when run from a Play client while preserving full checks in Edit/server context.
- 2026-07-10: Added Racing Phase 11S Stability Baseline Audit as `scripts/roblox_racing_phase11s_stability_baseline_audit.lua` plus `docs/racing-phase11s-stability-baseline-audit-2026-07-10.md` after Phase 11R playtest output looked healthy. Phase 11S is read-only and checks key remotes/config, racing services, client controllers, Phase 11Q/11R handoff markers, PB/reward/session asset baselines, route structure, and stale runtime race instances.
- 2026-07-10: User confirmed Racing Phase 11R works from playtest output: reward granted, `TimeTrialFinished`, transition active=false, camera restored to humanoid, `TimeTrialEnded`, and successful `OpenRaceEntry` after exiting.
- 2026-07-10: Added Racing Phase 11R Time Trial Exit Handoff Helper Repair as `scripts/roblox_racing_phase11r_time_trial_exit_handoff_helper_repair.lua` plus `docs/racing-phase11r-time-trial-exit-handoff-helper-repair-2026-07-10.md` after the user confirmed Phase 11Q worked and refreshed the mirror. The mirror showed the Phase 11Q helper marker and `local clientRoot` collapsed onto one comment line; Phase 11R fixes only that isolated helper source shape.
- 2026-07-10: User confirmed Racing Phase 11Q works; time-trial result exit no longer leaves the player stuck in driving HUD/state, and re-entry/teleport recovered.
- 2026-07-10: Added Racing Phase 11Q Time Trial Finish/Exit Handoff Repair as `scripts/roblox_racing_phase11q_time_trial_finish_exit_handoff_repair.lua` plus `docs/racing-phase11q-time-trial-finish-exit-handoff-repair-2026-07-10.md` after Phase 11P testing reported the result flow did not work and exiting left the client in driving HUD/state. The refreshed mirror showed Phase 11P installed, but the root issue is the missing `FreeRoamVehicleExited` handoff after time-trial finish/result exit/end. Phase 11Q rolls back Phase 11P result-copy polish and patches only `RaceEntryMenuClient_Active` to fire the existing driving-exit handoff.
- 2026-07-10: Phase 11P Time Trial Result Coach is marked unconfirmed/rejected as a baseline. Do not build further result polish on it until the finish/exit handoff is stable.
- 2026-07-10: Added Racing Phase 11P Time Trial Result Coach as `scripts/roblox_racing_phase11p_time_trial_result_coach.lua` plus `docs/racing-phase11p-time-trial-result-coach-2026-07-10.md` after Phase 11O V2 was confirmed working. Phase 11P is a UI-only guarded patch to the isolated race entry/results client that clarifies PB deltas, next-medal gap, prize wording, and the result `EXIT TO START` label without touching timing, rewards, arrows, VFX, matchmaking, driving, DataStore settings, global leaderboards, or the bootstrap.
- 2026-07-10: User confirmed Racing Phase 11O V2 works; the local PB board now closes when exiting the race entry menu without starting.
- 2026-07-10: Updated Racing Phase 11O Time Trial PB Board to V2 in `scripts/roblox_racing_phase11o_time_trial_pb_board.lua` after testing showed the board persisted when the race entry menu was exited without starting. V2 still replaces only the isolated `RacePersonalBestBoardClient_Active`, but now watches the entry menu visibility and hides the PB board when that owning menu closes.
- 2026-07-10: Added Racing Phase 11O Time Trial PB Board as `scripts/roblox_racing_phase11o_time_trial_pb_board.lua` plus `docs/racing-phase11o-time-trial-pb-board-2026-07-10.md` after Phase 11N was confirmed working. Phase 11O creates one isolated `RacePersonalBestBoardClient_Active` LocalScript that listens for `OpenRaceEntry`, uses the Phase 11N `GetTimeTrialPersonalBest` action, and shows local PBs by vehicle tier for the paired time-trial event. It does not patch the entry menu again and does not touch global/friends leaderboards, ghosts, rewards, route-guide config, arrows, VFX, matchmaking, driving, or the bootstrap.
- 2026-07-10: User confirmed Racing Phase 11N Time Trial PB Readout works. The current prototype PB UI baseline includes server action `GetTimeTrialPersonalBest`, time-trial vehicle-card PB text by event+tier, selected-vehicle PB status, and cache refresh from `TimeTrialFinished`.
- 2026-07-10: Added Racing Phase 11N Time Trial PB Readout as `scripts/roblox_racing_phase11n_time_trial_pb_readout.lua` plus `docs/racing-phase11n-time-trial-pb-readout-2026-07-10.md` after the Phase 11L V2 arrow repair was confirmed working. Phase 11N patches only `TimeTrialService_Active` and `RaceEntryMenuClient_Active` to expose `GetTimeTrialPersonalBest`, show PB text on time-trial owned vehicle cards by event+tier, cache PB lookups, and refresh the cache from `TimeTrialFinished`. Global/friends leaderboards, ghosts, reward config, route-guide config, arrows, VFX, matchmaking, driving, and bootstrap edits remain deferred/untouched.
- 2026-07-10: User confirmed Racing Phase 11L Arrow Visual Proxy Sync Repair V2 works. The current route-arrow visual baseline is marker `NTR_RACING_PHASE11L_ARROW_VISUAL_PROXY_SYNC_V2_TRANSPARENCY_RESTORE`: sync visual segments from server proxy `ParticipantSegments` and restore saved `NTR_ArrowOriginalTransparency` when showing visible arrow folders.
- 2026-07-09: Added Racing Phase 11L Arrow Visual Proxy Sync Repair as `scripts/roblox_racing_phase11l_arrow_visual_proxy_sync_repair.lua` plus `docs/racing-phase11l-arrow-visual-proxy-sync-repair-2026-07-09.md` after solo time-trial testing showed visual arrows could stop advancing after the early checkpoints while server arrow/barrier proxies still created and cleared correctly. Root cause: the Phase 11L client arrow visual window could drift from the server-owned session asset segment state. The repair canonically replaces only `RaceSessionAssetsClient_Active` so visuals read `ArrowBarrierProxies.ParticipantSegments` for the local run. The script was then updated to marker `NTR_RACING_PHASE11L_ARROW_VISUAL_PROXY_SYNC_V2_TRANSPARENCY_RESTORE` after testing showed the first proxy-sync version left all arrows invisible by not restoring saved `NTR_ArrowOriginalTransparency` from the Phase 10B hidden baseline.
- 2026-07-09: Added Racing Phase 11M Time Trial Personal Best Persistence as `scripts/roblox_racing_phase11m_time_trial_personal_best_persistence.lua` plus `docs/racing-phase11m-time-trial-personal-best-persistence-2026-07-09.md` after Phase 11L was confirmed working. Phase 11M installs `RacePersonalBestService_Active`, creates configurable `Config.Racing.PersonalBests`, and patches `TimeTrialService_Active` so server-authoritative time-trial finishes record saved PBs per player/event/tier before reward calculation. DataStore writes default off for prototype safety.
- 2026-07-09: Added Racing Phase 11L Multi-Session Visibility Owner as `scripts/roblox_racing_phase11l_multi_session_visibility_owner.lua` plus `docs/racing-phase11l-multi-session-visibility-owner-2026-07-09.md` after overlap testing showed entering a solo time trial while another player remained in a multiplayer race could hide the race vehicle body but still leak idle engine VFX. Root cause from the refreshed mirror: `RaceParticipantVisibilityClient_Active` and an older visibility loop inside `RaceEntryMenuClient_Active` both owned visibility with single-session state; `RaceSessionAssetsClient_Active` had the same overwrite risk for route arrows. Phase 11L canonically replaces the isolated visibility/session-assets clients with RunId-keyed multi-session owners and disables the old entry-menu visibility loop.
- 2026-07-09: Added Racing Phase 11K Time Trial Result Exit Cleanup as `scripts/roblox_racing_phase11k_time_trial_result_exit_cleanup.lua` plus `docs/racing-phase11k-time-trial-result-exit-cleanup-2026-07-09.md` after 2-player testing showed one player could exit a finished Silver time trial and then fail to re-enter, spawn a free-roam vehicle, or use Race browser teleport. Root cause from the mirrored source: `finishRun()` removed the active run, then the result-panel `EXIT` button called `CancelTimeTrial`, so no active run remained for cleanup. Phase 11K caches finished runs until result exit, adds `ExitFinishedTimeTrial`, and makes cancel fall back to finished cleanup.
- 2026-07-09: Added Racing Phase 11J Prototype Baseline Audit as `scripts/roblox_racing_phase11j_prototype_baseline_audit.lua` plus `docs/racing-phase11j-prototype-baseline-audit-2026-07-09.md` after Phase 11I was reported to work. Phase 11J is read-only and audits the prototype race/time-trial stack, including route structure, remotes/config, collision groups, key source markers through Phase 11I, and optional runtime race instance/vehicle state. Prototype scope now defers private/reserved race servers, advanced session assets, and player-created race tools.
- 2026-07-09: Added Racing Phase 11I Idle Engine VFX Flush as `scripts/roblox_racing_phase11i_idle_engine_vfx_flush.lua` plus `docs/racing-phase11i-idle-engine-vfx-flush-2026-07-09.md` after Phase 11H mostly fixed visibility leaks but idle engine VFX remained visible. Root is likely live `ParticleEmitter` remnants after `Enabled=false`; Phase 11I patches only the isolated racing visibility client to clear hidden ParticleEmitters/Trails and force `NTR_VFXRuntimeHost` parts hidden.
- 2026-07-09: Added Racing Phase 11H Visibility, VFX, And Name Tag Gate as `scripts/roblox_racing_phase11h_visibility_vfx_nametag_gate.lua` plus `docs/racing-phase11h-visibility-vfx-nametag-gate-2026-07-09.md` after Phase 11G fixed arrow collision but race/time-trial VFX and name tags still leaked across the session boundary. Phase 11H canonically replaces only the isolated racing participant visibility client and hides bodies, vehicles, VFX/lights/gui descendants, highlights, and humanoid name/health displays with a late-frame gate so VFX owners cannot re-enable hidden session effects before presentation.
- 2026-07-09: Added Racing Phase 11G Studio UserId Session Asset Fix as `scripts/roblox_racing_phase11g_studio_userid_session_asset_fix.lua` plus `docs/racing-phase11g-studio-userid-session-asset-fix-2026-07-09.md`. Phase 11F diagnostics showed `RaceCheckpoint` events fired but `ArrowBarrierProxies.ParticipantSegments` stayed `-1:0,-2:0`; root cause was `RaceSessionAssetService_Active` rejecting negative Studio local-server test UserIds during `UpdateParticipantSegment` and `RemoveParticipant`. Phase 11G accepts any non-zero numeric UserId so local multiplayer race collision windows and participant removal match production positive-UserId behaviour.
- 2026-07-09: Added Racing Phase 11F Runtime Isolation Diagnostic as `scripts/roblox_racing_phase11f_runtime_isolation_diagnostic.lua` plus `docs/racing-phase11f-runtime-isolation-diagnostic-2026-07-09.md` after the user reported Phase 11E did not affect live multiplayer race collision/VFX behaviour. This is read-only and should be run during the broken race state before another repair; it audits live Phase 11E source markers, collision-group policy, active race proxy segments, runtime vehicle collision groups, and VFX enabled counts.
- 2026-07-09: Added Racing Phase 11E Race Collision And VFX Isolation as `scripts/roblox_racing_phase11e_race_collision_vfx_isolation.lua` plus `docs/racing-phase11e-race-collision-vfx-isolation-2026-07-09.md` after Phase 11D testing confirmed exit cleanup/teleporting worked but multiplayer arrow/barrier collision stopped after the initial segment window, VFX still leaked between free roam and sessions, and race vehicles collided with each other. Phase 11E reapplies race participant collision groups on checkpoint progress, adds arrow-proxy debug attributes, disables participant-vs-participant collision for active race/session participants, and gates cached thrust VFX by `RaceVisibilityUpdate` without adding VFX rescans or rebuild loops.
- 2026-07-09: Updated `scripts/roblox_racing_phase11d_session_boundary_cleanup.lua` after Studio then reported `Could not find source anchor: RaceQueue reset on queue update`. Root cause: the race queue UI client had also drifted enough that individual event-branch anchors were brittle. The installer now replaces the isolated `RaceQueueClient_Active` state/leave handler/event handler by stable block boundaries instead of exact branch bodies.
- 2026-07-09: Updated `scripts/roblox_racing_phase11d_session_boundary_cleanup.lua` after Studio reported `Could not find source anchor: exitRacePlayer clean finished exit`. Root cause: the first installer made the `exitRacePlayer` source anchor too exact after the finished-return edge case was added. The script now replaces `exitRacePlayer` by function boundary before `callRaceRewardService`, while keeping the replacement guarded and idempotent for partial installs.
- 2026-07-09: Added Racing Phase 11D Session Boundary Cleanup as `scripts/roblox_racing_phase11d_session_boundary_cleanup.lua` plus `docs/racing-phase11d-session-boundary-cleanup-2026-07-09.md` after the first working multiplayer race load showed arrow collision/visibility and participant VFX leaking around race finish. Phase 11D removes finished racers from active race visibility/collision immediately, keeps the race result panel over a black fade until `EXIT`, despawns the finished vehicle while hidden, returns the player to the route teleport/start point on exit, clears arrow visuals on finish, reapplies grid participant collision after server-spawned race vehicles are staged, and adds an isolated participant visibility client for bodies/VFX. It preserves reward config, route-guide config, arrow folder layout, Phase 8H reset behavior, and the main bootstrap.
- 2026-07-09: Added `scripts/roblox_racing_phase11c_binding_lookup_repair.lua` after Phase 11C testing reported `TimeTrialService_Active:<line>: attempt to index nil with 'FindFirstChild'` inside `getRaceVehicleSpawner`. The repair replaces only the Phase 11C helper block in the isolated time-trial and race matchmaking services with a direct guarded `game:GetService("ServerScriptService")` lookup and clearer missing-binding messages. The main Phase 11C installer now uses the same safer helper.
- 2026-07-09: Added Racing Phase 11C Server Grid Vehicle Spawn as `scripts/roblox_racing_phase11c_server_grid_vehicle_spawn.lua` plus `docs/racing-phase11c-server-grid-vehicle-spawn-2026-07-09.md` after Phase 11B fixed event pairing but players still needed to spawn/sit in a vehicle before racing. Phase 11C adds a small garage-server `RaceVehicleSpawner` binding, removes free-roam spawn calls from the race entry menu, validates selected vehicles server-side, and spawns selected race/time-trial vehicles directly at the start grid when staging begins. It preserves reward config, route-guide config, arrows, and the Phase 8H reset model.
- 2026-07-09: Added Racing Phase 10B Folder Arrow Barriers as `scripts/roblox_racing_phase10b_folder_arrow_barriers.lua` plus `docs/racing-phase10b-folder-arrow-barriers-2026-07-09.md`. Phase 10B supersedes the example blocker workflow for route edge arrows by creating `ArrowMarkers.CheckpointA-B` segment folders, moving loose placed arrow groups into `Unassigned_Arrows` for manual sorting, replacing the isolated session asset service/client with folder-arrow runtime visibility/collision, and adding small segment-update hooks to the isolated time-trial/race services. It preserves reward config, route-guide config, Phase 8H reset behavior, and Phase 9A scoring.
- 2026-07-09: Updated Racing Phase 10B to remove the deprecated `PhysicsService:CreateCollisionGroup` fallback after Studio warned to use `RegisterCollisionGroup` instead. Rerunning Phase 10B replaces the isolated asset service with the warning-free collision group setup.
- 2026-07-09: Added `scripts/roblox_racing_phase10b_create_14_checkpoint_arrow_folders.lua`, a small helper for the current `ShiftedCanalSprint` route that explicitly creates `ArrowMarkers.Checkpoint0-1` through `Checkpoint14-0` plus `Unassigned_Arrows` without touching scripts or moving existing arrow assets.
- 2026-07-09: User reported Racing Phase 10B working nicely after sorting arrows into segment folders. Added Racing Phase 11A Race Placement Rewards as `scripts/roblox_racing_phase11a_race_placement_rewards.lua` plus `docs/racing-phase11a-race-placement-rewards-2026-07-09.md`. Phase 11A keeps time-trial rewards, adds race placement payouts through the existing `Rewards.Race` config and `BaseReward`, patches race finish payload/UI, and intentionally leaves reward config values, route-guide config, arrows, reset behavior, and leaderboard/DataStore systems untouched.
- 2026-07-09: Added `scripts/roblox_racing_phase11a_race_matchmaking_parse_repair.lua` after the first Phase 11A Play run reported `RaceMatchmakingService_Active:<line>: Expected <eof>, got 'end'`. The repair removes any partial Phase 11A race-reward bridge from the isolated matchmaking service and installs one canonical reward-enabled `finishEntry` block. The main Phase 11A installer now uses the same canonical replacement path.
- 2026-07-09: Added Racing Phase 11B Entry Event Pairing And Spawn Guard as `scripts/roblox_racing_phase11b_entry_event_and_spawn_guard.lua` plus `docs/racing-phase11b-entry-event-spawn-guard-2026-07-09.md` after matchmaking displayed `Race event not found: shifted_canal_sprint_tt` and starts could run the garage spawn path before staging. Phase 11B derives the paired race event id, validates race events before spawn/select, and skips garage respawn when the selected card is already the current vehicle. Full server-side selected-vehicle staging remains the recommended later root fix for swapping vehicles without free-roam fallback spawn.
- 2026-07-08: Added Racing Phase 10A Session Asset Collision Foundation as `scripts/roblox_racing_phase10a_session_asset_collision_foundation.lua` plus `docs/racing-phase10a-session-asset-collision-foundation-2026-07-08.md` after the user confirmed Phase 9A worked well. Phase 10A creates hidden route `SessionAssetMarkers`, server-owned `SessionAssetTemplates`, `RaceSessionAssetService_Active`, a lightweight client visibility helper, fixed participant/session-asset collision groups, and guarded hooks so time trials/races clone and clean session-only assets inside `RaceInstances.<RunId>.SessionAssets`. It keeps reward config, route-guide config, Phase 8H reset behavior, and Phase 9A lap/session scoring untouched.
- 2026-07-08: Added Racing Phase 9A Route Type And Lap Sessions as `scripts/roblox_racing_phase9a_route_type_lap_sessions.lua` plus `docs/racing-phase9a-route-type-lap-sessions-2026-07-08.md` after the user confirmed Phase 8H was working well. Phase 9A seeds route/event lap attributes, exposes `RouteType` through the route/config readers, adds a lap selector to the isolated race entry menu, loops circuit time trials by lap, tracks the best completed lap, and grants one reward from the best session result on finish/quit. It intentionally keeps reward multiplier config, route-guide config, checkpoint visuals, multiplayer race rewards, the bootstrap, and the Phase 8H respawn reset architecture untouched.
- 2026-07-08: Added Racing Phase 8H Respawn Reset System as `scripts/roblox_racing_phase8h_respawn_reset_system.lua` plus `docs/racing-phase8h-respawn-reset-system-2026-07-08.md` after the user chose the systematic respawn-on-reset option. Phase 8H stops trying to teleport a live physics vehicle: reset is now fade/presentation-only on the client, while the server clones the active race vehicle, destroys the old one, parents/pivots the clean replacement at the reset CFrame, zeroes momentum, reseats the player, updates the active session vehicle reference, and resumes normal race driving prep. This should restore no-momentum resets and checkpoint-facing without client vehicle handoff patches.
- 2026-07-08: Added Racing Phase 8G Reset Stability Rollback as `scripts/roblox_racing_phase8g_reset_stability_rollback.lua` plus `docs/racing-phase8g-reset-stability-rollback-2026-07-08.md` after Phase 8F still left reset-to-checkpoint disabling the car/camera and making the world stream low-res/weird. Output proved the yaw-only bridge fired successfully, so the issue was broader reset ownership. Phase 8G makes reset client presentation-only, removes duplicate client `StopVehicle`, restores the bootstrap handoff if the Phase 8F bridge is present, and rolls back Phase 8E anchored/network-owner reset helpers if present. This intentionally prioritises stable drivable resets over checkpoint-facing polish.
- 2026-07-08: Added Racing Phase 8F Reset Yaw Bridge And Button Text Repair as `scripts/roblox_racing_phase8f_reset_yaw_bridge_and_button_text_repair.lua` plus `docs/racing-phase8f-reset-yaw-bridge-button-text-repair-2026-07-08.md` after Phase 8E reset testing disabled the car/camera and made the world stream low-res/weird. Root cause: Phase 8E reused the full `FreeRoamVehicleSpawned` / `startDriving` handoff during an active reset, which was too broad. Phase 8F replaces that with a tiny bootstrap bridge that syncs only local `yawHeading` from the current vehicle root, keeps normal full driving handoffs for actual spawns, and cleans the session reset button text by removing tiny Michroma/wrapping in favour of fixed GothamBold.
- 2026-07-08: Added Racing Phase 8E Reset Handoff Yaw Sync as `scripts/roblox_racing_phase8e_reset_handoff_yaw_sync.lua` plus `docs/racing-phase8e-reset-handoff-yaw-sync-2026-07-08.md` after Phase 8D stopped reset spin but still preserved the vehicle's pre-reset facing. Root cause from the refreshed mirror: the active driving loop keeps an internal `yawHeading`, so server checkpoint pivots can be pulled back by the next driving heartbeat. Phase 8E keeps reset pose server-authoritative, removes the client reset `PivotTo`, briefly server-owns/anchors the vehicle during reset, and fires the existing driving handoff after reset so heading is re-read from the checkpoint-facing pose.
- 2026-07-07: Adjusted Racing Phase 8D reset orientation repair after repeated local `PivotTo(ResetCFrame)` calls made the car spin on reset. The local transition client now applies the checkpoint-facing reset CFrame once, then only clears linear/angular velocity during the settle window.
- 2026-07-07: Strengthened Racing Phase 8D reset orientation after the server-only checkpoint-facing patch did not change live behavior. Reset events now include `ResetCFrame`, and `RaceTransitionClient_Active` applies that CFrame locally while clearing client-owned vehicle momentum, so the physics owner should face the checkpoint part direction after reset.
- 2026-07-07: Updated Racing Phase 8D reset orientation so reset-to-checkpoint uses the completed checkpoint part's authored `CFrame` instead of automatically looking toward the next checkpoint. Track direction after reset is now controlled by rotating checkpoint parts in Studio.
- 2026-07-07: Strengthened Racing Phase 8D reset-to-checkpoint stopping after testing showed server-side velocity clearing still left race vehicles carrying momentum. `RaceTransitionClient_Active` now also clears the local player's owned vehicle velocity on `TimeTrialReset` / `RaceReset` and after a successful reset button response, repeated over a short settle window to handle client-owned physics.
- 2026-07-07: Tuned Racing Phase 8D so fade-to-black holds longer before unfading and reset-to-checkpoint re-zeroes vehicle velocity after the server driving handoff. Rerunning `scripts/roblox_racing_phase8d_session_transition_camera_fade.lua` now upgrades existing browser teleport fade timings as well as replacing the transition/session controls clients.
- 2026-07-07: Added Racing Phase 8D Session Transition Camera Fade as `scripts/roblox_racing_phase8d_session_transition_camera_fade.lua`. Phase 8D installs isolated `RaceTransitionClient_Active`, replaces the small session-controls client with `QUIT RACE`, adds fade/camera/HUD transition handling for start/reset/quit and browser teleport-to-start, and intentionally leaves rewards, route-guide config, checkpoint timing, and route attributes unchanged. The browser teleport fade hook is guarded source-anchor work against the isolated `RaceBrowserClient_Active`.
- 2026-07-07: Amended the racing roadmap with Phase 8D after Phase 8C testing showed the first-start camera issue still persists and quitting can leave the camera fixed at the old race location. Phase 8D is now the recommended next step before lap/infinite time trials: rename `EXIT TO START` to `QUIT RACE`, restore camera after start/reset/quit, add quick fade transitions around race teleports, and hide free-roam nav/exit-vehicle HUD while racing.
- 2026-07-07: Updated Racing Phase 8C after Studio reported `Could not find second source anchor: pre-stage handoff block`. Root cause: the installer assumed the race and time-trial pre-stage `FreeRoamVehicleSpawned` handoff blocks in `RaceEntryMenuClient_Active` were text-identical, but the time-trial branch is indented one level differently. The script now repairs both known branch shapes, accepts already-repaired markers, and prints the repaired block count.
- 2026-07-07: Added Racing Phase 8C Session Controls Polish as `scripts/roblox_racing_phase8c_session_controls_polish.lua` plus `docs/racing-phase8c-session-controls-polish-2026-07-07.md`. Phase 8C adds reset-to-last-checkpoint and exit-to-start controls for races/time trials, returns quitters to route teleport/start points while despawning race vehicles, tracks last completed gates server-side, and removes the pre-staging free-roam driving handoff so first start should match the cleaner Retry path.
- 2026-07-07: Added a project-wide implementation triage workflow to `AGENTS.md`, `docs/00_START_HERE.md`, and `docs/12_continuous_improvement_workflow.md`. Future work now distinguishes `follow:` execution mode from `suggest:` options mode and asks Codex to classify all implementation, system, tweak, and refinement requests before patching when a later/batched path may be more stable.
- 2026-07-07: Added Racing Phase 8B Multiplayer Race Drive Handoff Repair as `scripts/roblox_racing_phase8b_multiplayer_race_drive_handoff_repair.lua` plus `docs/racing-phase8b-multiplayer-race-drive-handoff-repair-2026-07-07.md` after Phase 8 queueing worked but both race vehicles stopped hovering/were not drivable at `GO`. Root cause: multiplayer `RaceStarted` did not perform the Phase 3E-style local `FreeRoamVehicleSpawned` handoff or streaming request. Phase 8B patches `RaceQueueClient_Active` to do that and changes race staging freeze to root-only anchoring while keeping rewards/route-guide config untouched.
- 2026-07-07: Added Racing Phase 8 Open-Category Multiplayer Race MVP as `scripts/roblox_racing_phase8_open_category_multiplayer_race_mvp.lua` plus `docs/racing-phase8-open-category-multiplayer-race-mvp-2026-07-07.md` after Phase 7B was confirmed working. Phase 8 installs `RaceMatchmakingService_Active`, `RaceQueueClient_Active`, queue remotes, `Config.Racing.Matchmaking`, selected-vehicle race queue entry, grid staging, countdown, placement updates, and race finish UI. Race cash rewards remain deferred, and `Config.Racing.Rewards` / route-guide tuning are untouched.
- 2026-07-07: Updated Racing Phase 7B so the free-roam Race browser closes automatically after a successful `TELEPORT TO START`. The installer now repairs already-installed Phase 7B clients with a tiny forward-declared `setOpen(false)` call and keeps reward / route-guide config untouched.
- 2026-07-07: Added Racing Phase 7B Race Browser Teleport as `scripts/roblox_racing_phase7b_race_browser_teleport.lua` plus `docs/racing-phase7b-race-browser-teleport-2026-07-07.md` after the Phase 7 browser menu was reported looking good. Phase 7B replaces waypointing with a server-authoritative `TELEPORT TO START` flow, creates editable `RaceRoutes.<RouteId>.TeleportPoints.RaceBrowserTeleportPoint` parts, installs `RaceBrowserTeleportService_Active`, despawns the player's vehicle after safely moving the character, and leaves `Config.Racing.Rewards` / `Config.Racing.RouteGuide` untouched.
- 2026-07-07: Added Racing Phase 7 Free-Roam Race Browser as `scripts/roblox_racing_phase7_free_roam_race_browser.lua` plus `docs/racing-phase7-free-roam-race-browser-2026-07-07.md`. The phase installs isolated `RaceBrowserClient_Active`, adds an `OpenRaceBrowser` BindableEvent, patches only the free-roam `RACE` tile click handler to open the browser, lists race/time-trial events, and adds a local start-zone waypoint. It intentionally does not edit `Config.Racing.Rewards`, reward multipliers, or `Config.Racing.RouteGuide`.
- 2026-07-07: Added Racing Phase 6B Global Reward Config And Rounding as `scripts/roblox_racing_phase6b_global_reward_config_rounding.lua` plus `docs/racing-phase6b-global-reward-config-rounding-2026-07-07.md` after Phase 6 was reported working well. Phase 6B rounds time-trial payouts to the nearest `$250`, moves multipliers into `Config.Racing.Rewards.TimeTrial` and `Config.Racing.Rewards.Race`, keeps only `BaseReward` on event folders, removes old per-track reward multiplier attributes, and leaves `Config.Racing.RouteGuide` untouched.
- 2026-07-07: Added Racing Phase 6 Time Trial Rewards Pack as `scripts/roblox_racing_phase6_time_trial_rewards_pack.lua` plus `docs/racing-phase6-time-trial-rewards-pack-2026-07-07.md`. The phase installs `RaceRewardService_Active`, creates `Config.Racing.Rewards`, adds a tiny server-only garage cash bridge, patches time-trial finish payloads for idempotent cash rewards, shows the payout on the Phase 4 results panel, and intentionally does not edit `Config.Racing.RouteGuide`.
- 2026-07-07: Added Racing Phase 5F Checkpoint Pill Label as `scripts/roblox_racing_phase5f_checkpoint_pill_label.lua` plus `docs/racing-phase5f-checkpoint-pill-label-2026-07-07.md` after the user wanted world-space checkpoint text with a small transparent black backing instead of no backing. Phase 5F keeps generated checkpoint frames off by default and adds configurable pill width, height, y-offset, background transparency, corner radius, stroke, and text size under `Config.Racing.RouteGuide`.
- 2026-07-07: Added Racing Phase 5E World Text-Only Checkpoint Guide as `scripts/roblox_racing_phase5e_world_text_only_checkpoint_guide.lua` plus `docs/racing-phase5e-world-text-only-checkpoint-guide-2026-07-07.md` after Phase 5D's fixed screen badge removed the spatial checkpoint cue. Phase 5E restores world-space checkpoint text above the physical checkpoint, removes the dark label panel/frame, disables generated checkpoint frames by default, and keeps the `3` second wrong-way delay.
- 2026-07-07: Added Racing Phase 5D Minimal Route Guide And Wrong-Way Delay as `scripts/roblox_racing_phase5d_minimal_guide_wrong_way_delay.lua` plus `docs/racing-phase5d-minimal-guide-wrong-way-delay-2026-07-07.md` after the checkpoint label/frame still blocked too much of the driving view. Phase 5D disables the large in-world checkpoint label by default, adds a small top-screen checkpoint badge, replaces the old full frame with faint corner ticks, and makes `WRONG WAY` appear only after `3` seconds of sustained wrong-way driving with checks throttled inside the existing heartbeat.
- 2026-07-06: Added Racing Phase 5C Checkpoint Label/Frame Opacity Repair as `scripts/roblox_racing_phase5c_checkpoint_label_frame_opacity_repair.lua` plus `docs/racing-phase5c-checkpoint-label-frame-opacity-repair-2026-07-06.md` after the checkpoint UI still looked too opaque. Phase 5C separates the readable label from the world frame: `CheckpointLabelTextTransparency = 0.2`, `CheckpointLabelBackgroundTransparency = 0.2`, and `CheckpointFrameTransparency = 0.8`, patches only `RaceRouteGuideClient_Active`, and updates the Phase 5 installer defaults.
- 2026-07-06: Added Racing Phase 5B Marker Dedupe And Transparency Repair as `scripts/roblox_racing_phase5b_marker_dedupe_transparency_repair.lua` plus `docs/racing-phase5b-marker-dedupe-transparency-repair-2026-07-06.md` after Phase 5 showed two stacked checkpoint markers and overly opaque frames. The repair patches only isolated Racing client scripts, disables the older Phase 3/4 `RaceNextGateSelection` / `RaceNextGateBillboard` marker path, keeps its cleanup hook, adds `Config.Racing.RouteGuide.CheckpointFrameTransparency = 0.8`, and updates the Phase 5 installer default so future route-guide installs use the lighter frame transparency.
- 2026-07-06: User confirmed Racing Phase 4 ran, tested, and worked well. Added Racing Phase 5 Route Guidance And Session Visuals as `scripts/roblox_racing_phase5_route_guidance_session_assets.lua` plus `docs/racing-phase5-route-guidance-session-assets-2026-07-06.md`. The phase installs an isolated `RaceRouteGuideClient_Active` instead of patching the working results client, renders local-only checkpoint/finish frames, dynamic next-gate chevrons, authored `ArrowMarkers`, and a wrong-way prompt, and seeds route-guide config plus route authoring folders. Collidable session assets remain deferred until race pockets/collision groups are safer.
- 2026-07-06: Added Racing Phase 4 Time Trial Results Pack as `scripts/roblox_racing_phase4_time_trial_results_pack.lua` plus `docs/racing-phase4-time-trial-results-pack-2026-07-06.md`. The guarded installer patches only isolated Racing scripts to add server-authoritative bronze/silver/gold/platinum calculation from per-tier event config, checkpoint split payloads, in-session personal bests per player/event/tier, and a themed finish panel with `RETRY` / `EXIT`. Cash rewards, persistent bests, route arrows/session assets, and multiplayer matchmaking remain deferred.
- 2026-07-06: Updated the race/time-trial plan so multiplayer races launch as one open queue/category per race event instead of tier-bracketed queues, while time trials remain tiered for medal targets and rewards. Compressed the next roadmap where safe: time-trial results/medals/PB UI first, route guidance/session assets next, guarded rewards after finish idempotency, then the free-roam Race panel and open-category multiplayer matchmaking.
- 2026-07-06: Added Racing Phase 3E release handoff repair as `scripts/roblox_racing_phase3e_release_drive_handoff_repair.lua` after time trials began but the vehicle was not drivable and nearby map assets streamed/disappeared/reappeared after countdown. Likely root cause: the client fired the existing free-roam driving handoff immediately after the garage spawn, before Racing teleported/froze/released the vehicle; at `GO`, the vehicle was released without reattaching the driving controller or resetting streaming focus. The repair patches only isolated Racing scripts so the server prepares the vehicle for driving/network ownership on release, and the client requests streaming around the route plus re-fires the existing `FreeRoamVehicleSpawned` handoff after `GO`.
- 2026-07-06: Added Racing Phase 3D event-pairing repair as `scripts/roblox_racing_phase3d_time_trial_event_pairing_repair.lua` after the Phase 3 menu opened but `START TIME TRIAL` from `RaceStartZone` left the menu visible and spawned the selected car at free-roam/customisation spawn locations instead of staging at the race grid. Root cause: the menu opened with the race event id and sent that id to `StartStagedTimeTrial`; the garage spawn succeeded first, then the racing service failed to resolve a matching time-trial event. The repair patches only isolated Racing scripts so the client resolves/checks the paired time-trial event before spawning and the server tolerates race event ids by finding the time trial on the same route.
- 2026-07-06: Added Racing Phase 3C client event repair as `scripts/roblox_racing_phase3c_client_event_repair.lua` after Phase 3B proved the server receives `E` (`Race entry prompt triggered by LucidityStudios...`) but the race menu still does not open. The repair patches only the isolated `RaceEntryMenuClient_Active` so it logs client startup/event receipt, does not wait on garage remotes before listening for race menu events, and wraps `showEntry` with a clear warning. It also installs a tiny `StarterPlayerScripts.RaceEntrySignalProbe_Active` to confirm whether `OpenRaceEntry` reaches the client at all. The Phase 3 installer now includes the safer client startup/event logging path.
- 2026-07-06: Added Racing Phase 3B prompt repair as `scripts/roblox_racing_phase3b_prompt_connection_repair.lua` after Phase 3 installed cleanly but pressing `E` did not open the race menu. Likely root cause: an existing `NTR_RaceEntryPrompt` could be present without a live `Triggered` connection after the isolated racing service source changed. The repair patches only `TimeTrialService_Active`, recreates race-entry prompts once on server startup, and logs when the prompt receives input. The Phase 3 installer was updated with the same prompt recreation fix for future reruns.
- 2026-07-06: Added Racing Phase 3 as `scripts/roblox_racing_phase3_entry_menu_staging_session.lua` plus `docs/racing-phase3-entry-menu-staging-session-2026-07-06.md`. The phase canonically replaces only the isolated Racing service/client scripts, disables the older Phase 2 `RaceClient_Active`, and installs `RaceEntryMenuClient_Active` for themed start-zone menu, track/map placeholders, `START RACE` / `START TIME TRIAL` / `EXIT`, owned vehicle cards from the existing garage profile, selected-vehicle spawning through existing garage actions, start-grid teleport/freeze/countdown, server timing after `GO`, lightweight `RaceInstances.<RunId>`, and first local participant visibility filtering. Medals, rewards, personal bests, matchmaking, cloned route pockets, and collidable race-only assets remain deferred.
- 2026-07-06: User confirmed Racing Phase 2's solo time-trial loop is working well, then requested a fuller entry/session flow. Amended `docs/race-time-trial-system-plan-2026-07-06.md` so the recommended next phase replaces instant `E` start with a themed race entry menu, track image/map, `START RACE` / `START TIME TRIAL` / `EXIT`, owned vehicle selection using the dealership/customisation card style, server-validated start-line staging/countdown, and first same-server race-session separation. The plan now treats arrows, jumps, barriers, and other race-only assets as participant/session assets, notes Roblox's same-server isolation limits, and defers medals/rewards until the menu/session foundation is stable.
- 2026-07-06: Added Racing Phase 2 as `scripts/roblox_racing_phase2_solo_time_trial_mvp.lua` plus `docs/racing-phase2-solo-time-trial-mvp-2026-07-06.md`. The phase installs isolated Racing remotes, `RaceRouteDefinition`, `RaceConfigReader`, `TimeTrialService_Active`, and `RaceClient_Active` for a server-authoritative solo time-trial loop on `ShiftedCanalSprint`. It enables checkpoint touch on route gates at runtime, treats `FinishLine` as the final gate even if old scaffold indices are stale, shows a local HUD/next-gate marker, and defers rewards, medals, personal bests, multiplayer, and polished arrow assets.
- 2026-07-06: User confirmed Racing Phase 1 was run and the first race zones/checkpoints were moved in Studio. Updated the race/time-trial plan to explicitly future-proof the system for player-created races through a source-agnostic `RaceRouteDefinition` / `RaceConfigReader` layer, with official Studio routes and future serialized player-created routes feeding the same checkpoint, HUD, matchmaking, and reward runtime. Also added route arrow guidance as first-class `ArrowMarkers` content rendered locally by the future route-guide client.
- 2026-07-06: Added Racing Phase 1 as `scripts/roblox_racing_phase1_audit_and_sample_route_setup.lua` plus `docs/racing-phase1-audit-and-sample-route-setup-2026-07-06.md`. The script defaults to read-only `AUDIT` mode and checks route folders, racing config/remotes, future Racing service/client folders, the existing free-roam Race placeholder, road markers, profile reward hooks, and live spawned-vehicle performance tier/index. It also safely compresses the non-gameplay route-authoring part of Phase 2 behind `MODE = "SETUP_SAMPLE"`, creating only an editable `ShiftedCanalSprint` route scaffold and `Config.Racing` attributes with tiered time-trial medal times.
- 2026-07-06: Added `docs/race-time-trial-system-plan-2026-07-06.md` as the current race/time-trial architecture plan. It recommends isolated Racing services/controllers, configurable event folders, tier-bracketed time-trial medal times, server-authoritative checkpoints/timing/rewards, physical start-zone prompts, free-roam Race panel integration, and a phased path from solo time trials to multiplayer matchmaking. Updated `docs/race-events.md` to mark the old 2026-05-26 prototype note as superseded.
- 2026-07-06: Added and user-confirmed Drive-In Customisation Phase 3B (`scripts/roblox_drive_in_customisation_phase3b_spawn_unlock_anchor_repair.lua`) after the first Phase 3 run installed the countdown client but failed to patch the bootstrap with `Could not find exact Start Driving SpawnVehicle block`. Root cause: the Phase 3 installer used Lua pattern-based `string.gsub` for a multi-line source block, so pattern characters in the source prevented the match even though the fresh mirror showed the block was present. Phase 3B uses plain line-window matching to insert only the unlock before the existing `Customise -> SpawnVehicle` call, the drive-in flow was reported working well, and the Phase 3 script was corrected to use the same safer approach going forward.
- 2026-07-06: Added Drive-In Customisation Phase 3 (`scripts/roblox_drive_in_customisation_phase3_countdown_unlock_repair.lua`) after Phase 2 testing showed the prompt was visible before entering the zone and `Start Driving` after drive-in customisation could leave the car undriveable with world streaming/reload weirdness. Root cause: the drive-in player hold lock could still be active when the normal garage `SpawnVehicle` handoff ran. Phase 3 replaces the isolated drive-in prompt client with countdown-only in-zone UI and patches the existing spawn button path to release `NTR_DriveInCustomisationActive` before spawning.
- 2026-07-06: Added Drive-In Customisation Phase 2 (`scripts/roblox_drive_in_customisation_phase2_garage_entry_world_prompt.lua`) after Phase 1 opened Build Modules but left the player/camera in free roam and the live vehicle visible. Phase 2 installs a world-space trigger prompt/countdown, local trigger-VFX toggling, a small server session-lock service plus hold point to hide/freeze the player, and a register-safe drive-in handoff repair that despawns the live vehicle and restores the normal garage preview camera/vehicle before opening Build Modules.
- 2026-07-06: Added Drive-In Customisation Phase 1 register-limit repair (`scripts/roblox_drive_in_customisation_phase1_register_limit_repair.lua`) after Play reported `Out of local registers when trying to allocate okController`. Root cause: the first drive-in bootstrap handoff added top-level local helpers to the already register-limited active client bootstrap. The repair replaces that handoff with a table-backed bridge and the Phase 1 installer was updated to use the register-safe form going forward.
- 2026-07-06: Added Drive-In Customisation Phase 1 (`scripts/roblox_drive_in_customisation_phase1.lua`) to create a driving-only customisation bay. It installs a movable `DriveInCustomisationTrigger`, a local-only visible zone/countdown client, config folders under `Config.Runtime/UI.DriveInCustomisation`, and a guarded bootstrap handoff that despawns the live driven vehicle and opens the existing garage UI directly to Build Modules for the driven vehicle instance.
- 2026-07-06: Added Free Roam Vehicle Spawn Phase 4D (`scripts/roblox_freeroam_vehicle_spawn_phase4d_despawn_speed_card_polish.lua`) after screenshots/testing showed parked despawn teleported the player back to the vehicle, the `10 MPH` spawn gate still used parked vehicle speed, and desktop car-menu cards/panel needed polish. The repair keeps already-exited players in place on despawn, applies the speed gate and vehicle-position spawn anchor only while actively seated/driving, narrows the desktop car panel to fit three compact cards, and removes pink card/image outlines from free-roam cockpit cards.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 4C (`scripts/roblox_freeroam_vehicle_spawn_phase4c_exit_stop_parked_hover.lua`) after the user confirmed Phase 4B worked but exiting still left the camera/drive HUD/controls active. The repair fires a local exit event, makes the main bootstrap call `stopDriving()` and return the camera to the humanoid, installs an isolated owner parked-hover client so exited vehicles keep floating without drive input, and hides the free-roam car menu after a successful cockpit-card spawn.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 4B (`scripts/roblox_freeroam_vehicle_spawn_phase4b_remote_handoff_repair.lua`) after Play showed `FreeRoamVehicleExitButton_Active` waiting forever on obsolete `ReplicatedStorage.HOVER_RACING_V2_GarageAction`. The repair replaces the exit-button client with the current `NeoTokyoRacers.Shared.Remotes.Garage.GarageInvoke` path, adds an explicit free-roam cockpit-card spawn-to-driving handoff, repairs owner lookup for seats inside sub-models, and disables the old automatic walk-up re-entry loop so the `E` / touch prompt owns parked vehicle entry.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 4 (`scripts/roblox_freeroam_vehicle_spawn_phase4_despawn_exit_prompt.lua`) to make the free-roam car-menu `DESPAWN` button destroy the current vehicle, add a separate bottom-centre driving-only `EXIT VEHICLE` button that parks the player 10 studs left of the driver seat, and install an owner-only `E` / touch enter prompt for parked vehicles.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 3 (`scripts/roblox_freeroam_vehicle_spawn_phase3_click_spawn.lua`) to patch the garage server with `SpawnOwnedVehicleFromFreeRoam` and the free-roam car cards so clicking an owned cockpit spawns/swaps into that vehicle at the nearest clear `NTR_RoadSpawnPoint`, with a server-side 10 MPH gate.
- 2026-07-05: Added the preferred Free Roam Vehicle Spawn Phase 2 blockout-road marker script (`scripts/roblox_freeroam_vehicle_spawn_phase2_blockout_road_markers.lua`) after the user confirmed curated road parts exist under `Workspace.Test + WIP Assets.Blockout.Roads`; it creates one lightweight invisible `NTR_RoadSpawnPoint` marker above each `Road` part centre only when the part colour is `RGB(95, 95, 95)` / `#5f5f5f`, skipping differently coloured wall pieces with the same name.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 2 (`scripts/roblox_freeroam_vehicle_spawn_phase2_road_spawn_markers.lua`) to create explicit `NTR_RoadSpawnPoint` road-centre markers from safer exact `Road` / `Road Asphalt` parts and seed `Config.Runtime.FreeRoamVehicleSpawn` before click-to-spawn is patched.
- 2026-07-05: Added Free Roam Vehicle Spawn Phase 1 audit (`scripts/roblox_freeroam_vehicle_spawn_phase1_audit.lua`) to inspect the existing server spawn/select/despawn hooks, free-roam cockpit card `VehicleId` attributes, free-roam config values, live player speed/profile state in Play mode, and road-snap candidates before implementing click-to-spawn.
- 2026-07-05: Added Dealership / Customisation Split Phase 11 (`scripts/roblox_dealership_customisation_split_phase11_sorted_cockpit_cards.lua`) to sort dealership cockpit cards by price ascending, owned customisation cards by per-vehicle rating descending, and category buttons alphabetically.
- 2026-07-05: Added Free Roam Car Menu Phase 3 (`scripts/roblox_freeroam_car_menu_phase3_owned_cockpit_cards.lua`) to replace the free-roam `Car` pop-out action list with owned cockpit image cards, per-vehicle tier/rating badges, responsive 2-column/1-column layout, and a fixed `Despawn` button.
- 2026-07-05: Added Free Roam Car Menu Phase 4 (`scripts/roblox_freeroam_car_menu_phase4_card_scaling_sort_future_spawn.lua`) to enlarge free-roam car-menu cockpit images, change the layout to 3 desktop columns and 2 mobile columns, remove badge glow/blue selected outlines, sort by rating descending, and add card attributes for the future spawn/select phase.
- 2026-07-05: Added Free Roam Car Menu Phase 5 (`scripts/roblox_freeroam_car_menu_phase5_image_fit_border_padding.lua`) to replace the Phase 4 image zoom with fitted images plus fixed inner padding, normalize car-card/image borders to the pink frame style, and align the `Despawn` button bottom padding with the side padding.
- 2026-07-05: Added Free Roam Car Menu Phase 6 (`scripts/roblox_freeroam_car_menu_phase6_card_surface_root_fix.lua`) to root-fix clipped/odd cockpit-card borders by separating the transparent grid click cell from an inner visible `CardSurface`, tightening the card around image/text content, and reducing mobile cockpit image max size while preserving future spawn/select card attributes.
- 2026-07-05: Added Free Roam Car Menu Phase 7 (`scripts/roblox_freeroam_car_menu_phase7_borderless_card_frames_compact_width.lua`) to remove `UIStroke` from free-roam cockpit-card/image borders, use filled frame layers instead, and narrow the car pop-out defaults to fit 3 compact desktop cards or 2 mobile cards.

This is a high-level summary, not a complete changelog of every script.

## Documentation

- 2026-07-05: Added Dealership / Customisation Split Phase 10 as `scripts/roblox_dealership_customisation_split_phase10_responsive_layout_polish.lua` after Phase 9 was reported working better but needed layout polish. Phase 10 shrinks the free-roam car icon scale, shortens the bottom-right Exit frame, lowers the right-panel action button, removes the `Categories` heading, narrows the mobile stats panel, gives the center grid more room, increases desktop cockpit-card text independently from mobile, and moves stat bar values to dark left-aligned text inside the bars.
- 2026-07-05: Added Dealership / Customisation Split Phase 9 as `scripts/roblox_dealership_customisation_split_phase9_badge_overlay_tight_cards.lua` after Phase 8 screenshots showed the inline tier/rating was clipped, card spacing remained too loose, dealership-card ratings did not match the right stats panel, and the free-roam car button was using cockpit `MenuImage`. Phase 9 moves tier/rating into a wider coloured badge over the cockpit image, tightens card spacing, uses the included-default-module dealership stats path for buyable cockpit ratings, and restores free roam to `FreeRoamNav.CarIcon` by default.
- 2026-07-04: Added Dealership / Customisation Split Phase 8 as `scripts/roblox_dealership_customisation_split_phase8_compact_card_polish.lua` after mobile/PC screenshots showed Phase 7 cards needed content-fit polish. Phase 8 keeps 4 desktop and 3 mobile columns, makes image boxes fill the card width with even padding, computes card height from image/title/price content to remove excess space below cost, and adds inline tier/rating to dealership cockpit cards using the base cockpit performance calculation.
- 2026-07-04: Added Dealership / Customisation Split Phase 7 as `scripts/roblox_dealership_customisation_split_phase7_responsive_cockpit_grid.lua`. It updates the Phase 6 cockpit-card helper block so desktop/laptop defaults to 4 cockpit cards per row, mobile defaults to 3, extra cards scroll vertically, and image/text/badge proportions scale from the calculated card width through `CockpitMenuCards` config values.
- 2026-07-04: Added `scripts/roblox_dealership_customisation_split_phase6_register_limit_repair.lua` after Phase 6 made Play fail with `Out of local registers when trying to allocate init`. Root cause: the Phase 6 cockpit-image helper block added too many top-level local helpers to the already register-constrained client bootstrap. The repair keeps the same UI behavior but moves those helpers to global function assignments, and the Phase 6 installer was updated so future installs do not recreate the local-register issue.
- 2026-07-04: Added Dealership / Customisation Split Phase 5 as `scripts/roblox_dealership_customisation_split_phase5_cockpit_menu_images.lua`. It changes the selected owned-cockpit action label from `Build Modules` to `Customise` while keeping the ModuleShop destination, creates shared cockpit `MenuImage` attributes, renders those images in dealership/customisation cockpit cards, and makes the free-roam car button prefer the current cockpit's image before falling back to `FreeRoamNav.CarIcon`.
- 2026-07-04: Updated Dealership / Customisation Split Phase 5 after Studio reported `Could not find source anchor for cockpit catalog MenuImage note`. Root cause: the server catalogue line shape had changed, and the attempted server edit was only a comment marker. The installer now audits the existing `V56_primitiveAttributes(cockpit)` catalogue path instead of patching server source, and uses granular idempotent checks for the bootstrap and free-roam source edits.
- 2026-07-04: Added Dealership / Customisation Split Phase 6 as `scripts/roblox_dealership_customisation_split_phase6_square_images.lua` after the user reported the cockpit image still showed the fallback pink bar. Phase 6 reads images from the live cockpit model as well as catalogue rows, accepts image attributes/values/decals, adds configurable square thumbnail card settings under `ReplicatedStorage.NeoTokyoRacers.Config.UI.CockpitMenuCards`, patches the grid cell/card layout, updates free-roam lookup, and removes the duplicate selected-cockpit rating above `Customise`.
- 2026-07-04: Added Dealership / Customisation Split Phase 1. `scripts/roblox_dealership_customisation_split_phase1_buy_only.lua` is a condensed guarded install/smoke script that makes the dealership buy-only, removes the owned-cockpit `Select` button, changes the starter Bruiser cockpit price to `$15000`, and stops fresh session profiles from owning `bruiser_01` for free while preserving existing saved ownership. The separate owned-cockpit customisation zone is deferred to the next phase.
- 2026-07-04: User reported Dealership / Customisation Split Phase 1 ran and appeared to be working. Added Phase 2 as `scripts/roblox_dealership_customisation_split_phase2_owned_customisation_zone.lua`, a condensed guarded install/smoke script that creates a separate customisation trigger zone, installs an isolated zone client, adds `SelectVehicleInstance`, and reuses the dealership-looking cockpit grid in an owned-cockpit customisation mode.
- 2026-07-04: User reported Dealership / Customisation Split Phase 2 worked as intended. Added Phase 3 as `scripts/roblox_dealership_customisation_split_phase3_instance_cards.lua`, a guarded install/smoke script that renders customisation cockpit cards per owned vehicle instance, removes aggregated `Owned xN` text, adds per-vehicle tier/index labels, and passes `VehicleId` into `SelectVehicleInstance`.
- 2026-07-04: Added `scripts/roblox_dealership_customisation_split_phase3_vehicle_summary_repair.lua` after the first Phase 3 install reported a nil call inside `V90_vehicleSummaries`. Root cause: the Phase 3 helper was inserted before `V56_totalStats` was lexically visible in the live server source. The repair adds `V90_summaryTotals(profile)` as a local fallback and the Phase 3 installer now includes the same fix.
- 2026-07-04: Added Dealership / Customisation Split Phase 4 as `scripts/roblox_dealership_customisation_split_phase4_rating_badge_build_modules.lua`. It replaces the rough Phase 3 rating fallback with the main total-stat logic shape, adds colour-coded tier badges to owned vehicle cards, and changes the customisation-zone action to open Build Modules / ModuleShop.
- 2026-07-03: Added Free Roam Map Stack Phase 2.11 button layering/config preservation repair. Stack buttons now render dark base, pink outline, inset gradient fill, hover overlay, then icon; rerunning the installer preserves existing `FreeRoamNav` Bool/Number/Color tuning values instead of resetting them.
- 2026-07-03: Added Free Roam Map Stack Phase 2.10 outline contrast and local text-glow controls. Stack-button gradient fills are inset so the pink outline remains visible, outline thickness/transparency and fill inset are configurable, and local free-roam pop-out labels/action buttons can use configurable text glow. Broader dealership/customisation text glow is deferred to a separate shared UI theme phase.
- 2026-07-03: Added Free Roam Map Stack Phase 2.9 outline/action-button correction. Stack-button gradients now sit on an inner fill layer so the purple outline remains visible, and pop-out action buttons no longer use the stack-button gradient.
- 2026-07-03: Added Free Roam Map Stack Phase 2.8 pop-out/button surface polish. Action pop-outs now align to the top of the stack and match its full height, stack/action buttons use configurable diagonal gradients plus purple outlines, and the old top/bottom bevel bars were removed from buttons while leaving the map frame styling intact.
- 2026-07-03: Added Free Roam Map Stack Phase 2.7 visibility correction. The Phase 2.6 driving-only map-stack gate was too strict; the stack now appears during normal on-foot free roam as well as driving, while still hiding when visible dealership/customisation UI is open.
- 2026-07-03: Added Free Roam Map Stack Phase 2.6 dealership/menu and laptop sizing repair. This reduced desktop/laptop sizing by about 20%, made the full-width car row match the grid row height and icon scale, and added the visible-menu hiding path. Its initial driving-only visibility gate was superseded by Phase 2.7.
- 2026-07-03: Added Free Roam Map Stack Phase 2.5 mobile helper repair. The Phase 2 installer now restores the V69 desktop-HUD helper block if the earlier Phase 2.4 visibility anchor removed it and then replaces only the exact `findGarageVisible()` function. This targets the Play-mode `MobileDriveControlsController_Active:<line>: attempt to call a nil value` regression near line 504.
- 2026-07-03: Added Free Roam Map Stack Phase 2.4 mobile visibility repair. The map-stack controller and patched `MobileDriveControlsController_Active` now treat garage/dealership UI as open only when a real visible `GarageRoot` or `DealershipRoot` exists, so hidden enabled ScreenGui shells should no longer suppress mobile driving controls. The mobile smoke line now includes `garageVisible`.
- 2026-07-03: Added Free Roam Map Stack Phase 2.3 mobile visibility repair. The map-stack controller now publishes the shared `MobileDriveInputState.IsDriving` signal when touch is enabled and driving is detected from the active drive HUD or vehicle-seat state, without forcing the mobile controls root visible. The client smoke also prints a mobile diagnostic line so future failures show `IsDriving`, mobile GUI enabled/root visible, and desktop drive GUI state.
- 2026-07-03: Added Free Roam Map Stack Phase 2.2 mobile polish to `scripts/roblox_freeroam_map_stack_phase2.lua`. Icons are now square centred overlays sized from the smaller button dimension, mobile stack defaults are smaller, the map-stack controller no longer fights mobile controls by forcing `Root.Visible`, and the isolated `MobileDriveControlsController_Active` is patched for stable driving visibility, bottom-centred boost/MPH, lower edge-aligned thumbstick/pedals, map-right-aligned gas pedal, and shared UI theme colours.
- 2026-07-03: Repaired Free Roam Map Stack Phase 2 after mobile testing. The installer now preserves non-empty uploaded icon asset IDs instead of resetting them, normalizes plain numeric asset IDs to `rbxassetid://...`, and shrinks the touch layout to a tighter top-right stack. Its first mobile visibility approach forced mobile controls from the map-stack controller and was superseded by the Phase 2.2 mobile-controls-owned visibility fix.
- 2026-07-03: Added Free Roam Map Stack Phase 2. `scripts/roblox_freeroam_map_stack_phase2.lua` canonically replaces only the isolated `FreeRoamNavController_Active` source with a top-right UI stack matching the user's sketch: square map placeholder, full-width car button, and 2x2 Shop/Race/Home/Settings buttons. The map is intentionally unconfigured; future minimap work should set `FreeRoamNav.MapImage` plus world-bounds calibration and then add player-position panning/heading rotation.
- 2026-07-03: Added Free Roam Left Navigation Phase 1. `scripts/roblox_freeroam_left_nav_phase1.lua` installs an isolated `FreeRoamNavController_Active` plus `ReplicatedStorage.NeoTokyoRacers.Config.UI.FreeRoamNav`, suppresses the old top-right drive exit menu and old right-side garage access toggle, and provides Car, Race, Garage/Home, Settings, and Dealership panels. Preferred plain white transparent overlay icons live under `assets/ui/icons/freeroam_nav_plain/`; upload the `freeroam_plain_*.png` files to Roblox and paste asset IDs into the matching config StringValues before treating the icons as final.
- 2026-07-02: The user reported the slope hover height compensation was working well. Keep `scripts/roblox_driving_slope_hover_height_compensation.lua` as the current slope-hover patch path, with broader checks still useful for steep/uneven terrain and high-speed slope transitions.
- 2026-07-02: Added `scripts/roblox_driving_slope_hover_height_compensation.lua`, a guarded Command Bar installer for slope-aware hover height damping. It fixes the uphill/downhill height drift root cause by damping corner hover springs against vertical velocity relative to expected slope-following motion instead of raw world-Y velocity, while preserving terrain alignment, banking, braking/accel pitch, and wobble layering. It creates editable `SlopeHover*` config attributes under `DRIVING_MECHANICS_EditAttributes`.
- 2026-07-02: The user reported the drift mini-boost acceleration gate and reverse-speed increase were working and looking good. Keep `scripts/roblox_driving_drift_boost_accel_gate_reverse_speed.lua` as the current drift/reverse patch path, with broader checks still useful for mobile and different builds.
- 2026-07-02: Added `scripts/roblox_driving_drift_boost_accel_gate_reverse_speed.lua`, a guarded Command Bar installer that requires acceleration on drift exit before awarding the drift mini-boost and raises reverse speed to a configurable default of `40 MPH`. It creates `ReverseMaxMph`, `DriftMiniBoostRequiresAcceleration`, `DriftMiniBoostAccelerationThreshold`, and debug attributes under `DRIVING_MECHANICS_EditAttributes`, then patches only the current `DrivingControllerV47` reverse-speed and drift-release blocks.
- 2026-07-02: The user reported the acceleration/braking pitch tilt looked great. Keep `scripts/roblox_driving_accel_brake_pitch_tilt.lua` as the current pitch-tilt patch path, with broader balance checks still useful across builds and boost speeds.
- 2026-07-02: Added `scripts/roblox_driving_accel_brake_pitch_tilt.lua`, a guarded Command Bar installer for configurable acceleration/braking pitch tilt. It creates editable `DRIVING_MECHANICS_EditAttributes` values for acceleration pitch, braking pitch, reverse pitch, boost extra pitch, max pitch, smoothing, deadzone, and debug readback, then patches only the current `DrivingControllerV47` align block so the pitch tilt stacks with hover wobble and banking.
- 2026-07-02: The user reported the speed-sensitive steering curve was working nicely. Keep `scripts/roblox_driving_speed_sensitive_steering_curve.lua` as the current handling-steering patch path, with broader balance checks still useful across builds and mobile controls.
- 2026-07-02: Added `scripts/roblox_driving_speed_sensitive_steering_curve.lua`, a guarded Command Bar installer for speed-sensitive vehicle turning. It creates editable `DRIVING_MECHANICS_EditAttributes` values for low/high-speed steering multipliers, curve exponent, smoothing, debug attributes, separate reverse steering control, and a `BoostSteeringMultiplier` that stacks during normal boost and drift mini-boost, then patches only the current `DrivingControllerV47` turn-rate block.
- 2026-07-02: Added `docs/12_continuous_improvement_workflow.md` and updated the new-chat/source-sync instructions after the Phase 17 popup and Phase 23-28 garage MVP work. Future chats now have explicit rules for learning from each phase, avoiding repeated source-anchor guesses, preferring isolated services or canonical replacement, condensing Studio scripts safely, and treating raw Studio paste files as non-commit artifacts.
- 2026-07-02: Confirmed Persistence Phase 28 final garage MVP audit. The smoke reported `Access UI present`, `VisitGarage OK`, `Customization UI visible`, `Customization persisted and UI reflected state. summary=SURFACES 2  DECOR 1`, `Interior state OK`, and `ReturnToCity OK`, completing the Phase 21-28 private garage MVP stack pending final mirror refresh/save-rejoin handoff.
- 2026-07-02: Added `scripts/roblox_persistence_phase28_garage_mvp_final_audit.lua` as the final garage MVP hardening checkpoint. The dual-mode script audits Phase 21-27 runtime objects in Edit mode and, in Play, checks access UI, owner garage entry, display vehicle, customization persistence/UI summary, interior state, return-to-city, and cleanup back to Private.
- 2026-07-02: Confirmed Phase 27 same-server garage access UI. The client smoke reported `Access UI shell visible`, `SetAccessMode OK. mode=Public`, `VisitGarage OK`, `State OK. inGarage=true visitingOwner=7915427645 accessMode=Public`, and `ReturnToCity OK`.
- 2026-07-02: Added `scripts/roblox_persistence_phase27_garage_access_ui_mvp.lua` for same-server garage access UI. The dual-mode script installs an isolated `GarageAccessClient_Active` LocalScript with `GARAGE`, `ENTER MINE`, `SET PUBLIC`, `SET PRIVATE`, typed owner-user-id visit, and `RETURN CITY` controls, then smoke-checks the Phase 23 access actions without patching the main dealership bootstrap.
- 2026-07-02: Confirmed Phase 26 customization validation. The client smoke reported `Applied validation presets. floor=true walls=true decor=true`, `UI summary reflected backend state. summary=SURFACES 2  DECOR 1 persisted=true`, and `ReturnToCity OK`, proving the Phase 24 backend and Phase 25 UI summary stay in sync.
- 2026-07-02: Added `scripts/roblox_persistence_phase26_garage_customization_validation.lua` as a no-new-gameplay-code validation phase after Phase 25 passed. The script preflights Phase 24/25 runtime objects in Edit mode, then in Play enters the owner's garage, applies surface/decor presets through the Phase 24 backend, waits for the Phase 25 UI summary to reflect them, and returns to the city.
- 2026-07-02: Confirmed Phase 25 after the timing repair. The follow-up smoke reported `UI panel visible`, `Customization backend OK`, and `ReturnToCity OK`, proving the owner-only in-garage customization panel appears and can reach the Phase 24 backend.
- 2026-07-02: Repaired `scripts/roblox_persistence_phase25_garage_customization_ui_mvp.lua` after the first smoke found the UI ScreenGui but failed because the owner panel had not become visible yet. The smoke now waits for visibility, the client polls rapidly during garage entry, and the UI accepts either `NTR_Phase21InPrivateGarage` or `NTR_Phase21GarageInteriorId` as the in-garage signal.
- 2026-07-02: Added `scripts/roblox_persistence_phase25_garage_customization_ui_mvp.lua` as the next garage persistence/customization phase. The dual-mode script installs an isolated owner-only `GarageInteriorCustomizationClient_Active` LocalScript with a compact in-garage panel for surface and decor presets, then smoke-checks panel visibility and the Phase 24 backend without patching the main dealership bootstrap.
- 2026-07-02: Confirmed Persistence Phase 24 after the readback repair. The follow-up smoke reported `Applied surfaces/decor. surfaces=4`, `Customization state OK. owner=7915427645 surfaces=2 decorations=1 persisted=true`, and `ReturnToCity OK`, proving the isolated garage customization service can style floor/walls, place a simple neon sign, persist state, and leave Phase 23's interior service untouched.
- 2026-07-02: Repaired `scripts/roblox_persistence_phase24_garage_surfaces_decor_mvp.lua` after the first smoke applied surfaces/decor but failed the readback assertion with `Expected at least two stored surface styles`. `GetCustomization` now counts live interior model surface/decor attributes as well as profile table entries, so the MVP state check follows what was actually applied in the garage.
- 2026-07-02: Added `scripts/roblox_persistence_phase24_garage_surfaces_decor_mvp.lua` after the canonical Phase 23 smoke passed. Phase 24 avoids more source-anchor patching by installing a separate `GarageInteriorCustomizationInvoke` remote and `GarageInteriorCustomizationService_Active` server script for garage floor/wall material colours plus simple decoration anchors/props, with profile-backed `Garage.Customisation` records and a dual-mode smoke.
- 2026-07-02: Confirmed Phase 23 through the canonical repair path. The client smoke reported `SetAccessMode OK`, `VisitGarage OK`, `InGarage=true`, `displayExists=true`, `ReturnToCity OK`, and cleanup back to `Private`, so `scripts/roblox_persistence_phase23_garage_interior_service_canonical_repair.lua` is now the confirmed Phase 23 baseline and the incremental `same_server_garage_visits` patcher is historical.
- 2026-07-02: Added `scripts/roblox_persistence_phase23_same_server_garage_visits.lua` after Phase 22 passed with `displayOk=true`, `displayExists=true`, and `displaySource=ProfileServiceCurrentVehicle`. The dual-mode script patches `GarageInteriorService_Active` with `SetAccessMode`, `InviteVisitor`, and `VisitGarage` actions, supports `Private`/`FriendsOnly`/`InviteOnly`/`Public` runtime access modes, records visitor state attributes, refreshes the owner's display vehicle for visits, and smoke-checks the path with a self-visit before broader multi-player UI work.
- 2026-07-02: Hardened `scripts/roblox_persistence_phase23_same_server_garage_visits.lua` after Studio reported `Could not find source anchor for Phase 23 visitor return prompt`. The optional prompt widening now logs and continues if live source formatting differs, while the remote `ReturnToCity` visit path and core access actions still install.
- 2026-07-02: Repaired `scripts/roblox_persistence_phase23_same_server_garage_visits.lua` after the Phase 23 Play smoke hit `GarageInteriorService_Active:<line>: attempt to call a nil value` during `VisitGarage`. The script now removes the unsafe `Players:GetPlayerByUserId(...)` / helper-call path and uses an inline `Players:GetPlayers()` owner lookup, with a fallback that replaces the whole `visitGarage` function if a partial install has drifted too far. Edit mode asserts the unsafe lookup is gone.
- 2026-07-02: Added `scripts/roblox_persistence_phase23_garage_interior_service_canonical_repair.lua` after repeated Phase 23 partial-install source drift made anchor repairs unreliable. The script replaces only the isolated `GarageInteriorService_Active` source with a canonical Phase 21-23 implementation, preserving the main garage action controller while avoiding more fragile text-splice repairs.
- 2026-07-02: Added `scripts/roblox_persistence_phase22_garage_display_vehicle_mvp.lua` after Phase 21 passed enter/state/return but reported `streamOk=false` in Studio. The dual-mode script installs `GarageDisplayRuntime`, patches the private garage interior entry to refresh a non-drivable anchored `DisplayVehicle_Runtime` from the current saved cockpit/modules, adds display fields to the garage state smoke, and records stream error text when `RequestStreamAroundAsync` fails.
- 2026-07-02: Hardened `scripts/roblox_persistence_phase22_garage_display_vehicle_mvp.lua` after Studio reported `Could not find source anchor for Phase 22 enter return payload`. The installer now inserts display fields after stable individual return-table lines and can complete a partial install where `GarageDisplayRuntime` or the entry refresh marker was already created.
- 2026-07-02: Added `scripts/roblox_persistence_phase21_private_garage_interior_mvp.lua` as the next condensed persistence/garage phase after Phase 20 passed with `RuntimeModule=GarageProfileRuntime`, `syncCount=4`, and `missingInstances=0`. The dual-mode script installs a private owner-only garage interior runtime, remotes, client streaming/fade helper, temporary dealership-adjacent elevator prompt, simple private room instance pool under `Workspace.NeoTokyoRacersWorld.Interiors.GarageInstances`, and return-to-city path without patching the main garage action controller.
- 2026-07-02: Added `scripts/roblox_persistence_phase20_garage_profile_runtime_extract.lua` as the first condensed server-helper extraction after Phase 19 passed with `installedInstanceSlots=4`, `missingInstances=0`, and `syncCount=4`. The dual-mode script installs `ServerScriptService.NeoTokyoRacers.Services.Garage.GarageProfileRuntime`, moves the proven instance compatibility sync into that ModuleScript, and patches `GarageActionController_Shadow_Disabled` to delegate through it while preserving visible garage behavior.
- 2026-07-02: Added `scripts/roblox_persistence_phase19_instance_compat_sync.lua` as the next condensed persistence phase after Phase 18 hydration passed with `Hydrated=true`, `source=ProfileService`, and one saved vehicle. The dual-mode script installs a server sync helper and smoke-checks from the CLIENT Command Bar. It copies legacy UI/customisation writes back into current vehicle/module instance fields before mirroring to ProfileService, covering cockpit colours, thrust colour, module colours, neon flags, upgrade levels, installed module instance links, and owned compatibility tables.
- 2026-07-02: Added `scripts/roblox_persistence_phase18_profile_source_handoff.lua` as the next condensed persistence phase after the Phase 17 module UI baseline was confirmed working. The single dual-mode script installs in Edit mode and smoke-checks from the CLIENT Command Bar in Play. It hydrates the active garage session from `ProfileService_Active` saved instance data on first profile access, rebuilding legacy compatibility fields while preserving `Vehicles`, `CurrentVehicleId`, `OwnedCockpitInstances`, `OwnedModuleInstances`, and owned garage properties for future garage interiors.
- 2026-07-02: Hardened `scripts/roblox_persistence_phase17_module_popup_absolute_correction_repair.lua` after Studio reported `Installer source text leaked into bootstrap unexpectedly`. The earlier script had already written the intended client source before a bad installer-side safety assert fired, so the updated repair is idempotent after a partial install and upgrades the correction to measure parent/UI scale before nudging the popup. This targets the user's observation that the BUY/LOCKED offset grows as the screen gets smaller.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_absolute_correction_repair.lua` after the runtime anchor diagnostic proved the selected card and `ModulePopupAnchor` were correct (`anchor center=(1017.4, 665.0)`) but the visible popup still landed at `centerX=943.9`, `bottom=613.1`. The repair keeps the clipped-carousel/anchor system and adds a measured absolute-position correction loop so the popup's actual centre/bottom is nudged onto the anchor despite parent scaling or overlay transform offsets.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_pool_target_reset_repair.lua` after the source-marker audit confirmed the anchor-target helper was installed but the diagnostic still showed `gap=57.5`. The likely root was pooled `TextButton`s retaining stale `NTRModulePopupTarget` attributes or `ModulePopupAnchor` children across reuse; the repair clears those in `pooledButton()` before each reused button is configured, so only the currently rendered selected module card can own the popup anchor.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_source_marker_audit.lua` after the post-repair alignment diagnostic still reported `gap=57.5`, which strongly suggests Studio may still be running the previous card-tracked overlay helper. The audit prints active popup helper markers and relevant install attributes from `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled`.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_anchor_target_repair.lua` after the alignment diagnostic showed `dx=-47.3` and `gap=57.5` instead of the expected near-zero horizontal offset and 6px vertical gap. The repair replaces the card-tracked overlay helper with an explicit selected-card `ModulePopupAnchor`, marks current rendered module cards with popup-target attributes, positions the overlay from that anchor, removes misleading X clamping, and hides the popup when the selected card is not sufficiently visible.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_alignment_diagnostic.lua`, a client-side Play-mode diagnostic for the remaining Build Modules popup alignment issue. It prints popup centre/bottom, carousel bounds, canvas position, nearest module-card centres, horizontal delta, and vertical gap so the next repair can target the actual coordinate mismatch instead of another guessed popup patch.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_card_tracked_overlay_repair.lua` as the next repair after the user confirmed the Studio mirror was up to date. It replaces the rejected action-rail helper with a register-safe `NTRPersistencePhase15` overlay tracker: module cards remain clipped inside the bottom carousel, while BUY/LOCKED/EQUIP tracks the clicked card's rendered centre with a 6px gap and clamps to the visible carousel bounds.
- 2026-07-02: Added `docs/persistence-garage-phase17-ui-handoff-2026-07-02.md` and updated the current UI notes after the user rejected the centred action-rail workaround. The desired Build Modules behavior remains BUY/LOCKED/EQUIP directly above the clicked module card while the carousel remains clipped inside the bottom frame. A fresh Studio mirror is required before another popup repair.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_action_rail_repair.lua` after the floating BUY/LOCKED/EQUIP popup continued to alternate between carousel clipping, stale-card, destroyed-reference, and offset issues. This was a fallback attempt that kept module cards clipped by replacing exact-card floating placement with a centred action rail above the module-options frame, but the user later rejected it because the action must appear directly above the clicked module card.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_destroyed_instance_repair.lua` after moving from Paint Cockpit to Build Modules could fail with `The Parent property of ModulePopup is locked, current parent: NULL`. The repair keeps the register-safe tracked-popup design but validates/rebuilds `UI.ModulePopup` when an earlier card-parented popup reference has been destroyed by module-card pooling.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_tracker_register_repair.lua` after the tracker-layer popup fix pushed the active client bootstrap back over Roblox's local-register limit at `V75Driving`. The repair preserves the clipped-carousel/tracked-popup behavior while moving popup tracker helpers onto the existing `NTRPersistencePhase15` table instead of adding top-level local functions.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_tracker_layer_repair.lua` after restoring carousel clipping caused BUY/LOCKED/EQUIP popups to offset again. This is the preferred root fix for the two competing issues: module cards stay clipped inside the carousel, while the popup is moved to a full-screen `UI.ModuleShop` overlay and tracked from the selected card's rendered centre every frame while visible.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_carousel_clip_popup_overlay_repair.lua` after the card-child popup repair centred BUY/LOCKED/EQUIP correctly but allowed module cards to spill outside the carousel into adjacent UI panels. The follow-up restores clipping on the module ScrollingFrame and positions only the popup on an overlay from the selected card's rendered centre.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_card_child_repair.lua` after the card-anchor/scope repair restored Build Modules but BUY/LOCKED/EQUIP popups still floated high/off-centre. The new repair avoids screen-coordinate and overlay drift by parenting the visible popup directly to the selected module card, centring it at the card's top edge with a 6px gap, parking it back on a stable UI parent when hidden, and hiding it before module-card rerenders, carousel scroll, Next, and Back.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_cockpit_paint_stage_scope_repair.lua` after the popup card-anchor patch could leave the dealership menu visible when selecting a cockpit. The repair fixes `showStage()` to hide `UI.ModulePopup` inline instead of calling `NTR_hideModulePopup()` before that helper is in lexical scope, restoring the cockpit-selection transition into Paint Cockpit.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_card_anchor_repair.lua` after the screen-layer popup repair still left a large vertical gap and imperfect centring. The follow-up creates an invisible top-centre anchor on the selected module card, positions BUY/LOCKED/EQUIP popups 6px above that anchor, keeps the popup on the overlay layer, and hides any visible module popup on Next, Back, or stage changes.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_screen_layer_repair.lua` after the fresh Studio mirror showed the Paint Cockpit Back lock had landed but BUY/LOCKED/EQUIP popups were still parented to the padded `ModuleOptions` panel. The repair creates a full-screen `ModulePopupLayer`, reparents the popup there, positions from the selected card's screen coordinates, keeps the popup above the bottom module frame with an 8px gap, and retries positioning across the next rendered frames.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_popup_position_and_cockpit_back_lock.lua` after the colour/sprint guard worked but BUY/LOCKED module popups still appeared left-offset on scrolled cards. The follow-up positions BUY/LOCKED/EQUIP popups from rendered card coordinates, repeats the placement after layout settles, moves the popup above the bottom module frame with a small gap, hides it on carousel scroll, and hides the Paint Cockpit Back button so players cannot return to the dealership list after buying/selecting a cockpit.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_card_colour_popup_sprint_guard.lua` after module picker polish revealed remaining colour, popup, and menu-input issues. It inverts buy-module card colours for the live theme so available cards are lighter and locked cards darker, mutes locked title/price text, centres BUY/LOCKED popups from actual rendered card positions, hides popups when the module carousel scrolls, and blocks sprint FOV changes while the garage UI is open.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_module_button_layout_polish.lua` for minor module-picker UI cleanup. It gives module option cards more vertical room so the bottom label is not clipped, simplifies the `OWNED MODULES` / `BUY MODULES` tab buttons to one vertically centred label with customise-section sizing, and changes locked buy cards to show `Locked` in muted text while buyable cards stay lighter and locked cards stay darker.
- 2026-07-02: Added `scripts/roblox_persistence_phase17_close_garage_drive_handoff_repair.lua` after dealership/customisation worked but Start Driving left the spawned vehicle non-hovering/non-driveable because the client errored inside `closeGarage`. The repair makes `closeGarage()` tolerate missing optional picker cleanup helpers and guards the final `SpawnVehicle -> closeGarage -> startDriving` handoff so a UI cleanup warning cannot block the driving controller.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_front_engine_family_and_module_card_text.lua` after the user confirmed the H/S/B colour picker recovery and found Front Engine only showed old `Engine V1` through `Engine V4` options. The script retires/hides old flat front-engine templates without deleting them, makes Phase AK Bruiser family front engines explicit visible `Front` catalogue entries, keeps rear engines explicit, refreshes engine slot metadata, and updates owned/buy module card text to top `Cockpit / Variant`, middle green price, and bottom `Owned xN`.
- 2026-07-01: Added Persistence Phase 17 module owned/buy tab repair scripts. `scripts/roblox_persistence_phase17_module_owned_buy_tabs.lua` keeps Phase 16 module purchase locks but repairs the picker so Front Engine and Rear Engine lists are separated, selecting a slot first shows `OWNED MODULES` and `BUY MODULES`, owned module copies render as separate instance cards, owned cards use a BUY-coloured `EQUIP`, buy cards use `BUY` and auto-equip the purchased instance, locked buy cards still preview, and `Buy Copy` / `Equip Copy` / `No Free Copy` wording is removed. `scripts/roblox_persistence_phase17_module_owned_buy_tabs_client_smoke.lua` checks front/rear engine catalogue separation and owned instance data.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_owned_buy_tabs_repair_v2.lua` after the first Phase 17 installer produced a client bootstrap parse error near line 408. The repair removes the partially-installed Phase 17 client helper by marker, reinstalls a cleaner v2 helper, replaces the module picker through the stable `renderModuleShop = function()` boundary, and updates the Phase 17 smoke to warn instead of hard-failing when front/rear engine metadata cannot be classified from the catalog.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_owned_buy_tabs_repair_v3.lua` after Studio still reported the line 408 client bootstrap parse error following the v2 repair. V3 removes any previous Phase 17 owned/buy helper block, reinstalls the client module picker with simpler Luau expressions, keeps the owned/buy tab behavior, and supersedes v2 for recovery.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_front_rear_engine_metadata_repair.lua` after the Phase 17 smoke reported `Front=0, Rear=30`. The repair makes front/rear engine identity explicit with `EnginePosition`, updates Engine1/Engine2 slot metadata, exposes `EnginePosition`/`RearEngine` through the garage catalog, refreshes the server slot guard and client helper to prefer that metadata, and updates the Phase 17 smoke output.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_client_parse_line408_cleanup.lua` after the Phase 17 smoke passed but Play still reported the client bootstrap line 408 parse error. The cleanup prints/stores the line-408 neighbourhood under `ReplicatedStorage.NTR_DEBUG.COPY_THIS_LINE408_SOURCE_DUMP` and joins stale dangling `or` / `and` continuation lines in the active client bootstrap when present.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_remove_orphan_l_line408.lua` after the line-408 diagnostic confirmed a single stray `l` line immediately before `-- NTR_PERSISTENCE_PHASE17_MODULE_TABS_V4`. The repair removes only that exact orphan line from the active client bootstrap.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_client_slot_helpers_and_engine_metadata_repair.lua` after entering the dealership surfaced `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:3475: attempt to call a nil value` on the client `sortedSlots()[1]` startup path. The repair restores the missing client category/slot/module lookup helpers before `modulesForSlot` and reapplies explicit front/rear engine metadata to Bruiser engines and slots.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_colour_picker_color3_repair.lua` after cockpit customisation surfaced `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:1928: attempt to call a nil value` inside `renderColourPicker`. The repair normalizes raw `Color3` and persisted table-shaped colour data before HSV conversion so cockpit/module paint controls can render.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_colour_picker_slider_signal_repair.lua` after the colour picker error persisted at shifted line 1962. The follow-up keeps the compact picker UI but guards the slider `InputBegan`/`InputEnded`/drag signal connections so the paint menu can render even if one signal hookup is unavailable in the patched client source.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_colour_picker_root_repair.lua` after the same line-1962 colour picker error persisted. The root repair replaces the old drag-slider `renderColourPicker` implementation with a simpler swatch plus RGB-step picker that keeps existing cockpit/module colour callbacks while removing the fragile slider signal path.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_colour_picker_register_limit_repair.lua` after the root colour picker repair pushed the active client bootstrap over Roblox's local-register limit at `mobileInputState`. The repair removes leftover bulky colour-picker helper locals and reinstalls the swatch/RGB-step picker through `NTRPersistencePhase15` table methods to reduce top-level register pressure.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_colour_picker_hsb_slider_restore.lua` to replace the temporary RGB-step fallback with register-safe draggable H/S/B gradient sliders matching the previous customisation feel.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_final_end_repair.lua` after the client parse repair exposed a garage server EOF syntax error. The repair restores only the missing final `end` before `-- V56_CONSOLIDATED_ACTION_CONTROLLER_END` and dumps the server tail if the expected shape is not found.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_force_final_end_repair.lua` as a more tolerant follow-up for the garage server EOF syntax error. It line-scans for the final V56 marker and inserts one missing `end` immediately before it, or dumps the tail if the marker is already preceded by `end`.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_duplicate_attach_header_repair.lua` after the refreshed mirror showed the garage EOF syntax error was caused by a duplicated `V85_attachDefaultModuleInstancesToCurrentVehicle = function(profile)` header near the Phase 17 slot guard. The repair removes only the duplicate header.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_total_stats_repair.lua` after the Phase 17 smoke surfaced `GarageActionController_Shadow_Disabled:927: attempt to call a nil value`. The repair restores the missing `V56_totalStats` helper before the profile summary and includes `BoostRechargeDelay` in the stat totals.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_foundation_repair_and_audit.lua` after the Phase 17 smoke next surfaced `GarageActionController_Shadow_Disabled:1297: attempt to call a nil value`. The repair normalizes the V85 attach header, restores the Phase 14 `V84_*` helper foundation before the V85 attach function so Luau local scoping is correct, restores `V56_totalStats` if missing, and audits the related Phase 14/16/17 server helpers together.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_line1445_audit.lua` after the Phase 17 smoke next surfaced `GarageActionController_Shadow_Disabled:1445: attempt to call a nil value`. The read-only audit prints the live Studio line window and helper-definition locations so the next repair can target the exact shifted failing call.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_catalog_repair.lua` after the line-1445 audit showed `V56_catalog` was missing while `GetInitial` still called it. The repair restores `V56_catalog`, slot/module catalog helpers, and Phase 17 engine metadata fields.
- 2026-07-01: Added `scripts/roblox_persistence_phase17_garage_server_startup_dependency_repair.lua` after the next Phase 17 smoke surfaced another shifted `GetInitial` nil call around line 1639. The repair restores and audits the whole startup dependency cluster together, including module-type helpers, Phase AK default/core module helpers, Phase 14 V84 instance helpers before V85 attach, catalog helpers, total stats, and the profile response path.
- 2026-07-01: Added Persistence Phase 16 module family lock/sorting scripts. `scripts/roblox_persistence_phase16_module_family_locks_and_sorting.lua` patches the active garage server and client bootstrap so modules are buy-locked by source cockpit family, extra Standard module copies cost money, each bought cockpit gets one included starter module set attached to that vehicle instance, and module option cards sort owned/free copies left, unlocked purchasable modules next, and locked modules right. `scripts/roblox_persistence_phase16_module_family_locks_and_sorting_client_smoke.lua` verifies source metadata, paid Standard copies, and server-side locked-module purchase blocking.
- 2026-07-01: Hardened the Phase 16 installer after Studio reported `Could not find function end anchor for Phase 16 module option cards`. The client `renderModuleOptions` replacement now uses the known Phase 15 function tail instead of depending on the next function being named `renderModuleShop`.
- 2026-07-01: Added Persistence Phase 15 duplicate-copy UI scripts. `scripts/roblox_persistence_phase15_duplicate_copy_ui.lua` patches the active client bootstrap so owned cockpits show copy counts plus `Buy Another`, and owned module options can `Equip Copy` or `Buy Copy` through the Phase 14 instance actions. `scripts/roblox_persistence_phase15_duplicate_copy_ui_client_smoke.lua` verifies the instance fields remain present and checks visible duplicate controls when the garage UI is loaded.
- 2026-07-01: Added Persistence Phase 14 instance inventory bridge scripts. `scripts/roblox_persistence_phase14_instance_inventory_bridge.lua` patches the active garage controller and legacy mapper so live profiles expose duplicate-capable `Vehicles`, `OwnedCockpitInstances`, and `OwnedModuleInstances` beside the existing legacy UI fields, with direct `BuyCockpitInstance`, `BuyModuleInstance`, and `EquipModuleInstance` server actions. `scripts/roblox_persistence_phase14_instance_inventory_client_smoke.lua` verifies the bridge from the client without adding visible duplicate-copy UI yet.
- 2026-07-01: Updated `scripts/roblox_persistence_phase13_garage_property_ownership_client_smoke.lua` after the user-reported Phase 13 smoke output showed the core `BuyGarageProperty` path passing before the visual garage UI loaded. The smoke now treats `HOVER_RACING_V2_GarageUI` as an optional/delayed check and asks for manual `Buy More` gallery verification after reaching the dealership desk instead of failing early.
- 2026-07-01: Added Persistence Phase 13 merged garage property ownership scripts. `scripts/roblox_persistence_phase13_garage_property_ownership.lua` adds real `BuyGarageProperty` support, priced garage-property catalogue entries, current-session `OwnedGarageProperties`, property-calculated capacity with Phase 7 legacy-capacity backfill, mapper/schema support, and updates `GaragePropertyMenuController` to call the new backend. `scripts/roblox_persistence_phase13_garage_property_ownership_client_smoke.lua` verifies the merged flow from the client.
- 2026-07-01: Added `scripts/roblox_persistence_phase13_garage_property_ownership_repair.lua` after Studio reported the first Phase 13 installer could not find the exact `V80_mutatingActions` source anchor. The repair completes partial Phase 13 installs by using a looser table-aware insert for `BuyGarageProperty = true` and independently checking the server action, profile response, client context, and controller backend switch.
- 2026-07-01: Added Persistence Phase 12 garage property menu controller extraction scripts. `scripts/roblox_persistence_phase12_garage_property_menu_controller_extract.lua` installs `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.Controllers.UI.GaragePropertyMenuController` and replaces the large Phase 9 menu render block in the active client bootstrap with a small controller bridge, keeping the existing `Buy More` modal/gallery behavior while reducing future register-pressure risk.
- 2026-07-01: Added Persistence Phase 11 cockpit-only Garage Spaces scripts. `scripts/roblox_persistence_phase11_garage_spaces_cockpit_only.lua` hides the Garage Spaces panel outside `CockpitShop` and refreshes that visibility on stage changes.
- 2026-06-30: Added Persistence Phase 10 garage UI layout/modal scripts. `scripts/roblox_persistence_phase10_garage_ui_layout_modal.lua` stacks `Garage Spaces` above `Available Cash`, shrinks the Categories panel around the left-column stack, and adds a 30% black full-screen modal backdrop behind the garage property menu.
- 2026-06-30: Added `scripts/roblox_persistence_phase9_register_limit_repair.lua` after Studio reported `Out of local registers when trying to allocate V75Driving` on startup after Phase 9. The repair replaces the Phase 9 top-level local helper functions with methods on one `NTRPersistencePhase9` table and updates the `Buy More` button call, keeping the garage property menu behavior while reducing top-level local register pressure.
- 2026-06-30: Added Persistence Phase 9 garage property menu scripts after Phase 8 worked but overlapped the Categories panel. `scripts/roblox_persistence_phase9_garage_property_menu.lua` moves the compact garage-space panel into the bottom safe area, hides the old next-price text, changes the button to `Buy More`, seeds `GaragePropertyCatalog`, and opens a garage property card gallery. The first card uses the Phase 7 capacity action as a temporary backend until a real `BuyGarageProperty` server phase is added.
- 2026-06-30: User confirmed Persistence Phase 8 worked functionally, but requested a non-overlapping layout and a shift from generic upgrades to buying physical garage locations around the map.
- 2026-06-30: Added Persistence Phase 8 garage capacity upgrade UI scripts. `scripts/roblox_persistence_phase8_garage_capacity_upgrade_ui.lua` adds a guarded exact-source patch to the active client bootstrap, creating a compact `Garage Spaces` panel and an `Upgrade` button that calls `UpgradeGarageCapacity`. Added source audit and client smoke scripts.
- 2026-06-30: User confirmed Persistence Phase 7 garage capacity upgrade worked as expected.
- 2026-06-30: Added Persistence Phase 7 garage capacity upgrade scripts. `scripts/roblox_persistence_phase7_garage_capacity_upgrade.lua` adds tunable capacity upgrade config, `GarageCapacity` session profile storage, `UpgradeGarageCapacity` server action, Garage capacity data in profile responses, and `LegacyGarageProfileMapper` support so upgraded capacity mirrors into ProfileService snapshots.
- 2026-06-30: User confirmed Persistence Phase 6 capacity gate worked as expected.
- 2026-06-30: Added Persistence Phase 6 garage capacity gate scripts. `scripts/roblox_persistence_phase6_garage_capacity_gate.lua` adds a guarded capacity check to the current `BuyCockpit` path, reading `Persistence_EditAttributes.StartingGarageCapacity` and blocking new cockpit purchases when owned cockpit count reaches capacity, while keeping already-owned cockpit selection unchanged.
- 2026-06-30: User confirmed Persistence Phase 5 passed after the `ImportProfileSnapshot` repair and Studio API access/DataStore save-load testing.
- 2026-06-30: Added `scripts/roblox_persistence_phase5_import_snapshot_binding_repair.lua` after the Phase 5 save audit saw no mirrored vehicles even after the Phase 4 client smoke. The repair adds a ProfileService-owned `ImportProfileSnapshot` BindableFunction and patches the garage mirror to import converted snapshots through that binding instead of mutating a table returned by `GetProfile`.
- 2026-06-30: Added Persistence Phase 5 opt-in DataStore mirror save scripts. The enable/disable scripts toggle `Persistence_EditAttributes.DataStoreEnabled`; the save audit verifies mirrored ProfileService snapshots can be written to DataStore after Phase 4 populates the mirror; the optional load audit verifies a fresh Play/rejoin loads saved vehicle/module instance counts. Phase 5 still does not make ProfileService the live garage source of truth.
- 2026-06-30: User confirmed Persistence Phase 4 passed all audits, including the guarded garage controller mirror patch, server dependency/source verification, and client `GetInitial` mirror smoke test.
- 2026-06-30: Added Persistence Phase 4 guarded garage session mirror scripts. `scripts/roblox_persistence_phase4_garage_session_mirror.lua` patches the active garage controller with exact source anchors so successful current garage actions mirror a converted instance-based snapshot into `ProfileService_Active` while V56 remains the live source of truth. Added separate server audit and client smoke scripts for dependency/source verification and `GetInitial` mirror verification.
- 2026-06-30: User confirmed Persistence Phase 3 server audit passed, including legacy cockpit/module conversion, equipped and unequipped module instances, colours, neon ownership, module upgrade levels, DataStore-safe conversion, and bridge BindableFunctions.
- 2026-06-30: Added Persistence Phase 3 legacy garage profile bridge-prep scripts. `scripts/roblox_persistence_phase3_legacy_profile_bridge.lua` installs `LegacyGarageProfileMapper` and `LegacyGarageProfileBridge_Active` so current V56 session-memory profile tables can be converted into the new instance-based schema without patching active garage actions yet. The audit verifies cockpit/module instance conversion, equipped versus unequipped modules, colours, neon ownership, module upgrade levels, DataStore-safe encoding, and server bridge bindables.
- 2026-06-30: User confirmed Persistence Phase 2 server audit passed with `ProfileService_Active` loaded, bindables present, default two-space/zero-vehicle profile, dirty marking, and dry-run `SaveNow` while `DataStoreEnabled=false`.
- 2026-06-30: Added Persistence Phase 2 ProfileService foundation scripts. `scripts/roblox_persistence_phase2_profile_service.lua` installs `ProfileService_Active`, runtime profile markers, and server BindableFunctions for `GetProfile`, `GetSummary`, `MarkDirty`, `SaveNow`, and `IsLoaded` while keeping `DataStoreEnabled = false` by default. The audit verifies Play-mode load, bindable access, dirty marking, dry-run save, and runtime markers without patching active garage/dealership/vehicle systems.
- 2026-06-30: Tightened the Phase 2 audit after it was run from the client Command Bar and yielded on `ServerScriptService.NeoTokyoRacers`. The audit now fails fast unless run from the SERVER Command Bar, and `scripts/roblox_persistence_phase2_client_probe.lua` provides a lightweight client-side attribute check.
- 2026-06-30: User confirmed Persistence Phase 1 installed in Studio and both Phase 1 scripts passed.
- 2026-06-30: Added Persistence Phase 1 schema foundation scripts and handoff doc. `scripts/roblox_persistence_phase1_profile_schema.lua` installs a non-live `PlayerProfileSchema` module plus `Persistence_EditAttributes` config for instance-based cockpit/module ownership, two starting garage spaces, garage access modes, DataStore-safe colour/material encoding, and future visitable instanced garages. `scripts/roblox_persistence_phase1_profile_schema_audit.lua` verifies duplicate module instances, the two-space capacity gate, and encode/decode safety without touching active gameplay scripts.
- 2026-06-30: Cleaned the prompt pack to use `C:\Users\Oscar\Documents\LUCIDITY\Codex---Neo-Tokyo-Racers`, removed failed VFX repair scripts from the current working baseline, and refreshed the Studio full snapshot mirror at 17:37:33 with 65 imported scripts.
- 2026-06-30: Added `scripts/roblox_vfx_mobile_delayed_attach_once.lua` as the narrow follow-up after the mirror-baseline VFX restore was confirmed working. The script patches only `VehicleVFXController`, adds configurable `MobileInitialAttachDelaySeconds`, delays mobile attach briefly, marks runtime hosts, and performs one socket attach pass without continuous rescans, clearing, or rebuild loops.
- 2026-06-30: Added `scripts/roblox_vfx_restore_mirror_known_good_baseline.lua` after the VFX repair sequence kept cycling between memory growth, 3-5 second cut-outs, all-on VFX, and unparented Beam buildup. The recovery restores the four vehicle-VFX-owned scripts/modules from the current mirror and only guarded VFX snippets in the main bootstrap, so other Studio changes since the first VFX patch can be preserved.
- 2026-06-30: Removed the failed intermediate VFX repair scripts from the current working baseline after testing showed the rescan/rebuild direction could cause RuntimeVFXController memory growth, cut-outs, all-on VFX, or unparented Beam buildup. Keep the restore and delayed attach-once scripts as the current VFX recovery path.
- 2026-06-30: Added `scripts/roblox_ui_shared_theme_back_exit_and_intro.lua` to add configurable shared `Back` and `Exit` UI theme colours, route dealership Back/Exit/close buttons through those values, and make the dealership intro objective UI read the same theme values as the dealership UI.
- 2026-06-30: Added `scripts/roblox_dealership_remove_cockpit_module_slots_text.lua` to remove the overlapping `Module Slots` count list from the selected cockpit stats panel. The cleanup leaves included-module stat calculations, Buy/Select behavior, gating, and the Build Modules screen unchanged.
- 2026-06-30: Added `scripts/roblox_mobile_drive_thumbstick_v2_4_large_edge_drift.lua` as the next mobile-control test after V2.3 was reported much better but still not ideal. V2.4 keeps zero deadzone/linear response, enlarges the inner circle to `1.4x`, makes the outer ring `1.35x` the enlarged inner circle, reduces invisible hit-area padding, and moves drift activation near the outer edge with `0.95` enter / `0.88` exit thresholds.
- 2026-06-30: Added `scripts/roblox_mobile_drive_thumbstick_v2_3_snappy_steering.lua` as the next mobile-control test. It removes the thumbstick deadzone by default, switches steering response to linear, shrinks the outer drift ring to `1.25x`, lowers the boost button near the ring, and documents the touch-jitter/small-screen verification risks.
- 2026-06-30: Added `scripts/roblox_lighting_night_mode_signal_repair.lua` for the Play-mode regression where night mode no longer switches street lights or window materials. The repair patches the lighting mode signal source, refreshes the isolated window/lamppost client controllers, and retags existing managed windows/lamppost lights without deleting assets.
- 2026-06-30: Cleaned up `prompts/01_start_every_session.md` with the current local repo path and normal repo-relative file paths, then refreshed the Studio full snapshot mirror. The fresh export was generated in Studio at 11:10:17 and imported 65 scripts, including the mobile thumbstick controller, window material controller, and night lamppost light controller.
- 2026-06-14: Added separate edit-mode lighting capture scripts for Day and ClearNight. Each captures the current Lighting properties, atmosphere, post effects, and active Sky, then rewrites the runtime preset module without exact source-text matching so the next fresh Play session uses the edited condition.
- 2026-06-13: Repaired the lamppost SurfaceLight installer after Studio found four same-named light children under the source MeshPart. The installer now selects the first light as its template, cleans duplicate siblings, and refreshes every target to exactly one cloned light.
- 2026-06-13: Added a rerunnable lamppost SurfaceLight installer. It clones the single `SurfaceLight lamppost` template onto every `lamppost neon` MeshPart, tags installed lights, and installs an event-driven client controller that enables them only during night mode.
- 2026-06-13: Lighting Phase AP was installed and user-confirmed working. Added separate edit-mode day and night Command Bar preview scripts that apply the full lighting preset, sky, post effects, preset attribute, and matching tagged-window MaterialVariant without entering Play mode.
- 2026-06-13: Added Lighting Phase AP window material migration and runtime controller. The Command Bar installer targets MeshParts with a direct `Windows` or `SurfaceAppearance Windows` SurfaceAppearance child, clears their mesh texture, removes the matching SurfaceAppearance, assigns `Windows Day`, tags them, and installs an event-driven client controller that switches to `Windows Night` for the current night lighting mode.
- 2026-06-11: Added the mobile thumbstick V2 visual sequence through `scripts/roblox_mobile_drive_thumbstick_v2_2_drift_feedback.lua`. The current design uses an outer ring with `1.8x` the inner radius, a darker translucent band, light-green idle border/text, coordinated red pointer/text/border drift cues, full outer-edge pointer travel, raised MPH/boost, and `1.275x` frameless pedals.
- 2026-06-08: Added `scripts/roblox_mobile_drive_thumbstick_install.lua`. The guarded installer replaces the four mobile steering/drift arrow buttons with a fixed forgiving horizontal thumbstick, adds analog steering plus drift enter/exit hysteresis through `MobileDriveInputState`, and creates editable tuning attributes without changing driving physics, camera, pedals, boost, desktop controls, or gamepad controls.
- 2026-06-08: Added Vehicle Phase AO module upgrade UI installer and audit. Phase AO removes the visible legacy Brakes, Converter, Fuel System, and generic upgrade controls; adds module-specific upgrade cards and preview/purchase flow; and replaces the right stats panel with an always-visible tier/index header plus contextual headline and detailed-variable rows.
- 2026-06-08: Vehicle Phase AO was installed and reported working well. The AL-AO performance, module-upgrade, contextual-stats, tier/index, and customisation UI sequence is now the confirmed vehicle baseline. A post-AO Studio mirror refresh remains required.
- 2026-06-08: Added Vehicle Phase AN module-specific upgrade installer, audit, and opt-in purchase smoke test. Upgrade ownership is keyed by module ID and upgrade ID, purchases are server-authoritative, catalogue/profile responses expose Phase AO data, and spawned module clones receive purchased Phase AM performance deltas.
- 2026-06-08: Phase AN read-only and purchase smoke tests passed for `MODULE_ENGINE_BRUISER_01_STANDARD`: Fuel Injection level 1 cost `$4000`, purchase succeeded, level advanced to 1, and profile rating increased from `D 407` to `D 410`. Added a final spawned-upgrade audit.
- 2026-06-08: Phase AN spawned-upgrade audit passed with 0 warnings. The spawned engine carried Fuel Injection level 1, EngineOutput `+2`, TopSpeed `+1`, and runtime rating `D 410`, confirming end-to-end upgrade effects.
- 2026-06-08: Added Vehicle Phase AM gated runtime integration, audit, and physics activation scripts. Phase AM writes full-build raw/normalized/headline/rating data to spawned vehicles, adds cockpit override/module delta tuning attributes, and bridges detailed variables into V75 driving behind a disabled-by-default compatibility switch.
- 2026-06-08: Added `scripts/roblox_vehicle_phaseAM_runtime_writer_repair.lua` after fresh spawned vehicles still lacked Phase AM runtime folders. The repair installs an isolated server vehicle-spawn watcher instead of applying another fragile garage controller patch.
- 2026-06-08: Vehicle Phase AM passed its runtime and physics gates. `VehiclePerformanceRuntimeService_Active` wrote all 17 raw variables, 17 normalized variables, 6 headline stats, and a `D 407` rating with 0 warnings; detailed V75 physics was enabled and reported working well.
- 2026-06-08: Added Vehicle Phase AL performance foundation and audit scripts. Phase AL installs isolated shared definitions/calculator/upgrade-definition modules plus editable normalization, headline weight, overall rating, tier, compatibility, and module-context config. It does not switch active driving, UI, purchases, or profile behavior.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_bruiser_modular_defaults.lua` and `docs/vehicle-phaseAK-bruiser-modular-defaults-2026-06-05.md`. Phase AK prepares the Bruiser module catalogue as per-cockpit Standard/Lightweight/Power engines, stabilisers, and boost plus Lvl 1-3 bumpers/spoilers/side pods; grants standard core modules with cockpit purchase/select; previews included defaults in dealership stats; and gates Customise Modules until engine/stabilisers/boost are equipped.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_register_limit_repair.lua` after Studio reported the active client bootstrap exceeded Roblox's local-register limit near the V75 driving block. The repair keeps the Phase AK UI behavior but moves helper functions onto one phase table to reduce top-level locals.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_server_core_gate_repair.lua` after Studio reported `SpawnVehicle` failing with a nil call inside the Phase AK server core-module gate. The repair removes the forward-local dependency and refreshes Engine1/Engine2 slot attributes to use the shared Engines catalogue.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_rear_engine_catalogue_repair.lua` to mirror the new Bruiser family-folder engine layout into `Engines_B`, point rear engine slots at `Engines_B`, and treat cockpit-included front/rear standard engines separately in server grants and dealership stats.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_module_entry_camera_repair.lua` so the Build Modules screen starts with the same preview distance/angle as the Front Engine camera immediately after cockpit purchase/paint, before any module slot is clicked.
- 2026-06-05: The first shared-config cockpit colour/defaults installer was generated but failed its guarded source preflight against the live server and was later removed. The per-cockpit attribute approach supersedes it.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_per_cockpit_default_colours.lua` to move cockpit starting paint defaults onto each cockpit model as editable `Default*Color` attributes and apply them when a cockpit is selected/bought.
- 2026-06-05: Added `scripts/roblox_vehicle_phaseAK_spawn_module_colour_sync.lua` so cockpit paint Primary/Secondary/Detail changes are copied into installed module colour records, keeping preview and spawned drivable vehicles visually aligned.
- 2026-06-08: User confirmed the Phase AK modular catalogue and follow-up repairs working, including mirrored `Engines`/`Engines_B` family folders, driving spawn, corrected cockpit/module camera views, per-cockpit default colour attributes, and spawned module colours matching preview. The earlier shared cockpit-default config approach is superseded by attributes on each cockpit model.
- 2026-06-05: Added `scripts/roblox_vfx_phaseAJ_thrust_preview_root_repair.lua` and `docs/vfx-phaseAJ-thrust-preview-root-repair-2026-06-05.md` to repair missing thrust VFX preview after Dealership Intro Phase 4 moved the local preview vehicle to `Workspace._NTR_ClientOnly.VehiclePreview`. Updated the staged Phase B preview vehicle generator to use the same local-only preview root.
- 2026-06-04: Added `scripts/roblox_character_sprint_controller_install.lua`, a safe Studio command-bar installer for an on-foot sprint controller under `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.Controllers.Runtime`. It removes the earlier broken `NTR_CharacterSprintDefaults`, stores sprint tuning in `ReplicatedStorage.NeoTokyoRacers.Shared.Config.CharacterMovement_EditAttributes`, and avoids consuming Shift so vehicle drift remains controlled by the driving system.
- 2026-06-04: User confirmed the character sprint install worked and moved/renamed the live `StarterPlayerScripts.NeoTokyoRacersClient.Controllers.Runtime` hierarchy to the clean controller names: `CharacterSprintController_Active`, `DriveHudController_Active`, `MobileDriveControlsController_Active`, and `RuntimeVFXController_Active`.
- 2026-06-04: Updated `scripts/roblox_character_sprint_controller_install.lua` with mobile auto-sprint. On touch devices, the sprint controller reads Roblox's standard PlayerModule move vector and starts sprinting when it reaches `MobileSprintMoveThreshold`, default `0.85`, while still disabling itself in `VehicleSeat`s so mobile/keyboard vehicle drift is unaffected.
- 2026-06-04: Added `docs/character-sprint-controller-2026-06-04.md` handoff. User confirmed sprint works in the clean runtime hierarchy; a blocked third-party custom sprint animation was resolved by disabling/replacing `AnimationId`, leaving Roblox's normal character animations active.
- 2026-05-28: Added dated current mechanics docs covering vehicle, lighting, LOD, traffic lights, mobile performance, customisation, UI, race events, and open-world city systems. These docs are a reference layer for future Codex/ChatGPT sessions and should not be treated as permission to rewrite unrelated systems.
- 2026-06-03: Added `prompts/`, a reusable ChatGPT/Codex prompt pack for session startup, Studio output debugging, feature planning, handoff, commit summaries, and docs database maintenance.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase0_audit.lua`, a read-only Studio command-bar audit for active client/server garage paths, legacy script state, world/intro marker presence, and likely auto-open/preview/purchase code paths.
- 2026-06-03: Added Dealership Intro Phase 1 marker setup docs and `scripts/roblox_dealership_intro_phase1_setup_markers.lua`, a safe rerunnable Studio command-bar script that creates editable intro spawn, desk trigger, camera, preview, and path markers without touching startup behavior.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase2_install_client.lua`, installing an isolated `DealershipIntroClient_Active` LocalScript for objective UI, local path arrows, camera intro, and desk distance detection. It expects the Phase 3 `OpenGarageFromIntro` hook for opening the full garage.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase3_gate_garage_startup.lua`, a guarded client patch that replaces the active bootstrap's auto `task.defer(init)` startup with a local `OpenGarageFromIntro` BindableEvent fired by the intro client at the desk.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase4_preview_after_purchase.lua`, a fragile guarded client patch that delays local preview vehicle creation until successful cockpit purchase/select, uses `Workspace._NTR_ClientOnly.VehiclePreview`, and reads dealership preview/camera markers.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase5_restore_preview_orbit_camera.lua`, restoring garage preview orbit camera behavior after Phase 4 so the preview camera starts from the marker but still rotates around the vehicle centre and responds to module selection focus.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase6_vehicle_exit_spawn_marker.lua`, creating `Workspace.NeoTokyoRacersWorld.Dealership.Spawn.VehicleExitSpawnPoint` and patching the active garage server controller so final drivable vehicles spawn from that editable marker.
- 2026-06-03: Added `scripts/roblox_dealership_intro_phase7_exit_button_reopen_gate.lua`, adding an Exit button to the first cockpit-buy menu and a leave/re-enter gate so exiting the dealership does not immediately reopen the menu.
- 2026-06-03: User confirmed Dealership Intro Phases 1-7 working, including desk-gated garage open, delayed preview, restored preview orbit camera, editable final exit spawn, and first-menu Exit/reopen behavior.
- 2026-05-29: Added Architecture Phase K command-bar migration plan/script to move `ReplicatedStorage.HOVER_RACING_V2_KIT` into `ReplicatedStorage.NeoTokyoRacers` domains with guarded source preflight.
- 2026-05-29: Updated Architecture Phase K preflight patching for registry/path-string source references to `00_EDIT_ME_FIRST` and `UI_THEME_DoNotRename`.
- 2026-05-29: Added Architecture Phase L command-bar deletion gate to verify no live dependencies remain, then destroy `ReplicatedStorage.HOVER_RACING_V2_KIT`.
- 2026-05-29: Confirmed Architecture Phase K and Phase L completed in Studio. The old `ReplicatedStorage.HOVER_RACING_V2_KIT` root is gone, with 0 legacy source hits and 0 legacy ObjectValue references in the Phase L report.
- 2026-05-28: Added target architecture reorganisation plan and Studio inventory report script for planning a safe future hierarchy migration.
- 2026-05-28: Added a dedicated Studio inventory report paste document for long Roblox Explorer hierarchy reports.
- 2026-05-28: Added hierarchy migration plan based on the pasted Studio inventory, with `Test + WIP Assets` explicitly excluded from migration.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase1_architecture_resolver.lua`, a non-destructive Studio command-bar script that creates future `NeoTokyoRacers` architecture roots, adds a compatibility `PathResolver`, and mirrors key config folders without moving live systems.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase2_live_references_registry.lua`, a non-destructive Studio command-bar script that adds visual ObjectValue references to current live systems and a `LiveSystemRegistry` bridge module for future targeted migrations.
- 2026-05-28: Added `scripts/roblox_studio_active_script_audit_v1.lua`, a read-only Studio command-bar audit that reports active scripts outside approved live roots and flags generic, temporary, WIP/test, and excluded-area scripts.
- 2026-05-28: Added `scripts/roblox_studio_suspicious_script_inspector_v1.lua`, a read-only Studio command-bar inspector that captures source previews, line counts, fingerprints, and keyword hints for unexpected/generic active scripts before any cleanup decisions.
- 2026-05-28: User manually removed two generic active `LocalScript`s from `StarterPlayerScripts`; `TEMP_LightingPreview` is intentionally kept as a day/night lighting preview tool.
- 2026-05-28: Updated audit/inspector scripts so `TEMP_LightingPreview` is treated as an intentional temporary lighting tool rather than accidental clutter.
- 2026-05-28: Added `scripts/roblox_studio_archive_disabled_legacy_scripts_v1.lua`, a cleanup script that archives disabled legacy `HOVER_RACING*` scripts into `ServerStorage.NeoTokyoRacers_LegacyArchive.DisabledScripts` without deleting them or touching active systems.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase3_config_registry_mirrors.lua`, a non-destructive config migration script that creates/refines `NeoTokyoRacers.Shared.Config`, adds live config references, refreshes generated config mirrors, and installs `ConfigRegistry` without changing live config values or switching live scripts.
- 2026-05-28: Added a lightweight Studio-to-GitHub script source mirror workflow: `scripts/roblox_studio_export_scripts_for_github_v1.lua`, `scripts/import_studio_script_export.py`, `docs/10_script_source_sync_workflow.md`, and `roblox/exported_scripts/`.
- 2026-05-28: Imported the current Studio script export into `roblox/exported_scripts` and added `docs/11_manual_script_copy_map.md` for manual source copy guidance.
- 2026-05-28: Reviewed the exported Studio script mirror and added `docs/script-architecture-review-2026-05-28.md`, documenting the current client/server modularity risks, mobile performance priorities, and staged migration order.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase4_architecture_staging_scaffold.lua`, a non-destructive Studio command-bar script that creates future service/controller/config folders and ObjectValue references without moving or changing live scripts.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase4b_prepare_traffic_service_shadow.lua`, a non-destructive Studio command-bar script that copies the live traffic controller into the new architecture as a disabled shadow script for future migration testing.
- 2026-05-28: Added `docs/phase5-ui-migration-plan-2026-05-28.md` and `scripts/roblox_hierarchy_phase5_ui_module_shadow_extract.lua` to stage UI helper modules and future UI folders without switching live UI behaviour.
- 2026-05-28: Added `docs/compressed-architecture-roadmap-2026-05-28.md` to reduce the remaining architecture work to six broader phases.
- 2026-05-28: Added `scripts/roblox_hierarchy_phase6_world_services_migration.lua`, which migrates traffic lights to the new world service location and stages disabled lighting/LOD shadow copies for later migration.
- 2026-05-28: Added `docs/phase7-shared-ui-helpers-2026-05-28.md` and `scripts/roblox_hierarchy_phase7_shared_ui_helpers_promote.lua`, promoting final shared UI helper modules and mirrored UI theme config without switching live UI behaviour.
- 2026-05-28: Added `docs/phase8-client-ui-controller-scaffold-2026-05-28.md` and `scripts/roblox_hierarchy_phase8_client_ui_controller_scaffold.lua`, creating the future client UI controller structure without switching live UI, driving, VFX, server actions, mobile controls, LOD, lighting, traffic, or assets.
- 2026-05-28: Added `docs/phase9-server-services-scaffold-2026-05-28.md` and `scripts/roblox_hierarchy_phase9_server_services_scaffold.lua`, creating the future garage/vehicle/profile/economy server service structure without switching the current live `GarageInvoke` action layer.
- 2026-05-28: Added `docs/phase10-runtime-controller-scaffold-2026-05-28.md` and `scripts/roblox_hierarchy_phase10_runtime_controller_scaffold.lua`, creating future runtime controller ownership targets for driving bootstrap, camera, HUD, mobile controls, VFX, and vehicle access without changing active runtime behaviour.
- 2026-05-28: Added `docs/phase11-architecture-readiness-audit-2026-05-28.md` and `scripts/roblox_hierarchy_phase11_architecture_readiness_audit.lua`, a non-destructive readiness audit for missing architecture paths, unexpected active scripts, accidental enabled future bootstraps, and remaining live legacy names.
- 2026-05-28: Added `docs/phase12-server-action-snapshot-2026-05-28.md` and `scripts/roblox_hierarchy_phase12_server_action_snapshot.lua`, capturing the live V56 server action layer as a non-live snapshot/parity reference before extracting server services.
- 2026-05-28: Added `docs/phase13-server-parity-harness-2026-05-28.md` and `scripts/roblox_hierarchy_phase13_server_parity_harness.lua`, a non-destructive parity gate that compares the live V56 hash against the Phase 12 snapshot and optionally checks the `GetInitial` response shape in Play mode.
- 2026-05-28: Corrected Phase 12 snapshot generation so `V56ActionLayerSnapshot` is valid Lua, updated Phase 13 to avoid unsupported server-side `OnServerInvoke` reads, and added `scripts/roblox_hierarchy_phase13b_client_getinitial_shape_check.lua` for the client-side `GetInitial` shape check.
- 2026-05-28: Added `docs/phase14-server-shadow-action-controller-2026-05-28.md` and `scripts/roblox_hierarchy_phase14_server_shadow_action_controller.lua`, creating a disabled shadow server action controller from the verified V56 block without switching live server ownership.
- 2026-05-28: Added `docs/phase15-server-action-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase15_server_action_owner_switch.lua`, a controlled server action owner switch with `SWITCH` and `ROLLBACK` modes.
- 2026-05-28: Phase 15 `SWITCH` was run and tested successfully. Output confirmed the active V56 controller now runs from `GarageActionController_Shadow_Disabled`; Phase 13 hash parity and Phase 13B client `GetInitial` shape checks passed after the switch.
- 2026-05-28: Added `docs/phase16-runtime-helper-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase16_runtime_helper_owner_switch.lua`, a controlled runtime helper owner switch for cached thrust VFX, mobile controls, and mobile HUD suppression.
- 2026-05-28: Phase 16 `SWITCH` was run and tested successfully. Runtime helper ownership moved into `NeoTokyoRacersClient.Controllers.Runtime` without reported gameplay regressions.
- 2026-05-28: Added `docs/phase17-driver-seat-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase17_driver_seat_owner_switch.lua`, a controlled driver seat keeper owner switch into the new vehicle service architecture.
- 2026-05-28: Phase 17 `SWITCH` was run and tested successfully. Driver seat position keeper ownership moved into `NeoTokyoRacers.Services.Vehicle` without reported gameplay regressions.
- 2026-05-28: Added `docs/phase18-lod-client-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase18_lod_client_owner_switch.lua`, a controlled LOD client owner switch into the new world controller architecture.
- 2026-05-28: Phase 18 `SWITCH` was run and tested successfully. LOD client ownership moved into `NeoTokyoRacersClient.Controllers.World` without reported gameplay regressions.
- 2026-05-28: Added `docs/phase19-lighting-service-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase19_lighting_service_owner_switch.lua`, a controlled server lighting owner switch into the new world lighting service architecture.
- 2026-05-28: Phase 19 `SWITCH` was run and tested successfully. Server lighting ownership moved into `NeoTokyoRacers.Services.World.Lighting` without reported gameplay regressions.
- 2026-05-28: Added `docs/phase20-thrust-preview-owner-switch-2026-05-28.md` and `scripts/roblox_hierarchy_phase20_thrust_preview_owner_switch.lua`, a controlled thrust preview owner switch into the new preview controller architecture.
- 2026-05-28: Phase 20 `SWITCH` was run and tested successfully. Thrust preview ownership moved into `NeoTokyoRacersClient.Controllers.Preview` without reported gameplay regressions.
- 2026-05-28: Added `docs/phase21-post-switch-architecture-audit-2026-05-28.md` and `scripts/roblox_hierarchy_phase21_post_switch_audit.lua`, a read-only checkpoint audit for all owner switches completed in Phases 15-20.
- 2026-05-28: Phase 21 audit passed cleanly: 11/11 expected active owners enabled, no unexpected active scripts, no active scripts in `Test + WIP Assets`, and only the intentionally remaining `HOVER_RACING_V2_Client` legacy owner active.
- 2026-05-29: Added `docs/main-client-extraction-plan-2026-05-29.md`, a five-phase plan for extracting `HOVER_RACING_V2_Client` into state/API/catalog modules, preview/colour controllers, garage screen controllers, a final bootstrap, and cleanup.
- 2026-05-29: Added `docs/main-client-phaseA-core-boundary-2026-05-29.md` and `scripts/roblox_client_phaseA_core_boundary_modules.lua`, creating staged Core modules for client state, garage API calls, catalog lookups, theme reads, and paint utilities without changing live client behaviour.
- 2026-05-29: Phase A was run and play-tested successfully by the user; added `docs/main-client-phaseB-preview-colour-2026-05-29.md` and `scripts/roblox_client_phaseB_preview_colour_modules.lua`, staging preview vehicle, preview camera, and colour picker modules without switching live client behaviour.
- 2026-05-29: Phase B was run and play-tested successfully by the user; added `docs/main-client-phaseC-garage-screens-2026-05-29.md` and `scripts/roblox_client_phaseC_garage_screen_controllers.lua`, staging dealership, cockpit paint, module shop, customisation, navigation, and stats panel controllers without switching live client behaviour.
- 2026-05-29: Phase C was run and play-tested successfully by the user; added `docs/main-client-phaseD-owner-switch-2026-05-29.md` and `scripts/roblox_client_phaseD_main_client_owner_switch.lua`, a controlled `SHADOW` / `SWITCH` / `ROLLBACK` owner-location switch for the remaining live `HOVER_RACING_V2_Client`.
- 2026-05-29: Phase D was run and play-tested successfully by the user; added `docs/main-client-phaseE-post-switch-audit-2026-05-29.md` and `scripts/roblox_client_phaseE_post_switch_audit.lua`, a read-only audit for confirming the new architecture-owned main client is active and the legacy `HOVER_RACING_V2_Client` is disabled.
- 2026-05-29: Phase E audit passed cleanly after rerunning Phase D with `MODE = "SWITCH"`: 11/11 expected active owners enabled, no unexpected active scripts, no active scripts in `Test + WIP Assets`, no active legacy-named `HOVER_RACING` scripts, and all staged Phase A-C modules present.
- 2026-05-29: Added `docs/world-phaseF-city-hierarchy-lod-migration-2026-05-29.md` and `scripts/roblox_world_phaseF_city_hierarchy_lod_migration.lua`, preparing a city hierarchy migration from `Workspace.GeneratedCityBlocks` to `Workspace.NeoTokyoRacersWorld.City.Block S#` plus a LOD root resolver fallback.
- 2026-05-29: Added `docs/world-phaseJ-far-lod5-assets-migration-2026-05-29.md` and `scripts/roblox_world_phaseJ_far_lod5_assets_migration.lua`, preparing migration of `ReplicatedStorage.FarLOD5` into `ReplicatedStorage.NeoTokyoRacers.Assets.World.FarLOD5Proxies` plus a LOD far-proxy resolver fallback.
- 2026-05-29: Added `docs/cleanup-phaseG-full-hierarchy-audit-2026-05-29.md` and updated `scripts/roblox_cleanup_phaseG_full_hierarchy_cleanup_audit.lua`, a read-only full hierarchy cleanup audit that classifies old inactive folders/scripts/assets before a separate deletion script is generated.
- 2026-05-29: Added `docs/cleanup-phaseH-delete-legacy-inactive-items-2026-05-29.md` and `scripts/roblox_cleanup_phaseH_delete_legacy_inactive_items.lua`, a targeted cleanup script with `DRY_RUN` / `DELETE` modes for removing the exact inactive legacy owners, rollback scripts, old report folders, and legacy archive confirmed by Cleanup Phase G.
- 2026-05-29: Added `docs/cleanup-phaseI-aggressive-migration-cleanup-2026-05-29.md` and `scripts/roblox_cleanup_phaseI_aggressive_migration_cleanup.lua`, a one-step aggressive cleanup for stale migration reports, reference ObjectValues, mirror-only generated config, non-live scaffold/shadow/snapshot code, and empty placeholder folders.
- 2026-05-29: Added `docs/cleanup-phaseM-post-kit-migration-audit-2026-05-29.md` and `scripts/roblox_cleanup_phaseM_post_kit_migration_audit.lua`, a read-only post-K/L audit for stale kit references, nil ObjectValues, empty compatibility folders, old report clutter, and remaining cleanup candidates.
- 2026-05-29: Added `scripts/roblox_cleanup_phaseM_print_action_sections.lua`, a read-only helper that prints only the actionable Phase M report sections for review.
- 2026-05-29: Added `scripts/roblox_cleanup_phaseM_runtime_root_probe.lua`, a read-only helper for checking whether `Workspace.HOVER_RACING_V2_WORLD` is still referenced before cleaning stale runtime ObjectValues.
- 2026-05-29: Added `scripts/roblox_architecture_phaseN_runtime_world_path_repair.lua`, a guarded source/object reference repair for retargeting old `Workspace.HOVER_RACING_V2_WORLD` runtime paths to `Workspace.NeoTokyoRacersWorld`.
- 2026-05-29: Updated Phase N after first Studio preflight to handle remaining `PLAYER_VEHICLES_Runtime` constant/variable source shapes in the garage action controller, driver seat service, and main client bootstrap.
- 2026-05-29: Phase N completed successfully in Studio: 10 source objects patched, 24 replacements applied, 8 ObjectValues repaired, `Workspace.NeoTokyoRacersWorld.SpawnPoints.VehicleSpawnPoint` created, and final old runtime source hits were 0.
- 2026-05-29: Added `scripts/roblox_cleanup_phaseO_stale_metadata_report_cleanup.lua`, a guarded cleanup for stale migration metadata, old report folders, old path StringValue text, and exact empty placeholder folders after the clean Phase M audit.
- 2026-05-29: Phase O follow-up audit passed: 0 warnings, 0 missing required paths, 0 source legacy hits, 0 stale ObjectValues, and 0 auto cleanup candidates. Updated Phase M to stop flagging required empty architecture containers such as `Config.Runtime`.
- 2026-05-29: Final Phase M action-section verification passed cleanly: every actionable section printed `None`, including review-before-delete candidates.

## Early System

- Initial command-bar scripts created starter folders, template parts, module categories, UI, money, and a short checkpoint race.
- The system moved from cable-tied modular slots to fixed category-based module slots.

## Fixed-Slot Category System

- `V13` and later scripts introduced a fixed-slot category system.
- `BRUISER` became the active vehicle category.
- Modules became interchangeable within a vehicle category.

## UI and Mobile

- Multiple responsive/futuristic UI patches refined the dealership, module selection, colour picker, customisation menus, and mobile controls.
- The UI settled toward dark translucent panels, light green borders, and futuristic text.

## VFX

- V23-V31 introduced VFX templates for hover dust, engine jets, boost, stabilisers, and custom VFX hierarchies.
- V64-V66 added cached thrust visual runtime and fixed a weld leak.

## Server/Action Layer Recovery

- V52-V56 rebuilt or repaired server/action logic after earlier patch failures.
- Later guidance: avoid large brittle server/client rewrites unless necessary.
- 2026-06-02: Added `scripts/roblox_architecture_phaseP_garage_runtime_startup_repair.lua` in the Git repo for the conservative post-Phase-N garage runtime startup repair. Phase Q supersedes it if the line 23 startup error remains.
- 2026-06-02: Added `scripts/roblox_architecture_phaseQ_garage_controller_header_repair.lua` to repair a post-Phase-N/P startup failure in the active garage action controller by replacing only the V56 startup header with migrated NeoTokyoRacers paths.

## Lighting

- 2026-07-14: Screenshot/value comparison proved the first FivePM/SevenAM text
  snapshot produces bright blue Picture 1, while the later SevenAM-labelled
  snapshot (`2026-07-13T23:28:12Z`) matches warm Picture 2. Added a targeted,
  preflighted canonical importer that replaces only FivePM/SevenAM environment
  tables and their separate Sky assets, preserves StageVisual config, and
  verifies the serialized module through a fresh require. Awaiting Studio run,
  visual confirmation, and mirror refresh.
- 2026-07-14: Refreshed mirror at `00:15:37` confirms AQ, AR, AS, all eight
  StageVisual folders, and CurrentLightingCaptureText are installed. Compared
  the pasted FivePM text snapshot against exported source: FivePM and SevenAM
  environment tables are identical except for their separate Sky names, and
  both StageVisual configs are Day windows / lights disabled / brightness 2.
  No redundant Studio importer was generated. The mirror currently shows the
  rapid-test `BaseDurationSeconds = 5`; restore production timing after testing.
- 2026-07-14: Added `scripts/roblox_lighting_capture_current_to_text_value.lua`,
  a non-invasive Edit-mode snapshot tool that creates or refreshes
  `LightingCycleConfig.CurrentLightingCaptureText`. Its deterministic Lua-table
  text includes Lighting, atmosphere, post-effects, active Sky properties,
  current preset name, and available config-owned StageVisual values. It changes
  no preset, controller, environment property, or unrelated system.
- 2026-07-13: Revised Lighting Phase AS after the selected-stage preview reported
  an infinite yield waiting for missing StageVisuals. Visual state is now solely
  config-owned: environmental capture preserves window/light attributes, Phase
  AS initializes brightness from an explicit default config value rather than a
  current light, and preview fails fast with the installer path if config is
  missing. Preview/runtime still applies config-owned window mode, enabled state,
  and brightness.
- 2026-07-13: Generated Lighting Phase AS to add easy per-stage StageVisuals
  config folders for WindowMode, StreetLightsEnabled, and StreetLightBrightness.
  Updated the reusable capture tool to save those values from the current Edit
  view, the selected-stage preview to restore them, the two isolated runtime
  visual controllers to consume the config event-first, and the audit to verify
  all eight stage folders. No server gameplay or unrelated system was changed.
  Awaiting Studio installation, verification, and mirror refresh.
- 2026-07-13: Generated Lighting Phase AR to add independent TenAM and ThreePM
  presets copied from the confirmed Day values. The isolated extension preserves
  all existing captures, updates the chronological schedule to eight stages,
  expands runtime preview keys to 1-8 with dynamic N/M lookup, updates the
  reusable capture/preview/audit tools, and keeps Day/Night duration doubled.
  Awaiting Studio installation, verification, and mirror refresh.
- 2026-07-13: User confirmed Lighting Phase AQ installed and worked, including
  capturing the prepared Edit-mode condition into FivePM. The reusable
  `roblox_lighting_capture_current_to_selected_stages.lua` and
  `roblox_lighting_preview_selected_stage_edit_mode.lua` tools are now the
  preferred preset-editing workflow. Post-confirmation Studio mirror refresh
  remains required.
- 2026-07-13: Generated Lighting Phase AQ as a preflighted canonical installer,
  reusable selected-stage capture/preview tools, read-only audit, and handoff.
  The six synchronized stages are Day, 5 PM, 8 PM, Night, 4 AM, and 7 AM with
  Day/Night duration weight 2 and all others weight 1. The installer preserves
  Day/ClearNight, captures the current Edit-mode look into FivePM, initializes
  independent paired presets, separates window/street-light controller ownership,
  and publishes explicit visual-state attributes. Awaiting Studio verification
  and mirror refresh.
- 2026-06-02: Added `scripts/roblox_lighting_phaseR_fogcolor_property_repair.lua` to fix the invalid `Fogcolor` preset key by changing it to Roblox's valid `FogColor` property and adding a compatibility alias in `LightingService_Active`.

## Vehicle Assets

- 2026-07-16: Generated `scripts/roblox_ui_garage_module_instance_cards_and_actions.lua`. Root cause of purchase auto-equip was the canonical client Buy callback issuing a second Equip request. The new guarded installer adds a shared module-instance view model/card renderer, separates Buy from Equip, renders each owned copy independently, supports future numeric ratings, configures lock artwork, and adds confirmed server-safe reassignment from another vehicle. Studio install and Play verification are pending.
- 2026-07-16: Generated `scripts/roblox_ui_garage_module_instance_customisation_authority.lua` as the prerequisite for automatic equip/backfill. The isolated server runtime makes physical module copies authoritative for colours, neon and upgrade compatibility, hydrates current slot views on selection/equip, and adds reference/owner invariants at the persistence boundary. UI layout and reassignment policy are intentionally unchanged; Studio and save/rejoin verification are pending.
- 2026-07-16: Corrected the authority installer after Play showed `V97_ModuleInstances` was nil. Its require declaration had been concatenated after a `--` comment and was therefore never executed. The same canonical installer now repairs the malformed installed line idempotently and verifies that exactly executable declaration before assignment.
- 2026-07-16: Customisation selection later exposed one saved module whose derived `EquippedVehicleId` claimed a vehicle although no vehicle slot referenced it. Updated the same authority installer—not a new repair phase—to reconcile owner flags from canonical vehicle-slot references before each garage request. This performs no instance creation/deletion/movement, reports the repair count, and keeps missing instances or duplicate references as hard failures.
- 2026-07-16: User confirmed physical module-instance customisation authority working well. Planned the next isolated phase as a read-only selected-instance preview adapter: card selection already supplies the physical instance ID, while the preview clone currently uses installed-slot colours/neon and the preview stats omit the selected instance's upgrades. The adapter will resolve saved appearance and upgrade allocation for local preview only, with before/after profile snapshot auditing and no garage mutation or persistence call.
- 2026-07-16: Generated `scripts/roblox_ui_garage_module_instance_readonly_preview.lua`. It installs a shared read-only client adapter, applies the selected physical copy's saved colours/neon/upgraded raw attributes to the existing preview clone, and calculates the existing stats panel from the selected copy's upgrade allocation versus the installed copy. It has no server request or profile-write path, fingerprints the client profile before/after every preview build, and transactionally patches only the isolated preview and garage application modules through three guarded exact anchors. Studio install and Play verification are pending.
- 2026-07-16: User confirmed the physical-instance preview, atomic purchase/equip/backfill, and shared card/modal stages working. Generated `scripts/roblox_ui_garage_module_presentation_refinement.lua` from the refreshed mirror. The transactional presentation-only installer reuses the shared shell/performance renderer, adds bright dynamic HSV gradients, fitted navigation and rail geometry, larger shared typography, configurable overview/neon icon attributes, `Thrust` copy, and explicit Standard/non-Standard upgrade states. It compiles all three isolated client modules before assignment and restores sources plus newly-created config attributes if assignment fails. Studio Play verification is pending.
- 2026-07-17: User confirmed the garage presentation refinement substantially improved the menus. Diagnosed missing browser previews and colour reversion as state-ownership defects: selected browser IDs did not select a preview data snapshot, and whole-vehicle cockpit paint never captured the physical module instances. Generated `scripts/roblox_ui_garage_vehicle_preview_and_paint_scope.lua`, which installs a shared read-only factory/owned preview projection, makes browser clicks mutation-free, separates whole-vehicle and cockpit-only paint, captures whole-vehicle module appearance atomically, and returns an authoritative profile after committed colour changes. Studio Play and save/rejoin verification are pending.
- 2026-07-17: Generated `scripts/roblox_ui_garage_action_icon_locked_card_refinement.lua` after the configurable garage icons were populated. The guarded presentation-only installer reuses the shared card pipeline, adds one configurable half-scale for Colour/Cosmetics/Performance/Neon artwork, and gives locked shop cards a dedicated upper layout with no vehicle/variant labels and a configurable enlarged raised lock. Unlocked, owned and equipped cards are unchanged. Studio Play verification is pending.
- 2026-07-17: After the icon refinement was confirmed visually, audited the planned module-rating work against the live V2 architecture. The canonical dealership still reconstructs only seven legacy fields while owned/spawned builds use the materialised V2 component runtime. Added the read-only `scripts/roblox_vehicle_module_rating_menu_alignment_audit.lua` and its handoff document. It compares E200 through S925 stock truth, menu divergence, raw driving inputs, fixed-reference module-rating ladders, upgrade-aware ratings and remaining legacy dependencies before a shared resolver replaces active compatibility logic.
- 2026-07-17: The performance-alignment audit returned `27/13/1`. All six canonical stock builds matched their calibrated tiers/PI, driving consumed the full V2 raw contract, and instance upgrades affected derived ratings. The sole blocker is 12 active legacy Level 1-3 bumper/side-pod/spoiler modules alongside the 72 V2 core modules. Corrected the audit's ambiguous `ENGINE_B` match, which had falsely grouped front engines with rear engines, and selected a single next installer: materialise those accessories, install one shared resolver, and migrate only active garage/preview consumers without expanding the register-limited bootstrap.
- 2026-07-17: Generated `scripts/roblox_vehicle_performance_canonical_resolver_and_module_ratings.lua` as that single guarded installer. It materialises the 12 accessory templates and 33 upgrade paths, adds flat V2 path deltas plus an idempotent accessory-instance migration, installs one pure shared resolver, removes active garage/preview use of `CalculateLegacy`, and feeds derived upgrade-aware ratings into the existing shared card view model. The confirmed spawn runtime adapter and register-limited dormant bootstrap are deliberately unchanged pending parity verification.

- 2026-07-13: Added Vehicle Performance V2 Phase 0: a read-only Studio audit plus isolated Google Sheet tier-template/V2 simulation scaffolding. No live rating, physics, upgrade, asset, hierarchy, or price values were changed.
- 2026-07-13: Vehicle Performance V2 Phase 0 live audit passed `21/2/0`. It confirmed the five-cockpit/72-module/23-upgrade foundation and reserved Zenith IDs. Warnings identified a corrected audit marker mismatch and a real live reverse-delay drift from documented `1.0` to `0.3`; drift drag remained correctly `0.28`.
- 2026-07-13: Generated Vehicle Performance V2 Phase 1 as a shadow-only config/definitions/calculator install with built-in diminishing-return, asymptotic-PI, cross-tier-delta, catalogue, and V1-source-preservation smoke tests. It also restores the confirmed one-second reverse delay through config only; runtime V2 rating/physics remain disabled.

- 2026-06-02: Experimented with cockpit car-light phases S through AH, covering native SpotLights, local projectors, root-only emitters, predictive hosts, and Beam visuals. These did not meet the desired quality bar at speed.
- 2026-06-03: Removed the cockpit car-light experiment scripts/docs from the active repo and added `scripts/roblox_vehicle_phaseAI_remove_cockpit_light_systems.lua` to remove installed helper scripts, light assets, root SpotLights, Beam/projector artifacts, and stale cockpit-light attributes from Studio. Front/rear long-range car lights are deferred.

## Driving

- V62 restored V47-style driving and replaced fallback driving paths with `DrivingControllerV47`.
- V67-V71 restored and corrected mobile driving HUD visibility.
- V72/V73 experimented with a fully scriptable chase camera but caused jitter and did not feel right.
- V74 restored the pre-V72/default Roblox camera feel with a light camera assist. User confirmed this worked well.
- V75 added boost recharge delay, editable boost module attributes, and low-speed hover wobble. Needs play-test confirmation unless confirmed later.

## Current Important Scripts

- `scripts/roblox_hover_racing_v74_pre_v72_camera_assist.lua`
- `scripts/roblox_hover_racing_v75_boost_delay_hover_wobble.lua`
- `scripts/roblox_architecture_phaseP_garage_runtime_startup_repair.lua`
- `scripts/roblox_architecture_phaseQ_garage_controller_header_repair.lua`
- `scripts/roblox_lighting_phaseR_fogcolor_property_repair.lua`
- Cockpit light removal/defer script: `scripts/roblox_vehicle_phaseAI_remove_cockpit_light_systems.lua`
- Dealership intro audit: `scripts/roblox_dealership_intro_phase0_audit.lua`
- Dealership intro marker setup: `scripts/roblox_dealership_intro_phase1_setup_markers.lua`
- Dealership intro client installer: `scripts/roblox_dealership_intro_phase2_install_client.lua`
- Dealership intro garage startup gate: `scripts/roblox_dealership_intro_phase3_gate_garage_startup.lua`
- Dealership intro preview-after-purchase patch: `scripts/roblox_dealership_intro_phase4_preview_after_purchase.lua`
- Dealership intro preview orbit camera fix: `scripts/roblox_dealership_intro_phase5_restore_preview_orbit_camera.lua`
- Dealership intro vehicle exit spawn marker: `scripts/roblox_dealership_intro_phase6_vehicle_exit_spawn_marker.lua`
- Dealership intro exit button/reopen gate: `scripts/roblox_dealership_intro_phase7_exit_button_reopen_gate.lua`
- No cockpit car-light renderer is current after Phase AI.
## 2026-07-17 - Canonical performance resolver and module-rating installer ready

- Generated `scripts/roblox_vehicle_performance_canonical_resolver_and_module_ratings.lua` from the confirmed fresh mirror.
- The guarded Edit-mode installer creates one shared `VehiclePerformanceResolver`, materialises the 12 remaining accessory modules into the V2 raw-stat contract, and adds the 33 existing accessory upgrade paths as flat V2 deltas.
- Active garage, module-card, and read-only preview calculations are routed through the shared resolver; the dormant bootstrap and spawn compatibility boundary are deliberately untouched.
- The installer compiles every generated source before mutation, performs an in-memory rollback on failure, and audits six stock targets, accessory coverage, active routing, and derived module ratings after assignment.
- Installed and user-confirmed working in Studio and Play. This is now the canonical garage calculation and module-rating baseline.

## 2026-07-17 - Canonical V2 runtime retirement installer ready

- Generated `scripts/roblox_vehicle_performance_v2_canonical_runtime_retirement.lua` after the resolver/rating phase passed testing.
- It replaces the two active compatibility facades and spawned performance writer with V2-only implementations while preserving their public function signatures.
- It deliberately retains the legacy calculator modules because the dormant bootstrap still requires them; deletion remains behind a later zero-reference gate.
- The guarded installer compiles all replacements, rolls back all three sources on audit failure, and verifies 6 cockpits, 84 modules, zero non-V2 active assets, six calibrated stock targets, and absence of active legacy calculation branches.

## 2026-07-17 - Upgrade point budgets and shared cards installer ready

- Generated `scripts/roblox_garage_upgrade_point_budget_shared_cards.lua` from the canonical resolver and shared listing-card baseline.
- Standard core modules gain three path choices with a two-point total budget; Lightweight and Power retain six points, and all allocations remain attached to physical module instances.
- Performance upgrades now reuse `ModuleListingCard`, including shared theme semantics and the existing card-centred popup.
- Added a themed point-budget strip with explicit popup clearance and runtime overlap auditing, plus next-point canonical stat preview.
- Tablet/phone scaling deliberately follows this phase so only the final shared desktop composition needs responsive tuning.
## 2026-07-17 - Garage upgrade-card hierarchy refinement ready

- Generated `scripts/roblox_garage_upgrade_card_hierarchy_refinement.lua` from the refreshed, confirmed upgrade-budget mirror.
- V1.1 keeps the correction in the same canonical installer after Play review: terminal statuses share grey, move slightly lower, and the used label receives an explicit in-panel right inset instead of an unsupported helper anchor.
- Corrected the V1.1 Command Bar parse stop (`Expected ')' ... got ']'`) before mutation by changing the bracket-ending migration anchor to an equals-delimited long string; no revert is required.

## 2026-07-17 - Garage path-local upgrade pricing fix ready

- Traced the cross-card price increase to the canonical V2 runtime using total module spend for every path's next cost; the UI was independently repeating the same calculation.
- Generated `scripts/roblox_garage_upgrade_path_local_pricing_fix.lua`. The shared V2 helper now prices from the selected path's own level, the server preview/purchase and catalog pass the path ID, and the UI displays the same resolver-owned quote.
- Shared two-/six-point budgets, saved allocations, performance effects and persistence are unchanged. Optional per-path price attributes are supported with module-level fallback.
- User confirmed the path-local pricing behavior and overall presentation working in Play. Preserve it as the pricing baseline before responsive mobile/tablet work.

## 2026-07-17 - Garage transient module-preview lifecycle fix ready

- Confirmed the driving vehicle was correct because server equipment remained authoritative; only client `State.PreviewModules` leaked across page boundaries.
- Generated `scripts/roblox_garage_transient_module_preview_lifecycle_fix.lua`, adding one local reset owner used by Paint-to-Build, Build Back, Build-to-Customise, Customise Back and Start Driving.
- Same-page Owned/Buy preview remains intact. No server, equipment, transaction, customisation or persistence state changes.
- Awaiting Edit install and Play boundary verification.
- Enlarges the shared point-budget panel, matches both budget labels to module-card title size, retains popup clearance, replaces source-vehicle repetition with the upgrade name, introduces grey/yellow/orange/red level tags, removes the redundant allocation badge and removes price duplication from the centred action popup.
- Presentation only: upgrade rules, prices, allocations, preview calculations, transactions and persistence are unchanged.
- Awaiting Studio Edit install and desktop Play verification before the shared tablet/mobile scaling pass.

## 2026-07-18 - Native dealership and customisation entry prompts ready

- Traced the mobile entry failure to `GarageEntranceController_Active`: it used a hidden custom `ProximityPrompt` plus a visual-only billboard, so keyboard E worked but touch had no clickable control.
- Generated `scripts/roblox_ui_garage_native_entrance_prompts_installer.lua`, replacing that isolated owner with the same Roblox-native prompt contract used by vehicles: E on keyboard, X on gamepad and hand/tap activation on touch.
- The existing garage session request and UI handoff remain authoritative. Prompts are suppressed during an active garage session, and the drive-in prompt is restricted to the player's own seated vehicle.
- Awaiting PC and touch Device Emulator verification.

## 2026-07-18 - Canonical garage flow, navigation, and colour V1 ready

- Generated `scripts/roblox_ui_garage_flow_navigation_colour_palette_installer.lua` against the refreshed responsive V1.2 and transient-preview baselines.
- Reuses one shared large icon-and-text action button, adds a no-rail Garage hub, and replaces Build's Owned/Buy rail with a two-card source picker and deterministic Back stack.
- Dealership purchase retains the whole-vehicle Paint step; owned-vehicle Customisation and drive-in entry route directly to the hub.
- Centralises Drive on the existing validation/preview-cleanup/spawn path and reserves a navigation lane above centred card popups.
- Adds a three-column H/S/B picker, configurable two-row palette, comma-formatted garage money, and the shorter `LIMIT REACHED` status.
- Client presentation only; no server, persistence, inventory, performance, driving, or racing sources are changed. Awaiting Edit install and Play verification.
- The first Edit attempt reached its post-install audit but queried `Disabled` on two ModuleScripts, which is not a valid property. Atomic rollback completed. V1.1 replaces that assertion with a structural module/parent audit in the same canonical installer; no revert or fresh mirror is required.

## 2026-07-23 - Vehicle cosmetics and empty-module routes V1 ready

- Generated `scripts/roblox_customisation_vehicle_cosmetics_and_empty_routes_v1.lua` against the confirmed three-workshop and physical module-instance authority baseline.
- Adds per-vehicle buyable Thrust Colour and true Attachment/SurfaceLight Underglow, direct owned sliders, atomic All Neon, and protected front/rear light classification.
- Adds short shared plus-card routing for missing Paint/Upgrade modules, exact Add Modules source navigation, post-transaction return, and a shared locked Owned Modules source when no compatible copy exists.
- Uses additive nested vehicle state, the existing full-vehicle persistence bridge, eight guarded source projections, pre-commit compilation, and transactional Edit rollback. Awaiting Studio install and Play verification.
- V1 was installed and mirrored. Runtime review found that the user's Bruiser 01 emitters were BaseParts parented beneath the legacy Attachment; that hierarchy does not physically attach Parts. The same canonical installer now advances to V1.2: attributed SurfaceLights are authoritative, a folder-based Part-emitter authoring contract is added without moving/replacing geometry, existing emitter Parts are made physics-safe, and legacy Attachment lights remain supported.
- V1.2 narrows runtime ownership to saved `Color` and purchase `Enabled` only. Global Brightness, Range, Angle and MaxEmitters config is retired; all other SurfaceLight properties and emitter count remain authored per cockpit, with a non-blocking mobile warning above four lights.

## 2026-07-23 - Driving hover-height config bridge V1 ready

- Confirmed the existing `HoverHeightStuds` NumberValue and `DriveTuning` projection were not used by the active driven, fallback or parked hover physics; all three still hard-coded `3`.
- Generated `scripts/roblox_driving_hover_height_config_bridge_v1.lua`, making the existing value authoritative for those three physics owners and the server-created vehicle diagnostic attribute.
- The installer is guarded, idempotent, pre-compiles all four projected sources and rolls back source/attribute changes on failure. It adds `0.5-8` tuning metadata and changes no other driving values.
- Later installed, user-confirmed and captured in the complete `22:22` mirror; the 2026-07-24 handoff entry closes this pending state.

## 2026-07-24 - Owned garage, customisation and hover handoff confirmed

- The user confirmed the latest owned-garage, three-workshop customisation, authored per-vehicle underglow and shared hover-height behaviour working.
- The complete `2026-07-23 22:22:32/33` Studio mirror contains the expected installed source revisions, configuration attributes, entry markers, underglow authoring folders and authoritative garage hierarchy.
- Closed the stale pending labels for Style UX, central icons, material icon sizing, lighting channels, mobile access/world entries, ClearNight V1.2, customisation flow, vehicle cosmetics V1.2 and hover-height V1.
- Added `docs/owned-garage-customisation-handoff-2026-07-24.md` as the next-chat baseline. Old installers remain recovery tools for their exact scopes; no Studio command is pending.
- Visitor admission, broad architecture reorganisation, arbitrary placement, catalogue virtualisation, richer offline invites and additional garage templates remain post-submission work. A clean-session cross-device/save-rejoin regression remains a release check.
