# Approved workflow improvement programme

Goal: reduce repeated reading, stale instructions and capture/delivery overhead while preserving review, recovery and verification. Gameplay and system owners are unchanged. Current progress/next action belongs in [start here](../00_START_HERE.md).

| Phase | Scope | Done when |
|---|---|---|
| 1 — Documentation | One current status, one delivery guide, compact durable AGENTS, open-only issue ledger, topic contracts, routing prompts and historical context | No conflicting baseline/run queue in the active entry path; links and tool references checked |
| 2 — Targeted capture | Live scoped inspection, versioned sources, source inventory, before/after records and relevant tags/config/hierarchy; adapt catalogue/preview and audit dependencies | Targeted captures prove required checks before routine full exports retire; full snapshots remain deliberate migration/recovery tools |
| 3 — Proportional delivery | One reviewable MCP delivery, lighter small changes, guarded connected changes and migration recovery | No manual copying/receiver/Git steps for ordinary supported tasks; no loss of preflight or recovery |
| 4 — Validation and handoffs | Reusable normal-start/transition/error/cleanup checks and compact evidence | API/UI/device evidence distinguished; relevant tests repeatable and unavailable environments explicit |

Phase 1 changes documents only. Full export policy and existing runtime/tooling remain unchanged until Phase 2. No automated sync, new game framework or production publish is implied.
The existing full mirror feeds scripts/performance_phase4/check_projection.py; scripts/audit_cleanup.py consumes the source manifest. Inspect and replace their dependencies deliberately in Phase 2.

Phase 1 verification: inspect changed Markdown/tool references and status consistency; confirm no script/source/mirror changes. Historical text is retained under docs/history/workflow-before-mcp-phase1. Rollback is the documentation commit only; no Studio recovery action.

Verification record (2026-09-22): 65 local links in changed active docs resolved; referenced tool files exist; stale prompt exporter names and repeated pending-playtest banners removed from the active entry path. Core AGENTS/lesson/workflow reading reduced from 657 to 137 lines. No runtime/scripts/mirror files changed.
