# Performance Phase 4 — authoritative templates and generated previews

Final evidence: **2026-09-22 12:47:08**, 164 sources, 44,464 nodes, 276,087 captured properties; exact expected source/full captured hierarchy/property parity and both generated outputs fresh. Zero unexplained changes. Repeat/rollback/reinstall/injected failure recovery pass. 749,912 pure calculation comparisons pass. Startup: 26 server/39 client ready, four opt-in skipped. Sandbox purchase, paint, spawn PI551/C, exit/re-entry/despawn, live preview (285 descendants) and Velocity upgrade ($6,050) pass. CAM-02 camera errors persist and truncated console output.

No script needs running: already installed through MCP. Canonical recovery script is scripts/roblox_performance_phase4_vehicle_previews.lua. Stop Play and use MODE ROLLBACK for deliberate recovery, then refresh the mirror; reverse this phase before Phase 3 and Phase 2. Exact preflight rejects later content drift.

User verification: inspect catalogue images, switch cockpits/modules, check preview paint/VFX/attachments and upgrade stats/costs, then drive, exit and re-enter. Save the edited Studio place after acceptance. No production publish performed.

Readiness: ownership, unchanged authority/saved IDs, exact projection, recovery and startup PASS. New transport/cancellation/multi-client isolation N/A (no selective delivery). User visual/input/device, representative join/memory/network, 15-player/soak DEFERRED under PERF-04. The original programme's meaningful join/memory acceptance remains open; only instance/metadata reduction is proved.

2026-09-22. User authorised continuation from Phase 3; specific visual/device acceptance is not inferred. Installed and agent-verified; user playthrough pending.

High-Risk contract: move the original ReplicatedStorage.Assets.Vehicles subtree intact to ServerStorage.Assets.Vehicles. Generate ReplicatedStorage.Assets.VehiclePreviews from that single authoring source, excluding only 84 upgrade-definition roots and their Folder descendants (333 folders / 1,479 attributes). Original models, meshes, parts, attachments, properties, paint channels, IDs, tuning and upgrade rules remain intact. No Workspace, WIP, staging/Archive, camera, persistence or economy mutation. No original asset deletion.

The selected first option is a static, generated preview representation. Its benefit is modest: 2,438 original catalogue instances versus 2,105 preview instances, 333 fewer replicated instances. All 729 BaseParts, 392 attachments, 39 lights and mesh/texture IDs remain needed client-side. No measured join/memory/FPS or graphics-download benefit is claimed. A selective delivery network is not justified solely by folder count; it remains a possible measured follow-up with mandatory two-client isolation proof. Do not call this phase a completed device-performance acceptance gate.

One authoring owner: ServerStorage.Assets.Vehicles. Preview copies are generated build outputs, never manually edited or treated as spawn templates/backups. PreviewVehicleClient, paint, VFX and shared UI remain their existing owners. VehicleCatalogData still owns immutable public calculation records; its relative template paths and revision are unchanged. GarageServer, OwnedGarageDisplay and OwnedGarageManagement use the originals; four client consumers use VehiclePreviews. PathResolver.VehicleCategories selects the correct side. No new runtime module, connection, loop, remote, cache, preload request, asynchronous selection or persistence schema is introduced.

Lifecycle/scale: static preview cost is bounded by the authored catalogue (2,105 instances currently), independent of player count; existing per-client preview clone creation/cleanup remains unchanged. Initial preview availability and rapid selection remain synchronous. Native LandscapeSensor and desktop/touch/controller inputs are unchanged; visual/device acceptance is pending. Templates are outside streamed Workspace; world streaming settings remain untouched. Security/new network transport and saved-data migration: N/A; existing authoritative server commands and no-save sandbox remain intact.

Canonical installer: scripts/roblox_performance_phase4_vehicle_previews.lua, INSTALL/AUDIT/ROLLBACK. It checks exact source baselines, captured authoring properties/attributes and duplicate sibling content; compiles before writes; clones only generated preview output and reparents the original root. Failure reverses changes. Rollback discards only the verified generated preview and returns original objects intact. No persistent game backup. Offline source generation uses reviewed literal navigation patterns, requires each match and exact live source equality; do not force it over drift.

Verification: fresh Phase 3 parity before install; exact mirror projection afterwards; unchanged canonical assets and retained preview geometry/properties; catalogue freshness; compile, repeat install, rollback/reinstall and injected failure recovery; normal startup, sandbox purchase/paint/spawn/upgrade/lifecycle; preview pad/ownership and user visual checks. iPhone 7, real client memory/network/join timing, 15-player and soak remain deferred release gates. CAM-02 is pre-existing and untouched. No production publish.

Future authoring: edit only the full server templates, regenerate both the public catalogue and preview projection through the content change's canonical installer, restart Play, then refresh and run scripts/performance_phase4/check_projection.py against the mirror. The frozen phase installer is for exact recovery, not a general refresh over later edits. Never repair generated preview copies by hand.
