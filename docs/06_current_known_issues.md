# Open issues and verification

Current task and installed baseline: [start here](00_START_HERE.md). Broad user playthrough confirmation does not close a specific unverified check.
Resolved milestones and prior measurements are retained in [the pre-consolidation ledger](history/workflow-before-mcp-phase1/docs/06_current_known_issues.md) and [older issues](history/known-issues-through-2026-08-02.md); historical items are not silently declared resolved.

| ID | Status / owner | Concern and next verification |
|---|---|---|
| LIGHT-01 | Visuals user-confirmed 2026-09-26; device/flow checks open | Warm sky lifted while retaining red-orange/pink/violet colours; twilight/night glare 1.5/0.45, sun size 9, five-second full warm holds. Cycle remains 720 seconds with strong horizon rays. Numerical full-cycle checks, selected runtime targets/images, normal startup, bounded daytime observation and guarded recovery pass. Entire 12-minute visual observation, physical iPhone 7/low graphics, normal garage/streaming/retention and two-client tests remain open. Direct Edit Lighting.SunRays changes are preserved, but Continuous uses its preset attributes during Play. See architecture/lighting-horizon-moon-validation.json. |
| PERF-06 | Acceptance open | General gameplay reported working. 2026-09-26: agent-driven normal UI (visible start-screen Play, dealership purchase with onboarding, DRIVE, keyboard driving, HUD exit/E re-entry, despawn/respawn, drive-in customisation, time-trial countdown release) passes with no console errors after the CAM-02 fix; this is Studio evidence, not the full 20-cycle/route matrix. Real iPhone 7, isolated multiplayer place and 30-minute/15-player soak unavailable. See architecture/performance-phase6-validation.md. |
| PERF-06-A | Resolved 2026-09-26 (Studio) | Cause: PresentationAudioClient weak-keyed binding table values captured the button, pinning buttons removed with Parent=nil (cleanup ran only on Destroying). Fix releases bindings when a button leaves PlayerGui. Comparable session: 56 retained TextButtons before, 0 after; click sounds still play. Long soak/device memory not measured. scripts/perf06a/verification.json. |
| CLEAN-02 | Physical asset decision | Staging has 78 catalogue IDs all present live, 76 limited part-shape matches; bruiser_01/02 differ. Six other models are nested cockpit visuals. Archive holds garage assets, decals, two ambiguously named test vehicles and sounds. All physical content preserved; explicit disposition required before deleting it or declaring the overall programme complete. |
| DATA-01 | High / production release gate | Failed-load writable defaults are blocked; sandbox/disabled-store sessions remain no-save. Isolated failure tests pass. Real save/rejoin and cross-server contention in an isolated published place remain required before release. |
| DATA-02 | High / rollout compatibility | Lease ownership, serialised saves, revision tracking and coordinated shutdown are installed. Old deployed servers ignore new lease metadata: drain old writers before deployment or rollback. No production publish performed. |
| NET-01 | Core.Net on every client remote | All 12 client->server handlers pass Core.Net (allowlist, payload sanity, rate limit, failure isolation, counters) since 2026-09-26; handler-specific validation unchanged. A full exploit audit of each handler's semantics remains open. |
| DRIVE-01 | User-confirmed 2026-09-26 | Steering V1.2 reverse/brake/three-point behaviour confirmed good by the user. |
| ECON-01 | Open (pre-existing) | If a garage request fails after MoneyService.Debit succeeded (e.g. a capture/validate failure after a debit), the in-memory cash stays spent. Refund designs were rejected in review (double refund with GarageModuleTransaction restore; free items where grants happen before the failure). Fix needs per-action atomic transactions (validate/capture before debit). |
| PB-01 | Open | PersonalBestServer writes time-trial personal bests to its DataStore even in the Studio no-save vehicle sandbox; 2026-09-26 synthetic RACE-01 test finishes may be stored as the dev account PB (global leaderboard disabled). Gate PB writes on the sandbox flag. |
| ARCH-P7 | Prepared, not installed | CameraService FOV/zoom arbitration with DrivingCameraClient and CharacterSprintClient migrated (scripts/architecture/p7, reviewed, 5/5 tests). Not installed because the Studio Play viewport was not rendering (0 FPS), so camera behaviour could not be observed. Install and verify with Studio visible. |
| TEST-01 | Iteration configuration | Studio replay and vehicle sandbox are both true. Saving is suppressed. Do not mistake this smoke test for save/rejoin proof. |
| PERF-01 | Device release gate | Controlled city-clone transition CPU median 27.37 → 14.11 ms; registration +1.64 ms, idle tick +0.037 ms. No overall FPS claim. Profile cold/warm laps, garage transitions and races on representative phones/tablets before further optimisation. |
| CAM-01 | Likely resolved by CAM-02 fix | Pre-Phase-5 VehicleCamera/CameraUtils error matched the NaN vehicle/camera path. 2026-09-26 drive-in customisation enter/exit showed no errors before or after the fix. Reopen only on recurrence. |
| CAM-02 | Resolved 2026-09-26 (Studio) | Root cause: DrivingClient kept writing forces for a frame after the server anchored the parked vehicle; anchored AssemblyMass is infinite so forces went NaN and, on unanchor (re-entry or race start), threw the vehicle to NaN, which DrivingCameraClient latched into zoom bounds. Fixed in both owners (zero forces while anchored; reject non-finite camera values). Before: 65+14 clamp errors per API cycle; after: 0 over 3 cycles plus normal UI and time-trial release. scripts/cam02/verification.json. |
| DEV-01 | Phase 4 / guarded | Trailer cameras, HUD hiding and lighting preview require Studio and explicit ClientTools attributes, all false by default. The missing TrailerShots wait is gone. Do not enable overlapping camera tools together. Opt-in trailer asset paths still require authored markers. |
| RACE-01 | Installed in Log mode 2026-09-26 | Server RaceIntegrity flags gate-to-gate segments faster than max(400 mph, 1.25x driving caps) plus jitter/gate tolerance. Studio: plausible synthetic runs unflagged; teleport runs flagged, and rejected in Enforce (no reward/PB/leaderboard). Switch to Enforce (dashboard config RaceIntegrityMode or ServerStorage.Config.Racing attribute) after real sessions show no [RACE-01] warnings for legitimate play. Real keyboard-driven full routes and multiplayer races not yet observed. |
| TOOL-01 | Capture coverage | Targeted records prove only selected hierarchy/properties/tags and inventoried script sources. Broaden scope or take a deliberate full checkpoint when necessary. Neither is a complete place backup; duplicate names require unique-root/multiset inspection. |
| TOOL-03 | Delivery limits | Generic delivery supports existing source bodies and primitive attributes only. Recovery is guarded and mock-tested; process loss, plugin callbacks and runtime side effects require explicit inspection/dedicated migration recovery. See architecture/proportional-mcp-delivery.md. |
| TOOL-04 | Evidence scope | Validation records enforce metadata/categories and pinned artifact integrity, not observation truth or complete test selection. Review actual evidence; resolved selected checks never close unrelated gameplay/device/save gates. |
| TOOL-02 | Import recovery | Two output-directory promotions are rollback-safe for ordinary I/O failure, not power-loss atomic. Recover retained .mirror-stage-* after interruption before retrying. |
| MAP-01 | Deferred prototype presentation | Prior playable-district boundary/prompt/route checks remain unverified here; physical asset work is outside the current architecture scope. |
| PERF-05 | Catalogue constraints | Cache is one public catalogue per server/Play session; authoring changes require restart. Cold concurrent reads may duplicate the initial transfer; detached caller copies allocate locally. Warm JSON reduction is not compressed-wire/FPS proof. |

No automatic historical repairs. Recovery requires the expected source/config state, reverse dependency order and post-recovery verification; see the [tool index](architecture/installer-index.md).
