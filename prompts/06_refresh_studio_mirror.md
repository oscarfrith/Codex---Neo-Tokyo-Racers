Refresh the Space Racers v1 mirror read-only from Studio. Read docs/10_script_source_sync_workflow.md, verify target place 121304917315753 and Edit mode, inspect Git, then run scripts/receive_studio_snapshot.py locally and scripts/studio_export_snapshot.lua in Studio. Discover a usable Python runtime if py is absent.

Run scripts/verify_studio_mirror.py and compare against the intended baseline. For the unchanged Phase 4 delivery use scripts/cleanup_phase4/verify_migration.py. Investigate drift rather than forcing an old layout. Report timestamp, source count, errors/warnings and known property/tag coverage. Do not write or commit the raw paste blob, modify Studio objects or publish.
