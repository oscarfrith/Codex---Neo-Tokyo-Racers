# Performance Phase 5 — reuse unchanged garage catalogue responses

Final evidence: **2026-09-22 13:01:09**, 165 sources, 44,465 nodes, 276,087 properties; exact source/full captured property projection with zero unexplained changes. Generated data/preview freshness also passes. Eight warm responses each **3,956 JSON bytes**, versus **177,251** before (97.8% reduction); first full response is 177,328 bytes with version fields. Typed catalogue canonical length 276,770 and checksum 771825752 match; original builder body is unchanged apart from its function name. Raw samples and medians are in scripts/performance_phase5; timing is scheduling-dependent, not isolated CPU proof.

Compile, repeat/rollback/reinstall, injected failure recovery and 21 isolated transport/guard checks pass. Startup: 26 server/39 client ready, four opt-in skipped. Live invalid/oversized revision rejection and unknown-revision full response pass. Sandbox purchase/paint/spawn PI551/C, exit/re-entry/despawn, Velocity upgrade ($6,050) and 285-descendant preview pass. Cash changes 1,000,000 -> 650,000 on purchase and another 6,050 on upgrade in fresh profiles; catalogue stays unchanged. Known CAM-02 camera errors recur and truncate console output.

**User check:** no script needs running; installed through MCP. Open/reopen the garage, browse cars/modules, buy/upgrade/paint and confirm prices/cash/stats refresh, then drive, exit/re-enter and check vehicle/race menus. Save the edited Studio place after acceptance.

**Recovery:** stop Play; run scripts/roblox_performance_phase5_catalogue_transport.lua with MODE ROLLBACK, then refresh the mirror. Reverse Phase 5 before Phase 4 and earlier recovery. No in-game backup or production publish.

Readiness: ownership, validated authority boundary, fresh-profile semantics, bounded cache/retry, install recovery and observed payload reduction PASS. Saved migration N/A (unchanged). Visible desktop/touch/controller playthrough, real iPhone 7/network/load/soak and production persistence remain DEFERRED under PERF-05 and existing release gates. No gameplay cadence or physical content change.

2026-09-22. Phase 4 accepted for continuation; no specific device/visual test inferred. Installed and agent-verified; user playthrough pending.

High-Risk contract: reduce measured repeated catalogue work and transfer while preserving every fresh profile read, command, price/calculation, UI owner/layout, preview/geometry, physics cadence and saved schema. Eight baseline GetInitial samples each contain 173,361 catalogue JSON bytes in a 177,251-byte total response. JSON size is a payload proxy, not measured compressed network traffic; round-trip latency includes Studio/network scheduling, not isolated server CPU.

Canonical owners: GarageServer builds its existing catalogue once per server/Play session and owns the frozen snapshot plus a server-lifetime GUID revision. Public catalogue construction remains byte-identical. GarageCatalogClient owns one immutable transport snapshot for the current remote; it never caches profiles, command results or balances. Existing GarageUI, desktop/mobile HUD and two race-entry callers keep their state/presentation ownership and current pcall error handling. Each Fetch sends its own GetInitial request and receives a fresh profile. Returned catalogues are detached copies, preserving consumers' former mutation isolation. Memory is bounded by one catalogue plus response copies; no per-player server cache, new remote, connection, polling, startup owner or persistent storage.

API: existing GetInitial accepts optional KnownCatalogRevision (string up to 64 bytes, rejected on other actions). Response adds CatalogProtocol=1 and CatalogRevision. Unknown/absent revision receives the full current catalogue; matching revision omits Catalog. This is one additive protocol, not a second legacy implementation. Client hydration restores the existing caller-facing Catalog field. A missing catalogue with no matching request snapshot permits one full read retry; failures never retry purchases or other commands. Request validation, rate limits, overlapping-command guard and authoritative mutation remain unchanged. Spoofing a revision can only suppress that caller's public catalogue, never alter its profile/price/ownership.

Revisions cover the complete existing response, including paint presets, cosmetics and category definitions. They are scoped to a running server, not saved IDs or the separate VehicleCatalogData build revision. Authoring changes require a Play/server restart, as the generated authoring workflow already requires; do not hot-edit server price/catalogue configuration. No runtime content writer is introduced. Cold concurrent requests may each carry a full catalogue before the first response arrives; requests are deliberately not coalesced because profile freshness/ordering must remain unchanged. Warm refreshes omit it.

Mobile/touch/controller, native orientation, streamed world and all visible interactions remain unchanged. No new async selection generation or preview lifecycle. Failure responses flow through existing UI handling. Security/compatibility tests cover invalid/oversized revisions, rate limits, fresh profiles, detached copies, per-remote cache isolation, bounded read recovery and remote exceptions. Device/load and visual acceptance remain pending. CAM-02 is pre-existing and untouched.

One installer: scripts/roblox_performance_phase5_catalogue_transport.lua, INSTALL/AUDIT/ROLLBACK. Seven exact source replacements and one ModuleScript addition, compile/preflight/reverse-order recovery; no in-game backup. Offline anchors are exact-count checked against frozen source, never guessed live. Verify normal startup, baseline catalogue equality, warm response sizes, sandbox purchase/paint/upgrade/spawn/lifecycle, rollback/reinstall/fault recovery and complete post-export property/source parity.

Other Phase 5 candidates (VFX/audio/physics/render cadence/pooling/broadcast scope) remain unchanged: no representative profiler evidence justifies a specific change. Target this proven cost without inventing unrelated optimisations. iPhone 7, compressed-wire, join/memory, 15-player/soak and published persistence gates remain open. No production publish.
