# Orange horizon duration and continuity

Approved scope: the user confirmed the V3 cycle looks better overall, requested a much stronger orange sunrise/sunset lasting before and after the horizons, and asked to investigate apparent lighting/fog jumps. Current state and next action belong only in [start here](../00_START_HERE.md).

## Contract and investigation

Retain the two-minute uniform sun motion, small native sun, rays, original Stepped recovery, six preset identities and existing renderer/server/context owners. Increase the orange palette and its duration; use broader transitions to reduce rapid changes. Preserve original shared presets/skies, physical assets, tags, saves and opt-in tools. This is a Standard refinement with the established canonical installer and targeted-capture safeguards. No new environment owner, module require through MCP, schema migration or publishing.

The fresh pre-change capture matched V3 with zero source or selected-node drift. All inventoried lighting writers were inspected; Continuous still routes through the sole LightingClient writer, and opt-in preview is disabled. A normal no-save client stood still for 122.009 seconds: 7,321 Heartbeat samples, one midnight wrap, both lamp switches, City context throughout, zero clock jumps and no lighting errors. Actual property values track the pure evaluator within ClockTime quantization/float tolerance (maximum Brightness difference 0.00139, Haze 0.00077, Glare 0.00179). No competing writer or discontinuous preset assignment was reproduced outdoors in that observation. The intentional window/lamp switch remains discrete at the horizons.

The V3 warm hold was only 0.4 game hours (two real seconds). Its nearby twilight blends lasted 1.1 game hours (5.5 seconds); the largest sampled single update changed glare by 0.0671 and colour components by 0.00942. The exact horizon views at 17.98 and 18.02 showed nearly identical fog/sky, with the intentional window change. This supports broadening short transitions and checking rendered atmospheric response; it does not prove the user's reported jumps cannot occur elsewhere. A location-specific report remains actionable separately from this standing-still test.

## Delivery evidence

The selected warm palette has Atmosphere Color RGB 255,165,75, Decay 145,110,115, Haze 2.2, Glare 1; Ambient 255,195,150, OutdoorAmbient 240,175,125, ColorShift_Top 255,155,80; ColorCorrection TintColor 255,245,230. Brightness/exposure stay at V3's 0.65/0. Other six-look properties remain unchanged, including original twilight/night/day appearance and V3 rays. A denser orange trial became too uniform; reduced haze retained a brighter sunward glow and gradient while keeping an orange afterglow.

Warm holds expand from 0.4 to 2 game hours, spanning 05:00–07:00 and 17:00–19:00. This is ten seconds per full orange hold at test speed, or 75 seconds at 900-second duration. Day hold is 09:30–14:30, twilight anchors 03:30/20:30, night hold 22:00–02:00. Moving adjacent anchors outward prevents longer orange holds squeezing the other fades. These are appearance timings only; the 24-hour solar sweep and 06:00/18:00 switches are unchanged. Normalized group fade curves remain mirrored and unchanged.

The sole source change relative to V3 is LightingClient's existing connection: PreRender replaces a capped Heartbeat timer that reset its accumulator to zero. The old timer discarded elapsed remainder and could update at uneven two/three-frame intervals. Applying the current target every rendered frame removes that cadence issue without a new loop, lag filter, context owner or state schema. This is a concrete stepping contributor; it does not establish that every reported visual jump had that cause. No new asset or coloured skybox is added.

The canonical installer retains the same cumulative 22 operations and exact rollback safeguards. The installed V3 bundle was used to roll back before rebuilding; V4 APPLY, full ROLLBACK, original-state AUDIT, reapply, CONTINUOUS and no-op repeat passed. Pure numerical checks pass (58,089 assertions, 1,201 samples), including extended orange holds before/after horizons, all look/fade seams, original artwork exclusions, mirrored fades and time scaling.

The final normal no-save Play cycle produced 7,321 samples over 122.006 seconds, zero clock jumps, one midnight wrap, City context throughout and no lighting errors. Both switches fired (06.0383 and 18.0036). Maximum observed per-Heartbeat brightness change fell from 0.05466 to 0.01588 (71%); haze from 0.02884 to 0.00908 (69%); glare from 0.06707 to 0.02816 (58%); maximum colour-component step from 0.00942 to 0.00328 (65%). These compare the combined cadence, timing and palette changes; they are not an FPS improvement or a measured pixel-discontinuity guarantee. [Comparison](../../scripts/continuous_lighting/evidence/v4/step-comparison.json).

Normal startup retained 26 ready server entries, 40 ready client entries and four skipped opt-in tools. Three controlled interior entry/releases restored City at the current phase, original indoor rays/latitude and seven Lighting children. This is not a normal garage-route acceptance test. The console contains the existing startup Player:Move warning and no new lighting errors. V3's eight original Stepped runtime comparisons remain historical; V4 preserves that server branch/source and fresh full rollback/reapply is verified.

Targeted after verification confirms only LightingClient source changed relative to V3, with edited existing Continuous milestone/palette attributes; no new root attributes, nodes or sky changes in this revision. Cumulative recovery still restores the same seven original sources, removes three modules, nine root attributes and three trees (53 nodes). All captured original object properties/tags and 158 original source bodies are unchanged.

See [validation](lighting-orange-continuity-validation.json) and `scripts/continuous_lighting/evidence/v4`. Paused screenshots represent selected desktop views only: [installed sunset](../../scripts/continuous_lighting/evidence/v4/v4_installed_sunset.jpg), [before sunrise](../../scripts/continuous_lighting/evidence/v4/v4_installed_055.jpg), [after sunrise](../../scripts/continuous_lighting/evidence/v4/v4_installed_065.jpg), [after-sunset palette trial matching final values](../../scripts/continuous_lighting/evidence/v4/v4_clear_185.jpg). The unchanged original Stepped presets remain the recovery reference; the before capture preserves the user-approved V3 source/config/palette. Remaining reported location-specific jumps are unconfirmed; investigate a precise location/time if they persist. Physical-device/low-graphics, normal garage/streaming and two-client checks remain open.

## Recovery and authoring

Set `Config.World.Lighting.CycleMode=Stepped` in Edit and restart Play for the original system, retaining custom Continuous tuning. Full removal uses the same `scripts/continuous_lighting/install.lua` ROLLBACK, which refuses artist/source/property drift. Before revisions, capture current authoring and use the installed bundle to roll back before rebuilding. No historical migration is pending. See [editable timing, palette and fade controls](lighting-authoring.md).
