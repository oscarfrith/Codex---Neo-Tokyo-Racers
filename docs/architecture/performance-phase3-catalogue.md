# Performance Phase 3 — catalogue and template separation

Final evidence: mirror **2026-09-22 12:31:08**, 164 sources, 42,359 nodes, 267,539 properties; exact expected source/full captured hierarchy/property parity, zero unexplained changes. Catalogue projection matches installed data. RS descendants 4,309 -> 4,313. Pure tests pass **749,912 comparisons**, six cockpits, 116 modules, 5,380 allocation cases plus owned-profile/selection/upgrade previews. Install/compile/repeat/rollback/reinstall/fault recovery pass. Startup: 26 server/39 client/four opt-in skipped. Sandbox purchase, paint, spawn PI551/C, exit, re-entry and despawn pass. Drive-in logs PAD PASS and OWNERSHIP PASS; visual preview acceptance remains pending. Existing CAM-02 errors truncated console output; no error-free claim. Windows receiver promotion failures restored prior outputs; bounded retries added with 11 passing pipeline tests.

**User check:** no installer run needed; already applied in Edit through MCP. Play and check all catalogue images, module comparisons, upgrades/costs, preview attachments/paint, then drive, exit and re-enter. Save the edited place normally after acceptance; no production publish performed.

**Recovery:** stop Play, set MODE to ROLLBACK in scripts/roblox_performance_phase3_catalogue.lua and run once; refresh the mirror. Roll this phase back before Phase 2 or older cleanup recovery. Installer rejects drift; do not force it over later content changes. Full-place backup remains external, with no in-game backups.

2026-09-22. Approved for implementation by the user's instruction to continue. This authorises advancing from Phase 2; it is not a new claim about specific device tests. Phase 3 installed and agent-verified; user playthrough pending.

High-Risk architecture contract: preserve all gameplay, profiles/IDs, prices, upgrade ordering/caps/costs, performance equations, UI geometry/state/visibility, preview attachments/paint/VFX, native mobile orientation and physical content. Server profile/economy owners remain authoritative. GarageUI and existing preview/renderers remain owners; the new immutable data/index modules own no gameplay state or startup. No new remotes, persistence format, asset relocation or world changes.

Generate a version-1 public catalogue from one canonical authoring tree: model attributes and upgrade folders in Assets.Vehicles.Categories. Four shared pure modules: VehicleCatalogData (generated immutable records), VehicleCatalog (stable-ID read API), VehicleDefinition (shared instance/record access) and VehicleTemplateIndex (bounded physical lookup through generated relative paths). Data projections include required raw stats, upgrade rules/costs, defaults, display/images, colours and IDs; author notes and migration flags stay out. Duplicate IDs/path IDs and missing default references fail generation. Current projection: six cockpits and 116 modules. Retired records remain available for explicit existing-instance semantics while normal listing resolution excludes them.

Six existing modules change: GarageUI, GarageModuleInstancePreviewAdapter, PreviewVehicleClient, VehiclePerformanceResolver, PerformanceRuntime and PerformanceUpgradeRuntime. The same calculator handles authoritative Instances and client records. UI images/upgrade capacity and resolver calculations no longer scan/depend on physical templates; preview cloning still intentionally needs models. Generated template paths replace category descendant scans without another connection/cache owner. Lookup returns nil on absent/replaced/mismatched templates; existing caller failure handling remains. Missing public definitions never silently use a second legacy implementation.

The public catalogue is immutable for a Play session and revisioned by projected content. Authoring is a build/install boundary, not live model-attribute hot reload. Run scripts/performance_phase3/check_catalogue.py against each fresh mirror. Future content edits must refresh the mirror, regenerate with scripts/performance_phase3/catalogue.py project(), replace generated VehicleCatalogData through that content change's canonical installer, validate drift/parity and restart Play. Never hand-edit the public copy. No new designer-visible tuning source is introduced. The phase's frozen builder is for exact recovery/reproduction, not blindly rebasing over later edits.

This preparatory phase adds four modules and about 197 KB generated source while physical templates remain replicated. It is not a client memory/download reduction yet. It removes repeated full catalogue scans and enables later template separation. No network payload shrinking is claimed; the existing GetInitial catalogue contract stays unchanged. No new generic framework or duplicated performance formula.

Canonical installer: scripts/roblox_performance_phase3_catalogue.lua, INSTALL/AUDIT/ROLLBACK. Exact baseline sources and authoring metadata preflight; compile before write; transaction/reverse-order recovery; no in-game backups. Live transformation is exact source assignment, not guessed anchor replacement. Offline transformation anchors are count-checked against frozen sources. Original models/configuration remain untouched. Scope uses existing World/WIP protections.

Verification: exhaustive isolated before/after pure calculations for all active factory builds, every module, all path allocations within per-path caps (including total-cap overflow), prices/catalogues/upgrade previews, retired/missing IDs and physical ID lookup. Load source in a whitelist-only isolated test environment, never require gameplay modules via MCP. Verify normal client/server startup, sandbox purchase/paint/upgrade/spawn/exit flows and preview creation; compare complete final source/hierarchy/property projection and independent data regeneration. User desktop/touch/controller playthrough remains separate. iPhone 7/network/15-player/soak and real persistence tests remain deferred release gates. CAM-02 remains a separate pre-existing camera issue.
