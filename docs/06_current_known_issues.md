# Current Known Issues

## New-system readiness automation deferred (2026-07-20)

- The proportional Fast/Standard/High-Risk rules, universal safeguards, readiness scorecard and reusable contract are now documented in `docs/14_new_system_readiness_standard.md` and `docs/15_new_system_contract_template.md`, with mandatory routing from `AGENTS.md` and the startup/delivery workflows.
- This phase intentionally adds no runtime framework and no Studio audit script. A reusable read-only structural readiness audit remains a future workflow improvement after the documented checks have been exercised on real new systems; automate recurring high-signal checks rather than speculative or noisy warnings.
- Until that audit exists, new chats must apply the documented contract and verification gates manually. This is a workflow limitation, not a gameplay or submission blocker.

## Owned garage Phase 12 confirmed; V1.2 HUD dropdown refinement next (2026-07-20)

- The user confirmed Phase 12 V1 working and refreshed the mirror at `2026-07-20 10:49:01`. Treat `NTR_OWNED_GARAGE_PHASE12_ACCESS_INVITATIONS_V1` as the rollback baseline.
- The same canonical installer is now V1.2. It keeps the persistent interior HUD as the presentation owner and refines its reusable anchored dropdown: both menus span the combined button width, have no shell/row borders, use gradient-only rows at the exact live button height, show distinct left icons and drive shared right-side chevrons.
- Access selection saves immediately and closes. Invite/revoke refreshes the same-server projection on open, updates in place, rejects duplicate/self invites through the existing server contract and displays a bounded scrolling list. Cash and Settings remain available because `NTR_OwnedGarageManagementOpen` is never changed.
- V1.2 changes only `GarageReplacementComponents`, `GarageInteriorModeController` and editable config attributes. Verify both dropdowns align to the complete two-button width; rows have no enclosing dark panel or border; chevrons rotate/reset on button, selection and outside dismissal; all four access glyphs appear; desktop/tablet/phone rows equal their anchor height; Settings/Cash remain interactive; and invite/revoke plus access persistence still survive close/rejoin. Uploaded icon IDs are optional and currently default to distinct glyph fallbacks.
- `GarageInteriorService_Active` still contains the legacy in-memory visitor/access path. Phase 12 does not patch, call or enable it, and keeps `EnableVisitors=false`; do not treat Public/Friends/Invite mode as visitor admission until a later phase explicitly transfers visitor lifecycle ownership.
- Offline persisted invite IDs remain visible for revoke, but new offline username lookup/search is intentionally deferred to avoid yielding user queries in the HUD render path.

## Owned garage Phase 9 confirmed; Phase 10 Decorations next (2026-07-19)

- The user confirmed Phase 9 V1.1 working and refreshed the mirror. Treat `NTR_OWNED_GARAGE_PHASE9_STRUCTURE_V1_1_ASSET_TEMPLATES` as the current rollback baseline.
- The refreshed template contains `DecorationAnchor1`, `DecorationAnchor2` and `DecorationAnchor3`, but the property definition exposes only the first two. Phase 10 corrects this mismatch before saving any placement state; do not retain the old `Rear1/Rear2/Rear3` attribute values as canonical IDs.
- Phase 10 is generated as `scripts/roblox_owned_garage_phase10_decorations_vertical_slice.lua`. It uses three fixed anchors, six categories and twelve editable placeholder assets. Arbitrary free placement, rotation, scaling, inventory quantities and visitor editing remain deferred; adding those now would enlarge persistence/exploit/mobile risk before the anchor workflow is proven.
- The first Phase 10 run reached its post-assignment audit and rolled back because that audit searched every projected source for the generic substring `NTR_OWNED_GARAGE_DECORATION`. `OwnedGaragePropertyCatalog` correctly used its target-specific marker `NTR_OWNED_GARAGE_PROPERTY_CATALOG_V5_DECORATION_ANCHORS`, so the generic check produced a false failure. The same installer now records and verifies each module's exact projected marker plus the new catalogue's exact marker. The rollback left Phase 9 current; do not create a separate recovery installer.
- Verify purchase deducts cash once, Place replaces only the chosen anchor, Remove clears only that anchor, preview cancels on Back/Exit, all three placements survive management close and save/rejoin, and Structure/display/entry/exit remain unchanged. Lighting remains disabled.

- Phase 7 is confirmed and mirrored at `15:32:55`. The remaining unchanged visuals and old issues are traced to the Phase 6 workspace controller bypassing canonical vehicle summaries, rebuilding the whole workspace on selection, and leaving presentation/audit loops active in production—not to failed Phase 7 persistence or revision logic.
- Run `scripts/roblox_owned_garage_phase8_canonical_vertical_slice.lua` once in Studio Edit mode. It uses exact uniqueness-checked source anchors against the refreshed Phase 7 mirror and compiles every projected source before mutation; any failure rolls the same transaction back.
- The first V1 run stopped during projected compilation before mutation because its new lifecycle `elseif` was inserted after the operation chain's closing `end`. V1.1 fixed that placement. Its next pre-mutation projection exposed two incorrect marker assumptions: the shared workspace is marked `NTR_GARAGE_WORKSPACE_CONTROLLER_V3`, while the mobile HUD uses `NTR_MOBILE_FREEROAM_UI_PHASE1O_MAJOR_MENU_SUPPRESSION` rather than the desktop performance marker. The same canonical file is repaired as V1.2 with target-specific markers; do not use a separate recovery script.
- V1.2 then reached the final projected source and stopped before mutation because a compressed mobile cash-position expression lacked one closing parenthesis. V1.3 replaces it with named, multiline layout values and invalidates layout when the inside-garage state changes as well as when viewport size changes. All earlier projected sources had already compiled during the V1.2 run; mobile is the final projection.
- V1.3 installed its existing-source changes, but the refreshed `16:06:21` mirror proves its new `OwnedGarageIcons` folder and `GarageInteriorHudController` did not persist and the config revision remained Phase 7. Play therefore yielded on the missing icon folder; it would later have yielded on the missing HUD module. The vehicle-card lifecycle also called `V90_vehicleSummaries` without the Player that `VehicleModuleUpgradeRuntime.CalculateProfile` requires.
- V1.4 installed and its hierarchy-safe source changes are present in the refreshed `16:19:27` mirror. The user reports the main flow working well. One assignment left the player walking in the interior with no management UI: the client treated any failed `GetManagementState` as an exit while its mutation refresh raced the server `ManagementUpdated` push. The four-card owned rail also used `LeftFitContent`, whose height calculation assumes 46-pixel text buttons, so only two larger cards were visible.
- V1.5 accepted V1.4, coalesced revision pushes while the local mutation/read owned refresh, retained the UI on temporary read failure, closed only after authoritative `InGarage=false`, and reused Build Modules' shared rail size/alignment. Its confirmed improvements are retained in V1.6; do not rerun an older revision or create a separate patch.
- V1.5 is now mirrored at `16:36:12`. It fixed the disappearing workspace and four-card clipping, but screenshots show its remaining cars are previews rather than saved assignments (`0/2` while a model is visible). Leaving/back therefore correctly rebuilds the empty saved state and removes the preview. The workspace also still routes cars through the module-category card renderer, the rail omits Build Modules' `.5` icon zoom, and both free-roam HUD owners ignore `NTR_OwnedGarageManagementOpen`.
- V1.6 was the attempted display-commit repair. It exposes `DISPLAY` only after preview success, validates the successful assignment response contains the intended saved slot before refreshing, uses captured slot/vehicle values, and renders both occupied spaces and choices through the exact dealership `Card` component/metrics. It also applies the shared `.5` rail artwork scale and disables the free-roam HUD only while management is open. The server `Profile.Assign` source already removes only the identical `VehicleId`; different saved vehicle IDs remain in different slots.
- The first V1.6 run stopped safely in read-only preflight at `Duplicate-only vehicle move contract is missing`. The assertion mistakenly searched `OwnedGarageDisplayAssignmentRuntime`, which is the transaction wrapper; the confirmed duplicate-move rule lives in `OwnedGarageProfileRuntime.Assign`. The same V1.6 installer now checks the correct owner. No source, hierarchy, config or saved state was changed by the failed run.
- V1.6 is present in the refreshed `17:03:54` mirror, but previewed cars still do not remain assigned. The mirror shows preview and commit are separate server actions, while the client still entrusts commit discovery to a row-local popup callback and performs another state read after mutation. The saved profile schema, revision transaction and duplicate-only move rule remain intact; do not introduce a second persistence owner.
- V1.7 is the current canonical repair. The owned controller publishes an explicit `SelectedAction` after preview success; the shared workspace mounts it through the same dealership popup while retaining legacy row actions for existing garage pages. A successful server mutation returns `ManagementState` in the same response, and the client validates/adopts that immutable state before showing `VEHICLE DISPLAYED`. The previous refresh remains only as a compatibility fallback.
- V1.7 is installed in the refreshed `17:18:01` mirror, and its UI action path now reaches the server. Persistence still fails because `GetProfile:Invoke` crosses a `BindableFunction` boundary: Roblox returns a copied table. Management was mutating that detached copy, while `MarkDirty` only marked the untouched live ProfileService session dirty. This explains all observed behaviour: the preview/response can show one assignment, menu close reloads the unchanged profile, and a later assignment starts from empty state again.
- V1.8 keeps the existing schema and assignment validator but moves every owned-garage write behind `ExecuteOwnedGarageCommand`, a ProfileService-owned binding that calls the isolated command runtime directly against the current live session table. Read models remain copied and immutable. Saved state commits before display-model rendering; presentation failure returns a warning without undoing saved state. Drive-in/out use a revision-checked compensation command if despawn/spawn/teleport fails.
- V1.8 installed successfully and is present in the refreshed `17:55:15` mirror. Display assignment/persistence is now substantially improved. Driving and selecting a garage still destroys the car while sometimes leaving the character outside: `DespawnForGarage` calls the generic helper that moves the player beside the vehicle, destroys it immediately and returns without proving seat detachment; management then calls `PivotTo` once and treats the call itself as success.
- Run `scripts/roblox_owned_garage_phase8_transition_completion.lua` once in Edit mode. It adds a garage-only preserve-position/wait-for-detachment option to the existing lifecycle owner, verifies the destination over configurable attempts before setting `NTR_OwnedGarageInside`, and respawns/compensates on failure. A driven vehicle already displayed in the selected property keeps its exact slot; otherwise the existing revisioned assignment/move/replacement contract applies.
- The same installer creates `Workspace.NeoTokyoRacersWorld.OwnedGarageExteriors.STARTER_TWO_BAY.FootExitSpawn` and `VehicleExitSpawn`. The vehicle marker is initially seeded 38 studs in front of the existing Kanda foot exit and may be moved in Studio. Future property definitions require their own `ExteriorSpawnId` and marker folder; saved profiles never store world CFrames.
- `ThrustPreviewController_Active:64` calls removed helper `applyThrustOnly` whenever a live vehicle appears/disappears. The installer removes only that call and keeps the cache used by the drive-camera fallback. Do not restore the helper: the preview bridge must not become a second live-vehicle VFX owner.
- Phase 8 must be tested with at least two distinct owned vehicles: correct names/images/ratings and descending rating order, used-in-other-garage cards moved right/grey/confirmed, preview without save, confirmed display count update, no duplicate assignments, tap-only desk/door/drive-out prompts, drive-out success/failure feedback, prompt suppression during management, fast repeated Browser/Management opens, and desktop plus phone HUD placement.
- Structure, Decorations, Lighting, Invitations and Visitors remain intentionally gated off. Access is visible in the persistent interior strip, but its richer visitor flow remains Phase 11 work.
- The Studio mirror is current through V1.8 at `17:55:15`; it does not yet contain the transition-completion sources or exterior markers. Refresh it after the new installer and Play checks pass.
- The first transition-completion run printed PASS, but the same Studio session-level Command Bar transaction fault retained source edits while dropping the revision attribute and `OwnedGarageExteriors` hierarchy. After restart, the canonical installer initially rejected the safe partial state because the stale thrust call was already removed. The same installer now accepts only the exact old or exact repaired thrust refresh body, restores its marker when necessary, and recreates/audits the missing property markers. No separate recovery installer is required.
- The corrected installer passed, Play is working well, and the refreshed `18:20` mirror contains 154 scripts, both `STARTER_TWO_BAY` exterior markers and all four transition source contracts. Phase 8 is closed. Phase 9 must replace the coarse legacy `SurfaceGroup` style path with section-addressed, catalogue-driven Structure state; it must not add a second persistence, geometry, preview or shared-card owner.
- Phase 9 V1 installed and the refreshed mirror proves its sources/template attributes persisted. Listing and colour/material preview work, but purchase returns `Structure is not enabled for this garage` because config still has `EnableStructure=false`; the client also failed to honour its own `Enabled` field. The same canonical installer is repaired as V1.1 to accept the installed V1 baseline, enable Structure only, guard disabled future categories and add the asset-template contract. Do not create or run a separate repair script.
- V1.1 creates `ServerStorage.NeoTokyoRacers.OwnedGarage.StructureAssets.StarterTwoBay.<Section>.Option01..Option04` and `StarterTwoBay.StructureSlots.<Section>`. Each option is a script/prompt-free Model with stable template/section/style/version attributes; options 2-4 are explicit placeholder clones that may be replaced or edited later. Runtime hides the original section shell and clones only the selected asset into `StructureRuntime`, positioned from its section slot. Desk, personnel exit, prompts, display pads/markers and vehicle owners remain outside the replaceable assets.
- The generated placeholder assets retain the current channel limitation: Front Wall has Primary/Secondary/Detail parts, while Left/Right/Back/Floor/Ceiling currently contain one Primary part. Secondary/Detail values remain saved and future-proofed; make them visible by adding appropriately attributed parts inside the relevant option model, not by changing persistence/runtime code.

## Owned garage Phase 7 reusable framework confirmed (2026-07-19)

- The ProfileService gate is resolved: the corrected Play audit passed `40/0/0`, including a stable five-second server-owned session sample.
- Phase 7 passed Edit and Play and is present in the refreshed `15:32:55` mirror. Focused evidence returned API/definition `2/2`, a second-read cache hit, and correct stale-write rejection (`false/true/revision 0`).
- Its definition, revision, idempotency, conflict and cache contracts are the required base for later garage phases. Phase 8 may change presentation/runtime controls but must not replace this state owner or saved schema.

## ProfileService ownership gate resolved (2026-07-19)

- Latest evidence: repair V1.1 and all three durable source contracts are present in the refreshed `14:45:19` mirror. Edit passed `22/0/0`; Play returned `39/0/1` only because BindableFunction returns copy tables, making caller-side root equality an invalid server-identity test. The audit-only correction now samples the server-owned marker object, generation and session ID for five seconds. No installer or mirror refresh is required before rerunning it.
- Phase 6 V1.1 is confirmed and committed at `ae490e7`. The refreshed source contains `NTR_PROFILE_SERVICE_SINGLE_FLIGHT_LOAD_V1` and `NTR_PROFILE_SERVICE_SESSION_OWNERSHIP_HARDENING_V1`, so duplicate concurrent loads and a late load replacing an already-active session are guarded.
- The gate failed conclusively: Edit was `10/0/4` and fresh Play server was `25/0/6`. The source allows a yielding load to outlive player departure, `PlayerRemoving` leaves in-flight/cleanup ownership behind, and no generation or stable session ID identifies the current Player lifecycle.
- Play also proved the authoritative profile and `OwnedGarage` table references changed during a two-second idle sample with `DataStoreEnabled=true`. The recurring garage `GetInitial` path is the owner conflict: it imports a legacy snapshot despite being read-only, while `ImportProfileSnapshot` replaces the profile root.
- The first canonical repair persisted its ProfileService and garage-action source changes. Follow-up Edit was `19/0/2` and Play was `35/0/3`: the two source-independent failures were convenience attributes that Studio dropped across the command boundary, while the real runtime failure isolated the remaining nested identity owner.
- The refreshed mirror shows `OwnedGarageProfileRuntime.Ensure` directly replacing an existing table during reset/schema normalisation and `Restore` replacing it during transaction rollback. Corrected installer V1.1 keeps the same canonical file, adds in-place nested reconciliation to those two paths, and uses source contracts as the durable installation evidence.
- Final corrected Play evidence is `40 PASS / 0 WARN / 0 FAIL`; no ProfileService rerun is required. Reopen this gate only if later evidence shows duplicate sessions, changing generation/session ID, or read actions importing snapshots.

## Owned garage Phase 6 confirmed baseline (2026-07-19)

- Phase 6 V1.1 passed its Studio and Play checks, was mirrored at `13:35:37`, and is committed as rollback baseline `ae490e7`. Do not rerun Phases 0-6.
- Canonical property/display state remains namespaced under `profile.OwnedGarage`; the existing `profile.Garage` vehicle-capacity contract and vehicle/cockpit/module records remain authoritative and preserved.
- The existing vehicle action owner remains the only live vehicle spawn/despawn bridge. The new owned-garage services own only the interior, display assignments, UI state and transitions defined in the replacement plan.
- Phase 7 is no longer blocked by ProfileService. Phase 6 remains the rollback baseline until Phase 7 is installed, tested and mirrored.

## Canonical garage confirmed baseline (2026-07-18 handoff)

- No known blocking defect remains in the current dealership/customisation garage flow. The user confirmed the final UI, responsive touch presentation, module flow, camera views and preview VFX behaviour working, and the `2026-07-18 23:14:48` mirror includes the final live markers.
- Preserve one shared Browser/Workspace UI implementation and one scaled touch composition. Do not create a separate mobile garage tree or copy approved renderers into page-specific implementations.
- Preserve `CachedThrustVisualRuntime` as the single preview template-VFX attachment owner. `ThrustPreviewController_Active` is a colour/input bridge only. `PreviewVFXMode` owns Idle versus ThrustColour presentation; do not use `ForceThrustPreview` to request hover because its old all-effects meaning is retired for preview models.
- The earlier name-based VFX suppression and dual-controller refinement attempts are superseded by `scripts/roblox_ui_garage_preview_vfx_single_owner_installer.lua`. Keep the older V1/V1.1 scripts as history only; do not layer another effect-toggle owner over the confirmed runtime.
- Optional release regression—not a current blocker—is one repeated PC/touch entry/exit smoke plus a 60-120 second instance/memory observation and save/rejoin verification before a public release build.
- Full current ownership, config, installer history and next-chat rules are in `docs/garage-canonical-handoff-2026-07-18.md`.

## Compact Customise category rail V2.2 awaiting Studio verification (2026-07-18)

- V2.1 unintentionally applied the new shared action-card geometry to the scrollable All/Cockpit/Thrust/module category rail, producing oversized buttons and undersized artwork.
- `scripts/roblox_ui_garage_customise_compact_rail_v2_2_installer.lua` restores only that rail's previous compact dimensions and icon scale. Build/Customise navigation keeps the new shared sizing; the sidebar remains floating.
- Require `7 PASS / 0 FAIL`. No server, persistence, inventory, preview, colour, mobile-layout or bootstrap behavior changes.

## Canonical garage flow refinement V2.1 awaiting Studio verification (2026-07-18)

- V2 is visually confirmed substantially improved. The remaining Neon Lights preview leak is a missed navigation call, not a second preview-state owner: `clearTransientModulePreview` already clears `PreviewNeonSlot`, while Customise category-card selection did not call it.
- `scripts/roblox_ui_garage_flow_refinement_v2_1_installer.lua` connects that boundary to the existing cleanup, restores All > Change Colour > Neon, removes the Customise rail backing surface and shares the bottom action-card geometry with both floating rails.
- Require `8 PASS / 0 FAIL` and test an unpurchased neon preview across category, Build/Customise, Back and Drive boundaries. Do not add another neon state or cleanup controller.

## Canonical garage flow refinement V2 awaiting Studio verification (2026-07-18)

- The refreshed mirror exposed the intermittent wrong first page as a routing race, not legacy UI revival: vehicle selection read mutable `State.ShopMode` after its server request yielded, and entrance events used an unordered binding loop.
- `scripts/roblox_ui_garage_flow_refinement_v2_installer.lua` captures an immutable selection mode and explicitly binds Dealership, owned Customisation and Drive-In. It also installs the approved shared header, paint palette, floating Build/Customise rail, All/Underglow actions and selected-card wheel forwarding in the same atomic phase.
- Run once in Edit mode, restart Play and require `12 PASS / 0 FAIL`. Repeatedly verify Dealership -> Paint and owned Customisation -> Garage before treating V2 as confirmed. Do not add a bootstrap route patch or a second mobile UI tree.
- Full scope and acceptance: `docs/garage-flow-refinement-v2-2026-07-18.md`.

## Canonical garage touch scaling gates confirmed (2026-07-18)

- Responsive Phase 0 passed the canonical owner and shared-shell checks and proved the existing composition remains separated at every simulated desktop, tablet and phone size.
- The current touch branch is not release-ready: `MobileMinScale=0.42` forces clipping at `740x360` and `667x375`; 30px actions render below 44px on every touch profile; module copy drops below 10px at `1024x768` and smaller; and the canonical owner has no explicit safe-area contract.
- The desktop runtime header's `-29.4` absolute Y is the exact 58px `IgnoreGuiInset` coordinate-basis difference, not a reason to move the confirmed desktop layout. Apply safe-area fitting only to touch.
- Responsive V1.1 installed and improved edge spacing, but its remaining physical minimums still made economy/actions/popups taller than the uniformly scaled PC controls, its `TextScaled` protection enlarged/wrapped budget copy, and its fixed `92px` rail contract sat below the confirmed free-roam position. The same canonical `scripts/roblox_ui_garage_responsive_scaled_touch_installer.lua` is now V1.2: it removes every visual-size override except arrows, uses the free-roam `68/82px` rail top, creates a three-card-width budget with fixed module-name typography, and disables point-limit truncation. Install V1.2 and verify phone/tablet pages; do not create a separate mobile garage tree.
- Full evidence: `docs/garage-responsive-phase0-audit-2026-07-17.md`.

## Shared module-card forwarding and modal geometry awaiting Studio verification (2026-07-16)

- The atomic physical-module transaction phase is user-confirmed working and is now the preserved baseline.
- The refreshed mirror proves the server catalogue already publishes `SourceCockpitDisplayName` and the real copy price, while `GarageReplacementComponents.ModuleListingCard` already implements pink/grey/cyan semantic states and green prices. The missing presentation was caused by `GarageWorkspaceController` not forwarding those row properties.
- `scripts/roblox_ui_garage_module_shared_cards_modal.lua` forwards the shared contract, sorts Owned cards by state before rating, uses the authoritative source cockpit display name, and changes the existing common modal backdrop from fixed `1600x900` to the complete canonical canvas.
- Verify source names, green prices, `Equipped -> Available -> In Use` order, selected cyan state, Equipped pink fill, In Use grey outline, full-screen background blocking and `[NTR Garage Module Cards] MODAL GEOMETRY PASS` at desktop and mobile sizes.

## Atomic module buy/equip transaction awaiting Studio verification (2026-07-16)

- The refreshed mirror contains the confirmed reference-reconciliation and read-only physical-copy preview baselines. `scripts/roblox_ui_garage_module_atomic_transactions.lua` is generated against those exact markers.
- The first run stopped safely at client compile preflight because a revision line comment consumed the closing `})` of the one-line Buy card. No Source assignment had started. The canonical installer now terminates that comment with an explicit newline; rerun the same file without reverting Studio.
- The installer adds one isolated transaction runtime, auto-equips a newly purchased copy, backfills a moved copy's old slot with the lowest-rated compatible available instance, and restores the complete profile snapshot if compatibility, required-slot or post-transaction validation fails.
- Studio still needs to confirm: one purchase creates one instance and deducts cash once; the purchased copy equips immediately; moving an in-use copy either swaps/backfills correctly or leaves all state unchanged; saved colours/neon/upgrades remain attached to their physical instance; save/rejoin preserves the final references.
- Dedicated module ratings remain deferred. Backfill uses explicit instance/template rating fields when present, then a deterministic source-vehicle and Standard/Lightweight/Power order.

## ProfileService duplicate startup load can replace an active session

- The refreshed 2026-07-16 mirror proves `loadProfile` can be launched for the same joining player by both `PlayerAdded` and the existing-player startup loop. Because the DataStore read yields before `sessions[userId]` is assigned, a later completion can directly replace the active profile table and bypass `ImportProfileSnapshot`.
- This was the cause of the garage inventory cleanup repeatedly returning from 70 to exactly 1428 records during `SaveNow`; every failed attempt safely restored and saved the original inventory.
- `scripts/roblox_profile_service_single_flight_load_guard_install.lua` is the generated permanent fix. It must pass in Edit mode and then be verified in a fresh Play session before this issue is marked resolved.
- The entry-only guard was insufficient in the sixth cleanup attempt: the exact live table still changed across `SaveNow`. `scripts/roblox_profile_service_session_ownership_hardening_install.lua` now also rejects late load commits and pins the cleanup session across the transaction. Studio verification remains outstanding.
- The corrupt-profile migration path is retired. For the current pre-release tester profile, the approved recovery is `scripts/roblox_player_main_profile_reset_edit_mode.lua`, run offline in Edit mode with guard-marker and DataStore read-back checks. ProfileService session ownership remains a separate architecture issue and must not be coupled to another data migration attempt.
- The targeted reset and fresh-profile persistence audit passed on 2026-07-16 with one vehicle, one cockpit, four canonical included modules and zero legacy records. Module inventory corruption is no longer present in the tester profile. The next open gate is controlled purchase/equip lifecycle verification; do not reopen the retired 1,428-record migration path.
- Controlled module lifecycle verification subsequently passed with five total instances after one purchase, four installed references, one purchased/equipped instance, one displaced included instance available and zero legacy records. The user subsequently chose immediate auto-equip as the intended Buy flow; the generated atomic transaction installer formalises that behavior and removes the earlier separate-equip requirement for new purchases.

## Garage module instance inflation and Standard upgrade state (2026-07-16)

- The read-only Play-server audit found 1,428 persisted module instances for 6 vehicles. Of these, 1,402 were sourced as `LegacyInstalledModules`, only 6 were purchases, and 1,378 claimed an equipped vehicle while having no vehicle-slot reference.
- The live garage request path still performs default attachment and legacy instance reconciliation before every action. Its mismatch branch replaces the slot with a new instance without detaching the previous record, producing the repeated orphan pattern. Do not hide or group these records in the UI and do not run a blanket inventory deletion.
- The next repair must first remove per-request instance creation, then dry-run a source-aware cleanup that preserves every referenced instance and every `BuyModuleInstance` record.
- The missing performance cards on the current four core modules are explained: Standard V2 modules intentionally have `Upgradable=false`, zero capacity and no path folders. Lightweight and Power variants are the upgradeable V2 variants. Show an explicit non-upgradable Standard state unless a separate balancing change deliberately adds Standard paths.
- Full evidence and cleanup guardrails are in `docs/garage-module-inventory-upgrade-audit-2026-07-16.md`.

## Canonical garage V3 camera initialization repair awaiting rerun (2026-07-15)

- The first V3 Play test confirmed the authoritative `GaragePreviewPad` placement and orbit camera were working, but camera reset/fade raised `attempt to index number with 'Parent'` and input cleanup raised `attempt to perform arithmetic (add) on nil and number`.
- Root cause was one multi-local declaration assigning `0` to `fadeFrame` while leaving `transitionSerial` nil. The existing canonical installer now initializes `fadeFrame`/`fadeTween` to nil and `transitionSerial` to zero explicitly.
- Rerun the same canonical garage installer in Edit mode against the refreshed V3 mirror. No revert or separate repair script is required.

## Garage Phase 1 V3 awaiting fresh-Play verification (2026-07-15)

The first V3 command stopped safely during preflight with `Missing source anchor: bootstrap open gate`. V2 had already replaced the intermediate attribute-based gate with the final unconditional existing-instance gate, but V3 checked for the superseded marker first. No mutation block ran. The same installer now treats the final open and drive-in markers independently: an installed V2 gate is skipped, an older conditional gate is upgraded, and a genuinely pristine gate is staged then upgraded.

The first Phase 1 installer printed PASS, but the subsequent mirror contained only its edits to existing Source. Studio had removed `GarageModuleArtworkRegistry`, `GarageActionAdapter`, `GarageApplicationController_Active`, the activation attribute and all eleven artwork folders. This made the bootstrap conditional gate inert and caused the exact legacy `setupUI` error plus Workspace registry yield seen in Play.

The V2 existing-instance approach worked substantially better in Play. The same canonical installer is now V3 and leaves its successful ownership gate/startup bridge unchanged. It replaces only the existing preview vehicle/camera modules, shared components, Workspace renderer and existing `ModuleShopUIController` host. Verify GaragePreviewPad placement, desktop/touch orbit, configurable fades, Customise artwork rail, image-free grouped Owned/Shop cards and the complete Start Driving flow. Do not rerun older remaining-menu or visual-repair installers.

Acceptance requires dependency and ownership PASS lines through Dealership, owned Customisation and Drive-In entry, with no legacy GUI, missing callback, artwork-registry yield, right-edge geometry failure, or Start Driving regression.

- Garage Phase 0 ownership/dependency audit completed at `pass=6 warn=1 blocker=6` static and `pass=0 warn=0 blocker=1` runtime. Browser initially printed presentation/geometry PASS, then Workspace yielded on missing `GarageModuleArtworkRegistry`; the refreshed hierarchy also shows an empty `ModuleArtwork` root with none of its eleven required folders. With Browser hidden, `HOVER_RACING_V2_GarageUI` became the only visible owner. Bootstrap startup separately fails at line 3675 because `showCashShop` is passed to `Connect` but has no definition. Do not rerun or repair V3/V3.1 or patch the legacy callback. Use the extraction map in `docs/garage-phase0-ownership-dependency-audit-2026-07-15.md`; the next implementation must stage and validate every dependency before switching ownership, and legacy deletion remains prohibited until the zero-reference gate passes.
- The first V3 install completed successfully at `186830` bootstrap characters, then Studio printed locked-Parent warnings for every obsolete module-artwork `StringValue`. The canonical sources were already installed; a Studio mirror/import listener was trying to re-parent references after the installer destroyed those old values. V3.1 changes only that migration mechanism: it detaches the obsolete artwork root intact and creates the clean attribute-only root atomically, avoiding per-instance destruction and warnings. No revert is required. An already-clean V3 installation can be tested directly or rerun with V3.1 idempotently.
- Garage Workspace V3 is generated as the first safe legacy-retirement boundary. It creates `CanonicalGarageGui` independently from `HOVER_RACING_V2_GarageUI`, gives Browser and Workspace one shared canonical `UIScale`, removes every `.new(UI.Gui, UI.Scale)` bridge, and audits the runtime parent/scale contract. `ModuleArtwork` is migrated to exactly eleven category folders with attributes only and zero child instances; `GarageModuleArtworkRegistry` supplies the same ordered definitions to Build Modules and Customise Modules. Run the same installer once, restart Play, and require V3 install/geometry/presentation PASS output before the later zero-reference removal of legacy constructors.
- The first V2 paste stopped at Command Bar line 249 with `Expected <eof>, got ']'` before any mutation. Two new component/browser insertion blocks had been placed before their surrounding long-string calls were closed, and two V1 upgrade anchors ended in `]` while using plain `[[...]]` delimiters. The canonical installer now closes each insertion independently and uses equals-delimited strings for bracket-ending anchors. No Studio revert is required; rerun the corrected same file.
- Garage Workspace V2 awaits Studio rerun and Play verification. It upgrades an already-installed V1 bridge in place, shares the confirmed vehicle-browser shell/stats implementation, removes the module-slot left rail, locks post-selection navigation, and reconciles core readiness with current vehicle module instances plus the legacy template mirror. Require `[NTR Garage Workspace V2] INSTALL PASS`, then verify that equipped Engine/Stabilisers/Boost can proceed to Customise and that every visited page prints a geometry PASS without right-edge clipping.
- The legacy/V3 dealership visual repair stack is superseded. The user approved the V1.4 browser result and refreshed the mirror. Do not rerun `roblox_ui_garage_canonical_experience.lua` for another geometry repair.
- The first replacement Play run exposed two startup faults: `UDim2.zero` is not a valid Roblox value, and connecting Exit through `UI.CanonicalGarageExit` produced a non-function connection during `setupUI`. V1.1 of the same installer uses `UDim2.fromOffset(0, 0)`, restores the original proven inline legacy Exit connection, gives the replacement browser its own inline close/session-release callback, and safely migrates the partial V1 install on rerun. It remains unconfirmed until Play succeeds. Then require `[NTR Garage Replacement Runtime] GEOMETRY PASS <viewport>:<mode>` without a matching failure and test the complete browser matrix.
- Screenshot review approved the replacement direction and produced V1.2 in the same installer: borderless gradient chrome panels, inset compact category buttons with action clearance, rounded cropped full-card vehicle images and larger centred name overlays, wrapper-free actions, explicitly ordered authoritative headline stats, compact top-right economy cards, and measured carousel overflow/centring with larger borderless arrows. Owned summaries previously exposed only `Overall`; the guarded server response now also returns its already-calculated `Headline`. Awaiting Studio rerun and visual/runtime confirmation.
- V1.2 Play review showed short rows still centred against an early pre-layout scroller width, Crop cut the 512x340 artwork vertically, the economy cards needed their established blue outline, and the header's first replacement surface looked too similar to legacy. V1.3 listens for final `AbsoluteSize`, converts rendered width back through the active `UIScale`, queues centring after layout, audits against the screen centre, uses full-height Fit with a bottom card-owned name plate, adds blue no-glow economy strokes, and copies the race `metricCard` transparency/corner/gradient contract directly. Awaiting Studio rerun and visual confirmation.
- V1.4 is the final narrow card/header polish currently awaiting rerun: vehicle images use `Fit` at `1.06` scale inside both holder and image corners, filling width with a small accepted top/bottom crop; module images remain `1.0`. Header title/subtitle increase to 17/12-equivalent sizing and both use the white `Text` token.
- `scripts/roblox_ui_garage_workspace_remaining_menus.lua` is awaiting Studio installation/verification for Paint, Build Module Slots, Owned/Buy Modules and module customisation. It uses the confirmed shell, shared image-first module cards, configurable module/slot artwork values and the shared card-relative popup. Verify Paint -> Slots -> Owned/Buy -> Customise -> Start Driving, module preview deltas, purchases/equips/upgrades, colour persistence, required-core gating and both PC/touch scaling. Require `[NTR Garage Workspace Runtime] GEOMETRY PASS` for each visited page without a matching failure.
- The first remaining-menu install attempt was correctly rejected before bootstrap assignment because the generated Source was `200083` characters and Studio requires less than `200000`. The same installer now removes only the superseded legacy slot/options/customise renderer bodies, retains their proven helpers/actions, preflights below a stricter `195000` ceiling, and is safe to rerun without reverting the controller/config objects created by the partial attempt. The `README_HOW_TO_IMPORT` locked-parent line is an exporter/plugin cleanup side effect following the failed Source write, not the garage failure cause.
- A later Play screenshot showed legacy cash/stats/navigation surfaces after a garage-property purchase. This was not another geometry defect: `GaragePropertyMenuController` calls the legacy capacity renderer after purchase, and the original canonical controllers only hid legacy roots once during `Show()`. The updated same installer adds a shared persistent presentation owner to both browser and workspace, suppresses retired roots whenever a canonical owner is visible, and reroutes property-profile refresh to the current canonical page. Verify property purchase in both browser and workspace and require `[NTR Garage Presentation Owner] PASS` without any corresponding `FAIL`.

## Mobile/racing Studio mirror refresh required (2026-07-14)

- The current mobile free-roam UI, mobile racing UI, unified race flow/countdown and Phase 1O major-menu suppression were user-confirmed working.
- The local snapshot still reports `2026-07-14 13:34:35` and predates the final race-flow/countdown and Phase 1O installs. Refresh the full Studio mirror before another source patch.
- Confirm the refreshed export contains `NTR_RACING_FLOW_COUNTDOWN_VISUAL_V2`, `NTR_RACING_FLOW_COUNTDOWN_GUIDE_GATE_V2`, and `NTR_MOBILE_FREEROAM_UI_PHASE1O_MAJOR_MENU_SUPPRESSION`.
- Do not commit `docs/studio-full-export-paste.txt`. Preserve unrelated driving/camera/lighting worktree changes.
- Use `docs/mobile-ui-racing-flow-handoff-2026-07-14.md` as the current UI handoff.

## Default vehicle camera V6.1 confirmed; tuning note (2026-07-17)

- V6.1 is installed, user-confirmed looking good, and mirrored at `2026-07-17 10:13:20`. Roblox owns continuous camera motion, collision and orbit; the bounded pre-camera framing setup works. Preserve this baseline and do not revive Scriptable V4/V5.
- Current mirrored framing is `DefaultPitchDegrees=0`, `DefaultHeightStuds=100`, `LookTargetHeightStuds=20`, `LookAheadStuds=4`, and `InitialLookApplyFrames=3`. Runtime clamps `DefaultHeightStuds` to `50`, so the saved `100` currently behaves as `50`; preserve it unless the user intentionally retunes.
- Current distance targets are `22/24/23/26` studs for default/acceleration/high-speed/boost. The non-monotonic acceleration/high-speed pair is user-tuned and confirmed acceptable; do not silently reorder it.

## Stat-scaled post-drift boost awaiting verification (2026-07-14)

- The refreshed source still uses a universal `0.57–1.78 s` / roughly `67–136` reward and pauses it behind normal boost. The generated installer changes this but is not a baseline until its PASS output and cross-tier Play checks succeed.
- Verify low/reference/high BoostForce and mapped BoostDuration differences, Burst/Endurance upgrade effects, the `0.90 s` default cap, and no queued reward after normal boost.
- Rollback values are `DriftMiniBoostStatScalingEnabled=0` and `DriftMiniBoostExpiresDuringNormalBoost=0`.
- Refresh the full Studio mirror after confirmation.

This file is intentionally conservative. Items are included only when they were mentioned in the chat or are reasonable verification steps after recent changes.

## Needs Play-Test Confirmation

- Unified Race Flow's current PC/mobile presentation and Phase 1O menu suppression were user-confirmed working. Before release, still run a two-player Race queue/countdown/finish/exit regression, attempt a vehicle change while queued, and run both current installers in `SMOKE` mode after the mirror refresh. This is release verification, not an invitation to reopen the confirmed visual layout.

- Driving Feel Phase 3 organised tuning is installed and reflected in the refreshed `2026-07-14 10:29:30` mirror with seven flat category folders. The canonical organiser is now extended in the same stage with six physical TopSpeed mapping attributes and three hard-preflighted related source anchors that replace raw-stat-as-MPH and hidden `260 MPH` limits. It preserves every existing category number; the mirror records `BaseForwardAcceleration=75`, `LaunchAccelerationMultiplier=0.8`, and `AerodynamicDragPerMphSquared=0.015`. The last value is 125 times the original Phase 3 default and may create an aerodynamic equilibrium below the mapped target; do not silently reset it. Awaiting organiser rerun, Forge/Vector/Viper speed verification, and another mirror refresh.

- Mobile Free-Roam UI Phases 1N and 1O are installed and user-confirmed working. Preserve equal square pedal geometry, configurable size/offset/gap, image opacity, input behaviour and major-menu suppression. Only the post-install mirror refresh remains outstanding.

- Mobile Free-Roam UI Phase 1M is installed and present in the `2026-07-13 21:09:59` mirror. Preserve its borderless gradient arrows, opacity attributes, invisible pedal cards, pressed feedback, and unchanged Thumbstick/Tilt behavior while testing Phase 1N geometry.

- Mobile Free-Roam UI Phase 1L was user-confirmed looking good and is present in the `2026-07-13 20:22:34` mirror. Preserve its centred Settings, PC-parity Cash, confirmation layout, safe-area response, and disabled-product behavior while testing Phase 1M controls.

- Mobile Racing UI Phases 1/1B were installed and user-confirmed working. Browser, Entry submenus, and unified Results reuse the scaled PC composition, and `SafeTop = 72` leaves the approved small gap beneath Roblox controls. Preserve this safe-area baseline while testing Phase 1L popups.

- Mobile Free-Roam UI Phase 1K was user-confirmed looking good and is present in the `2026-07-13 19:38:44` mirror. Keep its boost target/input behavior, circular plate layering, Exit alignment, speed/boost telemetry, arrows, pedals, Thumbstick, Tilt, and menu suppression intact while testing the separate mobile racing-menu phase.

- Racing UI Phase 16E and Phase 16F are installed, user-confirmed, and present in the refreshed mirror. The driving lag is much better and local authored course arrows appear. Phase 16F fixed an empty StreamingEnabled segment-part cache, retries missing streamed routes, and normalizes route lookup without restoring opponent markers or broad polling. LOD remains intentionally untouched.
- Driving Feel Phase 0 passed `11/10/0` (`pass/warn/fail`) in Edit mode. Its client Play run validly captured the spawned `C 538` vehicle stats and collidable `Default` root, but reported two false failures because `ServerScriptService` is hidden from the Client Command Bar; the canonical audit now skips server-only checks in client context. The first Phase 1 install patched the controller, but Play reported `Infinite yield possible ... WaitForChild("VehicleDynamicsModel")` and bootstrap `startDriving` nil calls because the new module was absent at runtime. Rerun the updated canonical `scripts/roblox_driving_feel_phase1_core_dynamics.lua` in Edit mode: it recreates the module and upgrades the require to a guarded non-yielding legacy fallback. Do not patch the bootstrap and do not rerun the older Phase AM installer.
- Driving Feel Phase 2.1 was installed and the user confirmed the revised drift momentum/reverse behavior feels much better. Its post-confirmation Studio mirror still needs refreshing before the next source patch. Wider Standard/Lightweight/Power balance comparison remains deferred; current tuning is documented in `docs/driving-feel-tuning-reference-2026-07-13.md`.

- PC free-roam UI Phase 4A is installed and confirmed working. The refreshed mirror contains the Phase 4A desktop HUD controller, north-up four-tile minimap, live speed/boost presentation, image-only map markers, responsive car menu, shared dealership confirmation flow, `FreeRoamHudTeleportInvoke`, and `FreeRoamHudTeleportService_Active`.
- Later free-roam UI phases are intentionally deferred while race/time-trial UI work proceeds. Do not reopen the superseded Phase 1-3 visual repair ladder unless Phase 4A regresses; use `scripts/roblox_ui_freeroam_pc_phase3d_image_only_map_markers.lua` only as the documented pre-teleport rollback.
- Cash product purchases and persisted settings remain non-functional visual concepts. They require separate server-authoritative receipt/persistence designs and must not be implied by the confirmed Phase 4A baseline.
- The north-up minimap is intentionally non-rotating. Earlier rotated ImageLabel and ViewportFrame approaches were rejected because rotated descendants escaped clipping or changed the supplied 2D artwork/coordinate basis.

- Racing Phase 11Z was confirmed working by the user on 2026-07-10 and is now locked as the current racing prototype release-candidate baseline. The user refreshed the Studio mirror and pushed Git before handoff. New racing chats should start from `docs/racing-next-chat-handoff-2026-07-10.md`, preserve Phase 8H reset, Phase 10B arrow folders, Phase 11L V2 arrow visual sync, Phase 11T/11U V2 result/HUD ownership, Phase 11W PB verification, and Phase 11Y finished-time-trial lifecycle cleanup.
- Racing UI desktop Gates A/B and responsive Gate C were approved on 2026-07-11. Phase 0 passed `103/1/0`; its only warning was the understood absent Phase 5F comment marker, while the current Phase 11L V2 session-asset owner passed. Phase 1 V4 was reported looking good; the same canonical `scripts/roblox_racing_ui_phase1_shared_shell_browser.lua` is now V5 with final header/divider/event-card gradient polish and awaits rerun/final confirmation. Global Top 20/world records and fastest-lap/highest-speed result cards remain separate server-data phases and must show honest unavailable states until those contracts exist.
- Race and time-trial event folders plus the route currently carry separate `DisplayName` attributes, so editing only the Race Catalog name does not update every existing menu. Phase 1F is generated as `scripts/roblox_racing_ui_phase1f_shared_display_name.lua`; it seeds `RaceCatalog.<Event>.SharedMenuDisplayName` from the edited Race `DisplayName` and installs an isolated synchronizer. After installing, edit `SharedMenuDisplayName` for future menu-wide renames.
- Racing Phase 1 was run by the user, the first race zones/checkpoints were moved in Studio, and the mirror was refreshed before Phase 2 generation. The refreshed mirror shows `ShiftedCanalSprint` with 14 checkpoints, start zones, spawn grid, and finish line. Route arrow assets should be planned through optional `ArrowMarkers` rendered locally by the route-guide client.
- Racing Phase 2 was installed/tested by the user and reported working well. It intentionally does not grant rewards, save personal bests, show medals, open a themed track menu, select owned vehicles, or isolate racers from free roam yet. The amended next phase should replace the instant prompt start with a dealership/customisation-themed race entry menu, track image/map, `START RACE` / `START TIME TRIAL` / `EXIT`, owned vehicle selection, start-line staging/countdown, and first same-server session separation.
- Racing Phase 3 is generated in Git as `scripts/roblox_racing_phase3_entry_menu_staging_session.lua`. Run it in Studio with `MODE = "INSTALL"`, restart Play, drive into `TimeTrialStartZone`, press `E` / tap, verify the themed menu opens, `START TIME TRIAL` opens owned vehicle cards, selected vehicle spawn/stages at the start grid, the car stays frozen during countdown, releases on `GO`, checkpoints/finish still work, and visibility restores after finish. It disables the old Phase 2 `RaceClient_Active` and installs `RaceEntryMenuClient_Active`. If install succeeds but `E` does not open the menu and Output looks clean, run `scripts/roblox_racing_phase3b_prompt_connection_repair.lua`, restart Play, and confirm Output prints `Race entry prompt triggered by <player>` when the prompt is activated. If that server line appears but the menu still does not open, run `scripts/roblox_racing_phase3c_client_event_repair.lua`, restart Play, and look for client Output lines starting with `[NTR Racing Phase 3 Client]` and `[NTR Racing Phase 3C Probe]`. If the menu opens but starting a time trial from `RaceStartZone` spawns the car in free roam/customisation instead of the race grid, run `scripts/roblox_racing_phase3d_time_trial_event_pairing_repair.lua`. If the countdown completes but the vehicle is not drivable or the world streams/flickers around the start line, run `scripts/roblox_racing_phase3e_release_drive_handoff_repair.lua`.
- Racing plan update: multiplayer races should not be tier-bracketed at launch. Use one open queue/category per race event, while time trials keep per-vehicle-tier medal targets. After Phase 6 rewards are tested, the next recommended implementation step is the free-roam Race panel integration before open-category multiplayer matchmaking.
- Racing Phase 4 was installed/tested by the user and reported working well on 2026-07-06. Phase 4 personal bests are session-only and cash rewards remain deferred.
- Racing Phase 5 was run and exposed a duplicate checkpoint marker, Phase 5C still left the guide too obstructive, and Phase 5D's fixed screen badge lost the spatial checkpoint cue. Racing Phase 5F is generated in Git as `scripts/roblox_racing_phase5f_checkpoint_pill_label.lua`; run it after Phase 5E, restart Play, and verify checkpoint text appears above the physical checkpoint with a small configurable black pill only around the text, generated checkpoint frames are off by default, dynamic arrows still work, and `WRONG WAY` appears only after `3` seconds of sustained wrong-way driving.
- Racing Phase 6 is generated in Git as `scripts/roblox_racing_phase6_time_trial_rewards_pack.lua`. It uses guarded source anchors against `TimeTrialService_Active`, `RaceEntryMenuClient_Active`, and the large `GarageActionController_Shadow_Disabled` to install idempotent time-trial cash rewards. Run it in Studio, restart Play, finish a time trial, confirm the result panel shows a payout, `leaderstats.Cash` increases, repeat finishes use `RepeatRewardMultiplier`, and `Config.Racing.RouteGuide` / Phase 5F checkpoint pill attributes remain unchanged.
- Racing Phase 6B is generated in Git as `scripts/roblox_racing_phase6b_global_reward_config_rounding.lua` after Phase 6 was reported working well. Run it in Studio, restart Play, finish a time trial, confirm the payout rounds to the nearest `$250`, confirm multipliers now live under `Config.Racing.Rewards.TimeTrial` and `Config.Racing.Rewards.Race`, confirm event folders only keep track-specific `BaseReward`, and confirm `Config.Racing.RouteGuide` remains unchanged.
- Racing Phase 7 is generated in Git as `scripts/roblox_racing_phase7_free_roam_race_browser.lua`. Run it in Studio, restart Play, click/tap the free-roam `RACE` tile, confirm the new Race browser opens with `TIME TRIALS` / `RACES`, select an event, press `TRACK START`, confirm the local waypoint appears above the start zone, then drive to the zone and press `E` / tap to confirm the existing Phase 3 entry menu still opens. Confirm `Config.Racing.Rewards.TimeTrial` and `Config.Racing.Rewards.Race` values are unchanged.
- Racing Phase 7B is generated in Git as `scripts/roblox_racing_phase7b_race_browser_teleport.lua` after the Phase 7 menu was reported looking good. Run it in Studio, move `RaceRoutes.<RouteId>.TeleportPoints.RaceBrowserTeleportPoint` to the desired arrival spot, restart Play, open the free-roam Race browser, select an event, press `TELEPORT TO START`, and confirm the browser closes automatically while the player appears at the teleport part and the old spawned vehicle disappears without pulling the player away. Then enter the nearby start zone and press `E` / tap to confirm the existing race entry menu still opens. Confirm reward and route-guide config values remain unchanged.
- Racing Phase 8 is generated in Git as `scripts/roblox_racing_phase8_open_category_multiplayer_race_mvp.lua` after Phase 7B was confirmed working. Run it in Studio, restart a local server with 2 players, open a race entry menu on both players, press `START RACE`, choose vehicles, confirm both players join the open-category queue, stage on separate `SpawnGrid` points, stay frozen through countdown, release on `GO`, progress through checkpoints, and receive placement at finish. Confirm time trials still stage/finish/pay normally, and confirm `Config.Racing.Rewards` / route-guide tuning values remain unchanged.
- Racing Phase 8B is generated in Git as `scripts/roblox_racing_phase8b_multiplayer_race_drive_handoff_repair.lua` after Phase 8 queueing worked but race-start release left both cars not hovering/drivable and nearby map content appeared to turn off. Run it after Phase 8, restart a 2-player local server, queue both players, and confirm both cars hover/drive immediately at `GO`, route/map content stays loaded, and solo time trials still work.
- Racing Phase 8H was installed/tested by the user and reported working well. Keep it as the current reset baseline: no client-side vehicle pokes, no yaw-sync bridge, no full driving handoff during reset. Future reset changes should extend the server respawn/reset-pose path instead of returning to live vehicle teleport patches.
- Racing Phase 9A was installed/tested by the user and reported working well. Keep its best-session-result payout model: infinite time trials should not grant per-lap cash.
- Racing Phase 10A created the first session asset collision foundation, but the current preferred arrow/barrier workflow is Phase 10B rather than duplicating `SessionAssetMarkers.Example_Blocker_Disabled`.
- Racing Phase 10B was reported working nicely after the user sorted the arrows into the segment folders. Keep `ArrowMarkers.CheckpointA-B` folders as the current route-edge arrow/barrier baseline. Refresh the Studio mirror so `roblox/studio_snapshot/` captures the final sorted arrow hierarchy before any fragile follow-up that depends on live hierarchy.
- Racing Phase 11A is generated in Git as `scripts/roblox_racing_phase11a_race_placement_rewards.lua`. Run it in Edit mode, restart a local server race, finish with at least one player, and confirm the race finish panel shows Gold/Silver/Bronze plus cash payout, `leaderstats.Cash` increases once, time-trial rewards still work, and `Config.Racing.Rewards.Race` values remain unchanged.
- If Play reports `RaceMatchmakingService_Active:<line>: Expected <eof>, got 'end'` after the first Phase 11A install, run `scripts/roblox_racing_phase11a_race_matchmaking_parse_repair.lua` in Edit mode, then restart Play. The Phase 11A installer has been updated to use the same canonical `finishEntry` replacement instead of separate source fragments.
- Racing Phase 11B is generated in Git as `scripts/roblox_racing_phase11b_entry_event_and_spawn_guard.lua` after matchmaking showed `Race event not found: shifted_canal_sprint_tt` and race/time-trial starts could unnecessarily run the garage spawn path. Run it in Edit mode, then verify `START RACE` uses the paired race event id, race validation happens before vehicle spawn/select, and choosing the current vehicle does not respawn it before staging. A later server-side staging-spawn phase is still recommended for selecting a different owned vehicle without any free-roam/customisation fallback spawn.
- Racing Phase 11C is generated in Git as `scripts/roblox_racing_phase11c_server_grid_vehicle_spawn.lua` after Phase 11B worked but players still had to spawn/sit in a vehicle before racing. Run it in Edit mode, restart Play, and verify selecting any owned vehicle from the race menu spawns that vehicle directly at the time-trial start or race grid when staging begins. The menu should no longer spawn cars through the free-roam/customisation path before countdown.
- If Phase 11C Play reports `TimeTrialService_Active:<line>: attempt to index nil with 'FindFirstChild'` inside `getRaceVehicleSpawner`, run `scripts/roblox_racing_phase11c_binding_lookup_repair.lua` in Edit mode, restart Play, and retest. The root is the first helper lookup being too brittle while finding the garage-side `RaceVehicleSpawner` binding.
- Racing Phase 11D is generated in Git as `scripts/roblox_racing_phase11d_session_boundary_cleanup.lua` after the first working multiplayer race load showed arrow collision/visibility and VFX leaks around race finish. Run it in Edit mode, restart Play, and verify arrow barriers collide during multiplayer races, finished racers fade to black with only the result UI visible, finished vehicles despawn while hidden, `EXIT` returns to the route teleport/start point, and free-roam players cannot see race/time-trial hover VFX. It intentionally preserves reward config, route-guide config, arrow folder layout, Phase 8H reset behavior, and the main bootstrap.
- Racing Phase 11E is generated in Git as `scripts/roblox_racing_phase11e_race_collision_vfx_isolation.lua` after Phase 11D exit cleanup worked but multiplayer race arrow/barrier collision stopped after the start-window segments, VFX still leaked between free roam and active sessions, and race vehicles could collide with each other. Run it in Edit mode, restart Play, and verify race arrows/barriers collide after checkpoint 2, `RaceInstances.<RunId>.SessionAssets.ArrowBarrierProxies.ParticipantSegments` changes as racers progress, race vehicles do not physically collide with each other, and race/time-trial VFX are hidden from nonparticipants without breaking normal free-roam VFX after exit.
- Racing Phase 11F diagnostics found the root of the local multiplayer arrow/barrier collision issue: Studio local-server players use negative UserIds (`-1`, `-2`), but `RaceSessionAssetService_Active` rejected `UserId <= 0` during segment updates/removal. Run `scripts/roblox_racing_phase11g_studio_userid_session_asset_fix.lua` in Edit mode, restart Play, and retest a 2-player local race. Verify `ArrowBarrierProxies.ParticipantSegments` advances beyond `-1:0,-2:0` as checkpoints are passed.
- Racing Phase 11G was reported to fix arrow collision, but VFX and name tags still leaked across race/time-trial visibility boundaries. Run `scripts/roblox_racing_phase11h_visibility_vfx_nametag_gate.lua` in Edit mode, restart Play, then verify free-roam clients cannot see active race/time-trial players, vehicles, VFX, or name tags; race/time-trial participants cannot see unrelated free-roam players/vehicles/VFX/name tags; and same-race participants remain visible to each other.
- Racing Phase 11H mostly worked, but idle engine VFX could still remain visible while other VFX were hidden. Run `scripts/roblox_racing_phase11i_idle_engine_vfx_flush.lua` in Edit mode, restart Play, and verify hidden race/time-trial vehicles no longer show idle engine flames. This should preserve same-race visible VFX and normal free-roam VFX after exit.
- Racing Phase 11I was reported to work. Run `scripts/roblox_racing_phase11j_prototype_baseline_audit.lua` in Edit mode and optionally during a solo time trial / 2-player local race to lock the prototype baseline before adding personal-best persistence. Treat `FAIL=0` as the gate; review warnings before moving on.
- Racing Phase 11K is generated in Git as `scripts/roblox_racing_phase11k_time_trial_result_exit_cleanup.lua` after 2-player testing showed one player could exit a finished Silver time trial and then fail to re-enter, spawn a free-roam vehicle, or use Race browser teleport. Run it in Edit mode, restart Play, finish a time trial on each player, press result-panel `EXIT`, then verify the player returns to the route teleport/start point and can re-enter, spawn near valid road markers, and teleport from the Race browser.
- Racing Phase 11L is generated in Git as `scripts/roblox_racing_phase11l_multi_session_visibility_owner.lua` after overlap testing found race/time-trial visibility could fight when one player stayed in a multiplayer race and another started a time trial. Run it in Edit mode, restart Play, and verify different active sessions cannot see each other's vehicle bodies, name tags, or idle VFX; players in the same race still see each other; race arrows remain visible for the player still racing; and free-roam players do not see active race/time-trial participants.
- Racing Phase 11L Arrow Visual Proxy Sync Repair V2 was confirmed working on 2026-07-10. Keep marker `NTR_RACING_PHASE11L_ARROW_VISUAL_PROXY_SYNC_V2_TRANSPARENCY_RESTORE` as the current route-arrow visual baseline; future arrow visual fixes must preserve both server proxy segment sync and `NTR_ArrowOriginalTransparency` restore.
- Racing Phase 11M is generated in Git as `scripts/roblox_racing_phase11m_time_trial_personal_best_persistence.lua` after Phase 11L was confirmed working. Run it in Edit mode, restart Play, finish a time trial, then finish a slower and faster run on the same event/tier to confirm PBs are kept/replaced correctly. DataStore persistence is configurable under `Config.Racing.PersonalBests` and defaults off; enable API services plus `DataStoreEnabled` only when doing the saved/rejoin test.
- Racing Phase 11N was confirmed working on 2026-07-10. Keep its local PB lookup action and vehicle-card readouts as the current prototype PB UI baseline.
- Racing Phase 11O V2 was confirmed working on 2026-07-10. Keep the isolated `RacePersonalBestBoardClient_Active` as the current local PB board baseline: it opens beside the entry menu and closes with the menu when the player exits without starting.
- Racing Phase 11P should not be treated as a confirmed baseline. Testing reported the result panel flow did not improve and exiting could leave the client still in driving HUD/state, blocking later re-entry/teleport.
- Racing Phase 11Q was confirmed working on 2026-07-10. Keep its finish/exit handoff direction as the current baseline: time-trial finish/result exit/end must fire `FreeRoamVehicleExited` to clear the main driving HUD/controller before re-entry or Race browser teleport.
- Racing Phase 11R was confirmed working on 2026-07-10 from playtest output: time-trial reward granted, `TimeTrialFinished` received, session HUD state cleared, camera restored to humanoid, `TimeTrialEnded` received, and race entry could open again.
- Racing Phase 11S V2 is generated in Git as `scripts/roblox_racing_phase11s_stability_baseline_audit.lua`. The first Play-client audit showed healthy client/route/session checks but false server failures because `ServerScriptService` is not replicated to clients. V2 skips server-only source checks in Play-client context. Run it in Edit mode and again after a finish/exit smoke in Play mode; treat `fail=0` as the gate before more result UI polish, DataStore PB verification, or broader competitive features.
- Racing Phase 11U V2 was confirmed working on 2026-07-10. Keep its narrow behavior as the current result/HUD baseline: Phase 11T owns the result coach, Phase 11U V2 hides only `NTR_RaceHud_Phase3.Panel`, and result exit/re-entry/teleport remain healthy. Do not broaden this cleanup back to result/control panels.
- Racing Phase 11V V2 passed from a Play-client smoke on 2026-07-10 with `pass=79 warn=1 fail=0`; the only warning was expected because server scripts are not replicated to the Play client. Keep it as the current prototype release audit gate before broader polish/multiplayer testing.
- Racing Phase 11W V2 was confirmed working on 2026-07-10 after PB save/rejoin testing. Missing `RacePersonalBestBindings` in Edit is expected because the PB service creates them during Play/runtime. Keep `scripts/roblox_racing_phase11w_time_trial_pb_datastore_verification.lua` as the PB persistence verification/control script.
- Racing Phase 11X is generated in Git as `scripts/roblox_racing_phase11x_prototype_release_candidate_audit.lua`. Run it in Edit mode, then again from the Play client after a solo time-trial finish/exit and after a 2-player same-server race smoke. Treat `fail=0` as the gate before choosing the next feature/polish branch.
- Racing Phase 11Y was confirmed working on 2026-07-10 after repeated finish/exit testing. Keep it as the current time-trial finish lifecycle baseline: finished TT vehicles must stay frozen/drive-disabled/pending cleanup until result exit confirms, and the result coach must not hide exit UI before cleanup succeeds.
- Racing Phase 11Z was confirmed working by the user on 2026-07-10. Keep `scripts/roblox_racing_phase11z_post_11y_release_candidate_audit.lua` as the post-11Y regression gate before larger racing branches. Review warnings for context, especially pending finished vehicles that are only valid while the result panel is still open.
- Same-server race/time-trial separation has an important Roblox limitation: true per-player physical isolation in the same exact world location is not automatic. The plan now recommends `RaceInstances.<RunId>` in a separated race pocket or cloned runtime route for collidable session assets, participant visibility filtering, and fixed collision groups. Reserved servers remain a later option for high-stakes public competitive races.
- Drive-In Customisation Phase 1 first install can push the active client bootstrap over Roblox's 200 local-register limit, surfacing as `Out of local registers when trying to allocate okController`. Run `scripts/roblox_drive_in_customisation_phase1_register_limit_repair.lua` in Edit mode, restart Play, and retest. Root cause: the first installer added new top-level local helpers to the already register-limited `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled`. Future patches must avoid adding top-level locals/large blocks to that bootstrap; use isolated controllers/modules plus tiny table-backed bridges.
- Drive-In Customisation Phase 1 is prepared as `scripts/roblox_drive_in_customisation_phase1.lua`. After running it, move/resize `Workspace.NeoTokyoRacersWorld.Dealership.Customisation.DriveInCustomisationTrigger`, then verify the visible zone appears only while driving, the `ENTERING CUSTOMISATION IN 3/2/1` countdown cancels when driving out, completion despawns the live vehicle and opens Build Modules for the driven vehicle instance, and the countdown is readable on mobile.
- Drive-In Customisation Phase 2 is prepared as `scripts/roblox_drive_in_customisation_phase2_garage_entry_world_prompt.lua` after Phase 1 opened Build Modules but left the player/camera in free roam and the live vehicle visible. Run it after the Phase 1 register repair, restart Play, then verify trigger VFX show only while driving, the countdown is an in-world prompt on `DriveInCustomisationTrigger`, the live vehicle despawns, the player is hidden/frozen, the garage preview camera/vehicle are active, and movement returns after leaving/spawning from the garage UI.
- Drive-In Customisation Phase 3/3B was installed and confirmed working on 2026-07-06. Keep a light regression watch that the prompt appears only inside the trigger, countdown text stays inside the frame, `Start Driving` releases the hidden hold point before spawning, the spawned car is immediately drivable, and the normal on-foot customisation route still works. If a future place copy only has the partial Phase 3 state, run `scripts/roblox_drive_in_customisation_phase3b_spawn_unlock_anchor_repair.lua`; it patches only the missing bootstrap unlock.
- Free Roam Vehicle Spawn Phase 4D is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4d_despawn_speed_card_polish.lua` after the user reported parked despawn teleported the player back to the vehicle, parked vehicle speed still blocked new spawns, and desktop car-menu cards/panel needed polish. Run it in Edit mode after Phase 4C, restart Play, and verify parked despawn leaves the player in place, the `10 MPH` gate applies only while actively driving, on-foot spawn uses the player position instead of the parked car, the desktop car panel fits three compact cards, and card/image boxes no longer have pink outlines.
- Free Roam Vehicle Spawn Phase 4C is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4c_exit_stop_parked_hover.lua` after the user confirmed Phase 4B worked but exiting kept the camera/drive HUD/controls active and the vehicle remained drivable. Run it in Edit mode after Phase 4B, restart Play, and verify successful cockpit-card spawn hides the car menu, `EXIT VEHICLE` returns the camera to the player, hides drive HUD/controls, leaves the player stationary, and leaves the car hovering/moving without accepting drive input until prompt re-entry.
- Free Roam Vehicle Spawn Phase 4B is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4b_remote_handoff_repair.lua` after Play reported `Infinite yield possible on ReplicatedStorage:WaitForChild("HOVER_RACING_V2_GarageAction")` from `FreeRoamVehicleExitButton_Active`. Run it in Edit mode after Phase 4, restart Play, and verify the first free-roam cockpit-card spawn immediately hovers/drives, the bottom-centre exit button appears while seated, walking into the hidden seat does not auto-enter, and `E` / touch prompt re-entry works.
- Free Roam Vehicle Spawn Phase 4 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase4_despawn_exit_prompt.lua`. After Phase 3 is working, run Phase 4 in Edit mode, then test in Play that the free-roam car-menu `DESPAWN` destroys the current vehicle, the driving-only `EXIT VEHICLE` button parks the player 10 studs left of the driver seat while leaving the car spawned, bumping the hidden driver seat no longer auto-enters, and the server-validated owner `E` / touch prompt re-enters the parked vehicle. If parked hover physics or engine VFX still stop after exiting, add a small parked-hover/VFX keeper service as the next isolated vehicle phase.
- Free Roam Vehicle Spawn Phase 3 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase3_click_spawn.lua`. After Phase 2 markers are in place, run Phase 3 in Edit mode, then test in Play that clicking an owned free-roam cockpit card spawns/swaps into that vehicle, blocks above 10 MPH, uses nearby `NTR_RoadSpawnPoint` markers, and reports `No clear road spawn nearby` instead of spawning into blocked geometry.
- Free Roam Vehicle Spawn Phase 2 blockout-road marker setup is now the preferred first pass: `scripts/roblox_freeroam_vehicle_spawn_phase2_blockout_road_markers.lua`. It uses curated `Workspace.Test + WIP Assets.Blockout.Roads` `BasePart`s named exactly `Road` with colour `RGB(95, 95, 95)` / `#5f5f5f`, skipping differently coloured wall pieces with the same name. After running it, inspect `Workspace.NeoTokyoRacersWorld.SpawnPoints.RoadSpawnMarkers` and remove/adjust any marker that is inside geometry before Phase 3.
- Free Roam Vehicle Spawn Phase 2 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase2_road_spawn_markers.lua`. Phase 1 confirmed live spawn/select/despawn hooks and Phase 7 card attributes are present, but no explicit road spawn markers existed. Run Phase 2 in Edit mode, inspect `Workspace.NeoTokyoRacersWorld.SpawnPoints.RoadSpawnMarkers`, and confirm markers sit on useful road centres before implementing Phase 3 click-to-spawn.
- Free Roam Vehicle Spawn Phase 1 is prepared as `scripts/roblox_freeroam_vehicle_spawn_phase1_audit.lua`. Run it in Studio before implementing click-to-spawn so the next phase can confirm whether the live free-roam controller mirror is current and whether closest-road spawning should use existing markers, generated road-centre markers, or a temporary player-offset fallback.
- Dealership / Customisation Split Phase 1 was run by the user and reported working on 2026-07-04. Keep a final mirror refresh open after the full split sequence is confirmed.
- Dealership / Customisation Split Phase 2 was run by the user and reported working as intended on 2026-07-04.
- Dealership / Customisation Split Phase 3 first install exposed `GarageActionController_Shadow_Disabled:<line>: attempt to call a nil value` inside `V90_vehicleSummaries` when opening customisation. `scripts/roblox_dealership_customisation_split_phase3_vehicle_summary_repair.lua` fixed the summary-total fallback and the user reported the customisation menu opened again. Keep verifying duplicate owned cockpits appear as separate cards, `Owned xN` is gone from customisation cards, selecting a duplicate passes the correct `VehicleId`, and Start Driving still spawns the selected/customised vehicle.
- Dealership / Customisation Split Phase 4 was run and reported looking good by the user on 2026-07-04. Owned vehicle card ratings should match the expected cockpit/build rating, each card should have a colour-coded tier badge, and the customisation-zone action should open `ModuleShop` rather than the final Customise colour screen.
- Dealership / Customisation Split Phase 5 was run after its catalogue-anchor repair and the user reported the fix worked, but a cockpit image placed on cockpit 1 still showed the fallback pink bar in dealership UI. Phase 6 was then installed and exposed a Roblox client compile failure on Play: `Out of local registers when trying to allocate init: exceeded limit 200`. Run `scripts/roblox_dealership_customisation_split_phase6_register_limit_repair.lua` in Edit mode before retesting if that error appears. Phase 7 added the 4-column desktop / 3-column mobile responsive grid. Phase 8 and Phase 9 improved the cockpit cards and were reported working better. Phase 10 was reported looking good by the user. Phase 11 is prepared in Git as `scripts/roblox_dealership_customisation_split_phase11_sorted_cockpit_cards.lua`; run it after Phase 10. Verify dealership cockpits sort cheapest-to-most-expensive, customisation owned cockpits sort highest-to-lowest rating, category buttons sort alphabetically, and customisation mode still matches the dealership layout.
- Mobile thumbstick V2.4 is installed and user-confirmed working well. Keep broader device checks open for small-screen fit, boost/thumb overlap, touch jitter, and multi-touch behavior.
- Vehicle Phase AL was installed and its audit passed with 5 cockpits, 72 active modules, 23 planned upgrades, and 0 warnings.
- Vehicle Phase AM is confirmed working. The isolated spawned-vehicle writer produced `17/17` raw variables, `17/17` normalized variables, `6/6` headline stats, a `D 407` test rating, and 0 audit warnings. Detailed physics was enabled and reported working well.
- Keep `VehiclePerformanceRuntimeService_Active` as the current runtime owner. The earlier garage-controller write hook did not produce the Phase AM folders on fresh spawn.
- Mobile/gamepad comparison and broader balance testing across Lightweight/Standard/Power builds remain useful follow-up verification.
- Vehicle Phase AN is confirmed working. Fuel Injection level 1 cost `$4000`, advanced the module from level 0 to 1, changed the profile and spawned rating from `D 407` to `D 410`, and reached the spawned engine as EngineOutput `+2` and TopSpeed `+1` with 0 audit warnings.
- Vehicle Phase AO was installed and reported working well. The legacy Brakes, Converter, Fuel System, and generic module Upgrade controls are no longer the current visible UI.
- Broader mobile/device verification remains useful for contextual detailed variables, horizontal upgrade-card scrolling, text fit, and Buy popup placement.
- The current garage profile is session-memory only. Phase AN module upgrade ownership has the same lifetime as existing cash, cockpit, and module ownership until a unified garage profile DataStore is introduced.
- Persistence Phases 1-8 were installed/tested in Studio. Phase 8 worked functionally but its capacity panel overlapped the Categories panel, and the design direction changed from generic capacity upgrades to physical garage-property purchases. Persistence Phase 9 was installed but caused the active client bootstrap to exceed Roblox's 200 local-register limit near `V75Driving`; run `scripts/roblox_persistence_phase9_register_limit_repair.lua` before retesting Phase 9. The active garage profile remains the V56 session-memory profile until a later source-of-truth bridge phase.
- Studio API services or a published/API-enabled experience are required for Phase 5 DataStore save/load audits. If save fails with an API/DataStore access message, disable saves again with `scripts/roblox_persistence_phase5_disable_datastore_mirror_saves.lua` and enable Studio API access before retesting.
- If the Phase 5 save audit reports that the mirrored profile has no vehicles after the Phase 4 client smoke, run `scripts/roblox_persistence_phase5_import_snapshot_binding_repair.lua`. The likely cause is that mutating the table returned by `GetProfile` does not update ProfileService's internal session table across BindableFunction boundaries; the repair adds `ImportProfileSnapshot` so ProfileService owns the replacement.
- After Phase 6, verify the cockpit purchase UX manually: owning/selecting existing cockpits should still work, buying a second cockpit should work if capacity is 2 and only one cockpit is owned, and buying a third cockpit should show `Garage full. Upgrade your garage to store more vehicles.`
- Phase 7 client smoke spends current-session test cash and increases `GarageCapacity` once. If rerun in the same session, capacity and price will already be higher.
- Phase 9 is a guarded exact-source follow-up against the large active client bootstrap after Phase 8. If it aborts, refresh the Studio mirror before writing a new UI patch. After install, verify the `Garage Spaces` panel on desktop and mobile sizes and confirm it does not overlap Categories, cash, cockpit grid, or bottom navigation.
- Phase 9 uses the Phase 7 `UpgradeGarageCapacity` action as a temporary backend for the first `Kanda Lift Bay` card. The proper next server phase should add `BuyGarageProperty`, owned garage property IDs, and capacity calculated from those owned properties.
- The Phase 9 register-limit repair keeps the same UI behavior but moves the new helper functions onto one `NTRPersistencePhase9` table so the large client bootstrap has fewer top-level locals.
- Persistence Phase 10 was installed and worked as expected. Keep broader desktop/mobile checks open for the stacked Categories, Garage Spaces, and Available Cash left column.
- Persistence Phase 11 was installed and worked as expected. `Garage Spaces` should appear only during `CockpitShop` and hide in cockpit paint, module shop, and customisation stages.
- Persistence Phase 12 was installed and worked as expected. The garage property card renderer now lives in `GaragePropertyMenuController`, keeping the active client bootstrap lighter before richer garage UI work.
- Persistence Phase 13 was installed through the repair path and the client smoke confirmed the core `BuyGarageProperty`, owned-property profile/mirror, and controller path on 2026-07-01. The original smoke had an over-strict visual UI wait and could fail before the player reached the dealership desk; the smoke now skips that visual check if `HOVER_RACING_V2_GarageUI` is not loaded yet. Manually verify after reaching the dealership desk that `Buy More` opens the garage property gallery, owned properties show as owned, and the two-space cockpit gate expands only after buying properties.
- If the first Phase 13 installer stops with `Could not find source anchor for garage mirror mutating action`, run `scripts/roblox_persistence_phase13_garage_property_ownership_repair.lua`. The first installer may already have updated catalogue/schema/mapper data before stopping; the repair completes the missing server action, mirror mutating action, profile response, bootstrap context, and controller backend switch.
- Persistence Phase 14 was installed and reported working well by the user on 2026-07-01. The active profile now has duplicate-capable instance fields/actions while preserving legacy UI fields.
- Persistence Phase 15 was installed and reported working well by the user on 2026-07-01. It exposes duplicate-copy controls for owned cockpits/modules without adding a full vehicle-instance picker.
- Persistence Phase 16 was installed and scripts worked well, but follow-up UI fixes were requested: front/rear engines must be split by slot, owned and buy module views should be separate bottom-menu layers, owned modules should appear as separate instance cards, and the `Buy Copy` / `Equip Copy` / `No Free Copy` wording should be removed.
- Persistence Phase 17 is installed and stabilised. It repairs the Phase 16 module picker with `OWNED MODULES` / `BUY MODULES` menu buttons, per-instance owned module cards, BUY/EQUIP actions, preview-on-click for locked modules, client/server front-rear engine slot guards, and the final scale-aware selected-card popup alignment. Keep broader desktop/mobile layout checks open around module cards, popups, and tab readability.
- Persistence Phase 18 was installed and smoke-tested on 2026-07-02. The active garage session now hydrates from `ProfileService_Active` saved instance data on first use; the client smoke reported `Hydrated=true`, `source=ProfileService`, and `savedVehicles=1`.
- Persistence Phase 19 was installed and smoke-tested on 2026-07-02. The client smoke reported `installedInstanceSlots=4`, `missingInstances=0`, `Phase19Synced=true`, and `syncCount=4`, confirming current vehicle/module instance references are clean after the compatibility sync.
- Persistence Phase 20 was installed and smoke-tested on 2026-07-02. The client smoke reported `RuntimeModule=GarageProfileRuntime`, `syncCount=4`, and `missingInstances=0`, confirming the big garage action controller is delegating the instance sync through `GarageProfileRuntime`.
- Persistence Phase 21 was installed and smoke-tested on 2026-07-02. The client smoke reported `EnterOwnGarage OK`, `InGarage=true`, and `ReturnToCity OK`; `streamOk=false` in Studio, so Phase 22 should capture the stream error text and keep the fade/teleport path tolerant when streaming requests are unavailable.
- Persistence Phase 22 was installed and smoke-tested on 2026-07-02. The client smoke reported `displayOk=true`, `displayExists=true`, `displaySource=ProfileServiceCurrentVehicle`, and `ReturnToCity OK`, confirming the private garage can show a non-drivable display clone of the saved vehicle.
- Persistence Phase 23 was installed through `scripts/roblox_persistence_phase23_garage_interior_service_canonical_repair.lua` and smoke-tested on 2026-07-02. The confirmed output included `SetAccessMode OK`, `VisitGarage OK`, `InGarage=true`, `displayExists=true`, `ReturnToCity OK`, and cleanup back to `Private`. Keep the earlier incremental Phase 23 script only as history; for this isolated service, canonical replacement proved more reliable than repeated source-anchor repairs.
- Persistence Phase 24 was installed and smoke-tested on 2026-07-02. After the readback-count repair, the client smoke reported `Applied surfaces/decor`, `Customization state OK. owner=7915427645 surfaces=2 decorations=1 persisted=true`, and `ReturnToCity OK`.
- The first Phase 24 smoke applied the floor/wall styles and neon sign but failed at `Expected at least two stored surface styles` because `GetCustomization` counted only profile table entries. This was repaired by counting both profile data and live interior model attributes; the follow-up smoke passed.
- Persistence Phase 25 was installed and smoke-tested on 2026-07-02. The client smoke reported the owner customization UI panel visible, `Customization backend OK`, and `ReturnToCity OK`.
- The first Phase 25 smoke found the `NTR_GarageInteriorCustomizationUI` ScreenGui but failed because the panel was not visible yet for the owner. The script now polls during the garage-entry transition, treats either `NTR_Phase21InPrivateGarage` or `NTR_Phase21GarageInteriorId` as garage presence, and prints the relevant attributes if visibility still fails.
- Persistence Phase 26 is prepared in Git as `scripts/roblox_persistence_phase26_garage_customization_validation.lua`. It does not install new gameplay code; it preflights Phase 24/25 objects in Edit mode and validates in Play that backend-applied surface/decor presets are reflected by the Phase 25 UI summary.
- Persistence Phase 26 was smoke-tested on 2026-07-02. The client smoke reported `Applied validation presets. floor=true walls=true decor=true`, `UI summary reflected backend state. summary=SURFACES 2  DECOR 1 persisted=true`, and `ReturnToCity OK`.
- Persistence Phase 27 was installed and smoke-tested on 2026-07-02. It installs the isolated same-server garage access panel for enter mine, set public/private, visit owner user ID, and return city.
- Persistence Phase 27 was smoke-tested on 2026-07-02. The client smoke reported `Access UI shell visible`, `SetAccessMode OK. mode=Public`, `VisitGarage OK`, `State OK. inGarage=true`, and `ReturnToCity OK`.
- Persistence Phase 28 was smoke-tested on 2026-07-02 and passed the final garage MVP audit. The client smoke reported `Access UI present`, `VisitGarage OK`, `Customization UI visible`, `Customization persisted and UI reflected state. summary=SURFACES 2  DECOR 1`, `Interior state OK`, and `ReturnToCity OK`. Remaining verification is a DataStore-enabled save/rejoin check when practical, then refresh/commit the Studio mirror while excluding `docs/studio-full-export-paste.txt`.
- Phase 24 intentionally adds only runtime/simple-profile garage surface and decoration data. It does not add a player-facing garage customization UI, decoration marketplace, cross-server garage visiting, or rich prop assets yet.
- The first Phase 17 installer caused a client bootstrap parse error near line 408, and the v2 repair may still leave the bootstrap unparsable on some Studio sources. Use `scripts/roblox_persistence_phase17_owned_buy_tabs_repair_v3.lua`, then restart Play before running the updated smoke. The Phase 17 smoke now warns, rather than fails, when the catalog cannot classify front/rear engine templates by metadata.
- If the Phase 17 smoke reports `Front=0, Rear=30`, run `scripts/roblox_persistence_phase17_front_rear_engine_metadata_repair.lua` in Edit mode. The repair adds explicit `EnginePosition` metadata to front/rear engine assets, exposes it in the garage catalog, and refreshes the Phase 17 client/server slot checks to prefer that field.
- If the Phase 17 smoke passes but Play still reports `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:408: Incomplete statement`, run `scripts/roblox_persistence_phase17_client_parse_line408_cleanup.lua` in Edit mode. It only touches the client bootstrap if it finds stale dangling `or` / `and` continuation lines near line 408; otherwise it stores the exact source window at `ReplicatedStorage.NTR_DEBUG.COPY_THIS_LINE408_SOURCE_DUMP.Value` and prints a large Output copy block for targeted follow-up.
- A confirmed Phase 17 line-408 source dump showed a single stray `l` immediately before `-- NTR_PERSISTENCE_PHASE17_MODULE_TABS_V4`. Run `scripts/roblox_persistence_phase17_remove_orphan_l_line408.lua` to remove that exact orphan line.
- After the server `GetInitial` repairs, entering the dealership can expose a client nil-call at `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:3475` on `sortedSlots()[1]`. Run `scripts/roblox_persistence_phase17_client_slot_helpers_and_engine_metadata_repair.lua`; it restores the missing slot/category/module lookup helper block and reapplies explicit front/rear engine metadata.
- Cockpit paint/customisation can expose `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:1928: attempt to call a nil value` inside `renderColourPicker` if persisted colour data reaches the picker as table-shaped data instead of raw `Color3`. Run `scripts/roblox_persistence_phase17_colour_picker_color3_repair.lua` to normalize colour values before HSV conversion.
- If the colour picker error shifts to `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled:1962`, run `scripts/roblox_persistence_phase17_colour_picker_slider_signal_repair.lua`. The shifted line maps to the compact slider input connection after the Color3 repair, so the follow-up guards slider signal hookups instead of changing the visual picker layout.
- If the same line-1962 colour picker failure persists, use `scripts/roblox_persistence_phase17_colour_picker_root_repair.lua` instead of another line-number patch. It replaces the fragile compact drag-slider path with a swatch plus RGB-step picker and keeps the existing paint/customisation server callbacks.
- The root colour picker repair can push the already-large client bootstrap back over Roblox's 200 local-register limit, surfacing as `Out of local registers when trying to allocate mobileInputState`. Run `scripts/roblox_persistence_phase17_colour_picker_register_limit_repair.lua` to remove bulky colour helper locals and reinstall the picker through `NTRPersistencePhase15` table methods.
- The RGB-step picker from the register-limit recovery is only a stability fallback. Once startup works, run `scripts/roblox_persistence_phase17_colour_picker_hsb_slider_restore.lua` to restore draggable H/S/B gradient sliders without adding new top-level helper locals.
- If Front Engine only exposes the old flat `Engine V1` through `Engine V4` options, run `scripts/roblox_persistence_phase17_front_engine_family_and_module_card_text.lua`. It keeps the old templates in place for rollback but retires/hides them from the catalogue, makes the Phase AK family front engines visible like rear engines, and changes module card text to `Cockpit / Variant`, green price, and `Owned xN`.
- If Start Driving spawns the car but it is not hover/drivable and the client stack points to `closeGarage`, run `scripts/roblox_persistence_phase17_close_garage_drive_handoff_repair.lua`. The likely cause is an optional colour-picker cleanup helper being absent after the Phase 17 recovery sequence; the repair makes garage closing tolerant and lets `startDriving` continue after successful `SpawnVehicle`.
- If module option cards clip their bottom text, or the `OWNED MODULES` / `BUY MODULES` tabs feel too tall/text-heavy, run `scripts/roblox_persistence_phase17_module_button_layout_polish.lua`. It enlarges the module-card scroll height, uses one centred label on the tab buttons, and changes locked buy cards to show `Locked` in muted text while keeping locked cards dark.
- If buy-module colours still show locked cards as lighter than available cards, or BUY/LOCKED popups are off-centre after horizontal scrolling, run `scripts/roblox_persistence_phase17_module_card_colour_popup_sprint_guard.lua`. It uses rendered card positions for popup centring, hides popups when the carousel scrolls, mutes locked card title/price, and also blocks sprint FOV changes while garage/customisation UI is open.
- If module popups are still offset after scrolling, run `scripts/roblox_persistence_phase17_module_popup_position_and_cockpit_back_lock.lua`. It repeats popup centring after the next rendered frame, moves the popup above the bottom module frame with an 8px gap, and hides the Paint Cockpit Back button so players cannot return to the dealership list after buying/selecting a cockpit.
- If the Paint Cockpit Back lock works but module popups still have the same offset, use `scripts/roblox_persistence_phase17_module_popup_screen_layer_repair.lua`. The fresh mirror confirmed the popup can still be distorted by being parented inside the padded ModuleOptions panel; this repair creates a full-screen popup layer and positions from the selected card's screen coordinates instead.
- If that screen-layer repair creates too much vertical space or the popup still feels detached, use `scripts/roblox_persistence_phase17_module_popup_card_anchor_repair.lua`. It creates an invisible top-centre anchor on the selected module card, places the popup 6px above that anchor, and hides the popup on Next, Back, and stage changes.
- If selecting/buying a cockpit no longer advances to Paint Cockpit after the card-anchor popup patch, run `scripts/roblox_persistence_phase17_cockpit_paint_stage_scope_repair.lua`. The likely cause is `showStage()` calling `NTR_hideModulePopup()` before that helper is in lexical scope; the repair changes `showStage()` to hide `UI.ModulePopup` inline.
- If Build Modules opens but BUY/LOCKED/EQUIP popups still appear high or off-centre, run `scripts/roblox_persistence_phase17_module_popup_card_child_repair.lua`. It avoids overlay/screen-coordinate drift by parenting the visible popup directly to the selected module card, then parks it safely before rerenders, scrolling, Next, and Back.
- If the card-child repair centres the popup but module cards spill outside the bottom frame, run `scripts/roblox_persistence_phase17_module_carousel_clip_popup_overlay_repair.lua`. It restores `ClipsDescendants` on the module carousel and positions only the popup on an overlay from the selected card's rendered centre.
- If carousel clipping is fixed but the popup is offset again, run `scripts/roblox_persistence_phase17_module_popup_tracker_layer_repair.lua`. It is the preferred root fix for both problems together: clipped carousel cards plus a popup on a `UI.ModuleShop` overlay that tracks the selected card's rendered centre every frame while visible.
- If the tracker-layer repair triggers `Out of local registers when trying to allocate V75Driving`, run `scripts/roblox_persistence_phase17_module_popup_tracker_register_repair.lua`. It preserves the same UI behavior but converts the popup tracker helpers from top-level locals into `NTRPersistencePhase15` table methods to reduce register pressure.
- If moving from Paint Cockpit into Build Modules reports `The Parent property of ModulePopup is locked, current parent: NULL`, run `scripts/roblox_persistence_phase17_module_popup_destroyed_instance_repair.lua`. It handles the stale destroyed `UI.ModulePopup` reference left by earlier card-parented popup attempts by rebuilding the popup on the overlay before hide/park/position operations.
- The Phase 17 card-tracked overlay repair fixed carousel clipping but BUY/LOCKED/EQUIP can still appear horizontally off-centre and too high above the clicked module card. The alignment diagnostic showed `dx=-47.3` and `gap=57.5`, confirming the popup is not using the intended selected-card anchor. Use `scripts/roblox_persistence_phase17_module_popup_anchor_target_repair.lua` next. It marks the current rendered selected card, creates an invisible top-centre `ModulePopupAnchor`, positions the overlay from that anchor, removes misleading X clamping, and hides the popup when the card is not sufficiently visible.
- If the diagnostic still reports `gap=57.5` after attempting the anchor-target repair, run the read-only `scripts/roblox_persistence_phase17_module_popup_source_marker_audit.lua` in Edit mode. If it reports the old card-tracked overlay helper is still installed, rerun the anchor-target repair, restart Play, and rerun the diagnostic.
- If the source-marker audit confirms `NTR_PERSISTENCE_PHASE17_MODULE_POPUP_ANCHOR_TARGET = true` but the diagnostic still reports `gap=57.5`, run `scripts/roblox_persistence_phase17_module_popup_pool_target_reset_repair.lua` in Edit mode. This clears stale popup target attributes and old `ModulePopupAnchor` children from reused pooled buttons before each render, preventing the overlay from following a recycled non-selected card.
- If the runtime anchor diagnostic shows exactly one target card and a correct `ModulePopupAnchor`, but the visible popup centre/bottom still does not match that anchor, run `scripts/roblox_persistence_phase17_module_popup_absolute_correction_repair.lua` in Edit mode. This keeps the selected-card anchor system and corrects the popup's actual `AbsolutePosition` after placement, covering UI scale or overlay transform offsets. The updated repair is safe to rerun after the earlier bad `Installer source text leaked into bootstrap unexpectedly` assert and now measures parent/UI scale before applying the correction.
- If the client parse error is cleared but the garage server reports `Expected 'end' ... got <eof>`, run `scripts/roblox_persistence_phase17_garage_server_final_end_repair.lua`. It restores the final top-level `do` close before `-- V56_CONSOLIDATED_ACTION_CONTROLLER_END` and prints a tail dump if the live source shape differs.
- If the exact final-end repair does not clear the EOF error, run `scripts/roblox_persistence_phase17_garage_server_force_final_end_repair.lua`. It inserts one closing `end` immediately before the final V56 marker unless that marker is already preceded by `end`, in which case it prints a tail dump for targeted follow-up.
- The refreshed mirror confirmed the garage server EOF error is caused by a duplicated `V85_attachDefaultModuleInstancesToCurrentVehicle = function(profile)` header near the Phase 17 slot guard. Run `scripts/roblox_persistence_phase17_garage_server_duplicate_attach_header_repair.lua`; it replaces only that exact duplicated header with a single header.
- After that repair, the Phase 17 client smoke can expose `GarageActionController_Shadow_Disabled:927: attempt to call a nil value`. The refreshed mirror shows `V56_totalStats(profile)` is still called by the profile/performance summary, but the helper block is missing. Run `scripts/roblox_persistence_phase17_garage_server_total_stats_repair.lua` in Edit mode, then restart Play and rerun the Phase 17 client smoke.
- If the next Phase 17 smoke exposes another garage server nil helper call around the `GetInitial` path, run `scripts/roblox_persistence_phase17_garage_server_foundation_repair_and_audit.lua` instead of continuing one-off fixes. It handles the Luau scoping issue where `V85_attachDefaultModuleInstancesToCurrentVehicle` can be defined before the Phase 14 `V84_*` helpers it calls, restores `V56_totalStats` if needed, and audits the expected server helper set afterward.
- If a later shifted line such as `GarageActionController_Shadow_Disabled:1445` still reports `attempt to call a nil value`, run `scripts/roblox_persistence_phase17_garage_server_line1445_audit.lua` and paste the Output dump. It is read-only and prints the live Studio source around the reported line plus helper-definition locations, avoiding another guessed patch against stale mirror line numbers.
- The line-1445 audit has shown `V56_catalog` missing while `GetInitial` still calls `Catalog = V56_catalog()`. Run `scripts/roblox_persistence_phase17_garage_server_catalog_repair.lua` to restore the catalog helper family and then rerun the Phase 17 client smoke in a fresh Play session.
- A later Phase 17 smoke reported another shifted `GetInitial` nil call at `GarageActionController_Shadow_Disabled:1639`. Use `scripts/roblox_persistence_phase17_garage_server_startup_dependency_repair.lua` before more one-off repairs; it checks declaration order as well as presence for the module-type helpers, Phase AK default/core module helpers, Phase 14 instance helpers, catalog helpers, total stats, and profile response helpers.
- Vehicle Phase AK and its follow-up repairs were reported working by the user. Keep mobile verification open for the centered required-modules popup and small-screen module option scrolling.
- Phase AK recovery scripts remain available for the resolved register-limit, server core-gate, rear-engine catalogue, camera, per-cockpit default colour, and spawned module colour-sync problems. Do not rerun them unless the matching regression returns.
- `V75` boost recharge delay and hover wobble were generated after `V74`, but no later user confirmation is present in this chat history.
- Confirm that `BoostRechargeDelay` is being read from installed Boost modules at runtime.
- Confirm that low-speed wobble is subtle enough and fades out by `20 MPH`.
- `scripts/roblox_driving_speed_sensitive_steering_curve.lua` was reported working nicely by the user. Keep broader balance testing open for different cockpit/module builds, mobile steering, reverse handling, and boost/drift mini-boost turn feel.
- `scripts/roblox_driving_accel_brake_pitch_tilt.lua` was reported looking great by the user. Keep broader balance checks open for different cockpit/module builds and whether boost extra pitch remains subtle at high speed.
- `scripts/roblox_driving_drift_boost_accel_gate_reverse_speed.lua` was reported working and looking good by the user. Keep broader checks open for reverse control at `40 MPH`, drift-exit boost timing on mobile, and different module builds.
- `scripts/roblox_driving_slope_hover_height_compensation.lua` was reported working well by the user. Keep broader checks open for very steep slopes, uneven terrain, high-speed slope transitions, and whether steering bank/brake pitch/boost pitch/hover wobble still layer cleanly across all builds.
- Mobile auto-sprint is prepared in `scripts/roblox_character_sprint_controller_install.lua`; verify on a mobile device/emulator that `MobileSprintMoveThreshold` feels right.
- VFX Phase AJ is prepared to repair thrust VFX preview after the dealership preview root moved; run and verify `scripts/roblox_vfx_phaseAJ_thrust_preview_root_repair.lua` if thrust VFX is missing in the customisation menu.

## Studio Export Mirror

- The Studio mirror was refreshed after Drive-In Customisation Phase 3B was confirmed working on 2026-07-06. The mirror includes `DriveInCustomisationSessionService_Active`, `DriveInCustomisationZoneClient_Active`, and the patched `NeoTokyoRacersClient_Bootstrap_Shadow_Disabled` with the drive-in unlock-before-spawn handoff. Do not commit `docs/studio-full-export-paste.txt`.
- The Studio mirror was refreshed on 2026-06-30 at 17:37:33 from Studio and imported 65 scripts. `roblox/studio_snapshot/hierarchy.md` now reflects the confirmed mobile thumbstick, UI theme, lighting, and VFX delayed attach-once changes.
- The Studio mirror was refreshed on 2026-07-02 and includes the rejected action-rail state. Refresh it again after installing and verifying `scripts/roblox_persistence_phase17_module_popup_card_tracked_overlay_repair.lua`.
- The Studio mirror was refreshed again on 2026-07-02 after Phase 24 passed, importing 121 scripts. It includes `GarageProfileRuntime`, `GarageDisplayRuntime`, `GarageInteriorService_Active`, `GarageInteriorCustomizationService_Active`, `GarageInteriorClient_Active`, and `GarageInteriorCustomizationInvoke`.
- The exported bootstrap mirror visible to Codex before Phase 11 still showed the Phase 9 marker while the user had already confirmed Phase 10 working in Studio. Refresh the Studio mirror after installing and verifying Phase 11 so `roblox/exported_scripts/` catches up to the live dealership/customisation source.
- Free Roam Car Menu Phase 3 was installed and basically worked, and Phases 4-6 improved sizing but the cockpit-card border could still look clipped/doubled. Phase 7 is prepared in Git as `scripts/roblox_freeroam_car_menu_phase7_borderless_card_frames_compact_width.lua`. Verify the `Car` pop-out uses filled-frame borders with no clipped card/image border, the panel width fits 3 compact desktop/laptop cards or 2 mobile cards, rating-descending order remains, `Despawn` still works, and card `VehicleId` / `CockpitId` attributes remain available for the next spawn/select phase.
- `docs/studio-full-export-paste.txt` is generated by the receiver as a fallback paste file and should not be committed.
- The fresh mirror shows active loose `StarterPlayerScripts` helpers for trailer/camera capture, including `LocalScript`, `TrailerMode.client.lua`, and `TrailerShot01Camera`. Review whether these are intentional filming tools before publishing a normal gameplay build.

## Camera

Resolved direction:

- Avoid fully scriptable chase camera every frame. It caused jitter and did not feel like the desired pre-V72 camera.
- Keep Roblox default vehicle camera as the base.
- Use a light assist for FOV and soft recentering.

Watch for:

- Camera assist fighting any older camera script.
- On-foot sprint FOV fighting vehicle camera assist if the player enters a vehicle while sprinting.
- Mobile touch camera input overlapping driving controls.
- FOV not restoring after exit.

## Character Movement

Recently confirmed:

- Character sprint install worked after running `scripts/roblox_character_sprint_controller_install.lua`.
- The user moved/renamed the live runtime hierarchy to include `CharacterSprintController_Active`, `DriveHudController_Active`, `MobileDriveControlsController_Active`, and `RuntimeVFXController_Active` under `StarterPlayer.StarterPlayerScripts.NeoTokyoRacersClient.Controllers.Runtime`.
- A blocked third-party sprint animation asset warning was resolved by disabling/replacing the custom `AnimationId`; Roblox's normal character animation still looked fine.

Watch for:

- The previous placeholder animation attempt may have left `StarterPlayer.StarterCharacterScripts.NTR_CharacterSprintDefaults` in Studio. The sprint installer removes it automatically.
- A custom sprint animation must be an uploaded Roblox `Animation` asset usable by the place owner/group. KeyframeSequences, Animator object IDs, and unshared third-party assets will not work directly.
- If `AnimationId` fails to load, set it to `rbxassetid://0` or a permitted animation asset. Sprint speed should still work even without a custom sprint animation.
- Mobile auto-sprint uses Roblox's standard `PlayerModule` move vector first, then falls back to `Humanoid.MoveDirection`. Verify on an actual mobile device/emulator that the threshold feels like "full push" rather than triggering too early.
- After future edits, confirm the sprint controller does not leave the humanoid at sprint speed after death, respawn, sitting, or exiting a vehicle.

## UI

Recently confirmed:

- Dealership Intro Phases 1-7 were installed and reported working on 2026-06-03.
- The full dealership menu opens from `GarageDeskTrigger` instead of immediately on spawn.
- The first-menu Exit button closes the menu and the menu reopens after leaving and re-entering the desk zone.
- Dealership Intro Phase 8 is generated for Studio install/testing. It adds a dynamic client-only arrow tether to the desk and DataStore-backed first-objective completion persistence.

Known sensitive areas:

- Shared theme back/exit colours and dealership intro objective theming were installed and reported working. `scripts/roblox_ui_shared_theme_back_exit_and_intro.lua` remains the recovery script for that sequence.
- After the shared theme patch, check older isolated HUDs such as mobile driving controls for any remaining hardcoded colours. The patch covers the active dealership bootstrap, shared UI theme helpers, and dealership intro objective, not a full visual audit of every historical UI script.
- Dealership selected-cockpit module-slot count text was removed and reported working. `scripts/roblox_dealership_remove_cockpit_module_slots_text.lua` remains the recovery script for that sequence.
- Free Roam Map Stack Phase 2 is generated in Git as `scripts/roblox_freeroam_map_stack_phase2.lua` and needs Studio install/testing after the Phase 2.11 button layering/config preservation repair. It supersedes the Phase 1 left rail with a top-right stack: square map placeholder, full-width car button, and 2x2 Shop/Race/Home/Settings buttons. Verify uploaded icon IDs still show as centred square overlays, desktop/laptop stack is about 20% smaller, the car row matches the other button row height/icon scale, stack buttons layer as dark base plus visible pink outline plus inset configurable gradient plus hover overlay plus icon, existing `FreeRoamNav` Bool/Number/Color tuning values are preserved on rerun, pop-out action buttons use solid fills without gradients, free-roam pop-out labels/action-button text has the configured glow, action pop-outs align with and match the full stack height, the stack shows during normal on-foot free roam and while driving, the stack hides while dealership/customisation menus are open, mobile stack remains close to the top-right corner, mobile driving controls no longer flicker, desktop drive HUD is hidden on mobile, boost is centred bottom with MPH above, gas pedal right edge aligns to the map stack right edge, and `CAR > EXIT VEHICLE`, `HOME` enter/return actions, and `SHOP > OPEN SHOP` still work. On mobile smoke, expect `driveGuiEnabled=true`, `garageVisible=false`, and then `IsDriving=true` / `mobileRootVisible=true`; if `garageVisible=true`, inspect which visible garage/dealership root is still open. If Play reports `MobileDriveControlsController_Active:<line>: attempt to call a nil value` near the desktop-HUD helper area, stop Play and rerun the updated Phase 2 script in Edit mode to restore the helper block before retesting. A later shared UI theme phase should apply the liked text-glow style consistently across dealership/customisation headers and main buttons.
- Phase Q appeared to restore garage/UI startup; confirm it still loads after a fresh Studio restart.
- The dealership intro markers are editable Studio placement controls; keep `Workspace.NeoTokyoRacersWorld.Dealership.Intro` and `Workspace.NeoTokyoRacersWorld.Dealership.Spawn.VehicleExitSpawnPoint` positioned after world/layout changes.
- Dealership Intro Phases 3-7 are guarded source-text patches; if the active bootstrap or intro client is regenerated, rerun audits before applying new dealership patches.
- Phase 8 replaces only the isolated intro client and adds `IntroProgressService_Active`; confirm it keeps the Phase 7 desk reopen behavior and does not show the objective/tether after rejoin.
- Studio DataStore API access is needed to verify Phase 8 persistence across leave/rejoin. If API access is off, completion may be session-only and warnings are expected.
- In multiplayer/local server testing, confirm `Workspace._NTR_ClientOnly.VehiclePreview` is visible only on the owning client.
- Mobile dealership scaling.
- PC drive HUD hiding on mobile.
- Customisation colour sliders on mobile.
- Left customisation bar overlapping bottom UI on small screens.

## Lighting

- FivePM and SevenAM in the current mirror still show the incorrect bright blue
  Picture 1 snapshot. Run
  `scripts/roblox_lighting_replace_5pm_7am_with_warm_snapshot.lua` to install the
  later warm Picture 2 snapshot, verify both Edit previews, then refresh mirror.
- The current-lighting text snapshot tool is installed and mirrored. The
  `2026-07-14 00:15:37` snapshot contains
  `LightingCycleConfig.CurrentLightingCaptureText` with the expected V1 marker.
- Lighting Phase AS is installed and mirrored; runtime verification remains.
  It creates `LightingCycleConfig.StageVisuals.<Preset>` folders with editable
  `WindowMode`, `StreetLightsEnabled`, and `StreetLightBrightness` attributes.
  Run it after Phase AR, then run the updated capture/preview tools and audit.
- Running the selected-stage preview before Phase AS produced an infinite-yield
  warning on `LightingCycleConfig:WaitForChild("StageVisuals")`. Root cause:
  StageVisuals had not been installed yet. The preview now fails immediately
  with the exact Phase AS installer instruction instead of waiting indefinitely.
- Lighting Phase AR is installed and mirrored; runtime verification remains.
  It adds TenAM and ThreePM as independent Day copies and expands the schedule
  to eight stages without recapturing FivePM. Run the updated Phase AQ audit
  afterward and refresh the Studio mirror.
- Lighting Phase AQ was installed and user-confirmed working on 2026-07-13,
  including the initial Edit-mode FivePM capture. The system is represented in
  the `2026-07-14 00:15:37` Studio mirror.
- The refreshed mirror has `BaseDurationSeconds = 5`, which is suitable for a
  rapid cycle test but probably not production timing. Restore the intended
  production duration after verification (the original default was `300`).
- Phase AQ controls every managed street light carrying the
  `NTR_NightLamppostLight` tag. If a particular street light does not follow
  8 PM/Night/4 AM, treat it as an asset tagging/install issue rather than adding
  a whole-world runtime scan.

- A repair script is prepared for the regression where Play-mode `N`/`M` lighting changes no longer switch windows or lamppost lights: run `scripts/roblox_lighting_night_mode_signal_repair.lua`. Root cause from the fresh mirror: `TEMP_LightingPreview` and `LightingService_Active` apply presets but do not reliably publish the `Lighting.NTR_LightingPreset` attribute that the Phase AP window/lamppost controllers watch.
- After running `scripts/roblox_lighting_phaseR_fogcolor_property_repair.lua`, confirm the `Lighting Fogcolor` warning no longer appears during Play startup.
- Lighting Phase AP was installed and user-confirmed working. Tagged windows
  switch between `Windows Day` and `Windows Night` with the lighting mode.
- Edit-mode day/night preview scripts were user-confirmed working.
- Edit-mode capture-to-Day and capture-to-Night scripts were added. Verify each
  in a fresh Play session after capture, and refresh the Studio mirror because
  these scripts change the live LightingPresets ModuleScript and stored Sky.
- The night lamppost SurfaceLight installer is generated and needs Studio
  verification. Confirm it finds the intended single template, copies it to all
  `lamppost neon` MeshParts, enables the lights at night, and disables them in
  day mode.
- The first lamppost installer run found four duplicate template lights under
  the same source MeshPart and aborted without changes. The installer now keeps
  the first light as the template and removes duplicate siblings during install.
- Phase AP removes matching SurfaceAppearance objects and clears MeshPart texture
  content. Use Roblox place version history for rollback; no in-game backups are
  created.
- After Phase AP is installed, refresh the Studio mirror so the new controller,
  tags, material assignments, and hierarchy are captured.

## VFX

Known sensitive areas:

- Current VFX baseline is confirmed working after `scripts/roblox_vfx_restore_mirror_known_good_baseline.lua` followed by `scripts/roblox_vfx_mobile_delayed_attach_once.lua`. Engine, boost, and drift VFX toggle correctly again, with no observed cut-out or RuntimeVFXController growth.
- Keep watching mobile/emulator VFX across fresh spawn, respawn/re-enter, and customisation preview. The delayed attach-once fix should keep total instances and unparented Beam counts stable over 60-120 seconds.
- Do not rerun the previous late-socket/rescan/rebuild repair ladder unless deliberately reproducing the failed experiment. Testing showed it can reintroduce RuntimeVFXController growth, 3-5 second cut-outs, all-on VFX, or unparented Beam buildup.
- Dealership Phase 4 moved the local preview vehicle to `Workspace._NTR_ClientOnly.VehiclePreview`; thrust VFX preview helpers must resolve this root before the old `Workspace.HOVER_RACING_V2_LOCAL_PREVIEW` fallback.
- Thrust colour should not flicker back to default after editing.
- Cosmetic neon and thrust-colour neon must remain separate.
- Front bumper optional neon previously did not update correctly.
- Stabiliser left/right VFX had breakage in earlier patches; verify directional drift VFX after any VFX runtime change.

## Vehicle Cockpit Lights

- Front/rear long-range car lights are intentionally deferred after the Phase S-AH experiments did not produce an acceptable result.
- Run `scripts/roblox_vehicle_phaseAI_remove_cockpit_light_systems.lua` in Studio if any cockpit-light helper output or objects remain.
- After Phase AI, Play output should not show any `[NTR Vehicle Phase U/Y/Z/AG/AH]` cockpit-light runtime messages.
- Do not rerun the removed cockpit light phases unless deliberately restoring an old experiment from Git history.

## Data/Folders

Known sensitive areas:

- The refreshed responsive V1.2 mirror is the source baseline for `scripts/roblox_ui_garage_flow_navigation_colour_palette_installer.lua`. The first V1 attempt rolled back because its audit queried the nonexistent `Disabled` property on ModuleScripts; V1.1 fixes only that audit and does not require a revert or refreshed mirror. The installer remains a guarded four-module client presentation transaction with rollback and awaits Studio verification. Require `12 PASS / 0 FAIL`, then test Dealership -> Paint -> Hub, owned Customisation -> Hub, drive-in -> Hub, the Build list/source/slots/hub Back stack, shared Drive, card-popup clearance on touch, palette persistence, comma formatting, and transient preview cleanup. Do not patch the legacy bootstrap or create a second navigation owner if a source guard fails; refresh and inspect the mirror instead.

- The native garage entrance prompt installer is generated as `scripts/roblox_ui_garage_native_entrance_prompts_installer.lua` and awaits Play verification. It deliberately replaces the old visual-only custom billboard with Roblox's default interactive `ProximityPrompt`; verify E on PC and tap/hand activation on touch at both Dealership and Customisation before treating it as confirmed.

- Canonical module-instance card/action V1 is generated as `scripts/roblox_ui_garage_module_instance_cards_and_actions.lua` and awaits Edit install plus Play verification. The confirmed root of automatic equip is client-side: the previous Buy callback called `BuyModuleInstance` and immediately called `EquipModuleInstance`; the server purchase action already created an unequipped copy correctly. The new phase removes that chain, renders physical copies separately through a shared view model, and adds explicit confirmed cross-vehicle reassignment. Do not add an auto-equip workaround to the server.
- V1 Play review exposed a shared-card forwarding gap: `GarageWorkspaceController.RenderCards` omits the new lineage, price, semantic-state and lock fields, causing `UNIVERSAL`, missing green prices and missing state colours. It also confirmed that module colours/neon still mutate legacy slot tables while live instance inventory is copied directly to persistence. Before automated reassignment/backfill, make instance customisation authoritative and implement purchase/equip/backfill as one atomic server transaction with no replacement-instance creation.
- Physical module-instance customisation authority from `scripts/roblox_ui_garage_module_instance_customisation_authority.lua` was user-confirmed working well on 2026-07-16. It adds an isolated server runtime, captures colour/neon/upgrade compatibility data into the installed instance, hydrates slot views from instances, and validates reference ownership before persistence.
- Module-card selection already stores `SelectedModuleInstanceId`; the read-only repair is now generated as `scripts/roblox_ui_garage_module_instance_readonly_preview.lua`. It installs one shared preview adapter for exact-copy colours, neon, upgraded clone attributes and upgraded stat deltas without any remote or profile write. A before/after profile fingerprint makes accidental preview mutation a hard runtime failure. The installer uses three guarded exact source anchors across two isolated client modules, so source drift is a hard stop rather than a broad replacement. Verify equipped, available and in-use copies before treating it as confirmed.
- The instance authority, read-only preview, atomic purchase/equip/backfill, and shared cards/modal are now user-confirmed. `scripts/roblox_ui_garage_module_presentation_refinement.lua` is the next generated installer and awaits Studio verification. It is presentation-only: verify bright H/S/B tracks, content-fitted Build navigation, the full-height shorter Customise rail, `Thrust` copy, configured overview/neon icons, larger header/stats/economy typography, an explicit Standard non-upgradeable message, and visible Lightweight/Power upgrade cards. If a non-Standard module reports `UPGRADE DATA UNAVAILABLE`, stop and inspect that module's V2 catalogue materialisation rather than adding a UI fallback.
- The presentation installer was user-confirmed visually improved. Browser vehicle previews and paint persistence still used ambiguous legacy current-profile state. `scripts/roblox_ui_garage_vehicle_preview_and_paint_scope.lua` is generated against the refreshed post-presentation mirror and awaits Studio verification. Verify that Dealership selection shows factory defaults without changing `CurrentVehicleId`, Customisation selection shows the chosen owned vehicle's exact saved cockpit/modules/neon/upgrades, initial `Paint Vehicle` changes the cockpit plus all physical module instances, and Customise > Cockpit changes only the cockpit. Then verify all scopes after menu transitions and save/rejoin. The installer uses guarded source anchors across isolated preview/application modules and one server action branch; unknown source shape is a hard stop.
- The first authority install placed `local V97_ModuleInstances = require(...)` on the same line after the inventory guard's `--` comment because Lua long-string opening-newline handling removed the intended separator. The declaration was commented out, producing `attempt to index nil with 'HydrateAll'` at the vehicle-selection bridge. The canonical installer now detects that exact malformed line, moves the declaration onto an executable line, and audits the corrected source shape. Rerun the same installer; no separate repair script is needed.
- A later `SelectVehicleInstance` request was blocked by `Equipped instance module_d93425e6b4a3 has no matching slot reference`. The preview phase did not cause this server-side state; the authority invariant exposed a stale derived `EquippedVehicleId` flag left by an earlier module move. The same canonical authority installer now reconciles those flags from the authoritative vehicle-slot map at request entry. It never removes or creates instances and still rejects missing/duplicate references. Rerun `scripts/roblox_ui_garage_module_instance_customisation_authority.lua`, restart Play and retry customisation.

- Vehicle Performance V2 remains shadow-only. The live V1 calculator remains linear/capped and `VehicleDynamicsModel` still has separate factor clamps; do not switch rating or physics until both consume one shared V2 curve definition.
- Vehicle Performance V2 Phase 0 passed live at `21 PASS / 2 WARN / 0 FAIL`. It found `ReverseEngageDelaySeconds = 0.3`; Phase 1 subsequently restored it to `1.0`, and the `2026-07-13 20:22:34` mirror confirms that value. Drift remains correct at full-drift coefficient `0.28`.
- The six target PI values and prices in the Sheet are guides, not authored ratings or live economy values. Current cockpit prices must remain unchanged during Phase 0.
- Upgrade-point migration to six points per module is deferred. Phase 5 defines and audits the paths only in shadow config; existing live definitions still use their current three-level behaviour until a later isolated migration is designed and tested.
- Vehicle Performance V2 Phase 1 passed `12/0/0` and the `2026-07-13 20:22:34` mirror contains its objects plus `ReverseEngageDelaySeconds = 1.0`. Phase 2 is generated as `scripts/roblox_vehicle_performance_v2_phase2_catalogue_calibration.lua`; it uses one guarded exact replacement inside the isolated V2 calculator, so an absent/duplicate anchor is a hard stop requiring mirror inspection.
- Phase 2's six builds and price values are shadow calibration guides only. Do not copy them onto live cockpit/module assets, create Zenith, change garage prices, or enable V2 runtime until the Phase 2 output passes and its mirror is refreshed.
- The first Phase 2 audit reported `6 PASS / 0 WARN / 6 FAIL` with correct headline values but inflated PI. Studio had reused the Phase 1 cached calculator after the installer changed its source, so the old zero-origin equation consumed the new smaller scale. The canonical Phase 2 installer now requires a short-lived calculator clone for validation and destroys it immediately. Rerun the canonical script; do not alter the calibrated values to compensate for cached output.
- Phase 2 later passed `8/0/0` and was mirrored at `20:53:08`. Phase 3 is generated as `scripts/roblox_vehicle_performance_v2_phase3_component_allocation.lua`; it is config-only and must pass its recombination and donor-ladder checks before Lightweight/Power variants, Zenith assets, live prices/stats, or runtime switching.
- Phase 3 was subsequently completed and mirrored at `21:01:42`. Phase 4 then passed `7/0/0` and was mirrored at `21:21:01`; all 48 variants improved PI, Lightweight/Power pairs stayed within `1.5 PI`, and all donor ladders were monotonic.
- Phase 5 is generated as `scripts/roblox_vehicle_performance_v2_phase5_upgrade_paths.lua`. It is config-only and defines three three-point paths, six total points, primary `3%` raw changes, a half-strength `1.5%` supporting change on engine Efficiency, and increasing tier-scaled point costs. Do not migrate live upgrades, assets, prices, rating, physics, or UI until its Output passes and the mirror is refreshed.
- The first Phase 5 run returned `9/0/12`. Every failure was an engine Lightweighting point at A/S because the complete build was already at the Weight curve's technical minimum, making further Weight reduction PI-neutral. The canonical script now replaces that path with Efficiency: Weight `-3%` plus EngineOutput `+1.5%` per point. Rerun the same script; do not lower the global Weight technical minimum or recalibrate confirmed stock profiles.
- Vehicle Performance V2 Phase 7 was user-confirmed working after its two validation-only checks were repaired in the same canonical script. Phase 8 is now generated as the single final stage: `scripts/roblox_vehicle_performance_v2_phase8_atomic_live_launch.lua`. Run it with `MODE = "PUBLISH"` in Edit mode, restart Play, then verify the complete dealership/customisation/cross-tier module/six-point upgrade/spawn/driving/racing/save-rejoin flow. It contains one fragile but hard-preflighted garage catalogue-call replacement. The server charges the exact configured next-point cost, but the legacy Phase AO card can display a static first-point price after mixed-path spending; report any mismatch before further UI work. The locally received mirror still shows `21:51:45`, so refresh it after the Phase 8 test. If live behavior regresses, rerun the same script with `MODE = "ROLLBACK_SWITCHES"`; full catalogue asset rollback uses Roblox version history because no in-game backups are created.
- The Studio cash grant helper is generated as `scripts/roblox_studio_cash_grant_hotkey.lua`. It is intentionally hard-disabled outside Studio, defaults to `=` / `$100,000` / `LucidityStudios`, and uses the existing garage cash/persistence bridge. After installation, restart Play and verify one press changes cash and leaderstats by exactly `$100,000`. Refresh the Studio mirror because it adds isolated config, remote, service, and client objects.
- Driving Feel Phase 3 is generated as `scripts/roblox_driving_feel_phase3_tier_curve_drift_drive.lua` but is not yet confirmed. It is intentionally one stage and contains two fragile exact controller replacements; both are hard-preflighted before mutation. Run in Edit mode, restart Play, and compare Forge E, Viper C, and Zenith S acceleration zones, steering, full boost drain/recharge, and 50–70 MPH throttle-held drift. Confirm braking, parking, one-second reverse, mini-boost, camera, VFX, UI, mobile, and racing remain healthy. The local mirror is still timestamped `2026-07-13 21:51:45`, so refresh it after the test.
- The first Phase 7 Edit run installed successfully but ended `15/0/2`. Both failures were false validation assumptions: PI preview used a zero-stat vehicle context, and the shadow guard looked for the V2 write marker in the service caller instead of the runtime write owner. The canonical Phase 7 installer now previews inside the complete Viper build and audits both correct sources. Rerun the same script; no rollback or separate repair script is required. Live V1 sources/hierarchy and all disabled V2 switches already passed.

- Default cockpit colours are edited on each cockpit model with `DefaultPrimaryColor`, `DefaultSecondaryColor`, `DefaultDetailColor`, `DefaultNeonColor`, `DefaultFrontLightsColor`, and `DefaultRearLightsColor`.
- Phase AK uses guarded source text replacement against the active garage server controller and client bootstrap. If either source changed since the current mirror, refresh the Studio export before running or editing the installer.
- Phase AM, Phase AO, and the mobile thumbstick installer use guarded source replacement against live scripts. The post-AO mirror was refreshed on 2026-06-08; after later Studio changes, any exact-match failure must be treated as a request for another fresh export, not bypassed with a broad replacement.
- Buyable modules need valid `Price` attributes.
- Phase 16 gives extra Standard module copies a server fallback price if their template `Price` is zero. For better balancing, add explicit `ExtraCopyPrice`, `ModuleCopyPrice`, `PurchasePrice`, or `Price` attributes to Standard modules instead of relying on the fallback.
- Boost modules should have `Boost`, `BoostDuration`, `BoostRecharge`, and `BoostRechargeDelay`.
- Module folder shape should stay simple and not reintroduce redundant colour-channel folders.
